# سجل الإصلاحات الأمنية — جولة تدقيق ما قبل الإطلاق (2026-07-25)

هذا الملف يوثّق حصراً الثغرات/الفجوات الأمنية المكتشفة والمُصلَحة أثناء جولة "تدقيق أمني شامل +
اختبار كل المسارات + جاهزية الإطلاق". لكل بند: الوصف، الخطورة، طريقة الاكتشاف (تحليل ثابت و/أو
اختبار حي فعلي)، الإصلاح، والتحقق الفعلي بعده. القرارات المعمارية غير الأمنية البحتة تبقى في
`DECISIONS.md`.

---

## 1. غياب كامل لرؤوس أمان HTTP (Security Headers) — Critical

**الاكتشاف**: `curl -sI http://localhost:3000/` أظهر عدم وجود أي من: HSTS، CSP،
X-Frame-Options، X-Content-Type-Options، Referrer-Policy، Permissions-Policy — بالإضافة لرأس
`X-Powered-By: Next.js` يُسرِّب هوية الإطار الخلفي لكل مهاجم يفحص التطبيق.

**المخاطر الفعلية بغيابها**: Clickjacking (لا `X-Frame-Options`/`frame-ancestors`)، احتمال
Downgrade Attack على HTTPS (لا HSTS)، MIME-sniffing XSS (لا `X-Content-Type-Options`)، تسريب
معلومات عبر `Referer` لروابط خارجية، وسهولة استهداف CVEs معروفة لإصدار Next.js محدَّد بمعرفة
`X-Powered-By`.

**الإصلاح**: `next.config.js` — إضافة `headers()` تُرجِع HSTS + CSP + X-Frame-Options: DENY +
X-Content-Type-Options: nosniff + Referrer-Policy + Permissions-Policy لكل المسارات، و
`poweredByHeader: false`. تفاصيل تصميم CSP (لماذا `img-src https:` بدل `'self'` فقط، ولماذا
`unsafe-inline`/`unsafe-eval` في `script-src`) موثَّقة كتعليق مباشر أعلى `next.config.js` نفسه.

**التحقق الفعلي**:
- `curl -sI` قبل/بعد: كل الرؤوس ظاهرة، `X-Powered-By` غائب تماماً.
- دورة كاملة إيقاف/`npm run build`/إعادة تشغيل: بناء ناجح بدون أخطاء.
- اختبار Playwright حي: تسجيل دخول تاجر اختبار حقيقي على تينانت معزول، بناء تدفق شات بوت فيه صورة
  خارجية حقيقية (لاختبار أن CSP الجديدة لا تكسر ميزة معاينة الصور المبنية مسبقاً)، حفظ — **صفر
  أخطاء CSP في الـconsole** (فقط 404 غير متعلقة مسبقاً لـ`/logo.png`).
- تنبيه إيجابي كاذب أول: فشل أول تشغيلة لاختبار Playwright بمهلة `waitForURL` — تم التحقق بسكربت
  تشخيصي أدق (استقصاء الـURL كل ثانية) بدل افتراض أنه نفس عطل Playwright المعروف من قبل في هذه
  الجلسة، وأثبت أن تسجيل الدخول ينجح فعلياً خلال ~2 ثانية بلا أي علاقة بالرؤوس الجديدة.

---

## 2. عدم تطابق تعليق توثيقي مع الاستخدام الفعلي لـ`superAdminDb` — Low (تصحيح توثيق فقط)

**الاكتشاف**: خلال مسح شامل (تحليل ثابت + اختبار حي معادٍ) لكل استخدامات `superAdminDb` (الاتصال
الوحيد الذي يتجاوز RLS عمداً عبر دور `app_superadmin` بصلاحية `BYPASSRLS`)، وُجد أن مهام
cron/العامل الخلفية (`chatbot/timeoutScan.ts`, `campaigns/triggerEngine.ts`, `queue/worker.ts`,
`whatsappQr/expireTrial.ts`) تستخدمه فعلياً — بشكل آمن ومقصود (تعداد `tenantId`/`id` فقط للاكتشاف
عبر المستأجرين، ثم إعادة التحويل الفوري لـ`withTenant()` للقراءة/الكتابة الفعلية) — لكن تعليق
التوثيق أعلى `superAdminDb` في `src/lib/db.ts` كان يصف الاستخدام المسموح به بـ"لوحة Super Admin
فقط"، بلا ذكر هذا النمط الثاني المشروع أصلاً.

**الإصلاح**: تحديث التعليق التوثيقي في `src/lib/db.ts` ليصف صراحة الاستخدامين المسموحين، مع الشرط
الصارم (تعداد المعرّفات فقط ثم إعادة التحويل لـ`withTenant`) وأمثلة الملفات الفعلية التي تتبعه.

**لا تغيير في السلوك أو الكود المنطقي** — تصحيح توثيق فقط لمنع سوء فهم مستقبلي قد يقود لاستخدام
`superAdminDb` لقراءة بيانات فعلية عبر مستأجرين (وهو ما لا يحدث حالياً في أي مكان بالكود).

---

## 3. اختبار عزل المستأجرين (RLS) — عدواني حي، لا ثغرة (تأكيد فقط)

**الطريقة**: إنشاء تينانتين معزولين حقيقيين (A و B) عبر `superAdminDb`، بيانات حقيقية (جهة اتصال،
محادثة، رسالة، قالب) لتينانت B، ثم 7 محاولات اختراق فعلية من سياق تينانت A عبر `withTenant`:
قراءة مباشرة بمعرّف B الحقيقي (جهة اتصال/محادثة/رسالة) → `null` في الحالات الثلاث. `findMany({})`
بلا أي شرط `tenantId` → 0 صف (لا تسريب). تعديل/حذف صفوف B مباشرة بمعرّفها → استثناء
`PrismaClientKnownRequestError` "Record not found" في الحالتين (رفض حقيقي من RLS، وليس مجرد فحص
منطقي في الكود). 3 محاولات حقن SQL عبر تمرير معرّف مستأجر مشوَّه لـ`withTenant()` نفسها (تضمين
`DROP TABLE`، `OR '1'='1`، حقن `SET LOCAL` إضافي) → رُفضت الثلاثة فوراً بواسطة
`TENANT_ID_PATTERN = /^c[a-z0-9]{20,30}$/` **قبل** أي استعلام SQL فعلي.

**النتيجة**: 7/7 محاولات اختراق فشلت كما هو متوقَّع. حاجز عزل المستأجرين سليم فعلياً وليس افتراضاً
نظرياً. لا إصلاح مطلوب — تأكيد فقط. تنظيف كامل لتينانتَي الاختبار بعد التحقق.

---

## 4. تحديث Next.js من 14.2.15 إلى 14.2.35 (تصحيح غير كاسر ضمن نفس الخط 14.x) — High/Critical جزئياً

**الاكتشاف**: `npm audit --omit=dev` أظهر 12 ثغرة (1 متوسطة، 10 عالية، **1 حرجة**) في الإصدار
المثبَّت 14.2.15 من `next`، من ضمنها ثغرة **حرجة** لتجاوز صلاحيات في Middleware
(Authorization Bypass) مُصلَحة فعلياً في إصدارات لاحقة ضمن نفس خط 14.x نفسه (لا تتطلب قفزة كسرية
لإصدار 16).

**الإصلاح المُطبَّق فعلياً**: ترقية `next` من `14.2.15` إلى أحدث تصحيح مستقر ضمن نفس الخط
`14.2.x` وهو `14.2.35` — تحديث بلا تغيير كسري (نفس الـminor version، تصحيحات فقط).

**التحقق الفعلي**:
- `npm audit` بعد الترقية: الثغرة **الحرجة** اختفت تماماً (لم تعد ضمن التقرير). العدد الكلي أصبح
  12 (1 متوسطة، 11 عالية، **صفر حرجة**) — تحسّن حقيقي في مستوى الخطورة الإجمالي، ليس مجرد إعادة
  تصنيف.
- `npx tsc --noEmit`: بدون أخطاء.
- `npm run build`: بناء إنتاج كامل ناجح لكل المسارات (~40 مساراً) بدون أي تحذير كسري جديد.

**ما تبقّى غير مُصلَح (High) وسبب عدم الترقية الكاملة لـ`next@16.2.11`**:
القائمة المتبقية من تحذيرات `npm audit` لحزمة `next` (RSC DoS، تسميم كاش، SSRF في `rewrites`/
WebSocket upgrade، تجاوز Middleware عبر i18n، XSS في CSP nonces، إلخ) تغطي مدى إصدارات واسعاً
جداً (`9.3.4-canary.0 - 16.3.0-canary.5`) لأن قاعدة بيانات الثغرات تُجمِّع كل الاستشارات معاً بغض
النظر عن أي إصدار أُصلحت فيه فعلياً كل ثغرة على حدة — **معظمها لا ينطبق فعلياً على هذا المشروع**
تحديداً بعد التحقق المباشر من الكود:
- لا يوجد `middleware.ts` في المشروع إطلاقاً (تأكيد: `ls middleware.ts src/middleware.ts` →
  الملفان غير موجودين) → كل ثغرات "Middleware bypass/cache poisoning" غير قابلة للاستغلال هنا.
- لا استخدام لـ`next/image` مع `remotePatterns` في أي مكان بالكود (تأكيد: بحث شامل، صفر نتائج) →
  ثغرات Image Optimizer DoS/تسريب قرص غير منطبقة.
- لا `rewrites()`/`redirects()` في `next.config.js` إطلاقاً → ثغرة SSRF عبر hostname في rewrites
  غير قابلة للتفعيل.
- لا خادم مخصص (`server.js`) — المشروع يعمل بـ`next start` القياسي فقط → ثغرة SSRF في Server
  Actions على خوادم مخصصة غير منطبقة.
- لا استخدام لـCSP nonces (التصميم الحالي في البند 1 أعلاه يعتمد `unsafe-inline` بدل nonce) →
  ثغرة XSS الخاصة بـnonces غير منطبقة بنفس الطريقة (وإن كان `unsafe-inline` نفسه قيداً معروفاً
  ومقبولاً موثَّقاً في `next.config.js`).
- لا استخدام لـ`beforeInteractive` في أي `<Script>` بالمشروع (تأكيد: بحث شامل، صفر نتائج).
- بواقي البنود (RSC DoS عام، Server Action payload غير محدود على Edge runtime) نظرياً منطبقة جزئياً
  لكن مخفَّفة فعلياً بوجود حد `bodySizeLimit: "5mb"` على Server Actions (موجود مسبقاً في
  `next.config.js`) وبطبقة `rateLimit.ts`/RBAC الموجودة أصلاً أمام كل Server Action حساس.

**القرار الهندسي**: الترقية الكاملة لـ`next@16.2.11` هي **قفزة إصدار رئيسي كسرية** عبر مشروع كبير
(App Router كامل، Server Actions، BullMQ عبر `serverComponentsExternalPackages`) تتطلب اختبار
انحدار شامل مستقل لا يجب تنفيذه على عجل ضمن جولة تدقيق واحدة، خصوصاً بعد أن ثبت أن الثغرة **الحرجة**
الفعلية already أُصلحت بالترقية الآمنة لـ14.2.35، وأن الأغلبية الساحقة من التحذيرات المتبقية غير
قابلة للاستغلال فعلياً في هيكلية هذا المشروع تحديداً. **يُوصى بترقية `next@16` كبند High ضمن أول
أسبوع بعد الإطلاق** (وليس Blocker)، مع خطة اختبار انحدار مستقلة مخصصة لها — تفصيل ذلك في
`LAUNCH_READINESS_REPORT.md`.

---

## 5. `uuid <11.1.1` (متوسطة، عبر `exceljs`) — لا إصلاح فوري، قرار هندسي موثَّق

**الاكتشاف**: ثغرة متوسطة (فحص حدود buffer ناقص في `uuid` v3/v5/v6) عبر تبعية عابرة من `exceljs`.

**لماذا لا يُطبَّق `npm audit fix --force` هنا**: الإصلاح المقترَح تلقائياً يُخفِّض `exceljs` إلى
الإصدار `3.4.0` — وهذا يُعيد بالضبط فئة الثغرات (Prototype Pollution / ReDoS) التي كانت السبب
الأصلي في اختيار `exceljs` بدل `xlsx` في جولة سابقة من هذا المشروع (موثَّق في `DECISIONS.md`).
استبدال ثغرة متوسطة الخطورة (buffer bounds check يتطلب استدعاء داخلي بمعامل `buf` مُمرَّر يدوياً —
غير مستخدَم فعلياً في أي استدعاء لـ`uuid` بهذا المشروع) بثغرة أعلى خطورة تاريخياً (Prototype
Pollution) قرار هندسي سيئ بوضوح.

**القرار**: تأجيل مقصود (Backlog/Low)، موثَّق صراحة كقرار مدروس وليس إغفالاً. إعادة التقييم عند
صدور نسخة أحدث من `exceljs` تحل تبعية `uuid` دون تراجع في `archiver`/`zip-stream`.

---

## 6. `brace-expansion`/`minimatch`/`archiver` (High، عبر `exceljs`) — نفس القرار أعلاه

نفس سلسلة التبعية العابرة من `exceljs@4.4.0` (`archiver` → `archiver-utils`/`zip-stream`/
`readdir-glob` → `glob` → `minimatch` → `brace-expansion`). ثغرة DoS محتملة عبر توسّع نمط طويل —
تتطلب تحكم مهاجم في نمط glob يُمرَّر لـ`exceljs` نفسه، وهو غير منطبق (لا مدخل مستخدم يُمرَّر كنمط
glob في أي كود بهذا المشروع؛ `exceljs` يُستخدَم فقط لتصدير/استيراد ملفات Excel داخلية). نفس قرار
التأجيل أعلاه: إصلاحها الوحيد المتاح تلقائياً هو تخفيض `exceljs` لإصدار أقدم بمخاطر أعلى.

---

## 7. الفوترة/الاشتراكات — الفجوة المركزية: مزوّد دفع وهمي بالكامل — **Blocker خارج نطاق الكود**

**الاكتشاف**: `src/lib/billing/payment-provider.ts` هو الطبقة الوحيدة للدفع في المشروع بالكامل —
`mockPaymentProvider.chargeSubscription()` يُعيد `{ success: true }` فوراً بلا أي اتصال شبكي
حقيقي، بلا أي مزوّد دفع سعودي/إقليمي مُهيَّأ (لا Moyasar، لا PayTabs، لا HyperPay، لا Tap، لا
Stripe). لا يوجد أي endpoint لـwebhook مزوّد دفع (فقط Meta/Zid/Salla). لا تُخزَّن بيانات بطاقة خام
في أي مكان (تأكيد: بحث شامل لـ`cardNumber|cvv|cardExpiry` — صفر نتائج، متوقَّع لأن المزوّد وهمي
أصلاً ولا يجمع بيانات بطاقة).

**لماذا هذا Blocker حقيقي وليس مجرد ثغرة تُصلَح بالكود**: ربط مزوّد دفع حقيقي يتطلب قراراً تجارياً
(أي مزوّد؟) وحساب تاجر حقيقي (Merchant Account) ومفاتيح API إنتاج حقيقية — لا يملكها الكود ولا
يمكن لأي أداة تحليل/تعديل كود أن "تُصلحه" بدون هذه المدخلات الخارجية. **هذا يعني أن المنصة حالياً
لا يمكنها تحصيل أي مبلغ حقيقي من أي تاجر — كل ترقية باقة تُسجَّل "مدفوعة" في الفواتير دون أي تحصيل
فعلي.** موثَّق مسبقاً في CHANGELOG.md من جولة بناء الفوترة الأصلية، ومؤكَّد هنا مجدداً كحالة لا تزال
قائمة. تفاصيله الكاملة في `LAUNCH_READINESS_REPORT.md` كأول بند في قسم "Blockers خارج نطاق الكود".

---

## 8. غياب كامل لدورة حياة انتهاء الاشتراك (Subscription Expiry) — Critical (مُصلَح)

**الاكتشاف**: `Subscription.currentPeriodEnd` كان يُكتب مرة واحدة فقط عند الإنشاء (التسجيل أو ترقية
الباقة) ثم لا يُقرأ إلا للعرض في الواجهة — لا يوجد أي كود (لا مهمة مجدولة، لا فحص عند تسجيل الدخول)
يتحقق من هذا التاريخ فعلياً. **نتيجة عملية**: أي تاجر على فترة تجريبية (TRIALING) تبقى حالته
TRIALING/ACTIVE للأبد حتى بعد انتهاء الفترة الفعلية، بلا أي انتقال لـPAST_DUE — يناقض تماماً نموذج
تحصيل الإيرادات الذي تفترضه صفحة الفوترة نفسها. هذا لا علاقة له بمهمة `whatsappQr/expireTrial.ts`
(تلك خاصة حصراً بانتهاء قناة واتساب التجريبية عبر QR، وليس بدورة حياة الباقة/الفوترة — تم التأكد من
عدم الخلط بينهما).

**الإصلاح**:
- `src/lib/billing/expireSubscriptions.ts` (جديد) — `scanAndExpireSubscriptions()`: يفحص كل
  الاشتراكات (`TRIALING`/`ACTIVE`) التي تجاوزت `currentPeriodEnd` عبر `superAdminDb` (تعداد
  `tenantId` فقط، نفس نمط `scanAndExpireQrTrials`/`scanAllTenantsTriggeredCampaigns` المُدقَّق
  مسبقاً)، ثم يحوّل حالة كل واحد لـ`PAST_DUE` عبر `withTenant()`.
- `src/lib/queue/worker.ts` — تسجيل مهمة BullMQ متكررة كل ساعة (`subscription-expiry-scan`)، بنفس
  نمط الطوابير المتكررة الموجودة حرفياً في نفس الملف.
- `src/lib/campaigns/limits.ts` — `getTenantCampaignLimits()` يُعيد الآن `subscriptionStatus` أيضاً.
- `src/app/dashboard/campaigns/actions.ts` — `createCampaign` يرفض إنشاء أي حملة جديدة إن كانت
  `subscriptionStatus` هي `PAST_DUE`/`CANCELLED` (بند 3 في البرومنت: إنفاذ فعلي من الخادم، ليس
  مجرد رسالة واجهة). **قرار متعمَّد**: لا يُحظَر تسجيل الدخول أو الوصول لصفحة الفوترة نفسها — التاجر
  يستطيع دائماً الدخول وترقية باقته، فقط الإرسال الفعلي (حملات) يُمنع، لتفادي حبس تاجر بلا طريقة
  للتعافي من حالته (خصوصاً أن مزوّد الدفع وهمي حالياً أصلاً — البند 7 أعلاه).

**التحقق الفعلي**: سكربت اختبار مباشر (تينانت معزول): ضبط `currentPeriodEnd` لتاريخ ماضٍ يدوياً،
استدعاء `scanAndExpireSubscriptions()` مباشرة، تأكيد التحول الفعلي لـ`PAST_DUE` في القاعدة، وتأكيد
أن `getTenantCampaignLimits()` يعكس ذلك فوراً. `npx tsc --noEmit` و`npm run build` نظيفان.

---

## 9. غياب رقم فاتورة تسلسلي وضريبة القيمة المضافة (VAT) في نموذج الفاتورة — High (مُصلَح جزئياً)

**الاكتشاف**: نموذج `Invoice` لم يتضمن أي رقم تسلسلي (`invoiceNumber`) ولا أي حقل ضريبة (لا
`vatRate` ولا `vatAmount`) — بحث شامل لـ`VAT|vatRate|taxRate|invoiceNumber` في `src/` أعاد صفر
نتائج قبل هذه الجولة.

**الإصلاح**:
- `prisma/schema.prisma` (`Invoice`): إضافة `invoiceNumber Int @unique @default(autoincrement())`
  (تسلسل Postgres ذاته — آمن ضد التزامن بطبيعته بلا أي قفل إضافي)، و`vatRateBps Int @default(1500)`
  (نقاط أساس؛ 1500 = 15%، النسبة السعودية القياسية وقت الكتابة)، و`vatAmountSar Int @default(0)`.
- **قرار مهم لتفادي زيادة سعرية غير مُعلَنة**: `priceMonthlySar`/`amountSar` تبقى كما هي تماماً —
  **شاملة الضريبة أصلاً** (نفس الرقم المعروض في صفحة الأسعار العامة دون أي إشارة "غير شامل
  الضريبة")، وتُحسَب `vatAmountSar` بالفصل العكسي (`amountSar × 1500 ÷ 11500`) بدل إضافة 15% فوق
  السعر المعروض — لو اعتُبر `amountSar` سعراً قبل الضريبة لكانت النتيجة تحصيل مبلغ أعلى مما يراه
  التاجر في صفحة التسعير، وهي مشكلة أخطر من غياب حقل الضريبة نفسه.
- `src/app/dashboard/billing/actions.ts` (`changePlan`): يحسب `vatAmountSar` ويُدرجه في الفاتورة.
- عرض رقم الفاتورة وتفصيل الضريبة في `src/app/dashboard/billing/page.tsx` و`src/app/admin/billing/page.tsx`.

**ما لم يُصلَح ولماذا (Blocker خارج نطاق الكود)**: هذا **ليس** فوترة إلكترونية معتمدة من هيئة الزكاة
والضريبة والجمارك (ZATCA/فاتورة) — الامتثال الكامل لمرحلة ZATCA الثانية يتطلب ختماً تشفيرياً (QR
مشفّر)، رقم تسجيل ضريبي حقيقي للمنصة، وربطاً فعلياً مع نظام Fatoora — قرار عمل/قانوني خارج نطاق أي
تعديل كود، يحتاج مزوّد حل فوترة إلكترونية معتمد فعلياً. تفاصيله في قسم Blockers بالتقرير النهائي.

**التحقق الفعلي**: سكربت اختبار مباشر — فاتورة حقيقية بسعر 199 ر.س: `vatAmountSar` المحسوب = 26 ر.س
(199 × 1500 ÷ 11500، مطابق للحساب اليدوي). `npx tsc --noEmit` و`npm run build` نظيفان.

---

## 10. تسابق حقيقي محتمل في `changePlan` (فواتير مكرَّرة تحت تزامن) — Medium (مُصلَح جزئياً)

**الاكتشاف**: `changePlan` كان "افحص ثم نفّذ" (check-then-act) بلا أي قفل: قراءة الباقة → "تحصيل"
وهمي → تحديث الاشتراك + إنشاء فاتورة — بلا `SELECT ... FOR UPDATE` ولا مستوى عزل `Serializable`.
طلبا ترقية متزامنان حقيقيان لنفس التاجر (نقرة مزدوجة/تبويبان) كانا سينشئان فاتورتين منفصلتين لنفس
الحدث دون أي منع.

**الإصلاح**: قفل صريح لصفّ `Subscription` (`SELECT ... FOR UPDATE`) في بداية معاملة `changePlan` —
يُسلسل الطلبات المتزامنة لنفس التاجر بدل تنفيذها بالتوازي الفعلي.

**الحد المتبقي (موثَّق، ليس إغفالاً)**: هذا يمنع فساد بيانات/قراءة حالة قديمة تحت التزامن، لكنه *لا*
يمنع طلبين متتاليين فعليين (بعد تسلسلهما) من كل منهما إنشاء فاتورته الخاصة — منع ذلك بشكل صحيح
يتطلب مفتاح تكرار (Idempotency Key) حقيقياً من مزوّد الدفع نفسه (نمط قياسي لدى Stripe/Moyasar
وغيرهما) — يُبنى طبيعياً مع الربط الفعلي بمزوّد دفع حقيقي (البند 7 أعلاه)، وليس قبله.

**التحقق الفعلي**: سكربت اختبار مباشر — طلبا `changePlan` أُطلقا متزامنين فعلياً (`Promise.all`) على
نفس التينانت: اكتملا بلا أي استثناء/تعليق (Deadlock)، وأنتجا رقمي فاتورة متتاليين مختلفين (لا تصادم
بيانات ولا تكرار رقم) — تأكيد أن القفل يعمل فعلياً تحت تزامن حقيقي.

---

## 11. مراجعة حدود الاستخدام (Usage Limits) — تأكيد سلامة الإنفاذ الفعلي (لا ثغرة)

مراجعة كل حقول `Plan` (`maxUsers`, `maxMessagesPerMonth`, `maxWhatsappNumbers`,
`campaignLimitsJson`, `chatbotLimitsJson`) مقابل نقاط الإنفاذ الفعلية في الكود:
- `maxUsers`: مُنفَذ فعلياً في `dashboard/settings/actions.ts` (فحص `activeUserCount` قبل دعوة عضو).
- `maxMessagesPerMonth`/`campaignLimitsJson`: مُنفَذ فعلياً في `dashboard/campaigns/actions.ts`
  (حد عدد الحملات/شهر، حد حجم الجمهور، حصة الرسائل المتبقية) — تحقُّق خادم حقيقي (Soft-lock)، ليس
  مجرد شريط تقدّم في الواجهة.
- `chatbotLimitsJson`: مُنفَذ فعلياً (مؤكَّد في جولة سابقة، `planLimits.ts`).
- `maxWhatsappNumbers`: **حقل تسويقي فقط حالياً بلا إنفاذ فعلي** — لكن ليس ثغرة: نموذج
  `Integration` يحمل `@@unique([tenantId, provider])`، أي تينانت واحد **لا يمكنه تقنياً** ربط أكثر
  من رقم واحد لكل نوع مزوّد (Meta/QR) بغض النظر عن قيمة الباقة — يستحيل تجاوز الحد لأن البنية نفسها
  تمنعه قبل وصول أي فحص منطقي. توسيع النظام لدعم عدة أرقام واتساب فعلياً لكل تينانت تغيير معماري
  أكبر من نطاق هذه الجولة (ميزة منتج جديدة، وليس إصلاح ثغرة) — موثَّق كملاحظة نطاق في التقرير
  النهائي، وليس كإصلاح مطلوب فوراً.
- `maxContacts`: **لا يوجد أصلاً كحقل في نموذج `Plan`** — ليس بُعد تسعير في هذا المنتج، فلا حاجة
  لإنفاذ لأنه لا يوجد حد مُعلَن لتجاوزه أصلاً.

---

## 12. جاهزية الإنتاج — مراقبة الأخطاء، صفحات الخطأ، النسخ الاحتياطي، فهرس Segment

**الاكتشاف**: لا يوجد أي مزوّد مراقبة أخطاء حقيقي (Sentry/Datadog/إلخ) في المشروع — الأخطاء تُسجَّل
فقط عبر `console.error` متناثرة في 8 ملفات، بلا أي تجميع/تنبيه مركزي. لا يوجد `error.tsx` ولا
`global-error.tsx` ولا `not-found.tsx` على مستوى `src/app` — أي خطأ غير مُتوقَّع في الرسم كان
سيعرض صفحة Next.js الافتراضية بدل تجربة متّسقة مع هوية المنصة. لا استراتيجية نسخ احتياطي موثَّقة أو
مبنية لقاعدة Postgres (لا سكربت، لا ذكر في أي ملف مشروع).

**الإصلاح**:
- `src/app/error.tsx` (جديد) — حدود خطأ على مستوى الجذر (Client Component)، تُسجّل الخطأ عبر
  `console.error` (أقل حد ممكن بدون مزوّد خارجي) وتعرض صفحة عربية متّسقة مع الهوية (زر "إعادة
  المحاولة").
- `src/app/global-error.tsx` (جديد) — يغطي فشل التخطيط الجذري نفسه (حالة لا يغطيها `error.tsx`).
- `src/app/not-found.tsx` (جديد) — صفحة 404 مخصّصة بدل صفحة Next.js الافتراضية.
- `scripts/backup-db.sh` (جديد) + `npm run db:backup` — نسخ احتياطي منطقي فعلي (`pg_dump | gzip`)
  لحاوية Postgres (`wa_crm_postgres`) مع تنظيف تلقائي للنسخ الأقدم من 14 يوماً. اختُبِر فعلياً على
  القاعدة الحقيقية (نسخة 100K أُنشئت وتحقّقت سلامتها عبر `gzip -t` ومعاينة محتوى SQL صالح).
- فهرس `Segment.tenantId` المفقود (مذكور سابقاً في البند الأول من هذا الملف).

**ما لم يُصلَح ولماذا (Blocker/قرار خارج نطاق الكود)**:
- **مراقبة أخطاء حقيقية (APM)**: ربط Sentry/Datadog أو مشابه يتطلب حساباً حقيقياً وDSN — لا يمكن
  لأي تعديل كود أن يُنشئ هذا. الحد الأدنى (تسجيل console + صفحات خطأ متّسقة) مُطبَّق الآن، لكنه
  ليس بديلاً عن تنبيه فعلي (Alerting) عند حدوث أخطاء في الإنتاج.
- **جدولة النسخ الاحتياطي والنسخ لموقع خارجي (S3 أو مشابه)**: قرار بنية تحتية حقيقي (أين تُستضاف
  القاعدة فعلياً في الإنتاج؟ Docker على VPS ذاتي، أم Postgres مُدار؟) — السكربت جاهز وفعلي، لكن
  ربطه بمجدوِل حقيقي (cron/Task Scheduler) والشحن لتخزين خارجي يحتاج قراراً بشرياً عن بيئة الإنتاج
  الفعلية، غير معروفة لأداة تدقيق الكود.

**التحقق الفعلي**: `curl` لمسار غير موجود يُظهر صفحة 404 المخصّصة الجديدة فعلياً (نص "هذه الصفحة
غير موجودة" ظاهر في الاستجابة الحية). دورة كاملة (إيقاف → `rm -rf .next` → إعادة بناء → إعادة
تشغيل) نظيفة. `npx tsc --noEmit` و`npm run build` بلا تحذيرات.

**ملاحظة منهجية من هذه الجولة**: أثناء إعادة التشغيل بعد Migration، تشغيل `npm run build` (بناء
إنتاج) بينما خادم `next dev` كان لا يزال يعمل على نفس مجلد `.next` سبّب فساداً مؤقتاً في cache/
manifests الخادم التطويري (استجابات 404 لكل الحزم الثابتة). الإصلاح: عدم تشغيل `next build`
بالتوازي مع `next dev` على نفس المجلد — إيقاف الخادم التطويري دائماً أولاً، أو تشغيل البناء في
مجلد منفصل. تحقُّق إضافي أثناء نفس الجلسة: قاعدة التطوير المحلية لم تكن مزروعة ببيانات
`prisma/seed.ts` عند بدء هذه الجولة (مفصول تماماً عن أي تغيير في هذه الجولة) — أُعيد زرعها
(`npm run db:seed`) لإتمام اختبارات الدخان الحية.

---

## 13. تدقيق بنية الاستضافة (Infrastructure) — سرّ نص صريح في `docker-compose.yml` ومنافذ حاويات مكشوفة على كل الواجهات — High (مُصلَح جزئياً)، بالإضافة لتأكيد سلبي مهم حول موقع المشروع داخل `htdocs`

**السياق**: هذا البند مختلف عن كل ما سبق في هذا الملف — تدقيق جولة 2026-07-25 غطّى الكود فقط ولم
يغطِ بنية الاستضافة. هذه الجولة (2026-08-02) اكتشفت أن جذر المشروع نفسه يقع داخل
`C:\xampp\htdocs\Whats app bot` — وهو مجلد جذر خادم Apache الخاص بتثبيت XAMPP على هذا الجهاز.

**الاكتشاف الفعلي (تحقّق حي، ليس افتراضاً)**:
- `tasklist`/`netstat` أثبتا أن `httpd.exe` (Apache) **يعمل فعلاً** على هذا الجهاز، ومربوط على
  `0.0.0.0:80` و`0.0.0.0:443` (كل الواجهات، وليس فقط `127.0.0.1`).
- فحص حي عبر `curl -v http://127.0.0.1/Whats%20app%20bot/.env`: النتيجة **404 حقيقي من تطبيق
  Laravel مختلف تماماً**، وليس محتوى `.env`. السبب: `httpd-vhosts.conf` يحتوي `VirtualHost` واحد
  فقط، `ServerAlias localhost 127.0.0.1`، بـ`DocumentRoot` مضبوط صراحة على
  `C:/xampp/htdocs/dr/maisam-platform/public` (مشروع آخر). بما أنه الـ`VirtualHost` الوحيد
  المعرَّف لهذا العنوان/المنفذ، فهو يلتقط كل الطلبات افتراضياً — **مجلد "Whats app bot" غير مكشوف
  فعلياً في الوقت الحالي**، رغم وجوده داخل `htdocs`.
- هذا التأكيد السلبي **لا يُلغي** الخطر البنيوي: أي تعديل مستقبلي لـ`httpd-vhosts.conf` (إضافة
  `VirtualHost` بلا `DocumentRoot` مقيَّد، أو حذف الـ`VirtualHost` الحالي) يعرّض فوراً `.env`
  (كلمات مرور القاعدة الحقيقية، `ENCRYPTION_KEY`، `NEXTAUTH_SECRET`،
  `META_WEBHOOK_VERIFY_TOKEN`)، `docker-compose.yml`، `backups/*.sql.gz`، والكود المصدري كاملاً.

**الإصلاح المُنفَّذ الآن (لا يتطلب قراراً بشرياً)**:
- `docker-compose.yml`: أُزيل السرّ الحقيقي (`POSTGRES_PASSWORD`) من نص الملف الصريح، واستُبدل
  بـ`env_file: .env.docker` (ملف جديد، مُضاف إلى `.gitignore` بجانب `.env`/`.env.local` الموجودين
  أصلاً).
- منافذ الحاويات المنشورة على المضيف قُيِّدت من `"55532:5432"`/`"56379:6379"` (كل الواجهات) إلى
  `"127.0.0.1:55532:5432"`/`"127.0.0.1:56379:6379"` (loopback فقط).

**التحقق الفعلي**:
- `docker ps` بعد إعادة التشغيل: `127.0.0.1:55532->5432/tcp` و`127.0.0.1:56379->6379/tcp` (بدل
  `0.0.0.0:...` سابقاً).
- التطبيق (`next dev`) استمر يعمل بلا انقطاع فعلي بعد إعادة إنشاء الحاويتين — طلب `GET /` أعاد
  `200` خلال ثوانٍ من إعادة التشغيل (الـPool أعاد الاتصال تلقائياً بعد قطع اتصال مؤقت متوقَّع).
- `npm audit --omit=dev` أُعيد تشغيله بعد التعديلات: **6 ثغرات (2 متوسطة + 4 عالية + 0 حرجة)** —
  أقل من الأساس الموثَّق سابقاً (12: 1 متوسطة + 11 عالية + 0 حرجة)؛ لا ثغرة حرجة جديدة.

**ما لم يُصلَح ولماذا (Blocker/قرار خارج نطاق الكود)**:
- **نقل المشروع فعلياً خارج `htdocs`**: لم يُنفَّذ في هذه الجولة. السبب: (1) بيئة الإنتاج المستهدفة
  لم تُحسَم بعد من قِبل صاحب المشروع (VPS/Linux؟ نفس جهاز Windows؟) فالمسار النهائي غير معروف؛ (2)
  الجلسة الحالية لأداة الترميز (Claude Code) مربوطة بمجلد العمل هذا تحديداً — نقله فعلياً كان
  سيقطع الجلسة الجارية وأي أدوات/محرر مفتوح عليه، وهذا إجراء يتطلب تنسيقاً مباشراً من المستخدم وليس
  تنفيذاً تلقائياً. **إجراء إلزامي قبل أي نشر حقيقي** يبقى قائماً كما هو موثَّق في خطة ما قبل
  النشر، بصرف النظر عن هذا التأجيل المؤقت.
- **تدوير الأسرار الفعلي** (`ENCRYPTION_KEY`, `NEXTAUTH_SECRET`, كلمات مرور Postgres الثلاث،
  `META_WEBHOOK_VERIFY_TOKEN`): لم يُنفَّذ. السبب: لا يوجد تسريب فعلي مؤكَّد (التحقق الحي أعلاه
  أثبت عدم الكشف)، والتدوير له كلفة حقيقية الآن (إسقاط كل الجلسات القائمة، وضرورة فك/إعادة تشفير أي
  توكنات Meta/Zid/Salla مخزَّنة قبل استبدال `ENCRYPTION_KEY`) — صاحب المشروع أكَّد صراحةً تأجيله
  عمداً حتى وقت النقل الفعلي لبيئة الإنتاج (دفعة واحدة بدل مرتين)، بعد اطّلاعه على الكلفة.
- **مزوّد دفع حقيقي بدل `mockPaymentProvider`**: قرار تجاري (Moyasar/PayTabs/HyperPay/Tap) لم
  يُحسَم بعد من قِبل صاحب المشروع.
- **ترقية Next.js v16، Sentry/APM، نسخ احتياطي مجدوَل + S3، مراجعة قانونية، فوترة ZATCA**: كل هذه
  بنود Post-launch/قرارات خارج نطاق الكود، كما هي موثَّقة في خطة ما قبل النشر — لم تتغيّر.
- ~~**تنظيف الملفات الشاردة** (`New مستند نصي.txt` الفارغ، `.smoke-shots/`)~~ — **تم**: حُذفا
  فعلياً بعد تأكيد صريح من صاحب المشروع (لا `git` في هذا المشروع بعد، فالحذف كان غير قابل للتراجع
  إلا بموافقة مباشرة).
