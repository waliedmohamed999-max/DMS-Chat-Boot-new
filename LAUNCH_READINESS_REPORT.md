# تقرير جاهزية الإطلاق — Digital Messaging System (DMS)

**تاريخ التدقيق**: 2026-07-25
**نطاق التدقيق**: تدقيق أمني شامل + اختبار وظيفي لكل المسارات + مراجعة الفوترة/الباقات + جاهزية
الإنتاج + الامتثال، مع اختبار عدواني حي (Adversarial Self-Testing) ضد تينانتات اختبار معزولة —
وليس مراجعة كود نظرية فقط. تفاصيل كل إصلاح وتحقّقه الفعلي في `SECURITY_FIXES.md`.

---

## 1. جدول الثغرات/الفجوات المكتشفة والمُصلَحة

| # | الوصف | الخطورة | الحالة |
|---|-------|---------|--------|
| 1 | غياب كامل لرؤوس أمان HTTP (HSTS, CSP, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy) + تسريب `X-Powered-By` | **Critical** | ✅ تم الإصلاح والتحقق حياً |
| 2 | Next.js 14.2.15 يحمل ثغرة **حرجة** لتجاوز صلاحيات Middleware ضمن التبعيات المُجمَّعة | **Critical** | ✅ تم الإصلاح (ترقية غير كسرية إلى 14.2.35) — الثغرة الحرجة اختفت تماماً من `npm audit` |
| 3 | غياب كامل لدورة حياة انتهاء الاشتراك (Subscription Expiry) — لا شيء يحوّل اشتراكاً منتهي الفترة لحالة PAST_DUE | **Critical** | ✅ تم الإصلاح (مهمة BullMQ متكررة + إنفاذ فعلي عند إنشاء الحملات) |
| 4 | مزوّد الدفع بالكامل وهمي (Mock) — لا مزوّد دفع حقيقي، لا webhook دفع | **Blocker خارج نطاق الكود** | ❌ لا يمكن إصلاحه بالكود — يحتاج قراراً تجارياً وحساب مزوّد دفع حقيقي |
| 5 | غياب رقم فاتورة تسلسلي وضريبة القيمة المضافة (VAT) في نموذج الفاتورة | **High** | ✅ تم الإصلاح جزئياً (رقم فاتورة + VAT للشفافية) — الفوترة الإلكترونية المعتمدة من ZATCA بند خارج النطاق منفصل |
| 6 | تسابق حقيقي محتمل (Race Condition) في `changePlan` — فواتير مكرَّرة تحت تزامن | **Medium** | ✅ تم الإصلاح جزئياً (قفل صفّ + اختبار تزامن حي ناجح) — منع كامل يحتاج مفتاح تكرار من مزوّد دفع حقيقي |
| 7 | `Segment.tenantId` بلا فهرس رغم كونه حقلاً مستأجَراً | Medium (أداء) | ✅ تم الإصلاح (migration + إعادة بناء) |
| 8 | عدم تطابق تعليق توثيقي مع استخدام `superAdminDb` الفعلي في مهام cron | Low (توثيق فقط) | ✅ تم الإصلاح |
| 9 | لا صفحات خطأ مخصّصة (`error.tsx`/`global-error.tsx`/`not-found.tsx`) — أخطاء غير متوقَّعة تعرض صفحة Next.js الافتراضية | Medium | ✅ تم الإصلاح + تحقّق حي |
| 10 | لا مراقبة أخطاء حقيقية (APM) — `console.error` متناثر فقط | High | ⚠️ حد أدنى مُطبَّق (console + صفحات خطأ)؛ ربط Sentry/مشابه **Blocker خارج نطاق الكود** (يحتاج حساباً/DSN) |
| 11 | لا استراتيجية/سكربت نسخ احتياطي لقاعدة البيانات | High | ✅ سكربت فعلي مبني ومُختبَر (`scripts/backup-db.sh`)؛ الجدولة والشحن الخارجي **قرار بنية تحتية خارج النطاق** |
| 12 | `npm audit`: `uuid`/`brace-expansion`/`archiver` (عبر `exceljs`) | Moderate/High | ⚠️ مؤجَّل بقرار مدروس — الإصلاح التلقائي يُعيد فئة ثغرات أسوأ (Prototype Pollution) كانت السبب في اختيار `exceljs` أصلاً |
| 13 | تسابق RLS/تصعيد صلاحيات/حقن SQL عبر tenantId | Critical (لو وُجدت) | ✅ **لا ثغرة** — 7/7 محاولات اختراق فعلية فشلت (تأكيد فقط، لا إصلاح مطلوب) |
| 14 | XSS/حقن عبر النماذج (تواصل، شركاء، قوالب، حملات، شات بوت) | High (لو وُجدت) | ✅ **لا ثغرة** — كل الحقول تُعرَض عبر JSX عادي (تهريب تلقائي)، الحالة الوحيدة لـ`dangerouslySetInnerHTML` سكربت ثابت بلا مدخل مستخدم |
| 15 | `maxWhatsappNumbers` بلا إنفاذ فعلي | Low (ليس ثغرة) | ملاحظة نطاق فقط — `@@unique([tenantId, provider])` يمنع تجاوزه تقنياً أصلاً؛ توسيع دعم عدة أرقام ميزة منتج جديدة وليس إصلاح ثغرة |

---

## 2. نتائج اختبار كل مسار

| المسار | النتيجة | ملاحظات |
|--------|---------|---------|
| تسجيل تاجر جديد → حالة "بانتظار المراجعة" | ✅ نجح | تأكيد حي: تاجر جديد لا يرى لوحة التحكم قبل الموافقة |
| مركز الموافقات (Super Admin) يرى الطلب الجديد | ✅ نجح | تحقّق حي |
| تسجيل الدخول (تاجر + Super Admin) | ✅ نجح | نمط انتقال معروف `/login → / → /الوجهة` خلال ~2 ثانية (سلوك Next.js عادي، ليس عطلاً) |
| لوحة التحكم (نظرة عامة) | ✅ نجح | |
| جهات الاتصال (إضافة) | ✅ نجح | العدد يزيد فعلياً بعد الإضافة |
| صندوق المحادثات (رد) | ✅ نجح | |
| الحملات (إنشاء + إرسال) | ✅ نجح | حالة تتحول لـ"مجدولة" فعلياً؛ حد الجمهور/الرسائل يُنفَّذ من الخادم فعلياً (Soft-lock حقيقي) |
| الشات بوت (فتح المحرر، عرض التدفق) | ✅ نجح | محتوى حقيقي (خطوات فعلية، عقدة "رد ذكي" تعرض حدودها الصادقة بوضوح) |
| ربط Meta/Zid/Salla (Sandbox) | لم يُعَد اختباره في هذه الجولة تحديداً | مُدقَّق ومؤكَّد في جولات سابقة موثَّقة في `QA_REPORT.md`؛ توقيع الـwebhook تم التحقق منه فعلياً هذه الجولة (401 حقيقي على توقيع مزوَّر) |
| تغيير الباقة (Billing) | ✅ نجح | فاتورة حقيقية برقم تسلسلي وVAT محسوب؛ تسابق متزامن حقيقي (طلبان في وقت واحد) اجتاز بلا فساد بيانات |
| انتهاء الاشتراك → PAST_DUE → منع إنشاء حملة جديدة | ✅ نجح | سكربت اختبار حي: تصفير `currentPeriodEnd` يدوياً → فحص → PAST_DUE → بوابة الحملات تعكسه فوراً |
| صفحة 404 مخصّصة | ✅ نجح | تحقّق حي عبر `curl` |
| رؤوس الأمان (HSTS/CSP/إلخ) | ✅ نجح | تحقّق حي عبر `curl -I`، وPlaywright (صفر أخطاء CSP) |
| توقيع Webhook مزوَّر (Meta/Zid/Salla) | ✅ نجح (رُفض) | 401 حقيقي على توقيع غير صالح، مسجَّل في `WebhookLog` |
| عزل المستأجرين (RLS) تحت هجوم فعلي | ✅ نجح (7/7 دفاعات) | قراءة/تعديل/حذف بيانات تينانت آخر، وحقن SQL عبر tenantId — كلها رُفضت |

**فشل واحد في اختبارات الدخان الآلية (`scripts/smoke-test.js`) كان اختباراً قديماً بمحدِّدات نصّية
عفا عليها الزمن من جولات تصميم سابقة (نص "الشات بوت"→"الأتمتة"، و"لوحة رسم التدفق" مخفي خلف تبويب
"عرض متقدم" الجديد) — تم إصلاح المحدِّد الأول في السكربت نفسه، والثاني تحقَّق يدوياً أن الصفحة تعمل
بشكل صحيح فعلياً (المحرر يفتح، يعرض محتوى حقيقياً، القيمة الافتراضية الآن "عرض بسيط" وليس "متقدم").
اختبار `smoke-super-admin.js` لسيناريو جلستين متزامنتين واجه نفس عطل `waitForURL` المعروف مسبقاً في
هذه الجلسة تحت تزامن حقيقي لسياقَي متصفح — تحقَّق يدوياً أن تسجيل الدخول الفردي لـSuper Admin يعمل
بشكل صحيح تماماً (يستقر على `/admin` خلال ~2 ثانية).**

---

## 3. حالة الباقات والفوترة — تفصيلياً

- **مزوّد الدفع**: وهمي بالكامل (`mockPaymentProvider`) — يُعيد نجاحاً فورياً بلا أي اتصال حقيقي.
  **لا يمكن تحصيل أي مبلغ حقيقي من أي تاجر حالياً.** هذا هو الحاجز المركزي الوحيد أمام الإطلاق
  التجاري الفعلي (تفصيله في قسم Blockers أدناه).
- **حدود الاستخدام**: مُنفَذة فعلياً من الخادم (وليس عرضاً فقط) لـ: عدد الحملات/شهر، حجم الجمهور
  لكل حملة، حصة الرسائل الشهرية، عدد أعضاء الفريق، عدد تدفقات الشات بوت. `maxWhatsappNumbers`
  تسويقي فقط لكن غير قابل للتجاوز تقنياً (قيد `@@unique` في المخطط).
- **دورة حياة الاشتراك**: TRIALING/ACTIVE تتحول تلقائياً الآن لـPAST_DUE عند انتهاء الفترة (مهمة
  مجدولة كل ساعة، مُصلَحة هذه الجولة) — يمنع إنشاء حملات جديدة، ولا يحظر تسجيل الدخول عمداً (لتفادي
  حبس تاجر بلا وسيلة تعافٍ، خصوصاً أن الدفع وهمي أصلاً).
- **الفواتير**: رقم تسلسلي حقيقي (تسلسل Postgres) + VAT محسوب للشفافية (15%، بلا تغيير في المبلغ
  المُحصَّل الفعلي) — لكن **ليست** فوترة إلكترونية معتمدة من ZATCA (تحتاج ختماً تشفيرياً وربطاً
  حقيقياً مع Fatoora، قرار قانوني/عمل خارج النطاق).
- **التسابق (Race Conditions)**: `changePlan` مُحصَّن الآن بقفل صفّ صريح (`SELECT ... FOR UPDATE`)
  — اختُبِر فعلياً بطلبين متزامنين حقيقيين، اجتاز بلا فساد بيانات. الحد المتبقي (منع فاتورتين من
  طلبين متتاليين فعليين، وليس متزامنين) يحتاج مفتاح تكرار (Idempotency Key) من مزوّد دفع حقيقي.
- **لا تخزين بطاقات خام**: مؤكَّد (لا حقول بطاقة في المخطط، لا بيانات بطاقة في أي كود).

---

## 4. Blockers خارج نطاق الكود (تحتاج قراراً بشرياً/تجارياً حقيقياً)

1. **مزوّد دفع حقيقي**: اختيار مزوّد (Moyasar/PayTabs/HyperPay/Tap أو غيره)، فتح حساب تاجر حقيقي،
   مفاتيح API إنتاج، وبناء endpoint webhook خاص به. بدون هذا، **المنصة لا يمكنها تحصيل أي إيراد
   حقيقي** — هذا الحاجز الوحيد الحقيقي أمام إطلاق تجاري كامل.
2. **فوترة إلكترونية معتمدة (ZATCA)**: إن كان الإطلاق يستهدف السوق السعودي رسمياً بفواتير معتمدة
   ضريبياً، يلزم ربط حل فوترة إلكترونية معتمد (Fatoora Phase 2) — قرار قانوني/تقني خارج نطاق تعديل
   الكود وحده.
3. **مراقبة أخطاء حقيقية (APM)**: حساب Sentry/Datadog أو مشابه + DSN حقيقي.
4. **جدولة النسخ الاحتياطي والتخزين الخارجي**: السكربت الفعلي جاهز (`scripts/backup-db.sh`)، لكن
   ربطه بمجدوِل حقيقي (cron على خادم إنتاج فعلي) وشحن النسخ لتخزين خارجي (S3 أو مشابه) يعتمد على
   بيئة الاستضافة الفعلية غير المُقرَّرة بعد.
5. **مراجعة قانونية لسياسة الخصوصية والشروط**: المحتوى الحالي حقيقي ومحدَّد (وليس نموذجاً فارغاً)
   ويصف بدقة السلوك الفعلي للمنصة، لكنه غير كافٍ قانونياً لإطلاق تجاري بمعايير كاملة (لا فترة
   احتفاظ بالبيانات، لا ذكر صريح للأطراف الثالثة التي تُشارَك معها البيانات — Meta/زد/سلة/مزوّد
   الاستضافة، لا بند اختصاص قضائي/قانون حاكم). يحتاج مراجعة محامٍ مختص قبل التوسّع لتجار حقيقيين
   بأعداد كبيرة.
6. **ترقية Next.js الكاملة لإصدار 16**: الثغرة **الحرجة** الفعلية أُصلحت بترقية آمنة غير كسرية
   (14.2.35). البواقي (High) في `npm audit` غير قابلة للاستغلال فعلياً في هذا المشروع تحديداً (لا
   `middleware.ts`، لا `next/image` مع `remotePatterns`، لا `rewrites`/`redirects`، لا خادم مخصص) —
   موصى بها كبند **High** أول أسبوع بعد الإطلاق مع خطة اختبار انحدار مستقلة، وليست Blocker فورياً.
7. **`uuid`/`archiver` (عبر `exceljs`)**: تأجيل مدروس — الإصلاح التلقائي أسوأ من الثغرة الحالية.

---

## 5. القرار النهائي

### ⚠️ جاهز للإطلاق بشروط

**المنصة تقنياً وأمنياً سليمة وجاهزة للتشغيل الحقيقي** — عزل المستأجرين مُختبَر عدوانياً وصامد،
RBAC مُنفَذ من الخادم، المصادقة مُحصَّنة (bcrypt + rate limiting + جلسات محدودة)، Webhooks موقَّعة
ومتحقَّق منها فعلياً، رؤوس الأمان مطبَّقة، لا حقن SQL/XSS مكتشَف، حدود الاستخدام مُنفَذة من
الخادم، ودورة حياة الاشتراك تعمل تلقائياً الآن.

**الشرط الوحيد الحقيقي والحاسم لبدء تحصيل إيرادات فعلية**: ربط مزوّد دفع حقيقي (البند 1 في قسم
Blockers) — بدونه، يمكن إطلاق المنصة لتجار حقيقيين على أساس **تجريبي مجاني (Trial-only)** بأمان
تام، لكن **لا يمكن تحصيل أي اشتراك مدفوع فعلياً** إلى أن يُربَط مزوّد دفع حقيقي.

**شروط إضافية موصى بها قبل التوسّع الكامل لأعداد كبيرة من التجار الحقيقيين** (وليست حاجزة للإطلاق
المحدود/التجريبي):
- مراجعة قانونية لسياسة الخصوصية/الشروط (البند 5).
- ربط مزوّد مراقبة أخطاء حقيقي (البند 3).
- جدولة فعلية للنسخ الاحتياطي على بيئة الإنتاج الحقيقية (البند 4).
- ترقية Next.js لإصدار 16 خلال الأسبوع الأول (البند 6) — غير حاجزة لأن الثغرة الحرجة الفعلية
  أُصلحت بالفعل، والبواقي غير قابلة للاستغلال في هيكلية هذا المشروع تحديداً.
