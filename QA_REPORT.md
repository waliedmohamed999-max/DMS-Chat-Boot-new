# تقرير ضمان الجودة (QA_REPORT.md)

**تاريخ البناء**: 2026-07-22
**الحالة العامة**: منصة تعمل فعلياً end-to-end على بيانات ومزودي بيانات حقيقيين (Postgres + Redis
حقيقيان عبر Docker)، مع وضع sandbox واضح المعالم للتكاملات الخارجية (Meta/زد/سلة) التي لا تتوفر
لها مفاتيح API حقيقية في بيئة البناء. **جاهزة كعرض تجريبي (demo) قابل للاستخدام الفعلي محلياً،
وتحتاج الخطوات المذكورة في "قبل الإطلاق التجاري الفعلي" أدناه قبل بيعها فعلياً لتجار حقيقيين.**

---

## -5. إضافة: مسار ربط Meta/واتساب المتكامل + إدارة القوالب واعتمادها (جولة 2026-07-23 الثامنة)

كان ربط واتساب زر "ربط الآن" وحيد بلا تفاصيل، ولوحة قوالب الرسائل تعتمد موافقة داخلية من مالك المنصة
كمحاكاة لمراجعة Meta. أُعيد بناء المسارين: معالج ربط حقيقي بخطوتين + لوحة صحة اتصال، ووحدة قوالب
ترسل فعلياً لمراجعة Meta وتتحدث حالتها تلقائياً عبر webhook.

### نتائج بوابة التحقق الذاتي (مطابقة لبنود القسم 6 من Master Prompt هذه الجولة)

1. **مسار الربط يفتح صندوق المحادثات فعلياً لرسالة واردة حقيقية**: ✅ تحقُّق فعلي بمتصفح Chromium
   حقيقي (`scripts/smoke-meta-templates.js`) — رُبط تاجر تجريبي جديد (`tenant-b`، غير متصل مسبقاً)
   عبر معالج "تسجيل الدخول المضمّن"، ثم أُرسل **webhook حقيقي كامل** (نفس شكل حمولة Meta الفعلي)
   إلى `/api/webhooks/meta` (لا اختصار اختباري، نفس نقطة النهاية التي يستخدمها Meta الحقيقي) بمعرّف
   الرقم الفريد الذي وُلِّد فعلياً لهذا التاجر عند الربط — وظهرت محادثة جديدة فعلياً في صندوق
   المحادثات خاصته خلال ثوانٍ، دون أي كود اختباري موازٍ لمسار الإنتاج.
2. **إنشاء قالب وإرساله للمراجعة، وتحديث حالته تلقائياً**: ✅ تحقُّق فعلي — قالب جديد أُنشئ وأُرسل،
   ظهر فوراً بحالة "بانتظار موافقة Meta"، وبعد 6 ثوانٍ (محاكاة زمن مراجعة واقعي) تغيّرت حالته في
   قاعدة البيانات فعلياً (تحقُّق مباشر بـ`psql`، وليس افتراضاً) إلى `APPROVED` دون أي تحديث يدوي أو
   إعادة تحميل مبادَرة من المستخدم — العامل (`npm run worker`) استلم مهمة BullMQ مؤجَّلة وطبّق القرار.
3. **سيناريو رفض قالب يعرض السبب الفعلي + زر تعديل وإعادة إرسال**: ✅ تحقُّق فعلي — قالب مرفوض (من
   بيانات الزرع، بسبب رفض واقعي نصي فعلي) عرض السبب بوضوح في القائمة، وزر "تعديل وإعادة إرسال" فتح
   المعالج مسبق التعبئة بكامل محتوى القالب الأصلي (الفئة، الاسم، النص) جاهزاً لإعادة الإرسال.
4. **موظف (Agent) لا يصل لهذا المسار إطلاقاً حتى بالرابط المباشر**: ✅ تحقُّق فعلي — كل من
   `/dashboard/integrations` و`/dashboard/templates` و`/dashboard/templates/new` تحجب المحتوى بالكامل
   (رسالة رفض ودّية أو إعادة توجيه خادمية لصفحة "ليس لديك صلاحية")، وليس فقط إخفاء أزرار الإدارة —
   وهذه فجوة RBAC حقيقية كانت موجودة في الصفحتين قبل هذه الجولة (كانتا تعرضان المحتوى الكامل لأي دور).

### أهم اكتشافين تقنيين أثناء هذه الجولة

1. **تصادم معرّف رقم واتساب بين تجار متعددين في Sandbox**: محاكاة الربط كانت تُسنِد نفس القيمة
   الثابتة لكل تاجر يربط حسابه عبر Embedded Signup — لم يظهر كخطأ في جولات سابقة لأن الـwebhook لم
   يكن يُطابَق فعلياً بمعرّف الرقم لتوجيه الرسائل (كان يُسجَّل فقط). بعد أن أصبحت المطابقة حقيقية
   (الجولة السابعة)، اكتُشف هذا أثناء **تصميم** اختبار ربط تاجرين مختلفين (قبل حتى تشغيله) وأُصلح
   فوراً بتوليد معرّف فريد لكل تاجر — تفاصيل كاملة في `DECISIONS.md` (قرار 53).
2. **`sendMessage` كان يعتمد على إعداد المنصة العام بدل حالة اتصال التاجر نفسه**: لو رَبَط تاجر رقماً
   حقيقياً فعلياً بينما إعداد المنصة العام لا يزال "sandbox" (الحالة الافتراضية الطبيعية لبقية
   التجار)، كانت رسائله ستُرسَل محاكاةً رغم ربطه الفعلي — يناقض جوهر هذه الجولة. أُصلح ليعتمد على
   `Integration.isSandbox` الخاص بهذا التاجر تحديداً (تفاصيل في `DECISIONS.md`، قرار 52).

---

## -4. إضافة: صندوق محادثات واتساب حقيقي متكامل (جولة 2026-07-23 السابعة)

كان صندوق المحادثات قائمة جانبية بلا شاشة محادثة فعلية فعّالة، والـwebhook الوارد من Meta يُسجَّل
فقط دون أي معالجة حقيقية. أُعيد بناء الوحدة بالكامل: ربط webhook فعلي، شاشة محادثة كاملة بكل أنواع
الوسائط، تصنيف حالة حقيقي، تعاون فريق، وبحث/فلترة.

### نتائج بوابة التحقق الذاتي (مطابقة لبنود القسم 5 من Master Prompt هذه الجولة)

1. **رسالة واردة (محاكاة واقعية في Sandbox) تظهر فوراً دون تحديث يدوي**: ✅ تحقُّق فعلي بمتصفح
   Chromium حقيقي (`scripts/smoke-inbox.js`) — الضغط على زر محاكاة "💬 نص" داخل محادثة مفتوحة، ثم
   الانتظار لدورة استطلاع واحدة فقط لـ`InboxLivePoller` (4 ثوانٍ) **دون أي `page.reload()`**، أظهر
   زيادة فعلية في عدد فقاعات الرسائل المعروضة (من 5 إلى 6) — تحديث حي حقيقي عبر `router.refresh()`
   المُطلَق تلقائياً عند اكتشاف رسالة جديدة، وليس افتراضاً نظرياً.
2. **رد من الواجهة يُتتبَّع بالكامل (أُرسل ← تم التسليم ← تمت القراءة) لحظياً**: ✅ تحقُّق فعلي —
   فور الإرسال ظهرت علامة ✓ واحدة (SENT)، وبعد ~1.5 ثانية (دون أي تفاعل إضافي من المستخدم) تحوّلت
   تلقائياً لـ✓✓ (DELIVERED) عبر مهمة BullMQ مؤجَّلة حقيقية تحدّث قاعدة البيانات فعلياً، وليس تأثيراً
   بصرياً وهمياً في الواجهة فقط.
3. **انتهاء نافذة الـ24 ساعة يجبر استخدام قالب معتمد بدل النص الحر**: ✅ تحقُّق فعلي — بعد ضبط
   `sessionWindowExpiresAt` لمحادثة حقيقية في الماضي مباشرة عبر قاعدة البيانات (محاكاة انتهاء
   النافذة، إذ لا يمكن انتظار 24 ساعة فعلية أثناء الاختبار)، اختفى حقل النص الحر تماماً من الواجهة
   (0 عنصر) وظهر منتقي قوالب معتمدة إلزامي بدلاً منه — إنفاذ فعلي في الواجهة **و**في الخادم
   (`sendReply` يرفض أي محاولة نص حر بعد انتهاء النافذة حتى لو استُدعيت مباشرة).
4. **إسناد محادثة لعضو فريق آخر يظهر فعلياً في محادثاته**: ✅ تحقُّق فعلي — إسناد محادثة لحساب
   "مدير متجر الأناقة للعطور" من واجهة المالك، ثم تسجيل الدخول بحساب المدير نفسه وتصفية القائمة
   حسب "العضو المسنَد إليه"، أظهر المحادثة فعلياً في نتائجه — وليس افتراضاً أن الإسناد "يجب أن يعمل".
5. **الملاحظات الداخلية لا تصل للعميل تحت أي ظرف**: ✅ تحقُّق فعلي على مستويين — الملاحظة ظهرت في
   لوحة "ملاحظات داخلية" الجانبية، **و**تم التحقق مباشرة من قاعدة البيانات أن لا صف `Message` جديد
   أُنشئ بنفس نص الملاحظة (العدد = 0) — تحقق من غياب التسريب على مستوى البيانات نفسها، وليس فقط
   عدم ظهورها بصرياً في منطقة الفقاعات (ضمان بنيوي: `InternalNote` جدول مستقل تماماً عن `Message`).

### أهم اكتشاف تقني أثناء هذه الجولة: خطأ حقيقي في منطق حالة المحادثة كان موجوداً من الجولة الأولى
أثناء فحص الكود القائم قبل البناء، اكتُشف أن `sendReply` القديمة كانت تضع **كل** رد من الموظف في
حالة `RESOLVED` (محلولة) تلقائياً — بصرف النظر عن كون المحادثة فعلاً مُغلَقة أم لا. هذا يعني أن أي
محادثة رُدَّ عليها مرة واحدة كانت تختفي من تبويب "يحتاج رد" **وتظهر كمحلولة زوراً** حتى لو كان
الموظف ينتظر رد العميل التالي فعلياً. أُصلح جذرياً عبر آلة حالة صريحة (`lib/inbox/conversationStatus.ts`)
تفرّق بين "رُدَّ عليه ويُنتظر العميل" (`OPEN`) و"أُغلق يدوياً فعلاً" (`RESOLVED`) — لا مسار تلقائي
يصل لـ`RESOLVED` بعد الآن إطلاقاً.

---

## -3. إضافة: وحدة الحملات المتكاملة End-to-End (جولة 2026-07-23 السادسة)

كانت صفحة "الحملات" جدولاً ساكناً بلا معالج إنشاء حقيقي، بلا صفحة تفاصيل كاملة، وحملة "استرداد
السلة" الوحيدة في البيانات التجريبية كانت تبدو حملة عادية "مكتملة" بدل حملة آلية مستمرة. أُعيد بناء
الوحدة بالكامل: معالج إنشاء متعدد الخطوات، شرائح جمهور بفلاتر حقيقية، صفحة تفاصيل مع تتبع تحويل
إيراد فعلي، وفصل واضح بين الحملات لمرة واحدة والحملات الآلية.

### نتائج بوابة التحقق الذاتي (مطابقة لبنود القسم 4 من Master Prompt هذه الجولة)

1. **إنشاء حملة بشريحة جمهور مبنية على فلتر حقيقي ومطابقة العدد المعروض للفعلي**: ✅ تحقُّق فعلي —
   فلتر "مرحلة العميل = عميل" على بيانات تنانت-a التجريبية (خمس جهات اتصال، اثنتان بمرحلة CUSTOMER،
   إحداهما ألغت موافقتها على الرسائل التسويقية) أظهر العدد الحي **1** في معالج الإنشاء — مطابق
   حسابياً تماماً (استُبعدت جهة الاتصال الأخرى تلقائياً بسبب `marketingOptIn=false`، وليس لأن العدد
   "بدا صحيحاً" — تحقّق عبر `scripts/smoke-campaigns.js` بمتصفح Chromium حقيقي).
2. **حملة تتجاوز حصة الباقة → قفل + توجيه للترقية بدل إرسال فعلي**: ✅ منطق الفحص موجود وينفَّذ
   فعلياً من الخادم في `createCampaign` (يقارن حجم الجمهور بـ `maxAudiencePerCampaign` وبرصيد
   الرسائل المتبقي `maxMessagesPerMonth - messagesUsedThisPeriod` **قبل** إنشاء أي صف Campaign،
   ويرفض برسالة `CampaignLimitError` تُترجم في الواجهة لنافذة ترقية) — تحقُّق الكود مباشرة وليس فقط
   تعطيل زر بالواجهة. لم يُختبر السيناريو حياً بمتصفح لأن تجاوز الحصة الفعلية لتينانت تجريبي يتطلب
   إما تعديل يدوي لرصيد `Subscription.messagesUsedThisPeriod` عبر `psql` مباشرة أو إنشاء عشرات
   الحملات المتتالية لاستهلاك الحصة — وثِّق هذا صراحة كفجوة تحقق (وليس إخفاءً) في القسم أدناه.
3. **صفحة تفاصيل حملة تعرض حالة كل مستلم فعلياً + سبب الفشل**: ✅ تحقُّق فعلي — حملة "حملة عرض
   الصيف" التجريبية تعرض 4 مستلمين بحالات مختلفة (REPLIED/READ/DELIVERED/FAILED) مع سبب فشل واقعي
   نصي ("الرقم محظور أو أوقف استقبال الرسائل التسويقية")، بالإضافة لإيراد تحويل حقيقي **265 ر.س**
   مُسنَد تلقائياً (وليس مُلفَّقاً في بيانات الزرع) عبر `linkCampaignConversions` الذي يُنفَّذ حياً
   عند فتح الصفحة، رابطاً طلباً حقيقياً بالعميل بالاسم خلال نافذة 7 أيام من إرسال الرسالة له.
4. **حملة استرداد السلة تظهر كحملة آلية مستمرة وليست "مكتملة"**: ✅ تحقُّق فعلي — تبويب "الحملات
   الآلية" يعرضها بحالة "🟢 نشطة" (وليس "مكتملة")، مع إحصائية تراكمية حقيقية ("مرات التشغيل"،
   "إيراد مسترد") تُحسب من عدد صفوف `CampaignRecipient` الفعلي ومجموع `conversionRevenueSar`، وليس
   عدّادات ثابتة. زر "🔍 فحص الآن" اختُبر حياً: زُرعت سلة متروكة إضافية جديدة بلا `CampaignRecipient`
   سابق، وبعد الضغط على الزر ارتفع "مرات التشغيل" من 1 إلى 2 فعلياً في نفس الجلسة (وليس منطقاً
   نظرياً فقط) — العامل الفعلي (`npm run worker`) استلم مهمة BullMQ حقيقية وأرسل الرسالة، وزاد
   `Subscription.messagesUsedThisPeriod` بالضبط بمقدار الرسائل المُرسلة (3400 → 3402 لرسالتين
   فعليتين خلال هذه الجولة من الاختبار — تحقُّق مباشر بقاعدة البيانات وليس افتراضاً).

### أهم اكتشاف تقني أثناء هذه الجولة: عملية العامل (`npm run worker`) لم تكن تعمل أصلاً

أثناء الاختبار الأول لهذه الجولة، اكتشفتُ أن جلسة العمل الحالية كانت تُشغِّل خادم Next.js للتطوير
فقط (`npm run dev`) بدون عملية العامل المنفصلة (`npm run worker`) — ما يعني أن أي حملة تُنشأ تبقى
"مجدولة" (`SCHEDULED`) إلى الأبد لأن لا شيء يستهلك قائمة انتظار BullMQ فعلياً. هذا لم يكن خللاً في
الكود، بل في بيئة التشغيل نفسها أثناء الاختبار. بعد تشغيل `npm run worker` كعملية منفصلة، أُعيد
الاختبار الحي كاملاً وتأكّد وصول الحملات فعلياً لحالة `COMPLETED` بعدادات حقيقية. **هذا يعني أن أي
نشر إنتاجي فعلي لهذه المنصة يحتاج عملية عامل دائمة التشغيل (`npm run worker`) كخدمة منفصلة مستقلة عن
خادم الويب** — موثّق هنا صراحة لأنه متطلب تشغيلي حقيقي وليس تفصيلاً بسيطاً.

### ما تم حله من فجوة كانت موثّقة سابقاً: جدولة الحملات + الحملات الآلية عبر BullMQ بدل Cron منفصل
الفجوة المذكورة سابقاً في هذا الملف ("لا Cron/Scheduler للحملات المجدولة") مُعالجة الآن جزئياً بشكل
حقيقي: الحملات المجدولة بتاريخ مستقبلي تُستخدَم عبر خيار `delay` المدمج في BullMQ (`enqueueCampaignSend`
مع `delayMs`)، والحملات الآلية تُفحص كل 5 دقائق عبر مهمة BullMQ متكررة (`repeat.every`) بدل احتياج
خدمة cron منفصلة بالكامل — نفس البنية التحتية للقوائم المستخدمة أصلاً، بدون اعتماد تقني جديد. **هذا
لا يُلغي الحاجة لعملية عامل دائمة** (الفقرة أعلاه)، لكنه يحل مشكلة "غياب آلية التنفيذ المؤجل نفسها".

---

## -2. إضافة: لوحة مالك المنصة كغرفة تحكم شاملة (جولة 2026-07-23 الرابعة)

طلب المستخدم توسيع لوحة مالك المنصة لتكون "اللوحة الأساسية التي بتدير وتبني منها كل شيء في
المنصة". حُدِّدت 4 فجوات عبر سؤال توضيحي (بدل التخمين) واختار المستخدم الأربعة معاً. كل ما يلي
تحقّق منه Playwright فعلياً عبر `scripts/smoke-admin-v2.js`:

### ✅ إدارة الباقات الكاملة (CRUD)
أنشأنا باقة جديدة بالكامل (اسم، سعر 777 ر.س، حدود مستخدمين/أرقام واتساب/رسائل، ميزتان) من
`/admin/plans` مباشرة → ظهرت فعلياً في الشبكة فوراً. عطّلنا نفس الباقة → الشارة تحوّلت لـ "معطّلة"
فعلياً. كل بطاقة باقة تعرض عدد المشتركين الحقيقي الحالي عليها.

### ✅ تبويبات بيانات شاملة لكل تاجر
فتحنا كل التبويبات الستة (الفريق، جهات الاتصال، الحملات، الشات بوت، قوالب الرسائل، الطلبات
والمنتجات) لتاجر حقيقي (`tenant-a`) والتأكد من عرض محتوى حقيقي مختلف في كل تبويب — وليس نفس
المحتوى مكرراً أو صفحة فارغة.

### ✅ موافقة قوالب الرسائل (تحاكي مراجعة Meta الفعلية)
تاجر قدّم قالب رسالة جديداً من `/dashboard/templates` → ظهر فوراً بحالة "بانتظار موافقة Meta"
(وليس معتمداً تلقائياً). ظهر الطلب في `/admin/approvals` بنفس واجهة الموافقات الموحّدة (نوع
`MESSAGE_TEMPLATE` الجديد). بعد موافقة مالك المنصة، رجع التاجر لصفحته ووجد القالب "معتمد" فعلياً.

### ✅ إعدادات المنصة العامة — وضع الصيانة
فعّلنا وضع الصيانة من `/admin/settings` مع رسالة مخصصة → تاجر حاول الدخول لحسابه فوراً بعدها ورأى
شاشة "المنصة تحت الصيانة حالياً" بدل لوحته (وليس بعد تأخير أو تحديث يدوي). أوقفنا الوضع → عاد
الوصول الطبيعي فوراً لنفس التاجر دون تسجيل خروج/دخول.

### 🐛 خطأ حقيقي اكتُشف وأُصلح في هذه الجولة تحديداً
`admin/settings/actions.ts` كرّر **نفس فئة خطأ RLS** المكتشفة 3 مرات في جولات سابقة (استخدام
اتصال خاضع لـ RLS بدل BYPASSRLS لكتابة سجل تدقيق) — رغم توثيقها الصريح مسبقاً في `DECISIONS.md`
والتعليقات البرمجية. هذا يؤكد أن التوثيق وحده غير كافٍ لمنع تكرار خطأ معروف؛ فقط الاختبار الفعلي
كشفه (عبر خطأ 500 حقيقي عند تفعيل/تعطيل وضع الصيانة). تم تدقيق الكود بحثاً عن حالات مشابهة أخرى
ولم توجد غيرها. يُنصح مستقبلاً بقاعدة lint آلية ترفض `rawDb` داخل أي `src/app/admin/**/actions.ts`.

---

## -1. إضافة: لوحة مالك المنصة كغرفة تحكم فعلية (جولة 2026-07-23 الثالثة)

نتائج التحقق الذاتي الستة المطلوبة صراحة في هذا البرومنت، كلها بمتصفح Chromium فعلي عبر
`scripts/smoke-super-admin.js` (وليس مراجعة كود نظرية):

### ✅ 1. تاجر جديد يظهر في "بانتظار المراجعة" قبل أي تفعيل تلقائي
سجّلنا تاجراً جديداً فعلياً عبر `/register`، سجّلنا دخوله فوراً بعدها → عرضت له المنصة شاشة
"حسابك بانتظار المراجعة" (وليس لوحة التحكم). فتحنا `/admin/approvals` بحساب مالك المنصة → الطلب
ظاهر فعلياً ببيانات النشاط التجاري والباقة المطلوبة.

### ✅ 2. تعليق تاجر نشط يرفض دخوله فعلياً فوراً
علّقنا `tenant-b` من صفحة تفاصيله، ثم حاولنا تسجيل الدخول بحساب `owner@tenant-b.sa` مباشرة →
رُفض الدخول برسالة "تم تعليق حساب متجرك مؤقتاً" والمستخدم بقي على `/login` (لم تُصدَر أي جلسة
إطلاقاً — الفحص داخل `authorize()` نفسه، وليس بعد الدخول). أُعيد تفعيل الحساب فوراً بعد التأكد.

### ✅ 3. تعديل حد باقة من لوحة الباقات يؤثر فوراً على كل تاجر عليها دون تعديل كود
غيّرنا "أقصى عدد تدفقات" لباقة الأساسية من 2 إلى 7 من `/admin/plans`، ثم فتحنا `/dashboard/chatbot`
بحساب تاجر على هذه الباقة فعلياً (`owner@tenant-b.sa`) → النص المعروض تحوّل فوراً لـ "3 / 7
تدفقات مستخدمة" بدون أي إعادة نشر أو تعديل كود. أُعيد الحد لقيمته الأصلية بعد التأكد.

### ✅ 4. Impersonate يظهر في Audit Log لكلا الطرفين
بدأنا جلسة انتحال لـ `tenant-a` من صفحة تفاصيله → ظهر شريط تحذير أحمر دائم. تحققنا من **سجلّين
منفصلين**: `platform.impersonation_start` في سجل تدقيق المنصة الشامل الجديد (`/admin/audit-log`)،
و`support.impersonation_started` في سجل نشاط التاجر نفسه (`/dashboard/settings`) — الطرفان يريان
الحدث. إنهاء الجلسة أعاد مالك المنصة للوحته الأصلية دون أي إعادة تسجيل دخول.

### ✅ 5. حساب Support Staff لا يصل للفوترة أو حذف تاجر
حساب `support@platform.sa`: رابط "الإيرادات والفوترة" غير ظاهر في القائمة الجانبية، **والوصول
المباشر لرابط `/admin/billing` عبر الرابط محظور خادمياً** (رسالة "ليس لديك صلاحية" وليس تحميل
الصفحة) — هذا اكتُشف كخطأ حقيقي أول مرة (الصفحة كانت ترمي 500 خام) وأُصلح ليكون حظراً ودياً
متسقاً. زر "حذف نهائي" غير ظاهر إطلاقاً في صفحة تفاصيل التاجر لنفس الحساب. بالمقابل، حساب
`billing@platform.sa` يصل لصفحة الفوترة فعلياً لكن **يُحظر من صفحة تفاصيل أي تاجر** (لا وصول
لبيانات محادثات/فريق التاجر)، تطبيقاً حرفياً لبند الفصل بين الأدوار.

### 🐛 أخطاء حقيقية اكتُشفت وأُصلحت في هذه الجولة (تفصيل كامل في CHANGELOG.md وDECISIONS.md §20)
1. 3 صفحات إدارة كانت تُصدّر خطأ 500 خام بدل رسالة صلاحية ودية عند وصول غير مخوَّل.
2. ثغرة تصريح: `dismissAnnouncement` كانت تثق بـ `tenantId` من العميل مباشرة بدل اشتقاقه من الجلسة.
3. تسريب كتالوج: باقات مخصصة (Enterprise) لتاجر كانت تظهر في كتالوج بقية التجار.
4. خطأ نسخ-لصق: حد "تدفقات الشات بوت" في صفحة تفاصيل التاجر كان يُحسب من حقل خاطئ تماماً.

### ⚠️ فجوة كانت موجودة ولم تُكتشف إلا بمراجعة نص البرومنت نفسه (وليس بالاختبار)
لم توجد أي صفحة لعرض Audit Log لمالك المنصة رغم أن كل الإجراءات كانت تُسجَّل فعلياً منذ الجولة
الأولى — أُضيفت `/admin/audit-log` في هذه الجولة تحديداً بسبب بند "يجب أن تكون [اللوحة] مراقبة
ذاتياً" الصريح في البرومنت. هذا تذكير بأن الاختبار الآلي وحده لا يكفي — قراءة متطلبات البرومنت
بندًا ببند ضرورية لاكتشاف "غياب ميزة" (وليس فقط "خطأ في ميزة موجودة").

### ملاحظة صدق: التواصل الجماعي in-app فقط
لا مزود بريد إلكتروني حقيقي مربوط (SendGrid/SES) في هذه البيئة، فالتواصل الجماعي (`/admin/
announcements`) يعمل كإشعار داخل لوحة التاجر فقط (in-app)، حقيقي وفعّال بالكامل، لكن ليس بريداً
إلكترونياً فعلياً — موثّق بوضوح في الصفحة نفسها. ربط مزود بريد حقيقي = إضافة adapter واحد فقط.

---

## 0. إضافة: وحدة الشات بوت — بوابات الباقة والأدوار (جولة 2026-07-22 الثانية)

نتائج التحقق الذاتي الخاصة بإعادة بناء وحدة الشات بوت (قوالب جاهزة، تفرّع شرطي، معاينة واتساب
حية، بوابات باقة وأدوار). كل الفحوصات التالية أُجريت بمتصفح Chromium فعلي (Playwright) عبر
`scripts/smoke-chatbot.js`، وليس مراجعة كود نظرية.

### ✅ حدود الباقات المطبَّقة فعلياً (Frontend + Backend)
| الباقة | أقصى تدفقات | العقد المتاحة | القوالب | التحليلات |
|---|---|---|---|---|
| الأساسية (Starter) | 2 | بداية/رسالة/سؤال/شرط/تحويل/نهاية | أساسية (3 قوالب) | أساسي |
| الاحترافية (Growth) | 15 | + رد ذكي + استدعاء API | كاملة | متقدم |
| الأعمال (Scale) | غير محدود | كل شيء + لغات متعددة | كاملة | متقدم + تصدير |

- تاجر Starter (`tenant-b`): قالب "الأسئلة الشائعة" ظهر مقفولاً فعلياً بشارة "🔒 يتطلب ترقية"،
  والضغط عليه فتح نافذة ترقية حقيقية. عقدتا "رد ذكي" و"استدعاء API خارجي" ظهرتا معطّلتين بشارة
  "🔒 Pro" في المحرر، والضغط عليهما فتح نفس نافذة الترقية — **وليس فقط إخفاءً بصرياً**.
- تاجر باقة أعلى (`tenant-a`، Growth/Scale): نفس القالب والعقد ظهرت **بدون** أي قفل.
- **إنفاذ خادمي مؤكَّد**: `getTenantChatbotLimits` (المصدر الوحيد للحدود) يُستدعى من server
  actions (`saveFlowNodes`, `createFlowFromTemplate`) قبل أي كتابة — تجاوز الواجهة عبر استدعاء
  مباشر للـ API سيُرفض من نفس المصدر، وليس فقط من شرط في الزر.

### ✅ حدود الأدوار المطبَّقة فعلياً
| الدور | عرض | تعديل | نشر | حذف مسودة | حذف منشور | اختبار |
|---|---|---|---|---|---|---|
| صاحب الحساب | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| مدير | ✅ | ✅ | ✅ | ✅ | ❌ | ✅ |
| موظف | ✅ | ❌ | ❌ | ❌ | ❌ | ✅ |

- حساب `agent@tenant-a.sa`: لا يظهر زر "+ إنشاء تدفق جديد"، والضغط على بطاقة تدفق يوجّهه لصفحة
  اختبار للقراءة فقط (`/dashboard/chatbot/[id]/test`) وليس المحرر.
- **محاولة تجاوز فعلية بالرابط المباشر**: كتابة رابط المحرر (`/dashboard/chatbot/[id]`) مباشرة في
  شريط العنوان بحساب موظف أُعيد توجيهها فعلياً (HTTP redirect من الخادم) لصفحة "ليس لديك صلاحية"
  — تم اختبار هذا بالضبط كما طلب البرومنت ("URL manipulation")، وليس افتراضاً.
- حساب `admin@tenant-a.sa` على التدفق المنشور من بيانات البذر: زر الحذف العلوي **غير ظاهر إطلاقاً**
  (المدير لا يقدر حتى رؤية الزر، فضلاً عن استخدامه) — يحتاج صاحب الحساب لحذف تدفق منشور.

### ✅ شرط "اختبار قبل النشر"
تدفق جديد بحساب `owner@tenant-b.sa`: زر "نشر التدفق" **معطّل فعلياً** فور الإنشاء (لم يُختبر بعد)،
وأصبح **مفعّلاً تلقائياً** بعد تشغيل اختبار حي واحد فقط، دون إعادة تحميل الصفحة. الإنفاذ مزدوج:
تعطيل بصري في الواجهة + رفض خادمي حقيقي في `togglePublish` لو استُدعي مباشرة قبل أي اختبار.

### ✅ معاينة حية بشكل واتساب + مكتبة قوالب
لقطات الشاشة في `.smoke-shots/chatbot-*.png` توثّق: نافذة ترقية القالب المقفول، محرر بثلاثة أعمدة
(خطوات/معاينة واتساب/اختبار حي)، وشارة القفل الصحيحة على كل عقدة حسب الباقة.

### ⚠️ ملاحظة صدق حول التحليلات (Analytics)
مستوى "متقدم/تصدير" في الباقات الأعلى **لا يعرض بيانات محادثات حقيقية مُصطنعة كأنها حقيقية** —
هذا قرار متعمد (راجع DECISIONS.md §11). الأساس الوحيد الصادق المتوفر حالياً هو عدد تشغيلات
"اختبار قبل النشر" الفعلية (`testRunCount`, `nodeTraversalJson`)، لأن محرك تنفيذ الشات بوت الحقيقي
غير مربوط بعد بمسار استقبال رسائل واتساب الواردة الفعلية (نفس الفجوة الموثقة في القسم 3 أدناه منذ
الجولة الأولى). لوحة تحليلات "معدل إتمام/نقطة تسرب" حقيقية تتطلب أولاً ربط هذا المحرك.

### 🐛 أخطاء حقيقية اكتُشفت وأُصلحت أثناء بناء هذه الجولة تحديداً
راجع القائمة الكاملة (6 أخطاء) في `DECISIONS.md §13` و`CHANGELOG.md`. الأهم: **فئة كاملة من
استعلامات RLS صامتة الفشل** كانت تُرجع كل التجار (حتى المشتركين بباقات مدفوعة) إلى حدود الباقة
الأساسية دون أي رسالة خطأ — اكتُشفت فقط لأن الاختبار الفعلي قارن سلوك تاجرين بباقتين مختلفتين
جنباً إلى جنب، وليس لأن الكود "بدا صحيحاً" عند القراءة. تم تدقيق كل استدعاء مشابه في المشروع بعدها.

---

## 1. نتائج بوابة التحقق الذاتي (مطابقة لبنود Master Prompt القسم 7)

### ✅ 1. عزل البيانات بين مستأجرين تجريبيين
اختُبر فعلياً بأربعة مستويات، وليس افتراضاً نظرياً:
- سكربت `prisma/verify-isolation.ts` يعمل ضد Postgres حقيقي (وليس mock)، ويؤكد:
  - Tenant A / Tenant B يريان فقط جهات اتصالهما (صفر تسريب).
  - محاولة Tenant A قراءة صف بعينه من Tenant B بالمعرف المباشر → **مرفوضة من قاعدة البيانات نفسها**.
  - محاولة إدخال صف بـ `tenantId` مزوّر من جلسة مستأجر آخر → **مرفوضة (RLS WITH CHECK)**.
- **اختُبر أيضاً عبر واجهة المستخدم الفعلية** (وليس فقط سكربت DB): تسجيل دخول كـ
  `owner@tenant-a.sa` يعرض "متجر الأناقة للعطور" وجهات اتصاله فقط؛ `owner@tenant-b.sa` يعرض
  "بوتيك لمسة" فقط — تم التحقق عبر `curl` بجلسات منفصلة ولقطات شاشة Playwright.
- **اكتُشفت وأُصلحت مشكلة حقيقية أثناء هذا الاختبار**: صورة Postgres الرسمية تمنح دور
  `POSTGRES_USER` صلاحية superuser تلقائياً، وPostgres يتجاهل RLS كلياً لأي superuser بغض النظر
  عن `FORCE ROW LEVEL SECURITY`. تم إصلاحها بإنشاء دورين محدودين (`app_runtime` بدون تجاوز،
  `app_superadmin` بـ BYPASSRLS فقط دون superuser) — موثّق بالتفصيل في `DECISIONS.md` و`README.md`.

### ✅ 2. الكود يبني بدون أخطاء ولا console errors في الشاشات الأساسية
- `npm run build` (Next.js production build): **نجح بدون أخطاء**، 20 مساراً.
- `npx tsc --noEmit`: **صفر أخطاء TypeScript** (strict mode).
- اختبار دخان فعلي بمتصفح Chromium حقيقي (Playwright، وليس محاكاة) ضد **نسخة الإنتاج**
  (`next build && next start`) عبر `scripts/smoke-test.js`: **صفر console errors** بعد تغطية
  تسجيل الدخول، لوحة التحكم، جهات الاتصال (إضافة+حذف)، الحملات (إنشاء+إرسال)، الشات بوت (تحرير+
  اختبار حي)، التكاملات، الفوترة، الفريق، ولوحة Super Admin.
- `scripts/smoke-register.js` يختبر تسجيل تاجر جديد من الصفر حتى تسجيل الدخول بحسابه — نجح بصفر
  أخطاء ضد نسخة الإنتاج.
- **أخطاء حقيقية اكتُشفت أثناء هذا الاختبار وأُصلحت فعلياً** (وليست افتراضية):
  1. `rawDb.auditLog.create()` في 4 ملفات actions كان يُستدعى خارج سياق المستأجر فيفشل بخطأ RLS
     حقيقي — أُصلح بـ `lib/audit.ts`.
  2. خطأ bidi في عرض RTL: نسب مثل "٣٤٠٠/١٠٠٠٠" كانت تنعكس بصرياً — أُصلح بـ `dir="ltr"` صريح.
  3. خطأ Prisma خام غير مقروء عند تكرار رقم جوال — أُضيفت معالجة صديقة للمستخدم.
  4. علاقة Prisma ناقصة (`CampaignRecipient.contact`) — أُضيفت + migration جديدة.

### ✅ 3. تدفق الحملة والشات بوت يعملان فعلياً من البداية للنهاية
- **الحملات**: تم إنشاء حملة فعلية عبر الواجهة → إرسالها عبر **BullMQ حقيقي متصل بـ Redis حقيقي**
  (وليس محاكاة inline) → عامل منفصل (`npm run worker`) عالجها فعلياً → حالة الحملة تحوّلت
  DRAFT → SCHEDULED → SENDING → COMPLETED خلال ثوانٍ، مع نتائج واقعية لكل مستلم (DELIVERED/FAILED
  بنسبة فشل عشوائية 8% تحاكي أرقاماً غير متاحة على واتساب فعلياً).
- **الشات بوت**: تم إنشاء تدفق، إضافة/حذف/إعادة ترتيب خطوات (شرط، رد AI، تحويل لموظف)، نشره،
  واختباره حياً عبر لوحة "اختبار حي" — الرسالة "أين طلبي؟" حرّكت الشرط بنجاح ثم أعطت رداً واقعياً
  عن حالة الطلب. كل هذا مصوّر في `.smoke-shots/06-chatbot-editor.png` و`07-chatbot-live-test.png`.

### ✅ 4. نقاط ربط Meta/زد/سلة موجودة بواجهة واضحة مع sandbox mode
- صفحة `/dashboard/integrations` تعرض حالة كل تكامل مع شارة "🧪 وضع تجريبي (Sandbox)" واضحة.
- الربط الفعلي (زر "ربط الآن") يعمل ويُنشئ سجل `Integration` حقيقي في قاعدة البيانات (بحالة
  CONNECTED، معرّف حساب وهمي واقعي، توكن مشفّر AES-256-GCM حتى لو وهمي).
- 3 نقاط webhook فعلية وتعمل (`/api/webhooks/{meta,zid,salla}`) بتحقق توقيع HMAC حقيقي، وسجل
  أحداث ظاهر في نفس الصفحة.

### ✅ 5. هذا الملف (QA_REPORT.md)
موجود ولا يطلب أي رد فعل — يلخّص الحالة والمخاطر فقط.

---

## 2. ما هو **حقيقي فعلياً** في هذا البناء (وليس محاكاة سطحية)

| الجزء | الحالة |
|---|---|
| PostgreSQL 16 | حقيقي، عبر Docker، مع RLS مفعّل فعلياً |
| Redis 7 | حقيقي، عبر Docker |
| BullMQ (قوائم الحملات) | حقيقي — عامل منفصل يعالج مهام فعلية |
| Rate Limiting لكل مستأجر | حقيقي — Redis INCR/EXPIRE، مُختبر منطقياً (راجع القسم 4) |
| Row Level Security | حقيقي — مُختبر ضد قاعدة بيانات فعلية بـ 4 سيناريوهات هجوم |
| تشفير مفاتيح التكاملات | حقيقي — AES-256-GCM فعلي (المفتاح نفسه placeholder للتطوير) |
| توقيع Webhooks (HMAC) | حقيقي التنفيذ، لكن **غير مُختبر ضد مفتاح Meta/زد/سلة حقيقي** (لا تتوفر مفاتيح) |
| إرسال رسائل واتساب فعلية | **محاكاة (sandbox)** — لا يوجد اتصال حقيقي بـ Meta Graph API |
| مزامنة طلبات زد/سلة | **محاكاة (sandbox)** — بيانات وهمية ثابتة، ليست استدعاء API حقيقي |
| الدفع (Billing) | **محاكاة (mock)** — لا يوجد Moyasar/PayTabs/Stripe حقيقي متصل |

---

## 3. المخاطر والفجوات المتبقية (بصراحة كاملة)

1. **لا مفاتيح API حقيقية**: Meta/زد/سلة/مزود دفع — المنصة مبنية بحيث ربطها الفعلي = ضبط متغيرات
   بيئة فقط (`INTEGRATIONS_MODE=live` + المفاتيح)، لكن هذا **لم يُختبر فعلياً** لعدم توفر حسابات
   تجريبية حقيقية من Meta/زد/سلة في بيئة البناء. المسار البرمجي (adapters) جاهز ومبني على توثيق
   كل منصة العلني، لكن التكامل الحقيقي الأول سيحتاج تصحيحات طفيفة متوقعة (أشكال payload فعلية،
   rate limits حقيقية من Meta).
2. ~~محرر الشات بوت تسلسلي وليس canvas حر~~ — **✅ تمت الترقية في الجولة الخامسة (2026-07-23)**:
   المحرر الآن لوحة رسم حرة حقيقية عبر `@xyflow/react` (عقد قابلة للسحب، اتصالات مرسومة يدوياً،
   مقبضا "نعم/لا" منفصلان لعقدة الشرط، خريطة مصغّرة). نموذج البيانات تحوّل من `trueNext`/`falseNext`
   إلى `FlowGraph{nodes, edges}` حقيقي كما كان متوقعاً في هذه الملاحظة بالضبط. تفاصيل في
   `DECISIONS.md` (قرارات 26–29) و`CHANGELOG.md`.
3. **لا اختبارات آلية (unit/integration tests) بخلاف سكربتي العزل والدخان**: الاعتماد الحالي على
   `verify-isolation.ts` + `smoke-test.js` + `smoke-register.js` + typecheck + build. لا يوجد
   Jest/Vitest أو CI pipeline. لبيع فعلي طويل الأمد، يُنصح بإضافة اختبارات وحدة لـ `lib/rbac.ts`،
   `lib/integrations/*`، ومنطق الحملات/الفوترة.
4. **لا CI/CD**: لا GitHub Actions أو أي pipeline آلي للبناء/الاختبار عند كل push.
5. **الفوترة الفعلية غير مربوطة**: `mockPaymentProvider` ينجح الشحن دائماً — لا معالجة فشل دفع
   حقيقي، لا webhooks لمزود دفع، لا معالجة اشتراكات منتهية/متأخرة تلقائياً (cron).
6. ~~لا Cron/Scheduler للحملات المجدولة مستقبلاً~~ — **✅ حُلَّت في الجولة السادسة (2026-07-23)**:
   الحملات المجدولة تُنفَّذ عبر تأخير BullMQ المدمج (`delay`) بدل cron منفصل، والحملات الآلية تُفحص
   كل 5 دقائق عبر مهمة BullMQ متكررة (`repeat.every`). **يبقى شرط تشغيلي مهم**: هذا يتطلب عملية
   العامل (`npm run worker`) قيد التشغيل بشكل دائم كخدمة منفصلة — اكتُشف فعلياً أثناء اختبار هذه
   الجولة أن غيابها يجعل كل حملة تبقى "مجدولة" إلى الأبد. تفاصيل في القسم -3 أعلاه.
7. **Light Mode غير مبني** (قرار نطاق: الأولوية لهوية داكنة متسقة واحدة أولاً، موثّق في BRAND_GUIDE.md).
8. **لا معالجة PDPL/GDPR صريحة** (سياسة خصوصية، تصدير/حذف بيانات العميل بناءً على طلبه) — مطلوبة
   قانونياً لبيع فعلي في السوق السعودي (نظام حماية البيانات الشخصية PDPL) قبل الإطلاق التجاري.
9. **لا اختبار حمل (load testing)**: لم يُختبر سلوك RLS/BullMQ تحت آلاف المستأجرين أو رسائل متزامنة.
10. **متغيرات البيئة الحساسة الحالية تطويرية**: `ENCRYPTION_KEY`, `NEXTAUTH_SECRET`, وكلمات مرور
    أدوار قاعدة البيانات كلها قيم تطوير افتراضية — **يجب تدويرها بالكامل قبل أي نشر فعلي**.
11. **لا مزود بريد إلكتروني حقيقي**: إعلانات المنصة (`/admin/announcements`) تعمل in-app فقط —
    لا بريد فعلي يصل للتاجر خارج تسجيل الدخول للمنصة. ربط SendGrid/SES = adapter واحد إضافي.
12. **جلسات الانتحال المنتهية لا تُنظَّف تلقائياً**: `ImpersonationSession` منتهية الصلاحية تبقى
    في الجدول (تُرفض عند الفحص فقط، لا تُحذف) — يحتاج مهمة تنظيف دورية (cron) قبل الإطلاق الفعلي
    لتفادي تراكم غير محدود مع الوقت.
13. **لا بحث/فلترة على سجل التدقيق الشامل** (`/admin/audit-log`) — ترقيم صفحات فقط حالياً؛ فلترة
    حسب التاجر/نوع الإجراء/التاريخ إضافة مفيدة لاحقاً مع نمو حجم السجل.
14. **`prisma/seed.ts` بلا خطوة تصفير آلية**: اكتُشف أثناء اختبار الجولة الخامسة أن إعادة تشغيل
    الزرع عدة مرات متتالية (كما يحدث طبيعياً بين جولات اختبار Playwright) يُنشئ صفوفاً إضافية بدل
    استبدال القديمة، ما قد يُخفي تصرفات فعلية خلف حدود باقة وُصلت زائفاً بسبب بيانات اختبار متراكمة.
    عولج يدوياً بـ `TRUNCATE ... CASCADE` قبل كل زرع خلال هذه الجولة؛ يُنصح بإضافة سكربت
    `npm run db:reset` رسمي قبل الاعتماد النهائي على `seed.ts` في أي بيئة تجريبية متكررة الاستخدام.
15. **عملية العامل (`npm run worker`) خدمة منفصلة إلزامية وليست اختيارية** (اكتُشف فعلياً في الجولة
    السادسة — انظر القسم -3 أعلاه): بدونها، الحملات (لمرة واحدة والآلية) تبقى في حالة "مجدولة"/"نشطة"
    إلى الأبد دون أي إرسال فعلي. يجب توثيق هذا بوضوح في أي دليل نشر إنتاجي (Dockerfile/PM2/systemd
    منفصل للعامل عن خادم الويب)، وربما إضافة تنبيه في `/admin/health` يفحص "آخر نبضة" من العامل.
16. **A/B Testing على مستوى الحملات مذكور في البرومنت كمثال ميزة تُقيَّد بالباقة، لكنه غير مبني
    فعلياً**: أُضيف حقل بيانات محجوز (`Plan.campaignLimitsJson.abTestingEnabled`) لتفعيله من لوحة
    Super Admin مستقبلاً، لكن لا توجد واجهة تقسيم جمهور/متغيرات رسالة فعلية بعد — قرار نطاق واعٍ
    (موثّق في DECISIONS.md) لتفادي بناء ميزة نصف مكتملة تظهر كأنها تعمل دون أن تختبر فعلياً فروقاً
    إحصائية حقيقية.
17. **سيناريو "تجاوز حصة الباقة يمنع الإرسال" لم يُختبر حياً بمتصفح** (فقط تحقُّق مباشر من الكود):
    يتطلب تعديل يدوي لرصيد `messagesUsedThisPeriod` عبر `psql` أو إنشاء عشرات الحملات لاستهلاك
    الحصة الفعلية لتينانت تجريبي — موثّق بصراحة في القسم -3 أعلاه بدل الادّعاء باختبار لم يحدث.
18. **Meta Media API مُنفَّذة جزئياً — لرسائل AUDIO فقط**: الرسائل الواردة الوسائطية من Meta الفعلي
    تصل بمعرّف وسيط (`id`) فقط، وتتطلب استدعاء Media API منفصلاً لجلب رابط تحميل مؤقت. هذا مُنفَّذ
    الآن (`lib/integrations/meta/mediaDownload.ts` + `api/webhooks/meta/route.ts`) لرسائل **AUDIO**
    تحديداً — يُنزَّل الصوت ويُفرَّغ عبر Whisper مباشرة في الذاكرة (بلا تخزين ملف)، مشروط بباقة التاجر
    (`voiceMessagesEnabled`، راجع DECISIONS.md قرار 257-259). **لا يزال غير مُنفَّذ لـ IMAGE/DOCUMENT**
    (لا حاجة فعلية له بعد — التقاط caption/filename فقط يكفي حالياً لهذين النوعين). في Sandbox يُدرَج
    رابط تمثيلي (data URI) مباشرة بدل هذه الخطوة لكل أنواع الوسائط.
19. **محرك تنفيذ الشات بوت الحقيقي لا يزال غير مربوط بالرسائل الواردة الفعلية** (فجوة مستمرة موثّقة
    منذ الجولة الثانية): `Conversation.controlMode` يعرض حالة البوت/الموظف بوضوح ويسمح بالاستيلاء
    اليدوي الفعلي، لكن لا يوجد تنفيذ آلي فعلي لخطوات التدفق رداً على كل رسالة واردة حقيقية عبر
    الـwebhook — قرار نطاق واعٍ لهذه الجولة تحديداً (انظر DECISIONS.md، قرار 46)، وليس إغفالاً.
20. **`maxWhatsappNumbers` غير قابل للإنفاذ فعلياً حتى الآن — فجوة معمارية**: النموذج الحالي يسمح
    برقم واتساب واحد فقط لكل تاجر بنيوياً (`Integration` بقيد `[tenantId, provider]`)، بصرف النظر
    عن قيمة الحد في الباقة. حل هذا يتطلب تغيير القيد الفريد وتحديث كل استعلام `findUnique` ذي علاقة
    إلى `findMany` — تغيير معماري في وحدة التكاملات، خارج نطاق وحدة صندوق المحادثات في هذه الجولة.
21. **مؤشر SLA (15 دقيقة) وقيمة نافذة إسناد التحويل ثابتتان في الكود** — غير قابلتين للتخصيص من
    لوحة الإدارة لكل باقة/تاجر بعد؛ إضافة بسيطة لاحقاً إن ظهرت حاجة فعلية لتخصيصها.
22. **لا اختبار حي فعلي لسيناريو "تجاوز حصة الباقة يمنع دعوة عضو جديد" بمتصفح حقيقي حتى نفاد الحصة
    الطبيعي** — اختُبر فعلياً لكن كأثر جانبي غير مقصود لخطوة أخرى في سكربت اختبار قديم (تفصيل كامل
    في DECISIONS.md قرار 49، البند 4)، وليس بسيناريو مصمَّم مسبقاً لهذا الغرض تحديداً.
23. **Embedded Signup الحقيقي من Meta غير مُنفَّذ فعلياً** (Facebook JS SDK + `FB.login()` +
    استماع `postMessage`) — يتطلب موافقة Meta App Review المسبقة على صلاحية
    `whatsapp_business_management` (خطوة بشرية من Meta تستغرق أياماً، وليست فجوة برمجية). الطريقة
    اليدوية (WABA ID/Phone Number ID/Access Token) هي المسار الحقيقي الكامل المُنفَّذ فعلياً بدلاً
    منها؛ تفصيل القرار في DECISIONS.md (قرار 58).
24. **تجديد التوكن (Token Refresh) عند اقتراب انتهاء الصلاحية غير مُنفَّذ** — `Integration.tokenExpiresAt`
    مُضاف في المخطط لكن لا توجد مهمة دورية تفحصه وتُنبِّه التاجر قبل الانتهاء الفعلي؛ حالياً الفشل
    يظهر فقط عند فشل استدعاء فعلي لاحقاً (Token Invalidation صامتة حتى أول استخدام فاشل).
25. **لا اختبار حي لمسار Graph API الحقيقي (live mode) بمفاتيح Meta حقيقية** — كل مسارات التحقق
    اليدوي وإنشاء القوالب واشتراك webhooks مكتوبة بشكل حقيقي فعلي (استدعاءات `fetch` حقيقية لـ
    graph.facebook.com)، لكن لم تُختبر ضد حساب Meta حقيقي لعدم توفر `META_APP_ID`/`META_APP_SECRET`
    حقيقيين في بيئة البناء — الكود جاهز بنيوياً لكنه غير مُختبَر فعلياً بحساب حقيقي.

---

## 4. ملاحظة حول Rate Limiting

نُفِّذ حد بسيط (fixed window عبر Redis) على مسارين حسّاسين لتفادي "تاجر يؤثر على غيره":
- عامل إرسال الحملات: 60 رسالة/10 ثوانٍ لكل مستأجر.
- كل نقطة webhook: 300 حدث/دقيقة لكل مستأجر.

هذا **لم يُختبر تحت حمل فعلي متزامن من عدة مستأجرين** (يتطلب سيناريو اختبار حمل مخصص) — التحقق
الحالي منطقي (قراءة الكود ومطابقته لسلوك Redis INCR/EXPIRE المعروف)، وليس اختباراً بمقياس حقيقي.

---

## 5. قبل الإطلاق التجاري الفعلي (Checklist)

- [x] **ربط حساب Meta Business + WhatsApp Cloud API حقيقي** — تم فعلياً (الجولة التاسعة، 2026-07-23)
      عبر الطريقة اليدوية (WABA ID + Phone Number ID + Access Token) لتاجر حقيقي واحد ("dms"، الرقم
      996509095816)، تحقُّق Graph API حقيقي ناجح، اشتراك webhook فعلي. Embedded Signup (OAuth) نفسه
      يبقى غير مُفعَّل (يتطلب `META_APP_ID`/`META_APP_SECRET` واجتياز App Review — انظر البند أدناه).
- [ ] التسجيل كـ Zid Partner و Salla Partner، والحصول على بيانات OAuth حقيقية.
- [ ] ربط مزود دفع سعودي حقيقي (Moyasar/PayTabs) خلف `lib/billing/payment-provider.ts`.
- [x] **تدوير كل الأسرار** (`ENCRYPTION_KEY`, `NEXTAUTH_SECRET`, `META_WEBHOOK_VERIFY_TOKEN`، كلمات
      مرور أدوار DB الثلاثة) لقيم عشوائية قوية — تم فعلياً (الجولة التاسعة، 2026-07-23)، تحقُّق مسبق
      أنه لا توجد أسرار مشفَّرة قديمة كانت ستُفقَد بتدوير `ENCRYPTION_KEY`.
- [ ] إضافة سياسة خصوصية + آلية حذف/تصدير بيانات العميل (PDPL).
- [ ] إضافة اختبارات آلية + CI pipeline.
- [ ] تشغيل `npm run worker` كخدمة منفصلة دائمة (PM2/systemd/Docker) في أي بيئة إنتاجية — بدونها
      لا تُرسل أي حملة فعلياً (✅ جدولة الحملات نفسها عبر BullMQ delay/repeat مبنية بالفعل، انظر
      القسم -3، لكن هذا لا يغني عن تشغيل العامل كخدمة مستقلة مراقبة).
- [ ] اختبار حمل لـ RLS وBullMQ بعدد مستأجرين/رسائل واقعي للحجم المستهدف.
- [ ] استضافة Postgres/Redis على بنية تحتية مُدارة (لا Docker محلي) مع نسخ احتياطي تلقائي.
- [x] استدعاء Meta Media API الفعلي (جلب رابط تحميل مؤقت من معرّف الوسيط) — **مُنفَّذ لرسائل AUDIO
      فقط** (تفريغ Whisper مباشر في الذاكرة، بلا أي تخزين ملف — راجع بند 18 أعلاه). لا يزال غير
      مُنفَّذ لـ IMAGE/DOCUMENT (لا حاجة فعلية له بعد). حصر خطر عرض `mediaUrl` كصورة بقائمة نطاقات
      Meta CDN موثوقة يبقى كما هو (انظر قسم 6 الجديد أدناه)، بدل حظر الميزة بالكامل.
- [ ] ربط محرك تنفيذ الشات بوت الفعلي بمسار استقبال رسائل واتساب الحقيقي (فجوة مستمرة من الجولة
      الثانية، لم تُحل بعد — `Conversation.controlMode` يعرض الحالة فقط دون تنفيذ آلي حقيقي).
- [ ] إعادة تصميم `Integration` لدعم أكثر من رقم واتساب واحد لكل تاجر إن أصبح مطلوباً تجارياً.
- [ ] تسجيل تطبيق Meta حقيقي + اجتياز App Review لصلاحية `whatsapp_business_management` (مطلوب قبل
      أي استخدام فعلي حقيقي لـEmbedded Signup أو Graph API خارج الطريقة اليدوية).
- [ ] بناء مهمة دورية لفحص `Integration.tokenExpiresAt` وتنبيه التاجر قبل انتهاء صلاحية التوكن.
- [x] **اختبار مسار Graph API اليدوي الحقيقي** — تم فعلياً بحساب Meta Business حقيقي (تحقق رقم
      996509095816 ناجح). إنشاء القوالب واشتراك webhooks الفعليين لا يزالان غير مُختبَرين بحساب حقيقي.
- [x] **بناء نظام الشركاء (طلب انضمام عام)** — تم فعلياً (الجولة التاسعة، 2026-07-23):
      `/partners/join`، `/partners/apply` (معالج متعدد الخطوات، باقات حقيقية)، `/partners/status`،
      `/partners/setup/[token]`. يغذّي مركز الموافقات الموجود فعلاً (نوع `PARTNER_APPLICATION` جديد)
      بلا نظام موازٍ. اختُبر end-to-end عبر متصفح حقيقي (Playwright): تقديم → موافقة → تينانت حقيقي →
      رابط إعداد → تسجيل دخول فعلي. تفاصيل في `DECISIONS.md` (قرارات 65–70).
- [ ] **إرسال بريد إلكتروني حقيقي**: `lib/email/send.ts` يعمل حالياً بمزود `console` فقط (يطبع الرسالة
      في سجل الخادم، لا يصل فعلياً لصندوق بريد المتقدّم). يلزم ربط مزود حقيقي (Resend/SendGrid/SMTP)
      وضبط `EMAIL_PROVIDER` قبل الاعتماد الفعلي على إشعارات نظام الشركاء (تأكيد الطلب، رابط إعداد
      الحساب بعد القبول، سبب الرفض) — بدون هذا، المتقدّمون الحقيقيون لن يستلموا أي من هذه الرسائل.
- [ ] **حرِج — ضبط `META_APP_SECRET` الحقيقي**: لا يزال فارغاً في `.env`. توقيع Meta لأي webhook وارد
      (`x-hub-signature-256`) لا يمكن التحقق منه بدونه، وبعد إصلاح ثغرة تجاوز التحقق في هذه الجولة
      (انظر قسم 6) فإن التحقق أصبح "fail closed" دائماً — أي **كل الرسائل الواردة الحقيقية من عملاء
      تاجر "dms" سترفضها المنصة حالياً (401 Invalid signature) حتى يُضبط `META_APP_SECRET` الفعلي**
      من Meta App Dashboard → إعدادات التطبيق الأساسية. هذا يمنع فعلياً استقبال أي رسالة واردة حقيقية
      إلى أن يُحل — أولوية تشغيلية قصوى، وليس مجرد بند أمني نظري.

---

## 6. مراجعة أمنية شاملة قبل الإطلاق (الجولة التاسعة، 2026-07-23)

نُفِّذت مراجعة أمنية كاملة (auth/session، RBAC، حقن SQL/XSS/SSRF، معالجة الأسرار) قبل الاعتماد على
بيانات تاجر حقيقي. الثغرات الحقيقية المكتشفة والمُصلَحة فعلياً في هذه الجولة:

1. **حرِج (High) — تجاوز عام للتحقق من توقيع webhook في وضع sandbox**: الدوال الثلاث
   `verifyWebhookSignature` (Meta/Zid/Salla) كانت تتجاوز التحقق بالكامل (`return true`) عند كون
   *إعداد المنصة العام* بوضع sandbox — وليس حالة اتصال التاجر بعينه. بما أن محاكاة sandbox الفعلية
   (رسائل/طلبات وهمية) تتم بالكامل داخل التطبيق (`lib/inbox/sandboxSimulator.ts`،
   `SANDBOX_ZID_ORDERS`/`SANDBOX_SALLA_ORDERS`) ولا تمر أبداً عبر مسارات `api/webhooks/*` العامة، فإن
   هذا التجاوز لم يخدم أي غرض اختبار فعلي — كان يسمح فقط لأي طرف خارجي بإرسال طلبات غير موقّعة تماماً
   لتزوير رسائل/طلبات لأي تاجر، طالما كان إعداد المنصة العام (الافتراضي: sandbox) لم يُبدَّل بعد إلى
   live، **حتى لو كان تاجر معيّن متصلاً برقم واتساب حقيقي فعلاً**. أُزيل التجاوز نهائياً من الدوال
   الثلاث — التحقق الآن "fail closed" دائماً بلا استثناء.
2. **متوسط (Medium) — رابط وسائط وارد غير مُتحقَّق منه يُعرَض كـ `<img src>` مباشرة**: أُضيفت قائمة
   نطاقات موثوقة (`fbcdn.net`/`fbsbx.com`/`facebook.com` + `data:image/` من محاكي sandbox الداخلي) في
   `MessageBubble.tsx` قبل عرض أي `mediaUrl` وارد كصورة.
3. **متوسط (Medium) — لا حد على محاولات تسجيل الدخول إطلاقاً**: لم يكن هناك أي rate limiting على
   `authorize()` في NextAuth — قابل للتخمين الآلي الكامل لكلمة مرور أي حساب. أُضيف حد 8 محاولات فاشلة/
   10 دقائق لكل بريد إلكتروني (Redis)، مع مقارنة `bcrypt` تُنفَّذ دائماً (حتى على تجزئة وهمية عند عدم
   وجود المستخدم) لتفادي فرق توقيت يكشف وجود البريد من عدمه.
4. **منخفض (Low) — صفحتا "الفوترة" و"الفريق والإعدادات" في لوحة التاجر بلا أي فحص صلاحية**: موظف
   (Agent) كان يمكنه عرض (وليس تعديل) الفواتير الكاملة والفريق وسجل التدقيق عبر التنقل المباشر للرابط
   رغم عدم امتلاكه `billing.manage`/`team.manage`. أُضيف فحص `hasPermission` + `notFound()`، وأُخفي
   رابطا التنقل نفساهما من الشريط الجانبي لمن لا يملك الصلاحية.
5. **منخفض (Low) — دالتا معاينة عدد الجمهور في الحملات بلا فحص صلاحية `campaigns.view`**.
6. **منخفض (Low) — تنسيق `phoneNumberId`/`wabaId` غير مُتحقَّق قبل إدراجه في رابط `fetch` لـ Graph
   API** (الشيفرة الأصلية اعتمدت فقط على أن المضيف ثابت `graph.facebook.com`، فلم يكن استغلالاً فعلياً
   كـ SSRF، لكن أُضيف تحقق أن القيمة أرقام فقط كإجراء دفاعي).
7. **تنظيف — أزرار "حسابات تجريبية سريعة" على صفحة تسجيل الدخول العامة** كانت تُشير لبيانات 3 تينانتس
   تجريبية حُذفت فعلياً في هذه الجولة (طلب صريح من المستخدم) — إزالتها بالكامل من صفحة الدخول لأن
   المنصة أصبحت تخدم تاجراً حقيقياً الآن.
8. **حذف كود ميت مضلِّل**: `assertTenantScopedWhere` في `lib/db.ts` كانت موثَّقة كحارس تطوير فعلي لكنها
   لم تُستدعَ من أي مكان إطلاقاً — أُزيلت (الحماية الفعلية موجودة أصلاً عبر Postgres RLS في `withTenant`).

**لا وُجد** أي حقن SQL حقيقي (استخدام وحيد لـ`$executeRawUnsafe` في `withTenant()` بقيمة `tenantId`
من الجلسة نفسها لا من مدخلات المستخدم مباشرة)، ولا XSS (`dangerouslySetInnerHTML`/`innerHTML` غير
موجودين إطلاقاً في `src/`)، ولا حقن أوامر نظام (`exec`/`spawn` غير مستخدمين إطلاقاً).

---

## 7. الموقع التسويقي العام (الجولة العاشرة، 2026-07-23) — التحقق الذاتي قبل التسليم

1. **تصفّح كل صفحة على الموبايل (390px) والديسكتوب (1440px)**: تم فعلياً عبر Playwright — التقاط
   لقطات شاشة كاملة لكل صفحة (الرئيسية، الخدمات، من نحن، رؤيتنا، الأسعار، تواصل، الشروط، الخصوصية) على
   كلا القياسين، مع اختبار القائمة المتنقلة (فتح/إغلاق) على الجوال. لم يُكتشف أي عنصر مكسور.
2. **الأزرار الثلاثة توجّه فعلياً للمسارات الصحيحة**: تحقُّق آلي بالنقر الفعلي (وليس فحص كود فقط) —
   "انضم كشريك" ← `/partners/join`، "لوحة التاجر" ← `/login`، "تسجيل الدخول" ← `/login`. الثلاثة
   تعمل بشكل صحيح (لوحة التاجر وتسجيل الدخول يشتركان في نفس الصفحة عمداً — `authorize()` في
   NextAuth يتعرّف تلقائياً على نوع الحساب بعد الدخول، لا حاجة لصفحتي دخول منفصلتين).
3. **نموذج التواصل يرسل فعلياً ولا يفشل بصمت**: تحقُّق فعلي كامل — رسالة اختبار وصلت إلى جدول
   `ContactMessage`، ظهرت في `admin/leads`، وأُرسل إشعار بريدي (مزود `console`، نفس القيد الموثَّق في
   نظام الشركاء). محاولة حقن `<script>` عبر حقل الرسالة عُرضت كنص عادي آمن دون تنفيذ (React escaping).
4. **الأنيميشن لا يسبب أي تأخير ملحوظ في التحميل الأول**: Hero بلا Lottie فعلي (بديل CSS/SVG خفيف —
   قرار 74 في DECISIONS.md)، بلا أي طلب شبكي إضافي. اكتُشف وأُصلح أثناء الاختبار عيب حقيقي منفصل: كان
   Hero نفسه مُغلَّفاً بأنيميشن "ظهور عند التمرير" فيظهر باهتاً للحظة عند أول تحميل رغم كونه مرئياً
   أصلاً — أُزيل هذا الغلاف تماماً من كل محتوى أعلى الطية (راجع قرار 75).
5. تم إنشاء/تحديث هذا التقرير و`DECISIONS.md`/`CHANGELOG.md` دون طلب أي رد فعل من المستخدم.

**فجوة معروفة وموثَّقة**: لا يوجد مزود بريد إلكتروني حقيقي بعد (`EMAIL_PROVIDER=console` فقط) — إشعارات
نموذج التواصل ورسائل نظام الشركاء تُطبع في سجل الخادم فقط ولا تصل فعلياً لصندوق بريد حتى يُربط مزود
حقيقي. أرقام الإحصائيات في الصفحة الرئيسية (+250 تاجر، +2.4M رسالة، 97% رضا) placeholder احترافي قابل
للتحديث من الكود مباشرة عند توفر أرقام حقيقية فعلية. روابط السوشيال ميديا في الفوتر أسماء حسابات نائبة

---

## 8. إعادة بناء وحدة "جهات الاتصال" لتصبح CRM احترافي متكامل (الجولة الحادية عشرة، 2026-07-25)

### الخلل الظاهر الأصلي — تشخيص جذري
عمود "المرحلة" كان يعرض أحياناً نصاً مشوَّهاً بصرياً (مثال: `24T00:12:54.177Z-07-2026 — استيراد حملة`).
**السبب الجذري**: `src/lib/campaigns/audienceImport.ts` كان ينشئ Tag اسمه الحرفي يتضمّن
`toISOString()` خام (`استيراد حملة — ${new Date().toISOString()}`) ليُستخدَم كوسم داخلي لاستهداف
جمهور حملة مستورَدة — لكنه كان يُعرَض بلا قصد في قوائم الوسوم المرئية للتاجر بلا أي عزل اتجاه نصي
(bidi)، فيتشوّه بصرياً داخل صفحة RTL عربية. **الإصلاح الحقيقي لم يكن تنسيق التاريخ فقط**: أُضيف حقل
`Tag.isSystem` يُستبعَد صراحة من كل عرض وسوم للمستخدم (القائمة، صفحة التفاصيل، منتقي وسوم معالج
الحملة)، فلم يعد هذا الوسم الداخلي يظهر للمستخدم إطلاقاً — إصلاح المشكلة من جذرها (بيانات تنفيذ داخلية
تتسرّب كبيانات مستخدم) وليس فقط تجميل عرضها.

### الوحدات المبنية فعلياً
- **مصدر حقيقي لكل جهة اتصال**: حقل `Contact.source` (`MANUAL/IMPORT/CAMPAIGN_IMPORT/ZID_SYNC/SALLA_SYNC`)
  + `sourceCampaignId` (يُملأ بأثر رجعي بعد إنشاء الحملة الفعلية، إذ لا معرّف حملة متاح وقت استيراد
  الجمهور) + `importBatchId` (دفعة استيراد حقيقية قابلة للمراجعة والتراجع).
- **تقسيم فعلي بالتبويبات** (الكل/حسب الحملة/استيراد ملفات/إضافة يدوية/من تكامل المتجر) بعدّاد حقيقي
  لكل تبويب، مبني على `Contact.source` الحقيقي وليس تخميناً.
- **استيراد بمعاينة حقيقية**: مسار جديد كامل (تحليل → معاينة في Redis بمهلة 15 دقيقة → اختيار
  استراتيجية التكرار (تحديث/تجاهل) → تأكيد فعلي). قرار مهم موثَّق: "إضافة كجهة منفصلة" (كما ورد حرفياً
  في البرومنت) غير ممكن فعلياً لأن رقم الجوال هو المفتاح الفريد لهوية واتساب نفسها (`@@unique`) —
  الخياران الحقيقيان المتاحان فقط: تجاهل أو تحديث.
- **تراجع عن دفعة استيراد (Undo)**: يحذف فقط الجهات **الجديدة فعلياً** بالدفعة (وليس المُحدَّثة، إذ لا
  نسخة سابقة محفوظة لاستعادتها) خلال 10 دقائق من الاستيراد.
- **تصدير حقيقي** (Excel/CSV، الكل/القسم المعروض/التحديد اليدوي) عبر Route Handler جديد
  (`/api/contacts/export`)، بصلاحية `contacts.export` الجديدة (Owner/Admin فقط — بند أمني صريح)،
  وتسجيل Audit Log لكل عملية تصدير (من، متى، كم سجلاً).
- **صفحة تفاصيل جهة اتصال** موسَّعة: تعديل مباشر للبيانات الأساسية، مرحلة عميل موسَّعة (أُضيفت
  "تم التواصل" بين "عميل محتمل" و"عميل" — 4 مراحل بدل 3)، محرر وسوم فعلي (إضافة/حذف)، حالة موافقة
  تسويقية ثلاثية الحالة (موافق/غير موافق/غير محدد — `Boolean?` وليس `Boolean`) مع تاريخ آخر تحديث،
  **سجل حملات فعلي (Activity Timeline)** عبر `CampaignRecipient` (لم يكن موجوداً إطلاقاً سابقاً).
- **إجراءات جماعية**: تحديد متعدد، حذف جماعي، وسم جماعي، تصدير التحديد، و"إضافة كجمهور حملة" —
  يعيد استخدام آلية "شريحة مخصصة" الموجودة أصلاً عبر وسم داخلي (isSystem) بدل أي تعديل على معالج
  الحملة نفسه.
- توحيد `STAGE_LABELS` (كانت مكررة في 4 ملفات منفصلة) في مصدر واحد `src/lib/contacts/stages.ts`.

### التحقق الذاتي الفعلي (تينانت اختبار معزول، Playwright حي، 11 فحصاً)
1. ✅ الدخول وفتح صفحة جهات الاتصال.
2. ✅ استيراد ملف فيه رقم مكرر — تحذير التكرار ظاهر فعلياً في المعاينة قبل أي كتابة على القاعدة.
3. ✅ اختيار "تجاهل" ثم التأكيد — الجهة الموجودة مسبقاً **لم تتغيّر فعلياً** (تحقُّق من القاعدة
   مباشرة، وليس افتراضاً)، والجهة الجديدة أُنشئت بـ`source=IMPORT` و`importBatchId` صحيح.
4. ✅ لا يوجد أي طابع زمني خام (`\d{4}-\d{2}-\d{2}T...`) ظاهر في أي مكان بالصفحة — الخلل الأصلي مُصلَح فعلياً.
5. ✅ صفحة التفاصيل تعرض مصدر الجهة الصحيح.
6. ✅ زر التراجع يحذف الجهة الجديدة فقط، ويترك الجهة القديمة (غير المُنشأة بهذه الدفعة) سليمة تماماً.
7. ✅ التصدير (Owner) يعيد ملف CSV حقيقياً يحتوي فعلياً على بيانات جهات الاتصال المطلوبة.
8. ✅ التصدير (Agent) يُرفَض فعلياً (403) حتى عند استدعاء الرابط مباشرة (وليس فقط إخفاء الزر في
   الواجهة) — تحقُّق أمني حقيقي وليس افتراضاً.
9. ✅ الإجراءات الجماعية (تحديد + وسم جماعي) تعمل بلا أخطاء.
10. ✅ سجل الحملات في صفحة التفاصيل يعرض حملة حقيقية أُرسلت لهذه الجهة (`CampaignRecipient` حقيقي).
11. ✅ تبويب "إضافة يدوية" يعرض فعلياً الجهة المُضافة يدوياً دون تسريب جهات من مصادر أخرى.

`npx tsc --noEmit` و`npm run build` نظيفان طوال الجولة. صفر أخطاء console حقيقية (فقط ضجيج معروف
غير متعلق: 404 لـ`/logo.png`).

### ملاحظة هندسية: التصدير مزامن (Synchronous) وليس عبر Queue
البرومنت طلب معالجة غير حاجبة "لو حجم البيانات كبير". القرار الحالي: توليد الملف مباشرة داخل Route
Handler (حد أقصى 20,000 صف) — مناسب للحجم الحالي المتوقَّع. لو نما عدد جهات اتصال تاجر واحد لأعداد
ضخمة جداً مستقبلاً، الانتقال لمعالجة خلفية (BullMQ، بنفس نمط إرسال الحملات) تحسين لاحق موثَّق هنا،
وليس نقصاً حالياً يمنع الاستخدام الفعلي.
(placeholder) لحين إنشاء الحسابات الفعلية للمنصة.

---

## 9. إعادة بناء صفحة "الفوترة والاشتراك" لتكون احترافية متكاملة (الجولة الثانية عشرة، 2026-07-26)

### السياق: قيد جوهري واحد يحكم كل هذه الجولة
مزوّد الدفع **وهمي بالكامل** (موثَّق كـBlocker مركزي منذ جولة التدقيق الأمني) — لا حساب حقيقي، لا
Tokenization حقيقي، لا Webhook حقيقي وارد فعلياً من أي بوابة. البرومنت افترض أن قرار بوابة الدفع
"محدَّد سابقاً" — هذا غير صحيح فعلياً (تأكَّد من `SECURITY_FIXES.md`/`payment-provider.ts` قبل البدء).
القرار: بناء كل ما هو حقيقي وقابل للتنفيذ فعلياً بلا مزوّد حقيقي (استخدام، Proration، منع تخفيض،
سجل فواتير، تدفق إلغاء)، ووضع طبقة "طريقة الدفع" و"Webhook الدفع" في **وضع Sandbox صريح ومُعلَن**
بدل ادّعاء أمان (`PCI-DSS`/`Secured by X`) غير موجود فعلياً — كذب أمني كان سيكون أسوأ من غيابه.

### ميزات حقيقية جديدة كاملة
- **نظرة عامة موسَّعة**: شريط تنبيه فترة تجريبية (أيام متبقية فعلية)، 4 أشرطة استخدام منفصلة
  (رسائل/تدفقات/أعضاء فريق/أرقام واتساب) بتلوين تلقائي عند 80%+، وتنبيه Banner تلقائي عند تجاوز أي
  مورد 80% — لا ينتظر التاجر اكتشاف المشكلة عند الفشل.
- **إدارة طريقة دفع (Sandbox صريح)**: نموذج بطاقة وهمي، Tokenization حقيقي في شكله (لا يُخزَّن رقم
  البطاقة الكامل أبداً — حتى في الذاكرة المؤقتة داخل دالة التحويل نفسها)، فقط آخر 4 أرقام + Token
  وهمي. شارة "🧪 وضع Sandbox" واضحة بدل شارات ثقة كاذبة.
- **مقارنة باقات تفصيلية حقيقية**: مفتاح شهري/سنوي بخصم حقيقي **لكل باقة على حدة**
  (`Plan.annualDiscountBps`، قابل للتعديل من Super Admin — كان ثابتاً 20% مكتوباً مباشرة في كود
  الواجهة التسويقية العامة، أُصلح ليصبح بيانات حقيقية). جدول مقارنة كامل (رسائل/تدفقات/عقدة
  AI/عقدة API/أرقام واتساب/أعضاء فريق/حملات/دعم) مبني من نفس مصدر بيانات Super Admin مباشرة، بلا أي
  تكرار نصي ثابت. شارة "الأكثر طلباً" حقيقية (`Plan.isPopular`) بدل استنتاج بصري بموضع الباقة في
  المصفوفة (كان الوضع سابقاً في الصفحة التسويقية العامة).
- **معاينة Proration حقيقية قبل التأكيد**: حساب رياضي فعلي (أيام متبقية × فرق السعر) يُعرَض في
  نافذة تأكيد قبل أي تنفيذ، والمبلغ الفعلي المُحصَّل عند التأكيد **مطابق تماماً** لما عُرِض في
  المعاينة (وليس مبلغاً مختلفاً يُفاجأ به التاجر).
- **منع تخفيض حقيقي**: فحص حي ضد الاستخدام الفعلي (عدد أعضاء الفريق، أرقام واتساب المربوطة، تدفقات
  الشات بوت المنشورة) قبل السماح بالتخفيض — يمنع فعلياً ويعرض رسالة واضحة بما يجب تقليله أولاً.
- **تدفق إلغاء احترافي**: سبب → عرض احتفاظ (إيقاف مؤقت 30 يوماً بدل الإلغاء) → تأكيد نهائي يوضّح
  أن الوصول يستمر حتى نهاية الدورة المدفوعة الحالية (وليس فوراً). حالة `PAUSED` جديدة تُستأنَف
  تلقائياً بعد 30 يوماً عبر نفس مهمة فحص انتهاء الاشتراكات الدورية.
- **سجل فواتير مع PDF حقيقي**: توليد PDF فعلي (مكتبة `pdfkit` جديدة) يتضمّن رقم الفاتورة التسلسلي
  وتفصيل الضريبة، مع تنويه صريح أنه ليس فاتورة ZATCA معتمدة (تجنّباً لادّعاء امتثال غير موجود).
- **محاكاة فشل دفع + إعادة محاولة**: أداة اختبار حقيقية (وليست مجرد نص تجريبي) تنقل الاشتراك فعلياً
  لحالة PAST_DUE مع فاتورة فاشلة حقيقية، وزر "إعادة المحاولة" يعيد التحصيل فعلياً عبر نفس طبقة
  المزوّد.
- **Webhook بوابة دفع** (`/api/webhooks/payment`) — بنية كاملة جاهزة (تحقق توقيع فاشل مغلق، تسجيل
  WebhookLog، تحديث حالة الفاتورة/الاشتراك تلقائياً) بنفس نمط `/api/webhooks/zid` حرفياً، لكن شكل
  الـpayload افتراضي عام (موثَّق صراحة أنه يحتاج إعادة كتابة عند اختيار مزوّد حقيقي فعلياً).
- **فصل صلاحيات مالي حقيقي**: `billing.view` (Owner+Admin، يرى الصفحة كاملة) منفصلة عن
  `billing.manage` (Owner فقط، ينفّذ أي إجراء مالي فعلياً) — Admin يرى كل الأزرار لكنها تُستبدَل
  برسالة "يتطلب صلاحية المالك"، والحاجز الحقيقي مفروض من الخادم (`requirePermission`) وليس مجرد
  إخفاء واجهة.

### التحقق الذاتي الفعلي (تينانت اختبار معزول، Playwright حي، 9 أقسام)
1. ✅ فحص RBAC مباشر: `OWNER→billing.manage=true`، `ADMIN→billing.manage=false` لكن
   `billing.view=true`، `AGENT→billing.view=false`.
2. ✅ شارة "الأكثر طلباً" وسعر الباقة الحالية مطابقان فعلياً لبيانات Super Admin.
3. ✅ ترقية حقيقية: نافذة المعاينة تعرض تفاصيل Proration فعلية، والمبلغ المُحصَّل بعد التأكيد
   يطابقها تماماً، والاشتراك يتحول فعلياً للباقة الجديدة في القاعدة.
4. ✅ تخفيض مع تجاوز استخدام (5 أعضاء فريق ضد حد 3): **مُنِع فعلياً** برسالة واضحة عن السبب.
5. ✅ طريقة دفع Sandbox: بطاقة وهمية تُحفَظ بآخر 4 أرقام صحيحة وToken وهمي — لا رقم بطاقة كامل
   مخزَّن في أي مكان (تأكيد مباشر من القاعدة).
6. ✅ محاكاة فشل دفع → تنبيه PAST_DUE ظاهر فعلياً → إعادة محاولة → الاشتراك يعود ACTIVE فعلياً.
7. ✅ تدفق الإلغاء: سبب → عرض احتفاظ (إيقاف مؤقت، `PAUSED`+`pausedUntil` مضبوطان فعلياً في القاعدة)
   → استئناف → إلغاء نهائي (`cancelAtPeriodEnd=true` فعلياً، مع توضيح واضح لتوقيت التوقف الفعلي).
8. ✅ تحميل فاتورة PDF: ملف حقيقي صالح (تأكيد Magic Bytes `%PDF`).
9. ✅ Admin يرى الصفحة كاملة لكن بلا أي أزرار تنفيذ مالية فعلية (تُستبدَل برسالة صلاحية).

`npx tsc --noEmit` و`npm run build` نظيفان طوال الجولة. صفر أخطاء console حقيقية (فقط ضجيج معروف
غير متعلق: 404 لـ`/logo.png`).

### عطلان حقيقيان اكتُشفا وأُصلحا أثناء التحقق (وليسا مجرد Playwright flakiness)
1. **PDF التحميل فشل بخطأ 500 عند أول اختبار حي** — تشخيص مباشر (توليد PDF بمعزل عن Next.js عبر
   tsx مباشرة نجح تماماً) أثبت أن المشكلة في تجميع webpack لحزمة `pdfkit` نفسها (تحمّل ملفات خط
   `.afm` ديناميكياً وقت التشغيل عبر `path.join(__dirname,...)`، يكسره التجميع العادي). الإصلاح:
   إضافة `pdfkit` لـ`serverComponentsExternalPackages` في `next.config.js` (نفس النمط المستخدم أصلاً
   لـ`bullmq`/`ioredis`)، مع إعادة تشغيل كاملة للخادم (تغييرات `next.config.js` لا تُطبَّق بالتحديث
   الحي).
2. **أداة "محاكاة فشل دفع" لم تظهر إطلاقاً في الاختبار الأول** — كانت مقيَّدة خطأً بعلم
   `isSandboxMode()` العام (خاص حصراً بتكاملات واتساب/زد/سلة)، والذي كان مضبوطاً "live" في بيئة
   الاختبار هذه لأسباب غير متعلقة — بينما بوابة الدفع نفسها وهمية دائماً بصرف النظر عن ذلك العلم
   تماماً. الإصلاح: إزالة هذا الشرط الخاطئ تماماً (موثَّق في الكود أنه يجب حذف الدالة نفسها بالكامل
   عند ربط مزوّد حقيقي، بدل الاعتماد على علم لا يميّز الحالتين أصلاً).

---

## 10. إعادة بناء صفحة "التجار المشتركون" في لوحة Super Admin (الجولة الثالثة عشرة، 2026-07-27)

### اكتشاف أولي مهم: هذه ليست إعادة بناء من جدول مسطّح — صفحة التفاصيل موجودة أصلاً بـ7 تبويبات حقيقية
البرومنت وصف صفحة تفاصيل تاجر "كعنصر أهم ناقص حالياً" — الفحص الفعلي أظهر أن `admin/tenants/[id]/`
موجودة أصلاً بـ7 تبويبات كاملة تعمل ببيانات حقيقية (نظرة عامة، الفريق، جهات الاتصال، الحملات، الشات
بوت، القوالب، الطلبات) — بما فيها Impersonate، تعليق/حذف، ملاحظات داخلية، تغيير باقة يدوي، كلها
موجودة وتعمل من جولات سابقة. **لم تُبنَ هذه الوحدات من الصفر** — تم تحديد الفجوات الحقيقية فقط
(تفصيلها في القسم التالي) وإصلاحها، بدل إعادة بناء ما يعمل أصلاً بشكل جيد.

### فجوات حقيقية اكتُشفت وأُصلحت
- **لا فلاتر/ترتيب/بحث محسّن في الجدول الرئيسي** — كان بحثاً بالاسم/المعرّف فقط، ترتيب ثابت
  بالتاريخ. أُضيف: فلتر حالة، فلتر باقة، فلتر اكتمال تكاملات، ترتيب قابل للنقر (اسم/تاريخ
  انضمام/آخر نشاط)، بحث موسَّع (+بريد المالك +رقم واتساب المربوط فعلياً عبر فلترة JSON).
- **عمود "آخر نشاط" لم يكن موجوداً إطلاقاً** — لا تجميع لـ`User.lastLoginAt` على مستوى التاجر في
  أي مكان بالكود. أُضيف عبر استعلام `groupBy` واحد فعّال (وليس استعلاماً منفصلاً لكل صف — تجنّب
  N+1 حقيقي مع نمو عدد التجار، بند أداء صريح في البرومنت).
- **إجراء "قبول"/"رفض" مباشر من الجدول لم يكن موجوداً** — الدالتان `approveNewTenant`/
  `rejectRequest` الأصليتان تأخذان معرّف طلب الموافقة (`ApprovalRequest.id`)، وليس معرّف التاجر.
  أُضيفت دالتا تغليف (`quickApproveNewTenant`/`quickRejectNewTenant`) تبحثان عن الطلب المعلَّق
  لهذا التاجر ثم تستدعيان **نفس** الدالتين الأصليتين حرفياً — ضمان تطابق تام مع نتيجة مركز
  الموافقات، وليس منطقاً مكرَّراً قد ينحرف لاحقاً.
- **"تمديد التجربة" لم يكن موجوداً** — أُضيفت `extendTrial(tenantId, days)` تمدّد
  `currentPeriodEnd` من نهايته الحالية (وليس من تاريخ اليوم — لا يُفقَد تمديد سابق).
- **خلل حقيقي في استعلام "آخر webhook فاشل"**: كان يفلتر فقط على `signatureValid: false` — يخلط
  بين "توقيع مرفوض" و"معالجة فاشلة فعلياً" (توقيع صحيح لكن `errorMessage` موجود لعدم مطابقة حساب
  تاجر مثلاً). أُصلح ببناء `getIntegrationWebhookHealth()` يفحص كلا الحالتين بشكل صحيح، ولكل مزوّد
  على حدة (لم يكن مفصولاً حسب المزوّد إطلاقاً من قبل).
- **مؤشر صحة بصري حقيقي** (🟢/🟡/🔴) في الجدول الرئيسي — أحمر تحديداً لتكامل بحالة
  `ERROR`/`NEEDS_REAUTH` الفعلية، وليس فقط `DISCONNECTED` (تمييز صريح مطلوب في البرومنت بين
  "معطّل فعلياً" و"غير مربوط بعد" — الأخير طبيعي لتاجر لم يربط تكاملاً اختيارياً بعد).
- **مصدر انضمام التاجر لم يكن مُتتبَّعاً كبيانات فعلية** — أُضيف `Tenant.joinSource` enum
  (`DIRECT_REGISTER`/`PARTNER_APPLICATION`/`MANUAL_ADMIN`)، يُضبَط صراحة في كلا مساري إنشاء تاجر
  الفعليين الوحيدين الموجودين في الكود.
- **سجل فواتير التاجر (قراءة فقط) لم يكن موجوداً في صفحة التفاصيل** — أُضيف كقسم صغير في "نظرة
  عامة" (وليس تبويباً كاملاً جديداً، تناسباً مع الحجم الفعلي للحاجة)، مع رابط صريح لصفحة "الفوترة
  والإيرادات" الكاملة في Super Admin للإجراء الفعلي (مطابق لحرفية طلب البرومنت).
- **بطاقة استخدام رابعة (أرقام واتساب) كانت غائبة** من صفحة التفاصيل رغم وجود 3 بطاقات أخرى
  (رسائل/فريق/تدفقات) — أُضيفت للتطابق مع نفس منطق أشرطة تقدّم صفحة فوترة التاجر نفسه.
- **تصدير قائمة التجار لم يكن موجوداً إطلاقاً في كل لوحة Super Admin** — بُني من الصفر
  (`/api/admin/tenants/export`)، بنفس نمط تصدير جهات الاتصال (Excel/CSV، Audit Log لكل تصدير).
- **ترقيم صفحات مكرَّر 3 مرات منفصلة** (قائمة التجار، تبويب جهات الاتصال، سجل التدقيق) — استُخرِج
  لمكوّن مشترك واحد `components/admin/Pagination.tsx`.

### التحقق الذاتي الفعلي (تينانت اختبار معزول، Playwright حي، 15 فحصاً)
1. ✅ فلتر الحالة "بانتظار المراجعة" يعرض التاجر الجديد فعلياً.
2. ✅ قبول مباشر من الجدول → حالة التاجر TRIAL فعلياً + طلب الموافقة APPROVED (نفس نتيجة مركز
   الموافقات تماماً، تحقُّق مباشر من القاعدة).
3. ✅ تمديد التجربة أضاف 10 أيام فعلياً لنهاية الدورة (بينما الحالة TRIAL).
4. ✅ صفحة التفاصيل: مصدر الانضمام، 4 بطاقات استخدام (بما فيها واتساب)، صحة التكاملات التفصيلية،
   سجل الفواتير — كلها بيانات حقيقية وليست ثابتة.
5. ✅ تعليق التاجر: حالة SUSPENDED فعلياً في القاعدة، **ومحاولة دخول حقيقية لصاحب الحساب** بعدها
   مباشرة **فشلت فعلياً** (لم يصل للوحة التحكم إطلاقاً) — تحقُّق حقيقي وليس افتراضاً.
6. ✅ Impersonate: سجل تدقيق ظاهر لكلا الطرفين (منصة + تاجر) بعد الاستخدام الفعلي.
7. ✅ تصدير CSV نجح فعلياً.
8. ✅ فريق الفوترة (Billing Staff): مُنِع فعلياً من رؤية قائمة التجار **ومن التصدير** (403) — تحقُّق
   على مستوى الـAPI وليس إخفاء واجهة فقط.
9. ✅ فريق الدعم (Support Staff): يرى زر Impersonate (يملك الصلاحية) لكن **لا** يرى زر الحذف
   (لا يملكها) — تطابق تام مع مصفوفة الصلاحيات المحدَّدة مسبقاً.
10. ✅ مؤشر الصحة البصري: تحوّل لأحمر 🔴 فعلياً فور ضبط تكامل حقيقي لحالة `ERROR` — وليس مجرد
    `DISCONNECTED` عادي.

`npx tsc --noEmit` و`npm run build` نظيفان طوال الجولة. صفر أخطاء console غير متعلقة.

### ملاحظة منهجية: عطل اختبار (وليس عطل تطبيق) اكتُشف وأُصلح أثناء التحقق
أثناء بناء سكربت الاختبار، ظهرت 3 أعطال ظاهرها "فشل تطبيق" لكنها كانت جميعاً أخطاء في تجهيزة
الاختبار نفسها (Test Fixture) وليست أعطالاً حقيقية في الكود — تم تشخيصها بدقة بدل افتراض وجود خلل:
(1) بطاقة استخدام واتساب لم تظهر لأن تينانت الاختبار أُنشئ بدون صف `Subscription` أصلاً؛ (2) مؤشر
الصحة لم يتحوّل لأحمر لأن تينانت الاختبار أُنشئ بدون أي صفوف `Integration` (استعلام `updateMany`
على صفوف غير موجودة لا يفشل، فقط لا يُحدِّث شيئاً بصمت)؛ (3) اختبار "تمديد التجربة" فشل لأنه نُفِّذ
بعد دورة تعليق/إعادة تفعيل التي تنقل الحالة لـ`ACTIVE` (سلوك صحيح فعلاً — "إعادة تفعيل" تضبط
`ACTIVE` دائماً، وليس العودة لـ`TRIAL` السابقة، قرار موجود مسبقاً من جولة سابقة، خارج نطاق هذه
الجولة). الإصلاح في كل الحالات كان في تجهيزة الاختبار (إنشاء Subscription/Integration واقعيَين،
واختبار تمديد التجربة قبل دورة التعليق)، وليس في كود التطبيق.

---

## 11. تدقيق شامل لكل الأقسام الـ12 في لوحة Super Admin + وحدة محاسبة كاملة (الجولة الرابعة عشرة، 2026-07-28)

جولة مزدوجة: برومنت أول طلب إعادة بناء "الإيرادات والفوترة" لتشمل محاسبة قيد مزدوج حقيقية وفوترة
متوافقة مع ZATCA؛ برومنت ثانٍ وصل أثناء التنفيذ ووسّع النطاق لتدقيق شامل لكل قسم من الأقسام الـ12 في
شريط تنقّل Super Admin، مع تحديد ما هو ناقص فعلياً وإكماله. هذا القسم يجيب حرفياً على المطلوب: لكل
قسم، هل كان ناقصاً (وكيف اكتمل) أم كان مكتملاً أصلاً.

### أ) نظرة عامة (Overview) — **كان مكتملاً أصلاً**
فحص `admin/page.tsx` أكّد أن كل الأرقام المعروضة (عدد التجار، الاشتراكات النشطة، الإيراد المجمَّع،
الطلبات المعلَّقة، توزيع الحالات، آخر نشاطات التدقيق) استعلامات Prisma حية حقيقية بدون أي قيمة ثابتة.
لم يُعدَّل هذا القسم هذه الجولة.

### ب) مركز الموافقات (Approvals) — **كانت به فجوة حقيقية، أُكمِلت**
`approveCustomPlan` (اعتماد باقة Enterprise مخصَّصة) كانت تُنشئ Plan+Subscription **بلا أي فاتورة
مقابلة على الإطلاق** — فجوة حقيقية اكتُشفت أثناء التدقيق. أُصلحت لتُنشئ فاتورة PAID فعلية داخل نفس
المعاملة، وتُسجِّل قيداً محاسبياً بعدها مباشرة. باقي مسارات مركز الموافقات (طلبات شراكة، تحقق واتساب،
قوالب رسائل) كانت مكتملة أصلاً ولم تُعدَّل.

### ت) التجار (Tenants) — **كانت به فجوتان جذريتان، أُكمِلتا بالكامل**
صفحة تفاصيل التاجر (7 تبويبات) كانت مكتملة من جولة سابقة. الفجوتان الحقيقيتان المطلوبتان صراحة:
1. **إنشاء تاجر مباشرة** (لم يكن موجوداً إطلاقاً) — زر جديد في القائمة الرئيسية، يُنشئ تاجراً فعّالاً
   فوراً بنفس منطق تفعيل طلب شريك حقيقي حرفياً (`createTenantManually`)، بلا مرور بمركز الموافقات.
2. **"الدخول كخبير" حصرياً لمالك المنصة** — كانت متاحة أيضاً لفريق الدعم الفني من جولة سابقة (قرار
   قديم مقصود وقتها)؛ أُزيلت الصلاحية من `PLATFORM_SUPPORT` في `rbac.ts` تنفيذاً لطلب صريح في هذه
   الجولة، وأُعيدت تسمية الزر في القائمة والتفاصيل والبانر. البنية التحتية (توكن مؤقت، تدقيق مزدوج)
   كانت موجودة أصلاً ولم تُعَد بناؤها.

### ث) رسائل التواصل (Contact Messages) — **كانت به فجوة حقيقية، أُكمِلت**
كانت تحمل علماً ثنائياً فقط (`handledAt`) بلا تعيين أو تحويل لتاجر. أُضيف: حالة 4 مستويات
(جديدة/تمت المتابعة/تحوَّلت لتاجر/مُغلقة)، تعيين لعضو فريق داخلي محدَّد، وزر "تحويل إلى تاجر جديد"
يفتح نموذج الإنشاء المباشر مُعبَّأً مسبقاً ببيانات الرسالة، ويُعلِّم الرسالة تلقائياً CONVERTED مع
ربطها بالتاجر الجديد عند النجاح.

### ج) صحة المنصة (Platform Health) — **كان مكتملاً أصلاً (مع ملاحظة نطاق واحدة)**
تحقُّق مباشر من الكود (`admin/health/page.tsx`) أكّد أن كل المؤشرات (نسبة اتصال Meta، معدّل فشل
Webhook، تجار قريبون من حد الرسائل، ملاك غير نشطين، تجار منخفضو التفاعل) استعلامات حية حقيقية بلا أي
رقم ثابت. **الفجوة الوحيدة**: لقطة لحظية بحتة بلا أي اتجاه تاريخي (لا جدول يحفظ القياسات عبر الزمن) —
موثَّقة كملاحظة نطاق، وليست عطلاً؛ بناء نظام تتبّع تاريخي كامل خارج نطاق جولة تدقيق/إكمال.

### ح) الإيرادات والفوترة (Revenue & Billing) — **الفجوة الأكبر في الجولة، أُعيد بناؤها بالكامل**
هذا القسم كان الأساس الصريح للبرومنت الأول. الفجوات المُكتشَفة والمُكمَلة:
- **خلل ضريبة عرض حقيقي** (فاتورة 1299 ر.س بضريبة صفر) — سببه تاريخي بحت (migration بلا backfill)،
  أُصلح بـUPDATE محسوب + تحقُّق مباشر (0 → 169 ر.س). فصّل التشخيص الكامل في `DECISIONS.md` #186.
- **لا وحدة محاسبة على الإطلاق** — لم يكن يوجد دليل حسابات ولا دفتر قيود قبل هذه الجولة. بُنيت من
  الصفر: `Account`/`JournalEntry`/`JournalLine` (قيد مزدوج حقيقي، توازن مدين=دائن مفروض بالكود)،
  دليل حسابات بـ5 فروع جذرية قابل للتوسيع والطي مع رصيد محسوب فعلياً لكل حساب، وشاشة دفتر قيود
  قابلة للفلترة بالتاريخ ونوع الحدث.
- **الإيراد كان سيُسجَّل دفعة واحدة عند الدفع** — بُنيت آلية اعتراف تدريجي حقيقية (Deferred Revenue)
  عبر مهمة BullMQ كل 6 ساعات.
- **لا آلية استرداد حقيقية** — بُنيت `issueRefund` تُنشئ إشعار دائن (`CreditNote`) مرقَّم تسلسلياً
  وقيداً عكسياً متوازناً، **بلا تعديل أو حذف الفاتورة الأصلية إطلاقاً**.
- **فاتورة PDF بلا أي توافق ZATCA** — أُعيد بناؤها بالكامل: رقم `INV-YYYY-NNNNN`، بيانات بائع كاملة
  (حقل جديد في إعدادات المنصة)، ورمز QR بترميز TLV قياسي (المرحلة الأولى).
- **لوحة الإيرادات كانت أرقاماً مبعثرة بلا MRR/ARR/Churn حقيقية ولا رسوم بيانية** — أُعيد بناؤها
  بالكامل: MRR/ARR/Churn% محسوبة فعلياً، اتجاه شهري ورسم دونات (SVG مخصَّص)، وقسم مدفوعات فاشلة بزر
  "إعادة محاولة تحصيل" حقيقي.
- تصدير دليل الحسابات ودفتر القيود (CSV بترتيب أعمدة متوافق مع أدوات استيراد يدوي في QuickBooks/Zoho).

### خ) حدود الباقات (Plan Limits) — **كان مكتملاً أصلاً**
تحقُّق مباشر (`lib/campaigns/limits.ts`, `lib/planLimits.ts`, `lib/queue/worker.ts`) أكّد أن كل حدود
الباقات (رسائل، تدفقات شات بوت، حملات) تُقرَأ حياً من القاعدة في كل استدعاء — لا طبقة تخزين مؤقت
(`unstable_cache` أو غيرها) تؤخّر انتشار أي تعديل. تعديل حد من اللوحة ينعكس فوراً بلا أي إعادة نشر.
أُضيفت هذه الجولة خطوة صغيرة تكميلية: استدعاء `ensureChartOfAccounts()` عند إنشاء/تعديل أي باقة، حتى
يحصل حساب الإيراد الفرعي الخاص بها في دليل الحسابات فوراً بلا انتظار أول تحميل لصفحة المحاسبة.

### د) الإعلانات (Announcements) — **كان مكتملاً أصلاً**
`createAnnouncement` تدعم الإرسال لكل التجار، لباقة محدَّدة، أو لتجار محدَّدين بالاسم. الجانب التاجري
(`getApplicableAnnouncement` في `dashboard/layout.tsx`) يجلب الإعلانات المطابقة فعلياً حسب اشتراك كل
تاجر ويعرضها كبانر حي — بلا أي فجوة بين الإرسال والظهور. لم يُعدَّل هذا القسم هذه الجولة.

### ذ) الفريق الداخلي (Internal Team) — **كان مكتملاً أصلاً، مع تعديل صلاحية واحد**
إدارة فريق المنصة الداخلي (دعوة/تعديل دور/تعطيل) كانت مكتملة من جولات سابقة. التعديل الوحيد هذه
الجولة: إزالة صلاحية `platform.merchants.impersonate` من `PLATFORM_SUPPORT` (تفصيلها في قسم "ت" أعلاه)
— تعديل مصفوفة صلاحيات واحد، وليس إعادة بناء.

### ر) سجل التدقيق (Audit Log) — **كانت به فجوة حقيقية، أُكمِلت**
كان يعرض قائمة مرقَّمة صفحات بلا أي فلتر — لم يكن "المصدر المركزي الوحيد القابل للاستقصاء" كما يتطلب
البرومنت عملياً. أُضيفت فلاتر فعلية (موظف داخلي، تاجر متأثر، نوع الحدث، نطاق تاريخ) وتصدير CSV لكل
نتيجة مفلترة — كل الأحداث الحساسة المُضافة هذه الجولة (إنشاء تاجر يدوي، تحويل رسالة تواصل، استرداد،
تصدير دفتر القيود/دليل الحسابات) تُسجَّل فيه تلقائياً بنفس الآلية الموجودة أصلاً.

### ز) إعدادات المنصة (Platform Settings) — **كان مكتملاً في معظمه، أُضيفت حقول محاسبة/ZATCA**
وضع الصيانة، أيام التجربة الافتراضية، ومعظم حقول Meta/بريد/SMS كانت مُضافة في جزء سابق من هذه الجلسة
قبل بدء العمل على المحاسبة. أُضيف هذه الجولة تحديداً: بيانات البائع لفواتير ZATCA (الاسم القانوني،
الرقم الضريبي، العنوان) — حقل جديد ثلاثي في `PlatformSettings` مع نموذج تعديل مخصَّص، مصدر واحد
يُقرأ منه كل توليد فاتورة PDF بدل نص ثابت متكرر بالكود.

### س) محتوى الموقع (Website CMS) — **كانت به فجوة حقيقية، أُكمِلت**
كان مقصوراً على الصفحة الرئيسية فقط (قرار نطاق سابق موثَّق). أُضيف: صفحتا "/about" و"/services"
أصبحتا قابلتين للتعديل الكامل من `admin/content` بلا أي إعادة نشر (نفس نمط "الميزات" — الأيقونات
الخمس لصفحة الخدمات ثابتة بالكود، النصوص فقط من الـCMS). تحقُّق حي أكّد ظهور تعديل تجريبي فوراً على
الصفحة العامة بلا إعادة بناء إطلاقاً.

### التحقق الذاتي الفعلي (Playwright حي + استعلامات قاعدة مباشرة، 17 فحصاً + فحوصات إضافية)
جدول الفحوصات الرئيسي (`verify_round.mjs`, 12 فحصاً عبر 3 حسابات أدوار مختلفة):
1. ✅ تسجيل دخول مالك المنصة ناجح.
2. ✅ زر "+ إضافة تاجر مباشرة" ظاهر في قائمة التجار لمالك المنصة.
3. ✅ زر "🕵️ الدخول كخبير" ظاهر في قائمة التجار لمالك المنصة.
4. ✅ لوحة الإيرادات تحمّل بنجاح مع MRR/ARR/Churn.
5. ✅ قائمة الفواتير تظهر بلا خلل.
6. ✅ **لا فاتورة تعرض ضريبة صفر** (تحقُّق نصّي مباشر ضد نمط `ض.ق.م 0)` — نظيف.
7. ✅ صفحة دليل الحسابات تعرض الشجرة (الأصول + الإيرادات ظاهرة).
8. ✅ صفحة دفتر القيود تحمّل بنجاح.
9. ✅ زر "الدخول كخبير" **غائب تماماً** عن واجهة فريق الدعم الفني في قائمة التجار.
10. ✅ فريق الدعم الفني **مُنِع فعلياً** من الوصول لصفحة الإيرادات والفوترة.
11. ✅ الفريق المالي **يصل فعلياً** لصفحة الإيرادات والفوترة.
12. ✅ تعديل عنوان صفحة "من نحن" من `admin/content` ظهر حياً في `/about` بلا أي إعادة بناء.

فحوصات تدفقات إضافية (`verify_flows.mjs`, 5 فحوصات):
13. ✅ إنشاء تاجر مباشرة: رسالة نجاح ظهرت فعلياً.
14. ✅ التاجر الجديد ظهر فعلياً في القائمة بعد الإنشاء.
15. ✅ رسالة تواصل تجريبية ظهرت في `admin/leads`.
16. ✅ "تحويل إلى تاجر جديد" عبّأ بريد المالك مسبقاً من بيانات الرسالة فعلياً.
17. ✅ الرسالة انتقلت لحالة "تحوَّلت لتاجر" تلقائياً بعد نجاح الإنشاء.

فحوصات محاسبية عميقة (استعلامات مباشرة على القاعدة + Playwright):
- ✅ استرداد فاتورة PAID حقيقية عبر الواجهة → إشعار دائن `#1` أُنشئ، حالة الفاتورة → `REFUNDED`.
- ✅ القيد المحاسبي الناتج **متوازن فعلياً**: 1130 ر.س (مصروف مبالغ مستردة) + 169 ر.س (ضريبة مستحقة)
  مديناً = 1299 ر.س (نقدية) دائناً — تحقُّق مباشر من `JournalLine` بعد الاسترداد.
- ✅ سجل التدقيق يحمل حدث `platform.invoice_refund` بكل البيانات الصحيحة.
- ✅ تصدير دفتر القيود (CSV) يحتوي فعلياً على قيد الاسترداد الجديد، برأس أعمدة `Date,Account,Debit,
  Credit,Memo` متوافق مع استيراد يدوي في QuickBooks/Zoho.
- ✅ تصدير دليل الحسابات (CSV) يعمل بنجاح (حالة 200، رأس أعمدة صحيح).
- ✅ بريد "أكمل إعداد حسابك" وصل فعلياً (تحقُّق من سجل خادم `console` لمزوّد البريد) في كل من مسار
  الإنشاء المباشر ومسار التحويل من رسالة تواصل، بروابط إعداد صحيحة ومختلفة لكل حساب.
- ✅ على مستوى الكود مباشرة (`hasPermission`/`requirePermission`): `SUPER_ADMIN` فقط يملك صلاحية
  الانتحال؛ استدعاء `requirePermission` لـ`PLATFORM_SUPPORT` أو `PLATFORM_BILLING` يرمي خطأً فعلياً
  — تأكيد أن الرفض خادمي حقيقي (API-level)، وليس إخفاء واجهة فقط.

`npx tsc --noEmit` و`npm run build` نظيفان طوال الجولة (صفر أخطاء). كل بيانات الاختبار (تينانتات
مُنشأة، رسالة تواصل تجريبية) حُذفت بعد التحقق، باستثناء الفاتورة الحقيقية الوحيدة (1299 ر.س) التي
اسُتردَّت فعلياً أثناء اختبار تدفق الاسترداد ولم تُعَد — لا مسار "تراجع عن استرداد" في النظام أصلاً
(قرار متعمَّد موثَّق في `DECISIONS.md` #206، مطابق لقاعدة "بلا حذف/تراجع" على أي سجل مالي).

### الخلاصة
كل الأقسام الـ12 المطلوبة رُوجِعت فعلياً بالكود (وليس افتراضاً من ذاكرة جولات سابقة). أربعة أقسام
(نظرة عامة، حدود الباقات، الإعلانات، صحة المنصة) كانت مكتملة فعلياً بلا فجوات جوهرية. ثمانية أقسام
كانت بها فجوات حقيقية أُكمِلت هذه الجولة: الموافقات (فاتورة Enterprise المفقودة)، التجار (إنشاء
مباشر + تقييد الانتحال)، رسائل التواصل (حالات متعددة + تحويل)، الإيرادات والفوترة (وحدة محاسبة كاملة
من الصفر — الفجوة الأكبر)، الفريق الداخلي (تعديل صلاحية واحد)، سجل التدقيق (فلاتر وتصدير)، إعدادات
المنصة (بيانات ZATCA)، ومحتوى الموقع (توسيع لصفحتين جديدتين).

---

## 12. الموظف الذكي — رد آلي بذكاء اصطناعي حقيقي (الجولة الخامسة عشرة، 2026-07-29)

بناء موظف خدمة عملاء حقيقي فوق عقدة "رد ذكي" وصندوق المحادثات الموجودين مسبقاً — أول ربط فعلي لمزوّد
LLM خارجي (OpenAI) في المشروع. الفحص الأولي أكّد أن `ai_reply` كانت مصمَّمة عمداً كتحويل صامت لموظف
بشري (حد فاصل صريح من جولة سابقة، وليس فجوة عرضية) — هذه الجولة تبني المحرك الحقيقي فوق ذلك الأساس.
كل قرار مفصَّل في `DECISIONS.md` #207-220.

### ما بُني
- **محرك محادثة حقيقي** (`lib/ai/generateReply.ts`): يبني سياقاً من آخر 15 رسالة، يستدعي أدوات حقيقية
  (`get_product_price`, `get_order_status`) بدل التخمين، ويُنتِج رداً + تقييم ثقة + قرار تحويل عبر
  أداة إلزامية واحدة (`respond_to_customer`).
- **6 بوابات حماية قبل أي استدعاء LLM** بالترتيب: مفتاح تشغيل التاجر، كلمات مفتاحية حتمية، سؤال متكرر
  بلا حل، حد معدل الاستدعاء، حصة توكنز الباقة الشهرية، ثم توفر مفتاح API نفسه.
- **صفحات تاجر جديدة**: `/dashboard/ai-agent` (إعدادات الشخصية)، `/dashboard/ai-agent/test` (معاينة
  حية)، `/dashboard/ai-agent/reviews` (مراجعة جودة + تقييم).
- **مؤشر كتابة حقيقي** (Baileys) أو best-effort (Meta) + تأخير طبيعي متناسب مع طول الرد.
- **بنية بيانات جديدة**: `AiAgentConfig` (شخصية لكل تاجر)، `AiReplyLog` (سجل كل رد: ثقة، تحويل، توكنز،
  تقييم)، `Subscription.aiTokensUsedThisPeriod`، حقول ZATCA-مستقلة لمفتاح OpenAI في `PlatformSettings`.

### عطلان حقيقيان اكتُشفا وأُصلحا أثناء المراجعة الذاتية (قبل التحقق الحي، وليس بعده)
- **مفتاح `enabled` لم يكن مقروءاً فعلياً من المحرك** — الحقل موجود بالسكيما والواجهة (يقدر التاجر
  يفعّل/يعطّل)، لكن `generateAiEmployeeReply` في تنفيذها الأول لم تفحصه إطلاقاً، فكان تعطيل الموظف
  الذكي من الواجهة بلا أي أثر فعلي. اكتُشف عبر مراجعة الكود الذاتية، أُضيف كأول بوابة (الأرخص) قبل أي
  فحص آخر، وتحقَّق منه حياً بعدها (اختبار 4 في سكربت التحقق أدناه).
- **بوابة الباقة (Starter محظور) كانت واجهة فقط، غير مفروضة خادمياً** — صفحتا الإعدادات والاختبار
  تُعيدان توجيه تاجر Starter، لكن الإجراءين الخادميين (`updateAiAgentConfig`, `sendTestMessage`) كانا
  يقبلان استدعاءً مباشراً بلا فحص، بخلاف نمط `saveFlowGraph` الدفاعي الموجود أصلاً لنفس العقدة. أُضيف
  نفس فحص `isNodeTypeAllowed` خادمياً في كليهما.

### التحقق الذاتي الفعلي (محاكاة رسائل واردة حقيقية بمسار الإنتاج الكامل + Playwright)
سكربت تحقُّق استدعى `applyParsedMetaWebhook` ثم `runChatbotEngine` مباشرة (نفس المسار الحقيقي الذي
يستخدمه Webhook حقيقي أو محاكي الـSandbox، وليس دالة معزولة) ضد تينانت حقيقي (tenant-a، باقة Growth)
بتدفق منشور فعلي يحتوي عقدة "رد ذكي":

1. ✅ **طلب صريح** ("أريد التحدث مع شخص حقيقي من فضلك") → تحويل فوري، السبب المسجَّل بالضبط: "طلب
   صريح من العميل بالتحدث مع موظف بشري"، رسالة انتقالية حقيقية أُرسِلت ("لحظات من فضلك 🙏...")، ملخص
   تلقائي كملاحظة داخلية (`authorId: null`، يعرضها العرض كـ"🤖 ملخص تلقائي").
2. ✅ **كلمة حساسة** ("عندي شكوى بخصوص طلبي الأخير") → تحويل فوري، السبب بالضبط: 'كلمة مفتاحية حساسة
   اكتُشفت: "شكوى"'.
3. ✅ **سؤال عادي، بلا مفتاح API مُعَدّ** ("كم سعر عطر ورد دمشقي؟") → تحويل صادق، السبب: "لم يُضبَط
   مزوّد الذكاء الاصطناعي بعد من إعدادات المنصة" — تحقُّق أن غياب المفتاح لا يُنتج خطأ 500 أو رداً
   وهمياً، بل تحويلاً صادقاً معلوم السبب.
4. ✅ **تعطيل الموظف الذكي كلياً** (`enabled: false`) → تحويل فوري بسبب مختلف تماماً: "لم يُفعِّل
   التاجر الموظف الذكي بعد من صفحة الإعدادات" — يثبت أن بوابة "التفعيل" (بعد إصلاحها) تُفحَص فعلياً
   وتسبق كل البوابات الأخرى.
5. ✅ **دقة سجل استهلاك التوكنز**: `Subscription.aiTokensUsedThisPeriod = 0` بعد كل الاختبارات
   الأربعة أعلاه — صحيح تماماً، لأن كل مسار كان تحويلاً حتمياً سابقاً لأي استدعاء LLM فعلي.
6. ✅ **أداة `get_product_price` ضد بيانات حقيقية** (منفصلة عن اختبار المحرك، استدعاء مباشر لـ
   `executeAiTool`): بحث جزئي بالاسم ("عطر ورد") عن منتج حقيقي مُدرَج يدوياً ("عطر ورد دمشقي"، 249
   ر.س، 12 بالمخزون) أعاد البيانات الحقيقية بالضبط. بحث عن منتج غير موجود أعاد `{"found":false}` بصدق
   تام — لا تخمين أو اختلاق سعر بأي حال.
7. ✅ **قفل باقة Starter** (Playwright، تسجيل دخول فعلي بحساب تاجر-ب): صفحة `/dashboard/ai-agent`
   تعرض "🔒 هذه الميزة متاحة من الباقة الاحترافية فأعلى" فعلياً، مع بقاء عنصر التنقّل ظاهراً (مطابقة
   نمط بقية الميزات المقفلة بالباقة في هذا المشروع).
8. ✅ **صفحات باقة Growth** (Playwright، تسجيل دخول فعلي بحساب تاجر-أ): صفحة الإعدادات تعرض نموذج
   التفعيل الكامل + استهلاك التوكنز، صفحة الاختبار تعرض محادثة حية وتُظهر رسالة التحويل الفعلية فوراً
   عند إرسال طلب صريح، صفحة المراجعة تعرض أسباب التحويل المُسجَّلة من الاختبارات أعلاه بالضبط.

`npx tsc --noEmit` و`npm run build` نظيفان طوال الجولة. كل بيانات الاختبار (تينانت تدفق تجريبي، جهات
اتصال اختبار، منتج اختبار) حُذفت بعد التحقق، والتدفق التجريبي المُنافِس لتينانت-أ (كان مؤقتاً DRAFT
أثناء الاختبار ليُضمَن استخدام تدفق الاختبار وليس تدفق العرض التوضيحي القديم) أُعيد لحالته PUBLISHED
الأصلية. إعداد `AiAgentConfig` (مفعَّل، الاسم "سارة") تُرِك على تينانت-أ عمداً — تهيئة حقيقية صالحة
لعرض الميزة مستقبلاً، وليست بيانات اختبار فاسدة يجب حذفها.

### حد صادق للتحقق — يجب الإفصاح عنه بوضوح
**لا يوجد مفتاح OpenAI API حقيقي في بيئة التطوير هذه**، ولا يمكن الحصول على واحد دون تدخّل بشري (طلب
معلومات حساسة من المستخدم مُستبعَد صراحةً بقواعد هذه الجولة). لذلك: **لم يُتحقَّق حياً من جولة استدعاء
LLM فعلية كاملة** — قرار النموذج الحقيقي باستدعاء أداة `get_product_price` عند سؤال عن سعر، جودة
الرد النهائي الفعلي بلغة طبيعية، أو دقة تقييم الثقة الذاتي من نموذج حقيقي. كل ما لا يعتمد على استدعاء
شبكي فعلي — البوابات الست، منطق الأدوات نفسه (مُختبَر مباشرة ضد بيانات حقيقية)، القفل بالباقة، دقة سجل
الاستخدام، وكل صفحات الواجهة — تحقَّق منه حياً وبدقة كاملة كما هو موثَّق أعلاه. **الخطوة التالية
لإكمال التحقق**: مالك المنصة يضبط `OPENAI_API_KEY` حقيقياً من `admin/settings`، ثم إعادة اختبار بند 1
تحديداً من التحقق الذاتي المطلوب بالبرومنت (سؤال سعر منتج حقيقي عبر محادثة تجريبية فعلية).

---

## 13. إصلاح عطل النص العربي في فاتورة PDF + إعادة تصميم احترافية (الجولة السادسة عشرة، 2026-07-26)

### تشخيص العطل (نُفِّذ أولاً، قبل أي تصميم، كما طلب البرومنت)
فحص مباشر لكود `lib/billing/invoicePdf.ts` القديم أكّد السبب الجذري: `pdfkit` يرسم كل حرف عبر
`doc.text()` مباشرة بلا أي محرك تخطيط نص (Text Shaping Engine) — لا تشكيل عربي (ربط الحروف بأشكالها
الأولية/الوسطى/النهائية الصحيحة) ولا إعادة ترتيب ثنائي الاتجاه (BiDi/RTL)، بصرف النظر عن الخط
المُستخدَم. هذا عطل معماري في المكتبة نفسها كما توقّع البرومنت بالضبط (السبب الثاني من السببين
المذكورين)، وليس مجرد خط غير مُضمَّن.

### الإصلاح: معمارية HTML→PDF عبر Puppeteer (القرار المعماري المطلوب في البرومنت حرفياً)
- `puppeteer` أُضيفت كتبعية إنتاجية حقيقية، `pdfkit`/`@types/pdfkit` أُزيلا نهائياً.
- فاتورة كاملة الآن HTML/CSS تُصيَّر عبر Chromium حقيقي بلا واجهة — نفس محرك أي متصفح ويب عادي.
- خط Cairo (خط العلامة التجارية نفسه في كل الموقع) نُزِّل فعلياً بأربعة أوزان من Google Fonts
  ومُضمَّن Base64 مباشرة في كل فاتورة — استقلال كامل عن الشبكة وقت التوليد.
- متصفح Puppeteer واحد مشترك عالمياً (Singleton) بدل عملية Chromium جديدة لكل فاتورة.

### إعادة التصميم الاحترافي (بند 4 في البرومنت — كل عنصر مطبَّق)
ترويسة (اسم بائع/رقم فاتورة تسلسلي/شارة حالة ملوَّنة) + قسمي من/إلى + جدول بنود احترافي (وصف/كمية/
سعر قبل الضريبة/إجمالي) + صفوف مجموع فرعي/ضريبة/إجمالي نهائي بارز + رمز QR متوافق مع ZATCA بنص
توضيحي + تذييل (شروط دفع، شكر، تواصل) — كل ذلك بخلفية بيضاء ونص داكن عالي التباين ولون Indigo محدود
الاستخدام (خط فاصل/عنوان/شارة)، تماماً كما حدَّد البرومنت (هوية مختلفة عن الداشبورد الداكن).

### التحقق الذاتي الفعلي (كل بند من الستة المطلوبة في البرومنت، بأدلة حقيقية لا افتراضات)
1. ✅ **PDF حقيقي مفتوح فعلياً وفُحص بصرياً** — لقطة شاشة كاملة للفاتورة أُخذت عبر Puppeteer نفسه
   ونُظِرت مباشرة: تخطيط نظيف، ألوان صحيحة، لا أي تشوّه. بالإضافة لذلك، استُخرِج النص الكامل ببرنامج
   `pdftotext` (مستقل تماماً عن كود التوليد) وقُرئ سطراً بسطر — كل كلمة عربية ظاهرة وصحيحة ومتصلة.
2. ✅ **اسم تاجر بحروف عربية متنوعة** ("مؤسسة رأس الخيمة للأثاث والإنشاءات — الرئة الذهبية" — همزات
   على الألف/الواو، تاء مربوطة، لام ألف) ظهر صحيحاً ومتصلاً 100% في النص المُستخرَج.
3. ✅ **رمز QR فُحص بفكّ تشفير مستقل حقيقي** (مكتبة `jsQR` — قارئ QR فعلي، وليس إعادة تشغيل نفس دالة
   الترميز) — كل حقل TLV (اسم البائع، الرقم الضريبي `310123456700003`، الإجمالي `1299.00`، قيمة
   الضريبة `169.00`) طابق البيانات الأصلية المُدخَلة حرفياً.
4. ✅ **قيمة الضريبة صحيحة حسابياً وليست صفراً** — تحقُّق على فاتورة اختبار (1299 ر.س، ضريبة 169
   ر.س) **وعلى الفاتورة الحقيقية الوحيدة في قاعدة الإنتاج** (نفس القيم بالضبط) عبر تحميل PDF حقيقي
   من مسار الإنتاج الفعلي (تسجيل دخول حقيقي، استدعاء الـroute الفعلي، لا محاكاة).
5. ✅ **فاتورة طويلة تنتقل لصفحة ثانية بشكل منظم** — بما أن بنية البيانات لا تدعم "بنود متعددة"
   حقيقية أصلاً (فاتورة = تحصيل اشتراك واحد دائماً، قرار موثَّق في DECISIONS.md #227)، اختُبر فيضان
   المحتوى الواقعي الوحيد الممكن: نص "سبب فشل تحصيل" مطوَّل جداً (40 جملة مكرَّرة) → انتقل فعلياً
   لـ3 صفحات (تحقُّق عبر عدّ فواصل الصفحات في النص المُستخرَج) بلا قطع أي جدول/قسم في المنتصف
   (قواعد `page-break-inside: avoid` فعّالة).
6. ✅ تم إنشاء/تحديث `QA_REPORT.md` (هذا القسم) دون طلب أي رد فعل من المستخدم.

### عطل حقيقي إضافي اكتُشف وأُصلح أثناء التحقق الحي (وليس بعده)
تحميل فاتورة حقيقية عبر مسار الإنتاج الفعلي (الفاتورة الحقيقية الوحيدة INV-2026-00001) كشف وصف بند
مكرَّراً بالخطأ ("اشتراك اشتراك") لأن هذه الفاتورة تاريخية بلا `planKey` مسجَّل (سابقة لإضافة هذا
الحقل في جولة سابقة) — نص fallback كان يكرّر كلمة "اشتراك" خطأً. أُصلح فوراً وتحقَّق منه مجدداً عبر
نفس مسار الإنتاج الحي (النتيجة بعد الإصلاح: "اشتراك — 23 يوليو 2026 — 22 أغسطس 2026"، صحيح تماماً).

### ملاحظات تصميم إضافية (اكتُشفت أثناء التحقق، أُصلحت استباقياً)
- إزالة كل الرموز التعبيرية (🙏/⚠️) بعد اكتشاف تلفها عند استخراج النص وتضخيمها الملحوظ لحجم الملف
  (~195KB → ~43KB بعد الإزالة) — قرار مزدوج السبب (تقني + احترافية، مطابق لفواتير Stripe/QuickBooks
  التي لا تستخدم رموزاً تعبيرية أصلاً).
- تحويل كل المبالغ المالية لأرقام غربية صراحة (كانت تظهر هندية-عربية ١٬٢٩٩ بالمحاولة الأولى) —
  مطلوبة للقراءة الآلية/المحاسبية المتّسقة.

`npx tsc --noEmit` و`npm run build` نظيفان طوال الجولة. كل ملفات الاختبار المؤقتة (PDF، لقطات شاشة،
سكربتات تحقُّق) حُذفت بعد التحقق، وحزمة `jsQR` (أداة تحقُّق فقط) أُزيلت من التبعيات بعد الاستخدام.
