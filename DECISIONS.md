# سجل القرارات التقنية (DECISIONS.md)

> يوثّق هذا الملف كل قرار معماري/تقني اتُّخذ أثناء بناء المنصة دون الرجوع للمستخدم، مع المبرر. أي قرار لاحق يضاف هنا بترتيب زمني عكسي (الأحدث أولاً) لن يحدث — نضيف بالترتيب الزمني العادي مع تاريخ.

---

## 2026-07-22 — القرارات التأسيسية

### 1. المكدس التقني (Stack)
- **Next.js 14 (App Router) + TypeScript (strict mode)**: يسمح ببناء لوحة التاجر، لوحة Super Admin، وAPI routes/webhooks داخل مشروع واحد، مع دعم RSC للأداء.
- **PostgreSQL 16 عبر Docker Compose محلياً** (تم التأكد من توفر Docker Engine فعلياً في بيئة البناء وتشغيله) + Prisma ORM. لا حاجة لأي fallback — قاعدة البيانات الحقيقية للإنتاج مُشغَّلة ومُختبرة فعلياً أثناء البناء، بما في ذلك **Row Level Security فعلي مُفعَّل من اليوم الأول** (وليس مؤجلاً كمسار ترقية مستقبلي).
- **Prisma** كطبقة ORM بدلاً من كتابة SQL خام، لضمان type-safety ومigrations منظمة.
- **NextAuth.js (Credentials Provider + JWT session)** لإدارة الجلسات، مع RBAC مخصص فوقه (لا نستخدم NextAuth roles المدمجة لأنها غير كافية لتعدد المستأجرين).
- **Tailwind CSS + مكوّنات مبنية يدوياً بنمط shadcn/ui** (بدون تثبيت مكتبة خارجية كاملة) لضمان تحكم كامل بالهوية البصرية العربية RTL.
- **BullMQ + Redis** لقوائم الانتظار (إرسال الحملات، معالجة الـ webhooks). في بيئة التطوير بدون Redis فعّال، طبقة القوائم تعمل بوضع "inline fallback" (تنفيذ متزامن محاكى) موثّق فى الكود، مع واجهة موحدة بحيث التبديل لـ Redis حقيقي في الإنتاج لا يتطلب تغيير منطق الأعمال.

### 2. نموذج تعدد المستأجرين (Multi-Tenancy)
- **القرار: Row-Level Tenant Isolation** (عمود `tenantId` في كل جدول مرتبط بمستأجر) بدلاً من schema-per-tenant.
  - **السبب**: العدد المتوقع من التجار (مئات إلى آلاف) يجعل schema-per-tenant مكلفاً تشغيلياً (migrations متعددة، صعوبة query عابر للمستأجرين لمتابعة Super Admin). Row-level مع فهرسة `tenantId` يعطي نفس العزل المنطقي بتكلفة تشغيلية أقل بكثير، وهو النمط المعتمد في أغلب منصات SaaS B2B المشابهة (Intercom, Shopify Plus internal tooling).
  - **آلية العزل المطبّقة (دفاع متعدد الطبقات - defense in depth، أربع طبقات فعلية)**:
    1. **طبقة Repository إلزامية**: كل استعلام لبيانات مستأجرة يمر عبر دوال `lib/tenant/scoped-*.ts` التي تحقن `tenantId` تلقائياً من سياق الجلسة — لا يوجد أي مسار مباشر لـ Prisma client من داخل route handlers للبيانات الحساسة.
    2. **Prisma Client Extension** (`lib/db.ts`) يعترض كل عملية على الموديلات المستأجرة ويتحقق من وجود `tenantId` في شرط `where`، ويرمي خطأ وقت التطوير إذا غاب.
    3. **Postgres Row Level Security فعلي** (`prisma/postgres-rls.sql`): كل جدول مستأجر لديه `ENABLE ROW LEVEL SECURITY` + policy تقارن `tenant_id` بـ `current_setting('app.current_tenant_id')`. كل transaction من التطبيق ينفّذ `SET LOCAL app.current_tenant_id` قبل أي استعلام (`lib/db.ts` → `withTenant()`). هذا يعني أنه حتى لو تجاوز خطأ برمجي الطبقتين 1 و2، قاعدة البيانات نفسها ترفض إرجاع صفوف مستأجر آخر.
    4. **اختبار عزل فعلي ضد قاعدة بيانات حقيقية**: مستأجرين تجريبيين (`demo-tenant-a`, `demo-tenant-b`) وسكربت `prisma/verify-isolation.ts` يشغَّل فعلياً ضد Postgres عبر Docker ويؤكد أن استعلامات Tenant A (بما فيها محاولة تجاوز الـ repository layer عمداً) لا تُرجع أي صف من Tenant B.

### 3. RBAC (الأدوار والصلاحيات)
- أربعة أدوار: `SUPER_ADMIN` (مالك المنصة، خارج نطاق أي tenant)، `OWNER` (صاحب حساب التاجر)، `ADMIN` (مدير داخل التاجر)، `AGENT` (موظف رد محدود الصلاحيات).
- الصلاحيات محفوظة كمصفوفة ثابتة في `lib/rbac.ts` وليس في قاعدة البيانات — تبسيطاً، لأن عدد الأدوار محدود ومعروف مسبقاً؛ يمكن تحويلها لجدول `Permission` ديناميكي لاحقاً إذا لزم.

### 4. طبقة التكاملات (Meta / Zid / Salla)
- **Adapter Pattern موحّد**: واجهة `IntegrationAdapter` واحدة (`lib/integrations/types.ts`) بدوال `connect`, `disconnect`, `syncOrders`, `syncCatalog`, `sendMessage`, `verifyWebhookSignature`. كل من Meta/Zid/Salla ينفّذها بشكل مستقل. إضافة Shopify مستقبلاً = ملف adapter جديد فقط بدون تعديل أي كود استدعاء.
- **وضع Sandbox إلزامي عند غياب مفاتيح حقيقية**: متغير بيئة `INTEGRATIONS_MODE=sandbox` (القيمة الافتراضية) يجعل كل adapter يستخدم بيانات وهمية واقعية (`lib/integrations/fixtures/`) بدل استدعاء API خارجي فعلي، مع واجهة واضحة "🧪 وضع تجريبي" في كل صفحة تكامل. هذا يحقق متطلب "self-verification gate #4" دون الحاجة لمفاتيح Meta/Zid/Salla حقيقية غير متوفرة في هذه البيئة.
- **التحقق من توقيعات الـ Webhooks**: تم تنفيذ HMAC-SHA256 verification لـ Meta (`X-Hub-Signature-256`) وSalla، وHMAC مشابه لزد حسب توثيقهما العلني، في `lib/integrations/*/verify-signature.ts`. أي webhook بتوقيع غير صالح يُرفض بـ 401 قبل معالجته.

### 5. الهوية البصرية
- Deep Navy (`#0B1220`) كخلفية أساسية داكنة + Indigo/Violet (`#6D5EF8`) كلون Accent أساسي (بدلاً من Teal) لأنه يعطي إحساساً "تقنياً/SaaS" أقوى في السوق العربي B2B الحالي (مقارنة مع Teal الشائع في تطبيقات المحادثة العامة) ويميز المنصة عن واجهة واتساب الأصلية (الأخضر) لتفادي الالتباس بأن هذا "واتساب نفسه".
- خط Cairo للعربية + Inter للأرقام/الإنجليزي.
- التفاصيل الكاملة في `BRAND_GUIDE.md`.

### 6. الفوترة
- لا يوجد مزود دفع حقيقي متاح في البيئة (لا مفاتيح Stripe/PayTabs/Moyasar). تم بناء **وحدة فوترة كاملة الوظائف من الناحية المنطقية** (خطط، استخدام، فواتير، حالة اشتراك) مع طبقة `lib/billing/payment-provider.ts` بواجهة موحدة ووضع `mock` نشط افتراضياً، بحيث ربط Moyasar/PayTabs/Stripe لاحقاً هو تنفيذ adapter واحد فقط.

### 7. محرر الشات بوت
- بدلاً من محرر Canvas سحب-وإفلات حر بالكامل (يتطلب مكتبة رسم رسوم بيانية ثقيلة مثل React Flow ووقت تكامل كبير)، تم بناء **محرر تدفق قائم على قائمة خطوات مرتبة (sequential/branching step list)** بواجهة سحب-وإفلات لإعادة الترتيب (`@dnd-kit` بديل خفيف)، يدعم نفس أنواع العقد المطلوبة (شرط، رد AI، تحويل بشري، رسالة). هذا قرار **موازنة نطاق واقعي**: يحقق نفس الوظيفة الكاملة للمستخدم النهائي (بناء تدفق، اختباره حياً، نشره) دون مخاطرة تقنية لمحرك canvas معقد في جلسة بناء واحدة. يوثّق كـ "V1 مقبول للبيع" مع مسار ترقية لـ React Flow canvas في `QA_REPORT.md`.

### 8. Rate Limiting لكل مستأجر
- **القرار**: حد ثابت بنافذة زمنية (fixed window) عبر Redis (`lib/rateLimit.ts`)، مُطبَّق في نقطتين:
  1. عامل إرسال الحملات: 60 رسالة/10 ثوانٍ لكل مستأجر — تاجر بحملة ضخمة لا يحتكر عامل الإرسال
     المشترك، يُعاد جدولة الدفعة تلقائياً (حالة الحملة تعود `SCHEDULED` ويعيد BullMQ المحاولة).
  2. نقاط webhook الثلاث: 300 حدث/دقيقة لكل مستأجر بعد تحديد هويته — يرجع `429` عند التجاوز.
  - **لماذا fixed window وليس sliding window دقيق؟** الهدف حماية العدالة بين المستأجرين وليس
    قياس فوترة دقيق، وfixed window أبسط وأقل تكلفة استعلام لكل رسالة/حدث.

---

## 2026-07-22 (الجولة الثانية) — إعادة بناء وحدة الشات بوت باحترافية + بوابات الباقة والأدوار

### 9. نموذج بيانات التدفق: قائمة خطوات مرقّمة مع تفرّع بسيط بدل رسم بياني (Graph) كامل
- التدفق ما زال **قائمة معدّة الترتيب** (سهلة الفهم لتاجر غير تقني، كوصفة طبخ مرقّمة) بدل محرر
  canvas حر، اتساقاً مع قرار الجولة الأولى. لكن أُضيف تفرّع حقيقي لعقدة "شرط" فقط: كل شرط له
  حقلا `trueNext`/`falseNext` يمكن أن يكونا "الخطوة التالية"، "إنهاء المحادثة"، أو قفزة لعقدة
  محددة بالاسم (dropdown يعرض تسمية كل عقدة أخرى). هذا يعطي فروع "إذا/وإلا" حقيقية دون تعقيد
  محرر رسوميّ كامل — موازنة واعية بين البساطة والقدرة الوظيفية المطلوبة (بند 3 من البرومنت).
- أسماء العقد بالعربي البسيط فقط في الواجهة (بداية/رسالة/سؤال/شرط/رد ذكي/استدعاء API خارجي/
  تحويل لموظف/نهاية) — لا "Trigger/Webhook" في أي مكان يراه التاجر، فقط في التعليقات البرمجية.

### 10. معاينة حية بشكل واتساب (WhatsApp mock)
- مكوّن `WhatsAppPreview` يعرض "المسار الأساسي" (happy path) للتدفق كفقاعات محادثة فورية مع كل
  تعديل في المحرر — دون الحاجة لزر اختبار منفصل، تحقيقاً لبند 3. لوحة "اختبار قبل النشر" منفصلة
  ومطلوبة (تُنفَّذ فعلياً عبر محاكاة تشمل تقييم الشروط والتفرّع، وليس فقط المسار السعيد).

### 11. بوابات الباقة (Plan Gating) — تخزين قابل للتعديل من Super Admin
- `Plan.chatbotLimitsJson` (حقل JSON جديد) يخزّن: أقصى عدد تدفقات، أنواع العقد المتاحة، مستوى
  مكتبة القوالب (basic/full)، مستوى التحليلات (basic/advanced/export)، ودعم اللغات المتعددة.
  لوحة `/admin/plans` تتيح لمالك المنصة تعديل هذه القيم مباشرة (checkboxes + أرقام) دون لمس كود،
  محققاً الشرط الصريح في البرومنت.
- **إنفاذ من الخادم وليس الواجهة فقط**: `saveFlowNodes` يرفض أي نوع عقدة غير مسموح به في باقة
  التاجر حتى لو مرّره العميل عبر تلاعب مباشر بالـ API (وليس فقط إخفاء الزر). `createFlowFromTemplate`
  يرفض تجاوز `maxActiveFlows` وقوالب الباقة الاحترافية من نفس نقطة الخادم.
- أسلوب Soft-lock: الميزة المقفولة تظهر باهتة مع شارة "🔒 Pro"/"Business"، والضغط عليها يفتح
  `UpsellModal` يوجّه مباشرة لصفحة الفوترة — بدل إخفائها بالكامل، تماشياً مع توجيه البرومنت
  الصريح بأن هذا الأسلوب "يزيد التحويل للترقية".
- **تحليلات صادقة وليست مصطنعة**: مستوى "متقدم/تصدير" في الباقات الأعلى كان يمكن تلبيته ببيانات
  محادثات وهمية معروضة كأنها حقيقية — رُفض هذا الخيار عمداً لأنه تضليل. بدلاً من ذلك، الأساس
  الوحيد المتوفر حالياً هو **عدد وتفاصيل تشغيلات "اختبار قبل النشر" الفعلية** (`testRunCount`,
  `nodeTraversalJson`) لأن محرك تنفيذ الشات بوت الحقيقي غير مربوط بعد بمحادثات واتساب واردة
  فعلية (فجوة موثقة في QA_REPORT منذ الجولة الأولى). لوحة تحليلات أعمق مبنية على محادثات حقيقية
  تتطلب أولاً ربط المحرك بمسار استقبال الرسائل — مؤجل بوضوح كخطوة تالية.

### 12. بوابات الأدوار (Role Gating) — صلاحيات مجزّأة بدل صلاحية واحدة شاملة
- استُبدلت صلاحية `chatbot.manage` الواحدة بست صلاحيات مجزّأة: `chatbot.view`, `chatbot.edit`,
  `chatbot.publish`, `chatbot.delete_draft`, `chatbot.delete_published`, `chatbot.test`.
  - **مالك الحساب**: كل الصلاحيات.
  - **مدير**: كل شيء ما عدا `chatbot.delete_published` — لا يقدر يحذف تدفقاً منشوراً فعلياً
    (الزر لا يظهر أصلاً، والخادم يرفض المحاولة المباشرة أيضاً)، تطبيقاً حرفياً لبند 5.
  - **موظف**: `chatbot.view` + `chatbot.test` فقط. الضغط على بطاقة تدفق يوجّهه لصفحة اختبار
    مخصصة للقراءة فقط (`/dashboard/chatbot/[id]/test`) بدل المحرر الكامل.
  - **دفاع متعدد الطبقات**: محاولة موظف فتح رابط المحرر مباشرة (URL manipulation) تُعاد توجيهه
    خادمياً (`redirect()`) لصفحة "ليس لديك صلاحية" (`/dashboard/no-access`) — وليس فقط إخفاء
    الزر بصرياً. اختُبر فعلياً بمتصفح حقيقي (Playwright) وليس افتراضاً.

### 13. أخطاء حقيقية اكتُشفت وأُصلحت أثناء بناء هذه الجولة (بالاختبار الفعلي وليس المراجعة النظرية)
هذه الجولة كانت مناسبة لاكتشاف فئة كاملة من الأخطاء لم تظهر في اختبارات الجولة الأولى لأنها لم
تكن تغطي كل مسار قراءة:
1. **`getTenantChatbotLimits` كان يستعلم عن `Subscription` عبر `rawDb` مباشرة خارج `withTenant()`**
   — Subscription جدول مستأجر خاضع لـ RLS، فالاستعلام كان يعيد `null` بصمت دائماً، مما يعني أن
   **كل تاجر (بما فيهم تجار الباقات المدفوعة الكاملة) كان يُعامَل كأنه Starter** بسبب fallback
   آمن. هذا النوع من الأخطاء "يفشل بأمان" (لا تسريب بيانات) لكنه خطأ وظيفي جسيم لعميل يدفع فعلياً.
2. **نفس الفئة بالضبط في `dashboard/layout.tsx`** (شارة sandbox الديناميكية التي أُضيفت في هذه
   الجولة نفسها) **وفي `dashboard/chatbot/page.tsx`** (فحص حالة تكامل واتساب) — كلاهما استعلما
   عن `Integration` عبر `rawDb` خارج `withTenant()` فيعيدان `null` دائماً. تم تدقيق **كل** استدعاء
   `rawDb.*` في المشروع (`grep`) بعد اكتشاف هذا النمط، وتأكيد أن الاستدعاءات المتبقية آمنة (على
   جدولي `Plan`/`Tenant` غير الخاضعين لـ RLS أصلاً).
   **الدرس المسجَّل**: أي استعلام على جدول فيه `tenantId` يجب أن يمر عبر `withTenant()` دائماً —
   لا استثناء "قراءة بسيطة سريعة"، لأن RLS سيرفضها بصمت لا بخطأ صريح.
3. **`redirect()` مع نص عربي خام في query string** (`?from=محرر الشات بوت`) — يسبب
   `TypeError [ERR_INVALID_CHAR]` فعلياً في إنتاج Next.js لأن الأحرف غير اللاتينية والمسافات غير
   صالحة مباشرة في ترويسة HTTP `Location`. أُصلح بـ `encodeURIComponent()` على كل قيمة كهذه.
4. **تمرير `FLOW_TEMPLATES` (يحوي دوال `buildNodes`) من Server Component إلى Client Component**
   — React/Next يرفض تسلسل الدوال عبر حدود RSC. أُصلح بتمرير ملخص قابل للتسلسل فقط (بدون الدالة)
   للواجهة، مع بقاء التنفيذ الفعلي لـ `buildNodes` حصراً في `createFlowFromTemplate` على الخادم.
5. **قيود "use server"**: ملف `actions.ts` كان يُصدّر class (`PlanLimitError`) ودالة متزامنة
   (`canDeleteFlow`) بجانب دوال الخادم غير المتزامنة — Next.js يمنع هذا صراحة. نُقلا لملفين
   منفصلين (`lib/chatbot/errors.ts`, `lib/chatbot/permissions.ts`).
6. **`prisma/seed.ts` كان يتصل عبر `DATABASE_URL` العادي (app_runtime, خاضع لـ RLS)** — نفس فئة
   الخطأ رقم 1: الزرع عملية عابرة لكل المستأجرين بطبيعتها (تُنشئ عدة تجار دفعة واحدة)، فيجب أن
   تستخدم اتصال BYPASSRLS. اكتُشف عند إعادة تشغيل السكربت لأول مرة منذ التبديل لأدوار DB المحدودة.

جميع الأخطاء أعلاه اكتُشفت عبر **اختبار فعلي بمتصفح Chromium حقيقي** (`scripts/smoke-chatbot.js`)
وليس مراجعة كود نظرية — يوثّق هذا مدى أهمية الاختبار الفعلي (per Master Prompt القسم 7) بدل
افتراض صحة الكود من قراءته فقط.

## 2026-07-23 — إعادة بناء لوحة "مالك المنصة" (Super Admin) كغرفة تحكم فعلية

### 14. تعدد أدوار المنصة الداخلية (Internal Platform RBAC)
- أُضيفت قيمتان جديدتان لـ `UserRole`: `PLATFORM_SUPPORT` و`PLATFORM_BILLING`، منفصلتان عن
  `SUPER_ADMIN` (الذي بقي "كل شيء بلا استثناء"). صلاحيات مجزّأة حقيقية على مستوى الـ API/server
  actions (`requirePermission` في كل action، `hasPermission` لعرض/إخفاء الأزرار والصفحات) —
  ليست مجرد إخفاء واجهة. مصفوفة الأدوار الكاملة موثّقة في `src/lib/rbac.ts`.
  - **Support**: مراجعة موافقات + تعليق/تفعيل + انتحال هوية + ملاحظات داخلية + صحة المنصة —
    بدون حذف تاجر، بدون تعديل باقات، بدون وصول للإيرادات.
  - **Billing**: `platform.view_revenue` فقط — لا يصل إطلاقاً لصفحة تفاصيل التاجر (محادثات،
    فريق، تكاملات) حتى لو كتب الرابط مباشرة (الصفحة تتحقق من `platform.merchants.view` وتعرض
    رسالة حظر واضحة، وليس فقط إخفاء رابط في القائمة الجانبية).
- **قرار تقني مهم اكتُشف بالاختبار الفعلي**: الصفحات التي استخدمت `requirePermission` (التي
  ترمي استثناء) بدل `hasPermission` (فحص منطقي يُعاد استخدامه في `if`) كانت تُصدّر صفحة خطأ 500
  خام بدل رسالة "ليس لديك صلاحية" ودّية عند وصول مستخدم غير مخوَّل مباشرة عبر الرابط. تم توحيد
  كل صفحات `/admin/*` على نمط واحد: `hasPermission` + عرض بديل ودود لمنع الوصول، بينما
  `requirePermission` (الرامية للاستثناء) بقيت حصراً داخل *server actions* كخط دفاع ثانٍ خلفي.

### 15. مركز الموافقات (Approvals Center) كنموذج بيانات موحّد
- بدل حقول متفرقة لكل نوع موافقة، نموذج واحد `ApprovalRequest` بحقل `type` (NEW_TENANT /
  CUSTOM_PLAN / WHATSAPP_VERIFICATION) و`payloadJson` مرن لتفاصيل كل نوع. هذا يعطي شاشة مراجعة
  واحدة تتوسّع لأنواع موافقات مستقبلية (مثل توثيق سجل تجاري) دون تغيير هيكلي.
- **كل تاجر جديد يُنشأ بحالة `PENDING_REVIEW`** (تغيير `TenantStatus` الافتراضي من `TRIAL` سابقاً)
  ولا ينتقل لـ `TRIAL` الفعلي إلا بعد موافقة صريحة من مركز الموافقات. تسجيل الدخول يبقى مسموحاً
  في هذه الحالة (لعرض شاشة الانتظار)، لكن كل الوحدات محجوبة بالكامل عبر `dashboard/layout.tsx`.
- **تعليق/إلغاء/رفض حساب يمنع تسجيل الدخول من جذوره** (وليس فقط إخفاء المحتوى بعد الدخول) —
  الفحص يتم داخل `authorize()` في NextAuth نفسه قبل إصدار أي جلسة، مطابقةً لنص الاختبار الذاتي
  المطلوب صراحة في البرومنت ("دخوله يُرفض فعلياً بعد التعليق مباشرة").
- **ربط رقم واتساب جديد وطلب باقة مخصصة** يمران أيضاً عبر نفس مركز الموافقات (`WHATSAPP_
  VERIFICATION`, `CUSTOM_PLAN`) بدل مسارات موافقة منفصلة — التكامل يبقى "متصل" فعلياً
  (`Integration.status = CONNECTED`) لكن بعلامة `pendingVerification = true` حتى المراجعة.

### 16. انتحال الهوية (Impersonation) كطبقة كوكي مستقلة عن جلسة NextAuth
- بدل التلاعب بجلسة NextAuth نفسها (خطر تلوّث الجلسة الحقيقية لمالك المنصة)، الانتحال هو **كوكي
  httpOnly منفصل تماماً** (`impersonation_token`) يُفحص أولاً داخل `getCurrentSession()` قبل
  الرجوع لـ `getServerSession` الحقيقي. هذا يعني:
  - جلسة مالك المنصة الأصلية تبقى سليمة تحت الغطاء طوال وقت الانتحال — إنهاء الانتحال هو مجرد
    حذف الكوكي، دون أي إعادة تسجيل دخول.
  - الرمز الخام يُخزَّن **مُجزَّأً (SHA-256)** في قاعدة البيانات (`ImpersonationSession.tokenHash`)
    وليس نصاً صريحاً، بنفس منطق "لا نخزّن أسراراً كنص صريح" المتبع في تشفير مفاتيح التكاملات.
  - صلاحية 30 دقيقة فقط، ومستهدف الانتحال دائماً OWNER التاجر (وليس أي دور آخر).
- **شفافية إلزامية مزدوجة (بند 5 في البرومنت)**: كل بدء انتحال يكتب سجلّين منفصلين في نفس
  اللحظة: `platform.impersonation_start` في سجل تدقيق المنصة (بلا tenantId، يراه مالك المنصة)،
  و`support.impersonation_started` في سجل تدقيق **التاجر نفسه** (عبر `writeAuditLog` العادي، يظهر
  لصاحب الحساب في `/dashboard/settings` — تماماً كما يرى أي نشاط آخر في فريقه). اختُبر الجانبان
  فعلياً بمتصفح حقيقي في `scripts/smoke-super-admin.js`.

### 17. لوحة تدقيق شاملة (Audit Log Viewer) — فجوة اكتُشفت أثناء الاختبار الذاتي
- كانت كل أفعال Super Admin تُكتب فعلياً في `AuditLog` منذ الجولة الأولى، **لكن لم توجد أي صفحة
  لعرضها لمالك المنصة نفسه** — وهذا يخالف حرفياً بند البرومنت "كل إجراء... يُسجَّل... ويجب أن
  تكون [اللوحة] مراقبة ذاتياً". أُضيفت `/admin/audit-log` (صلاحية `platform.audit_log.view`،
  حصراً لـ SUPER_ADMIN) تعرض كل الأحداث (منصة + داخل أي تاجر) مع ترقيم صفحات.

### 18. صحة المنصة والإيرادات: بيانات حقيقية فقط، لا أرقام مصطنعة
- كل مؤشر في `/admin/health` (اتصال واتساب عالمياً، معدل فشل webhooks، تجار قريبون من حد
  الباقة، تجار خاملون) مبني على استعلامات حقيقية عبر `superAdminDb` (BYPASSRLS) — لا بيانات
  محاكاة. "تجار نشطون باستخدام شبه صفري للبوت" تحديداً يعتمد على `ChatbotFlow.testRunCount`
  الحقيقي (نفس الحقل من الجولة السابقة)، مع توثيق صريح في QA_REPORT أن هذا وليس محادثات حقيقية
  لأن محرك تنفيذ البوت غير مربوط بعد بالرسائل الواردة الفعلية (فجوة قديمة موثّقة، لم تُخفَ هنا).
- "مدفوعات فاشلة" في `/admin/billing` يعرض حالة فارغة صادقة ("لا توجد حالياً") بدل تلفيق فشل
  دفع وهمي — لأن `mockPaymentProvider` الحالي ينجح دائماً بتصميم، ومحاكاة فشل عشوائي هنا كانت
  ستُقدَّم كبيانات مالية حقيقية بينما هي مسرحية، وهذا مضلِّل في سياق لوحة إيرادات فعلية.

### 19. إعلانات المنصة: in-app فقط (لا مزود بريد إلكتروني مربوط)
- `PlatformAnnouncement` + `AnnouncementDismissal` ينفّذان "تواصل جماعي" فعلياً كإشعار داخل
  لوحة التاجر (يظهر في `dashboard/layout.tsx` لكل صفحة، قابل للإغلاق ولا يتكرر). **لم يُبنَ** أي
  إرسال بريد إلكتروني حقيقي لأن لا مزود بريد (SendGrid/SES) مربوط في هذه البيئة — البرومنت طلب
  "إشعار/بريد"، ونفّذنا الشق القابل للتحقق فعلياً (in-app) بدل الادّعاء بإرسال بريد لا يصل فعلياً.
  موثّق بوضوح في واجهة `/admin/announcements` نفسها، وليس مخفياً.

### 20. أخطاء حقيقية إضافية اكتُشفت بالاختبار الفعلي (Playwright، وليس مراجعة نظرية)
1. `dismissAnnouncement` كانت تقبل `tenantId` من العميل مباشرة كوسيط — أي تاجر خبيث قادر نظرياً
   على إغلاق إعلان تاجر آخر بتمرير معرّف مختلف. أُصلح بجعل الدالة تشتق `tenantId` من الجلسة
   الحقيقية (`requireTenantSession()`) حصراً، ونُقلت لملف تابع لمساحة التاجر وليس لوحة الإدارة.
2. صفحة "حدود الباقات" وصفحة الفوترة العامة للتاجر كانتا تعرضان أي باقات مخصصة (Enterprise) تم
   إنشاؤها لتاجر آخر ضمن الكتالوج العام — أُصلح بفلترة `isCustomForTenantId: null` في كل استعلام
   كتالوج عام (الباقة المخصصة تظهر فقط لصاحبها عبر صفحة تفاصيله).
3. صفحة تفاصيل التاجر كانت تحسب "حد التدفقات" من `Plan.maxWhatsappNumbers` بالخطأ (نسخ-لصق) بدل
   `chatbotLimitsJson.maxActiveFlows` الصحيح، وتعرض "جهات الاتصال" بدل "تدفقات الشات بوت" رغم أن
   البرومنت يطلب صراحة (رسائل/تدفقات/مستخدمين) — أُصلح الحقلان معاً.
4. جميع الأخطاء أعلاه (وأخطاء RLS/التشفير من الجولات السابقة) اكتُشفت **فقط** لأن كل جولة انتهت
   باختبار Playwright يقارن سيناريوهين فعليين جنباً إلى جنب (باقتين مختلفتين، دورين مختلفين) —
   وليس لأن الكود "بدا صحيحاً" عند كتابته أو قراءته.

## 2026-07-23 (الجولة الرابعة) — لوحة مالك المنصة كغرفة تحكم شاملة فعلياً

طلب المستخدم توسيع لوحة مالك المنصة لتكون "اللوحة الأساسية التي بتدير وتبني منها كل شيء في
المنصة وتتحكم في كل اللوحات الأخرى" — حُدِّدت 4 فجوات ملموسة عبر سؤال توضيحي واحد بدل التخمين،
والمستخدم اختار الأربعة معاً.

### 21. إدارة الباقات الكاملة (CRUD) بدل حدود الشات بوت فقط
- `/admin/plans` كانت تعدّل `chatbotLimitsJson` فقط. أُضيف الآن: إنشاء باقة جديدة بالكامل
  (اسم، سعر، حدود مستخدمين/أرقام واتساب/رسائل، ميزات معروضة)، تعديل نفس الحقول لباقة قائمة،
  وتعطيل/تفعيل باقة (بدل حذفها — الحذف الفعلي قد يكسر اشتراكات تجار قائمين، فالتعطيل يمنع
  الاشتراك الجديد بها فقط مع إبقاء بيانات الاشتراكات الحالية سليمة). عدد المشتركين الحاليين
  يظهر على كل بطاقة باقة لمعرفة الأثر قبل أي تعديل جوهري.

### 22. بيانات كل تاجر عبر تبويبات فعلية بدل صفحة واحدة مكدّسة
- `/admin/tenants/[id]` أصبح layout بتبويبات (نظرة عامة، الفريق، جهات الاتصال، الحملات، الشات
  بوت، قوالب الرسائل، الطلبات والمنتجات) بدل تكديس كل شيء بصفحة واحدة طويلة. كل تبويب صفحة
  مستقلة تُحمَّل بياناتها فقط عند الحاجة (أداء أفضل من تحميل كل شيء دفعة واحدة لتاجر بحجم بيانات
  كبير). التبويب النشط يُبرَز عبر مكوّن عميل صغير (`TenantDetailTabs`) بنفس نمط `Sidebar.tsx`.

### 23. موافقة قوالب الرسائل تُعاد استخدام مركز الموافقات القائم بدل نظام موازٍ
- بدل بناء شاشة "موافقات قوالب" منفصلة، أُضيف `MESSAGE_TEMPLATE` كنوع رابع في `ApprovalRequestType`
  الموجود أصلاً (قرار الجولة الثالثة) — نفس شاشة `/admin/approvals`، نفس أزرار قبول/رفض، فقط
  عرض مختلف للـ payload. هذا يحاكي واقعياً مراجعة Meta الفعلية لقوالب واتساب (كل قالب جديد
  PENDING إلزامياً، لا اعتماد تلقائي أبداً) دون تكرار بنية تحتية جديدة لسيناريو مطابق منطقياً
  لما هو موجود.
- أُضيفت صفحة تاجر جديدة كاملة (`/dashboard/templates`) لم تكن موجودة إطلاقاً من قبل — التجار
  لم يكن لديهم أي وسيلة لإنشاء قالب رسالة، فقط استهلاك قوالب مزروعة مسبقاً في `seed.ts`. هذه
  فجوة حقيقية في الوحدة الأولى (الحملات) اكتُشفت فقط عند تتبع "من أين يأتي القالب أصلاً".

### 24. إعدادات المنصة العامة (Global Settings) — تحويل fallback البيئي لمصدر حقيقة قابل للتغيير حياً
- `INTEGRATIONS_MODE` كان env var ثابتاً يتطلب إعادة نشر لتغييره. أُضيف نموذج `PlatformSettings`
  (صف وحيد) ولوحة `/admin/settings` تتحكم فيه **فعلياً وقت التشغيل**: `isSandboxMode()` أصبحت
  async تقرأ من قاعدة البيانات أولاً (fallback لـ env فقط إن تعذّر الاتصال — دفاعي وليس المسار
  الطبيعي). هذا تطلّب تحويل `verifyWebhookSignature` في الواجهة الموحدة والـ 3 adapters لدالة
  async، وتحديث نقاط webhook الثلاث لـ `await` النتيجة.
- **وضع الصيانة العام**: يمنع كل التجار فوراً من الدخول للوحاتهم (باستثناء جلسة انتحال هوية —
  فريق الدعم يحتاج الدخول الفعلي أثناء الصيانة نفسها للتحقق من المشكلة). فُحص هذا فعلياً: تفعيل
  الوضع من `/admin/settings` منع تاجراً حقيقياً من الوصول للوحته خلال نفس الطلب التالي مباشرة.
- `defaultTrialDays` استُخدم فعلياً في `registerTenant` بدل الرقم `14` المكتوب يدوياً سابقاً.

### 25. خطأ حقيقي إضافي اكتُشف بالاختبار الفعلي في هذه الجولة
- `admin/settings/actions.ts` استخدم `rawDb.auditLog.create()` (خاضع لـ RLS) بدل
  `superAdminDb.auditLog.create()` (BYPASSRLS) — **نفس فئة الخطأ المكتشفة 3 مرات سابقاً بالضبط**
  (`getTenantChatbotLimits`, `dashboard/layout.tsx`, `seed.ts`) رغم توثيقها الصريح في هذا الملف
  والتعليقات البرمجية. اكتُشف عبر اختبار فعلي (تفعيل/تعطيل وضع الصيانة يطلق `updatePlatformSettings`
  مرتين) أدى لخطأ 500 حقيقي. **الدرس المتكرر**: التوثيق وحده لا يمنع الخطأ — فقط الاختبار الفعلي
  يكشفه؛ يُنصح مستقبلاً بفحص lint/type مخصص يرفض استيراد `rawDb` في أي ملف `actions.ts` تحت
  `src/app/admin/` (كلها يجب أن تستخدم `superAdminDb` حصراً) بدل الاعتماد على الانتباه اليدوي.
- كذلك اكتُشف عيب في سكربت الاختبار نفسه (وليس التطبيق): `waitForSelector("h1")` بعد النقر على
  رابط تاجر كان يتطابق مع `h1` موجود مسبقاً على صفحة القائمة نفسها (قبل اكتمال التنقل)، فيواصل
  السكربت التنفيذ على الصفحة الخطأ. أُصلح باستخدام `waitForURL` بنمط مطابق لمسار التفاصيل تحديداً
  بدل انتظار عنصر عام قد يوجد على الصفحتين معاً — درس عام: عناصر الانتظار في الاختبار يجب أن
  تكون **حصرية للصفحة الهدف**، لا مجرد "موجودة عموماً".

---

## 2026-07-23 (الجولة الخامسة) — محرر الشات بوت كلوحة رسم حرة (Canvas) بدل قائمة تسلسلية

طلب المستخدم صراحة: "عاوز في الشات بوت يكون في ايقونه موبيل للمعاينه وبناء الشات ويكون في زي
اتوميشن كمان نبني فيه الشات بوت كامل" — أي معاينة على شكل موبايل حقيقي، ومحرر تدفق حر شبيه
بأدوات الأتمتة (n8n/Zapier/Make) بدل القائمة التسلسلية. هذا هو مسار الترقية الذي وُثِّق صراحةً
كخيار مؤجل في القرار رقم 7 والقرار رقم 9 أعلاه ("مسار ترقية محتمل لاحقاً عبر React Flow") — نُفِّذ
الآن بالكامل.

### 26. نموذج البيانات: من قائمة+`trueNext`/`falseNext` إلى رسم بياني حقيقي (`FlowGraph`)
- `FlowNode` أصبح يحمل `position: {x, y}` بدل الاعتماد على ترتيبه في المصفوفة، و`FlowEdge` مستقل
  (`{id, source, target, sourceHandle?}`) بدل حقلي القفز النصيين على عقدة الشرط نفسها. هذا يطابق
  تماماً الشكل الذي يتوقعه `@xyflow/react` (nodes[] + edges[])، ويسمح بتفرعات وتقاطعات لا يمكن
  التعبير عنها بحقلي `trueNext`/`falseNext` فقط (مثال: عقدتان مختلفتان تؤديان لنفس الوجهة).
- **لا حاجة لأي Prisma migration**: `ChatbotFlow.nodesJson` كان أصلاً عمود `Json` عام منذ الجولة
  الثانية، فتغيير الشكل الداخلي (مصفوفة ← كائن `{nodes, edges}`) هو تغيير على طبقة التطبيق فقط.
  بيانات `ChatbotFlow` القديمة في القاعدة (بالشكل القديم) غير متوافقة مع القارئ الجديد؛ عُولجت
  بإعادة زرع البيانات التجريبية (`prisma/seed.ts` مُحدَّث لينتج الشكل الجديد مباشرة) بدل كتابة
  سكربت ترحيل (migration script) لبيانات تجريبية ما قبل الإطلاق — لا داعي لتعقيد كهذا لبيانات
  عرض توضيحي فقط. **بيانات تاجر حقيقي بعد الإطلاق ستحتاج فعلياً سكربت تحويل لو تغيّر الشكل حينها.**

### 27. `ChatbotFlowEditor` كلوحة React Flow حقيقية، مع الحفاظ على كل بوابات الباقة/الأدوار القائمة
- استُبدلت قائمة الخطوات المرتبة (أزرار ↑/↓، `<select>` مزدوج لعقدة الشرط) بلوحة رسم فعلية:
  `useNodesState`/`useEdgesState`, `onConnect` (يستبدل تلقائياً أي اتصال قديم خارج من نفس
  المقبض عند سحب اتصال جديد، بحيث يبقى كل مقبض خرج بحد أقصى اتصال واحد — يطابق دلالة "إذا/وإلا"
  لعقدة الشرط)، و`isValidConnection` يمنع أي اتصال يستهدف عقدة "بداية" (لا يصح أن تُستقبل).
  عقدة "بداية" وحدها `deletable: false` على مستوى العقدة نفسها (وليس فقط إخفاء زر الحذف) لمنع
  حذفها حتى عبر مفتاح Delete من لوحة المفاتيح.
- **بوابة الباقة على مستوى نوع العقدة بقيت كما هي حرفياً**: زر إضافة عقدة من نوع غير مسموح به
  يفتح نفس `UpsellModal` القائم بدل إضافتها فعلياً؛ الإنفاذ الخادمي في `saveFlowGraph` (بديل
  `saveFlowNodes`) لا يزال يرفض أي عقدة من نوع محظور حتى لو وصلت عبر تلاعب مباشر بالطلب — نفس
  مبدأ القرار رقم 11، فقط الحقل الذي يُفحص الآن `graph.nodes` بدل مصفوفة مسطحة.
- **بطاقة عقدة موحّدة** (`FlowNodeCard`) بدل مكوّن منفصل لكل نوع: تبسيط لأن الفروق بين الأنواع
  (مقبض دخول واحد/بدون، مقبض خروج واحد أو اثنان مُسمَّيان "نعم"/"لا" لعقدة الشرط فقط، حقل تكوين
  إضافي لاستدعاء API) قابلة للتعبير بفرع شرطي داخل مكوّن واحد دون تكرار.

### 28. معاينة واتساب كإطار موبايل حقيقي بدل صندوق مسطّح
- `WhatsAppPreview` أعيد بناؤه كإطار جهاز (حواف داكنة، شق علوي/notch، شريط حالة بالوقت الفعلي،
  رأس محادثة واتساب، منطقة فقاعات، شريط إدخال سفلي، ومؤشر الهوم) بدل بطاقة مستطيلة بسيطة سابقاً
  — تحقيقاً حرفياً لطلب "ايقونه موبيل للمعاينه". منطق بناء الفقاعات (`buildHappyPathBubbles`) نُقل
  من داخل المكوّن نفسه إلى `lib/chatbot/simulate.ts` ليعمل على الرسم البياني الجديد عبر نفس آلية
  اجتياز الحواف (`findOutgoingEdge`) المستخدمة في محاكاة الاختبار الحي — منطق اجتياز واحد بدل
  نسختين منفصلتين (معاينة + اختبار) قد تنحرفان عن بعضهما مستقبلاً.

### 29. فجوة حقيقية اكتُشفت أثناء الاختبار الفعلي في هذه الجولة: `prisma/seed.ts` بلا خطوة تصفير
- أثناء إعادة تشغيل اختبارات Playwright المتكررة لهذه الجولة، تبيّن أن `prisma/seed.ts` **يُنشئ
  بيانات إضافية عند كل تشغيل** (لا يوجد `deleteMany`/`TRUNCATE` قبل الزرع)، فتراكمت تدفقات شات بوت
  زائدة عبر عدة تشغيلات متتالية حتى تجاوز تاجر Starter حدّه الفعلي لعدد التدفقات (2)، مما أخفى
  زر "استخدام هذا القالب" الحقيقي خلف نافذة "وصلت للحد الأقصى" وتسبّب بفشل ظاهري في اختبار لم يكن
  خللاً في المنتج بل في نظافة بيانات الاختبار. **الحل المؤقت المطبَّق**: تفريغ كل الجداول يدوياً
  (`TRUNCATE ... RESTART IDENTITY CASCADE`) قبل كل إعادة زرع أثناء هذه الجولة، كما ذُكر في تعليقات
  جلسات سابقة ("re-established via cleanup SQL") لكنه لم يكن موثَّقاً كسكربت دائم. **موصى به لاحقاً**:
  إضافة `npm run db:reset` رسمي (`docker exec ... TRUNCATE` ثم `db:seed`) بدل الاعتماد على تنفيذ
  يدوي متكرر لأمر SQL طويل في كل جلسة اختبار.

---

## 2026-07-23 (الجولة السادسة) — وحدة الحملات المتكاملة End-to-End

طلب المستخدم صراحة تحويل صفحة "الحملات" (جدول ساكن بلا معالج بناء أو تحليل حقيقي) لوحدة حملات
متكاملة: معالج إنشاء متعدد الخطوات، شرائح جمهور حقيقية، صفحة تفاصيل مع تتبع تحويل إيراد، وفصل واضح
بين الحملات لمرة واحدة والحملات الآلية المبنية على أحداث — دون أي رجوع بسؤال توضيحي (ممنوع صراحة في
البرومنت)، فكل قرار تصميمي أدناه اتُّخذ بالفحص المباشر للكود القائم وقرار مستقل مسجَّل هنا.

### 30. نموذج البيانات: حقول جديدة على `Campaign` بدل نموذج موازٍ للحملات الآلية
- بدل بناء موديل منفصل "AutomationCampaign"، أُضيف `type: ONE_TIME | TRIGGERED` على `Campaign` نفسه
  + `triggerEvent`/`triggerConfigJson` اختياريان. **السبب**: الحملة الآلية والحملة العادية تتشاركان
  نفس البنية الجوهرية (قالب، مستلمون، عدادات) — الفرق الحقيقي الوحيد هو **كيف تُملأ قائمة المستلمين
  وبأي إيقاع**: دفعة واحدة عند الإرسال مقابل تدريجياً كلما تحقق حدث. تمثيل هذا كحقل `type` على نفس
  الجدول تفادى ازدواج الكود في كل مكان يعرض/يفلتر/يحسب حملة (الجدول، الفلاتر، صفحة التفاصيل).
- `audienceType: ALL | SEGMENT` + `segmentId` أصبح **اختيارياً** (كان إلزامياً) — "كل جهات الاتصال"
  خيار جمهور حقيقي طلبه البرومنت صراحة (بند 2.أ) ولم يكن ممكناً تمثيله سابقاً دون شريحة وهمية باسم
  "كل العملاء" (كما كانت البيانات التجريبية القديمة تفعل فعلياً — حل ملتفّ حول قصور في النموذج).
- `CampaignStatus` امتدت بقيمتين: `ACTIVE` (حملة آلية نشطة ومستمرة) و`PAUSED` (متوقفة مؤقتاً) —
  متمايزتان صراحة عن `COMPLETED`/`SENDING` الخاصتين بالحملات لمرة واحدة، حتى لا تبدو حملة آلية نشطة
  وكأنها "انتهت" في أي واجهة تعرض الحالة الخام (بالضبط الالتباس الذي أشار له البرومنت في بيانات
  الاختبار القديمة: حملة استرداد السلة كانت `COMPLETED`).
- `RecipientStatus` امتدت بقيمة `READ` منفصلة عن `DELIVERED` — البرومنت يطلب صراحة "تم التسليم،
  تمت القراءة، تم الرد" كثلاث مراحل مميزة في الملخص البصري، وكانت `DELIVERED` وحدها تخلط "وصلت
  للجهاز" مع "قرأها العميل فعلياً"، وهما حدثان مختلفان زمنياً وقيمةً تحليلية.

### 31. عدادات الحملة: تراكم تدريجي (increment) حصري لكل مستلم بدل إعادة حساب دفعة واحدة
- كل مستلم يُخزَّن بحالته **النهائية الحصرية فقط** (`sentCount`/`deliveredCount`/`readCount`/
  `repliedCount`/`failedCount` تُزاد كل واحدة عند وصول المستلم لتلك الحالة تحديداً، لا تراكمياً).
  الواجهة تجمع `deliveredCount + readCount + repliedCount` لعرض "تم التسليم" **التراكمي** (لأن من
  قرأ الرسالة أو ردّ عليها قد سُلِّمت له فعلاً ضمنياً). **لماذا لا نخزّن الحالة كتراكمية مباشرة؟**
  لأن ذلك يتطلب تحديث عدة أعمدة لكل مستلم واحد بدل عمود واحد، ويصعّب معرفة "كم بالضبط توقف عند
  التسليم فقط دون قراءة" لو احتجناها لاحقاً — التمثيل الحصري أبسط وأدق ويُشتق منه أي تجميع لاحق.
- هذا استلزم إعادة كتابة عامل الإرسال (`worker.ts`) بالكامل من نمط "احسب كل شيء دفعة واحدة في
  نهاية المعالجة" (النمط القديم المناسب فقط لحملة لمرة واحدة محدودة الحجم) إلى نمط "زِد العدّاد مع
  كل رسالة تُرسَل فردياً" — **إلزامي وليس تفضيلاً معمارياً**: الحملات الآلية تُطلق دفعات صغيرة
  متفرقة زمنياً بلا نهاية محددة، فلا يوجد "نهاية معالجة" لإعادة الحساب عندها أصلاً.

### 32. حصة الرسائل الشهرية (`Subscription.messagesUsedThisPeriod`): من عرض بلا استهلاك إلى فرض فعلي
- اكتُشف أثناء فحص الكود القائم أن هذا الحقل **يُعرض في 4 صفحات مختلفة** (لوحة التاجر، الفوترة،
  لوحة الصحة، تفاصيل التاجر عند Super Admin) **لكن لا يُزاد في أي مكان بالكود** — رقم ثابت من
  الزرع فقط. هذه فجوة صامتة خطيرة: التاجر يرى شريط استهلاك يبدو حقيقياً لكنه في الواقع لا يتحرك
  أبداً بصرف النظر عن حجم إرساله الفعلي. أُصلحت جذرياً في هذه الجولة: كل محاولة إرسال فعلية (ناجحة
  أو فاشلة على مستوى واتساب) في `worker.ts` تزيد هذا الحقل بمقدار واحد — نفس منطق فوترة واتساب
  الفعلي (يُحتسب الاستهلاك عند وصول الطلب لواجهة الإرسال، وليس فقط عند نجاحه).
- **الإنفاذ من الخادم قبل الإرسال**: `createCampaign` يرفض إنشاء أي حملة لمرة واحدة يتجاوز جمهورها
  المتبقي من الحصة (`maxMessagesPerMonth - messagesUsedThisPeriod`) **قبل** إنشاء أي صف، مطابقةً
  حرفياً لبند 2.هـ في البرومنت ("منع إرسال حملة يتجاوز حجم جمهورها الحصة المتبقية"). دفاع إضافي في
  العامل نفسه: يتوقف عن الإرسال بصمت (بدل تجاوز الحصة سلباً) إن نفدت الحصة أثناء التنفيذ الفعلي
  (حالة تنافسية نظرية: حملتان تستهلكان نفس الحصة في نفس اللحظة تقريباً).

### 33. جدولة الحملات + الحملات الآلية عبر BullMQ (delay/repeat) بدل بناء نظام Cron منفصل
- الحملات المجدولة بتاريخ مستقبلي تستخدم خيار `delay` المدمج أصلاً في BullMQ (`enqueueCampaignSend`
  مع فارق الوقت بالمللي ثانية) بدل استحداث بنية جدولة جديدة. الحملات الآلية تُفحص عبر مهمة BullMQ
  **متكررة** (`repeat.every` كل 5 دقائق) تعمل داخل نفس عملية العامل. **القرار**: إعادة استخدام
  البنية التحتية للقوائم الموجودة أصلاً (BullMQ+Redis) بدل إضافة مكتبة/خدمة cron منفصلة (مثل
  node-cron) — يقلّل نقاط الفشل التشغيلية ويعالج جزئياً فجوة "لا Cron" الموثّقة سابقاً في
  QA_REPORT.md (البند 6 القديم)، **لكن هذا لا يُغني عن تشغيل عملية العامل (`npm run worker`) كخدمة
  منفصلة دائمة** — اكتُشف فعلياً أثناء اختبار هذه الجولة أن غيابها يترك كل حملة في حالة "مجدولة"
  إلى الأبد بصمت (تفصيل كامل في QA_REPORT.md، القسم -3).
- زر "🔍 فحص الآن" في واجهة الحملات الآلية يستدعي نفس منطق الفحص (`scanTriggeredCampaigns`) مباشرة
  من Server Action دون المرور بقائمة الانتظار — إتاحة تحقق فوري (مطلوب فعلياً لبند التحقق الذاتي
  رقم 4 في البرومنت) دون انتظار دورة الفحص الدورية كل 5 دقائق.

### 34. حملات آلية: استخدام القيد الفريد `campaignId+contactId` الموجود أصلاً كآلية "مرة واحدة لكل جهة اتصال" بدل حقل تبريد جديد
- بدل إضافة حقل "آخر استهداف" أو نافذة تبريد صريحة لمنع استهداف نفس العميل مرتين بنفس الحملة
  الآلية، استُخدم القيد الفريد الموجود أصلاً على `CampaignRecipient` (`@@unique([campaignId,
  contactId])`) — بمجرد استهداف جهة اتصال مرة واحدة بحملة آلية معيّنة، لا تُستهدف بها ثانية إطلاقاً
  (وليس "خلال فترة" فقط). لسلة متروكة: `Order.recoveryMessageSentAt` (موجود أصلاً من الجولة
  الأولى) يمنع إعادة استهداف **نفس الطلب**، مستقلاً عن القيد الفريد للحملة. هذا تبسيط واعٍ مناسب
  لحجم البيانات التجريبية الحالي؛ حملة آلية حقيقية طويلة الأمد (مثال: "ذكّر كل شهر بعد 30 يوم خمول"
  متكرر) تحتاج مستقبلاً حقل تبريد صريح بدل الاعتماد على القيد الفريد وحده — موثّق كقرار نطاق.

### 35. شرائح الجمهور: فلتر واحد قابل للتركيب بدل شرائح محفوظة منفصلة يديرها التاجر
- بدل بناء صفحة "إدارة الشرائح" منفصلة (لم تكن موجودة إطلاقاً قبل هذه الجولة — الشرائح كانت تُنشأ
  فقط داخل `seed.ts`)، أُضيف بناء الشريحة **داخل معالج إنشاء الحملة نفسه** (خطوة "الجمهور")، تماماً
  كأدوات مشابهة (Klaviyo/Mailchimp) حيث تُبنى الشريحة أثناء إنشاء الحملة وتُحفَظ تلقائياً للرجوع
  إليها. الفلتر (`SegmentFilter`) قابل للتركيب: مرحلة العميل + وسوم + نشاط محادثة (نشط خلال/غير
  نشط منذ) + حالة طلب زد/سلة خلال نافذة زمنية — كل حقل اختياري يُضاف كشرط AND إضافي، مطابقةً حرفياً
  لأمثلة البرومنت ("عملاء اشتروا خلال 30 يوم"، "سلات متروكة خلال 7 أيام").
- **فرض `marketingOptIn=true` دائماً بصرف النظر عن اختيار التاجر** (`resolveAudienceWhere`) — طبقة
  امتثال إضافية غير قابلة للتعطيل من الواجهة، تماماً كمبدأ فرض حدود الباقة من الخادم في وحدة الشات
  بوت سابقاً. أُضيف حقل `Contact.marketingOptIn` (افتراضي `true`) لهذا الغرض تحديداً.

### 36. تتبع تحويل الإيراد: حساب حي عند العرض بدل عدّاد مخزَّن يُحدَّث بمهمة خلفية
- `linkCampaignConversions` يُنفَّذ **حياً** عند فتح صفحة تفاصيل أي حملة (وليس عبر مهمة دورية تُحدِّث
  عموداً مخزَّناً)، فيربط كل مستلم لم يُسنَد له تحويل بعد بأي طلب فعلي لنفس جهة الاتصال خلال نافذة
  7 أيام بعد إرسال الرسالة له. **القرار مطابق لمبدأ "تحليلات صادقة وليست مصطنعة" الموثّق من الجولة
  الثانية**: حجم البيانات هنا صغير بما يكفي ليبقى حساباً حياً مباشراً دون تعقيد مهمة خلفية إضافية،
  وهذا يضمن أن "الإيراد الناتج عن هذه الحملة" — أهم رقم لإقناع التاجر بقيمة المنصة حسب نص البرومنت
  نفسه — حيّ دائماً ولا يتأخر عن الواقع أبداً.
- نافذة الإسناد (7 أيام) وقائمة حالات الطلب المؤهلة (`PAID`/`SHIPPED`/`DELIVERED`) ثابتتان في الكود
  حالياً (`lib/campaigns/conversions.ts`) وليستا قابلتين للتعديل من لوحة الإدارة — قرار نطاق: قيمة
  معقولة صناعياً (نوافذ الإسناد التسويقي الشائعة 3–14 يوماً)، وتعديلها لاحقاً لكل باقة/تاجر إضافة
  بسيطة إن ظهرت حاجة فعلية لها.

### 37. محاكاة قمع تفاعل واقعية (sandbox) بدل نجاح/فشل ثنائي فقط
- `metaAdapter.sendMessage` كان يرجع نجاح/فشل فقط (92%/8%) برسالة خطأ واحدة ثابتة. أُضيف
  `pickRandomFailureReason()` (أسباب واقعية متنوعة: رقم غير صحيح، انتهاء نافذة الـ24 ساعة، حظر
  الرقم، خارج التغطية) و`simulateEngagementFunnel()` (طبقة تفاعل تالية للنجاح: ~65% تُقرأ، ومن
  المقروء ~20% يُرَد عليه) — يعطي ملخصاً بصرياً واقعياً بنسب متدرجة منطقية (مُرسل > تسليم > قراءة >
  رد) بدل كل الرسائل الناجحة "مُسلَّمة" فقط دون أي تمايز، مع توثيق صريح في كل تعليق برمجي أن هذا
  محاكاة (طالما لا يوجد اتصال Meta حقيقي بإشعارات قراءة/رد فعلية) وليس بيانات حقيقية.

### 38. A/B Testing: علم بيانات محجوز بدل ميزة منتصفة البناء
- البرومنت ذكر "A/B Testing" كمثال على ميزة تُقيَّد بالباقة، وليس كمتطلب بناء صريح. بناء A/B Testing
  فعلي (تقسيم عشوائي للجمهور، متغيرات رسالة متعددة، قياس دلالة إحصائية للفائز) نطاق عمل منفصل بحجم
  مقارب لوحدة الحملات نفسها. **القرار**: أُضيف حقل `Plan.campaignLimitsJson.abTestingEnabled`
  (قابل للتفعيل من `/admin/plans`) كعلم بيانات محجوز فقط، دون أي واجهة A/B فعلية — تفادياً لبناء
  ميزة نصف مكتملة تبدو متاحة للتاجر دون أن تُنتج فعلياً نتائج اختبار حقيقية. موثّق صراحة في
  QA_REPORT.md كفجوة معروفة (وليس ميزة "مخفية" بالخطأ).

### 39. خطأ حقيقي إضافي اكتُشف بالاختبار الفعلي في هذه الجولة: `prisma/seed.ts` بلا خطوة تصفير (تكرار من الجولة الخامسة، سبب مختلف هذه المرة)
- نفس فئة الخطأ الموثّقة في القرار رقم 29 (الجولة الخامسة) تكرر ظهورها هنا: تشغيل `seed.ts` عدة
  مرات متتالية أثناء اختبار وحدة الحملات أدى فعلياً لتجاوز تاجر Starter التجريبي حده الحقيقي لعدد
  الحملات، فأخفى زر الإنشاء الحقيقي خلف نافذة ترقية زائفة السياق (كانت المشكلة في بيانات الاختبار
  المتراكمة، وليس في منطق `createCampaign` نفسه). عولج بنفس الأسلوب (`TRUNCATE ... CASCADE` قبل كل
  زرع)، ويؤكد مجدداً التوصية بإضافة `npm run db:reset` رسمي بدل تكرار هذا الحل اليدوي في كل جولة.
- **خطأ تشغيلي منفصل اكتُشف بالاختبار الفعلي**: عملية عامل BullMQ (`npm run worker`) لم تكن تعمل
  إطلاقاً في جلسة العمل قبل هذه الجولة (خادم Next.js للتطوير فقط)، فبقيت كل الحملات "مجدولة" بلا
  إرسال فعلي حتى اكتُشف الخلل وشُغِّلت العملية يدوياً — تفصيل كامل في QA_REPORT.md، القسم -3.

---

## 2026-07-23 (الجولة السابعة) — صندوق محادثات واتساب حقيقي متكامل

طلب المستخدم صراحة تحويل صندوق المحادثات من قائمة جانبية بلا شاشة محادثة فعلية إلى واجهة واتساب
حقيقية بالكامل: ربط لحظي فعلي بـ Meta Cloud API، شاشة محادثة كاملة بكل أنواع الوسائط، تصنيف حالة
حقيقي (وليس عداداً ثابتاً)، تعاون فريق كامل، وبحث/فلترة — دون أي رجوع بسؤال توضيحي.

### 40. محرِّك ويب هوك موحّد: نفس دالة المعالجة لـwebhook حقيقي ولمحاكي Sandbox
- `lib/integrations/meta/webhookHandler.ts` يحتوي `parseMetaWebhookPayload()` (يحلّل الشكل الفعلي
  الموثّق علنياً لحمولة Meta Cloud API: `entry[].changes[].value.messages[]/statuses[]`) و
  `applyParsedMetaWebhook()` (يطبّق النتيجة على قاعدة البيانات: إنشاء/تحديث جهة اتصال، محادثة،
  رسالة، وتحديثات حالة). **القرار الجوهري**: مسار الـwebhook الحقيقي (`api/webhooks/meta/route.ts`)
  ومحاكي Sandbox (`lib/inbox/sandboxSimulator.ts`) يستدعيان **نفس هاتين الدالتين بالضبط** — الفرق
  الوحيد بينهما هو طبقة النقل الخارجية (التحقق من التوقيع وتحليل HTTP request مقابل استدعاء دالة
  مباشر داخل server action) — لا يوجد أي منطق معالجة رسائل مكرر قد ينحرف لاحقاً بين المسارين
  (بند 4 الصريح في البرومنت: "الانتقال لرقم حقيقي لاحقاً لا يتطلب إعادة بناء").
- **فجوة موثّقة صراحة**: Meta الفعلي يرسل `id` وسيط فقط (يتطلب استدعاء Media API لجلب رابط مؤقت)،
  غير مُنفَّذ هنا لعدم توفر حساب Meta حقيقي للاختبار. في Sandbox يُدرَج رابط الوسائط مباشرة في
  الحمولة نفسها (حقل إضافي يمر عبر نفس `mediaUrl`)، فلا فرع كود إضافي، لكن الاتصال الحقيقي الأول
  سيحتاج تنفيذ خطوة تحميل الوسائط الفعلية من Meta — موثّق في QA_REPORT.md وليس مخفياً.

### 41. آلة حالة المحادثة: 4 حالات صريحة بدل 3، لحل تناقض لم يذكره البرومنت صراحة
- البرومنت عرّف 3 حالات (جديد/يحتاج رد/تم الحل) لكن لم يحدد أين تذهب محادثة **بعد رد فعلي** وقبل
  رسالة العميل التالية — ليست "جديدة" (رُدّ عليها)، ليست "تحتاج رد" (رُدّ عليها فعلاً)، وليست
  "محلولة" (لم يُغلقها أحد يدوياً). حسم هذا الغموض بإضافة حالة رابعة `OPEN` ("بانتظار العميل")
  بدل إجبار الرد على إحدى الحالات الثلاث بشكل غير دقيق (كان الكود القديم يضع **كل** رد كـ"محلولة"
  تلقائياً — خطأ حقيقي مكتشَف أثناء فحص الكود القائم قبل البناء، وليس افتراضاً نظرياً).
  المنطق الكامل في `lib/inbox/conversationStatus.ts`:
  - `NEW`: أول رسالة على محادثة **جديدة كلياً** لم تكن موجودة من قبل.
  - `NEEDS_REPLY`: أي رسالة واردة على محادثة **موجودة مسبقاً**، بصرف النظر عن حالتها السابقة (حتى
    لو كانت `RESOLVED` — إعادة فتح ضمنية، تطابق تعريف "يحتاج رد" حرفياً: "آخر رسالة من العميل").
  - `OPEN`: عقب أي رد (بوت أو موظف) على محادثة `NEW`/`NEEDS_REPLY`.
  - `RESOLVED`: **فقط** عبر الزر اليدوي الصريح — لا مسار تلقائي يضع محادثة هنا إطلاقاً.
  - أُضيف تبويب "الكل" في الواجهة (غير مطلوب صراحة) ليبقى الوصول لمحادثات `OPEN` ممكناً من القائمة
    دون إجبار المستخدم على تذكّر أن "بانتظار العميل" اسم الحالة الرابعة — شفافية بدل إخفاء حالة.

### 42. نموذج الرسائل: امتداد بدل نموذج مزدوج للوسائط والحالة والاقتباس
- `Message` امتد بحقول `type` (نص/صورة/مستند/صوت/موقع/رد تفاعلي/قالب)، `mediaUrl`/`mediaMimeType`،
  `latitude`/`longitude`، `quotedMessageId` (علاقة ذاتية)، و`status`/`statusUpdatedAt`/`failureReason`
  (ذات معنى للصادرة فقط). **القرار**: امتداد نفس الجدول بدل جدول "MediaMessage" منفصل، لأن كل حقل
  اختياري وطبيعة الرسالة الأساسية (اتجاه، مرسل، محادثة) واحدة بصرف النظر عن نوعها — نفس مبدأ توسيع
  `Campaign` بدل نموذج حملات آلية موازٍ في الجولة السادسة.
- **`READ` حالة منفصلة عن `DELIVERED`**: البرومنت يطلب صراحة ثلاث علامات متمايزة (✓/✓✓ رمادي/✓✓
  أزرق) — لم تكن `DELIVERED` وحدها كافية للتمييز بين "وصلت للجهاز" و"قرأها العميل فعلياً".

### 43. ملاحظات داخلية وردود سريعة: جداول منفصلة تماماً، وليس حقل على Message
- `InternalNote` نموذج مستقل تماماً عن `Message` (وليس `Message` بحقل `isInternal: true`) —
  **قرار أمان بنيوي مقصود**: البرومنت يشدد "لا تصل للعميل تحت أي ظرف" — فصلهما في جدولين مختلفين
  يجعل هذا **استحالة بنيوية** (لا يوجد أي مسار كود يقرأ `InternalNote` ويرسلها عبر `metaAdapter`
  إطلاقاً، فالخطأ البرمجي الذي قد يُسرّب ملاحظة كرسالة يصبح غير قابل الحدوث بدل معتمد على فحص
  `if (isInternal)` في كل نقطة إرسال قد يُنسى لاحقاً). نفس فلسفة فصل انتحال الهوية عن جلسة NextAuth
  في جولة سابقة — فصل بنيوي بدل علم بيانات يعتمد على انضباط باقي الكود.
- `QuickReply` نموذج مستقل بسيط (اختصار + نص)، تُدار من صفحة مستقلة `/dashboard/inbox/quick-replies`
  بدل نافذة منبثقة داخل شاشة المحادثة — تبسيط: لا حاجة لتعقيد modal لعملية CRUD نادرة الحدوث.

### 44. مزامنة لحظية: Polling عميل مستقل بدل WebSocket/SSE (الحد الأدنى المسموح صراحة في البرومنت)
- لا بنية WebSocket/SSE قائمة في المشروع، وبناء واحدة يتطلب خادماً طويل الأمد منفصلاً (البرومنت
  نفسه يسمح صراحة بـ"Polling قصير المدى كحد أدنى" في بند أ). التنفيذ: `InboxLivePoller` (مكوّن
  عميل بلا واجهة، مُثبَّت في `layout.tsx`) يستطلع `/api/inbox/unread-count` كل 4 ثوانٍ؛ عند اكتشاف
  رسالة واردة جديدة (تغيّر `latestMessageId`) يُشغّل نغمة تنبيه (Web Audio API مولَّدة برمجياً —
  لا ملف صوتي خارجي) وينفّذ `router.refresh()` لتحديث المحادثة المفتوحة فعلياً.
- **قرار معماري بسبب قيد حقيقي في Next.js App Router**: صفحات `layout.tsx` **لا تستقبل** خاصية
  `searchParams` (توثيق Next.js الرسمي — فقط `page.tsx` يستقبلها)، بينما قائمة المحادثات (المعروضة
  داخل `layout.tsx` لتبقى ظاهرة بجانب أي محادثة مفتوحة) تحتاج فلاتر/بحث مبنية على معاملات الرابط.
  الحل: `ConversationList` أصبح مكوّن عميل مستقل يقرأ الفلاتر عبر `useSearchParams()` (خطاف عميل
  يعمل بصرف النظر عن قيد الخادم أعلاه) ويستطلع نقطة API مخصصة (`/api/inbox/conversations`) كل 4
  ثوانٍ بنفسه — هذا وحّد أيضاً متطلب "التحديث اللحظي للقائمة" مع متطلب "الفلاتر/البحث" في نفس آلية
  الاستطلاع بدل بناء آليتين منفصلتين.

### 45. مؤشر SLA: حد ثابت 15 دقيقة، وليس قابلاً للتخصيص لكل باقة/تاجر
- `isSlaBreached()` يعتبر أي محادثة "يحتاج رد" تجاوزت 15 دقيقة منذ آخر رسالة عميل بلا رد "متأخرة"
  (⏰ أحمر في القائمة). قيمة ثابتة في الكود (`lib/inbox/conversationStatus.ts`) بدل إعداد لكل باقة —
  البرومنت لم يطلب تخصيصاً لكل باقة، وقيمة عامة معقولة صناعياً (معظم أدوات دعم العملاء تستخدم
  نافذة تحذير 10-30 دقيقة) كافية لتحقيق الغرض دون تعقيد إضافي غير مطلوب.

### 46. تحكم البوت: عرض حالة + زر استرجاع فعلي، دون ربط محرك تنفيذ حقيقي بالويب هوك (فجوة مستمرة موثّقة)
- البرومنت يطلب "لو الشات بوت يتحكم... أظهر ذلك بوضوح" — هذا **متطلب عرض**، وليس طلباً صريحاً
  ببناء محرك تنفيذ بوت كامل مربوط بالرسائل الواردة الحقيقية (ميزة منفصلة بحجم مقارب لوحدة الشات
  بوت نفسها). نُفِّذ: `Conversation.controlMode` (BOT/HUMAN)، افتراضي BOT عند إنشاء محادثة جديدة
  إن كان للتاجر تدفق منشور، مع بادج واضح وزر "🙋 الاستيلاء من البوت" فعلي (يُسجَّل في Audit Log).
  **لم يُبنَ**: تنفيذ فعلي لخطوات التدفق رداً على كل رسالة واردة حقيقية — هذه فجوة **مستمرة من
  الجولة الثانية** ("محرك تنفيذ الشات بوت الحقيقي غير مربوط بعد بمحادثات واتساب واردة فعلية")، لم
  تُحل هنا عمداً (خارج نطاق هذا البرومنت تحديداً)، وتبقى موثّقة بصراحة في QA_REPORT.md وليست مخفية
  خلف بادج "🤖 يتحكم البوت" الذي قد يوحي خطأً بأنها مكتملة.

### 47. حصة الأعضاء (`maxUsers`): إنفاذ عند الدعوة، لا بنية "حضور لحظي" فعلية
- البرومنت يطلب تقييد "عدد الأعضاء اللي يقدروا يستخدموا صندوق المحادثات في نفس الوقت" — هذا يوحي
  بتتبع حضور لحظي (من متصل الآن فعلياً)، وهي بنية غير موجودة في المشروع (تتطلب WebSocket/session
  presence tracking، خارج نطاق التنفيذ الحالي بالكامل). **القرار**: إنفاذ `Plan.maxUsers` (موجود
  في المخطط منذ الجولة الأولى **لكنه لم يكن يُفرض في أي مكان بالكود إطلاقاً** — فجوة صامتة اكتُشفت
  بفحص الكود القائم، من نفس فئة فجوة `messagesUsedThisPeriod` في الجولة السادسة) عند **دعوة عضو
  جديد** (`settings/actions.ts`) — العدد الكلي للأعضاء النشطين هو المقياس العملي المتاح فعلياً،
  وهو يخدم نفس الهدف التجاري (تقييد الوصول حسب الباقة) دون تلفيق بيانات "متصل الآن" غير حقيقية.

### 48. عدد أرقام واتساب (`maxWhatsappNumbers`): فجوة معمارية موثّقة، لم تُحل في هذه الجولة
- النموذج الحالي (`Integration` بقيد `@@unique([tenantId, provider])`) يسمح بربط **رقم واتساب واحد
  فقط لكل تاجر بنيوياً** — لا يمكن تمثيل "أكثر من رقم" أصلاً بغض النظر عن قيمة `maxWhatsappNumbers`
  في الباقة. حل هذا يتطلب تغيير القيد الفريد (`[tenantId, provider, externalAccountId]`) وتحديث كل
  مكان يستعلم `findUnique({tenantId_provider})` إلى `findMany` — تغيير معماري في وحدة **التكاملات**
  وليس في وحدة **صندوق المحادثات** موضوع هذا البرومنت تحديداً. **قرار نطاق واعٍ**: لم يُبنَ هنا،
  وموثَّق صراحة كفجوة معمارية في QA_REPORT.md بدل تجاهله بصمت أو بناء دعم أرقام متعددة كأثر جانبي
  خارج نطاق الطلب الحالي.

### 49. أخطاء حقيقية اكتُشفت بالاختبار الفعلي في هذه الجولة
1. **تكرار فئة الخطأ المعروفة**: مسار الـwebhook الحقيقي كتب `WebhookLog` (جدول مستأجر خاضع لـRLS)
   عبر `superAdminDb` مباشرة بدل `withTenant()` أثناء المسودة الأولى لإعادة الكتابة — اكتُشف
   وأُصلح **قبل** التشغيل الفعلي هذه المرة (بمراجعة ذاتية أثناء الكتابة، وليس بعد فشل فعلي) بفضل
   التوثيق المتراكم لهذا النمط تحديداً عبر 5 حوادث سابقة موثّقة في هذا الملف.
2. **دعوة عضو فوق حد الباقة كانت تُسبِّب انهياراً خاماً (React error overlay) بدل رسالة ودّية**:
   نموذج دعوة عضو في `/dashboard/settings` كان `<form action={inviteTeamMember}>` مباشرة بلا طبقة
   التقاط أخطاء عميل — أي استثناء يُرمى من الخادم (بما فيها إنفاذ `maxUsers` الجديد في هذه الجولة)
   كان يظهر كشاشة انهيار كاملة للمستخدم الحقيقي، وليس فقط لسكربت الاختبار. أُصلح بمكوّن عميل صغير
   (`InviteTeamMemberForm`) يلتقط الخطأ عبر `try/catch` داخل `useTransition` ويعرضه كشريط تنبيه —
   نفس النمط المستخدم أصلاً في معالج الحملات ومحرر الشات بوت، كان ناقصاً هنا تحديداً.
3. **تعارض تشغيلي حقيقي غير متعلق بكود التطبيق**: تشغيل `npm run build` (إنتاج) بينما خادم التطوير
   (`npm run dev`) يعمل على نفس مجلد `.next` أفسد حالة الخادم التطويري (خطأ `MODULE_NOT_FOUND` عند
   طلب `/login`) — لأن الأمرين يتشاركان نفس مخرجات البناء ويتعارضان عند التشغيل المتزامن. الحل:
   إيقاف كل عمليات Node، حذف `.next`، وإعادة تشغيل خادم التطوير فقط. **درس تشغيلي**: لا تُشغَّل
   `next build` و`next dev` على نفس المجلد في آن واحد أثناء هذا النوع من الاختبار التكراري.
4. **اكتشاف غير متوقع أثناء إعادة تشغيل سلسلة اختبارات الدخان الكاملة**: خطوة "ترقية الباقة" في
   `smoke-test.js` (من جولات سابقة) تنقر أول زر "الترقية لهذه الباقة" ظاهر في الشبكة — وبما أن
   الباقات مرتبة تصاعدياً بالسعر، هذا **يخفّض** فعلياً تاجر Growth إلى Starter (وليس يُرقّيه) كأثر
   جانبي غير مقصود من تصميم السكربت القديم. هذا كشف بدوره أن إنفاذ `maxUsers` الجديد يعمل بشكل صحيح
   تماماً حين نفّذته خطوة "دعوة عضو" اللاحقة على تينانت أصبح الآن فعلياً على Starter (بحد 3 أعضاء،
   والعدد الحالي 3 بالضبط) — لم يكن خطأ في الإنفاذ، بل سلوك تصميم سكربت قديم غير مرتبط بهذه الجولة.
5. **تصادم محدِّدات في سكربتات الاختبار (وليس تطبيق) بسبب إضافات هذه الجولة والجولة السادسة**:
   محدِّد href في `smoke-test.js` طابق رابط "⚡ الردود السريعة" الجديد قبل أي رابط محادثة فعلي (نفس
   بادئة `/dashboard/inbox/`)، ومحدِّد نص "حفظ حدود" في `smoke-super-admin.js` أصبح غامضاً بعد
   إضافة نموذج "حفظ حدود الحملات" في الجولة السادسة. كلاهما أُصلح بتحديد أدق (استثناء صريح أو نص
   كامل غير مبهم) — درس متكرر: أي إضافة UI جديدة يجب مراجعة سكربتات الدخان القائمة لمحدِّدات قد
   تتصادم معها، وليس افتراض عزلها التلقائي.

---

## 2026-07-23 (الجولة الثامنة) — مسار ربط Meta/واتساب المتكامل + إدارة القوالب واعتمادها

طلب المستخدم مساراً كاملاً لربط حساب Meta/واتساب فعلياً (يُفعِّل صندوق المحادثات الحقيقي المبني في
الجولة السابعة) بالإضافة لوحدة إدارة قوالب ترفع طلب اعتماد حقيقي لـMeta وتتحدث حالتها تلقائياً —
دون أي رجوع بسؤال توضيحي.

### 50. استبدال مسار موافقة القوالب عبر مالك المنصة بمسار Meta الفعلي — تجاوز تصميم الجولة الرابعة عمداً
- الجولة الرابعة بنت "موافقة قوالب الرسائل" كنوع رابع (`MESSAGE_TEMPLATE`) أعيد استخدامه من مركز
  موافقات مالك المنصة القائم، موثَّقةً صراحةً وقتها بأنها "تحاكي مراجعة Meta الفعلية" — كانت هذه
  محاكاة مقصودة لعدم وجود مسار Meta حقيقي بعد. الآن وقد بُني هذا المسار فعلياً، أصبح مسار الموافقة
  القديم عبر مالك المنصة **متجاوَزاً عمداً**: `createTemplate` لم يعد يُنشئ صف `ApprovalRequest` من
  نوع `MESSAGE_TEMPLATE` إطلاقاً؛ بدلاً من ذلك يستدعي `submitTemplateForApproval` (طبقة Meta الفعلية)
  التي تُرسل القالب حقيقياً لمراجعة Meta وتتحدث حالته عبر webhook حقيقي أو محاكاة مؤجَّلة واقعية.
  **لم تُحذف** بنية `MESSAGE_TEMPLATE` من `ApprovalRequestType`/`ApprovalCard.tsx`/`admin/approvals`
  (تبقى صالحة لعرض أي طلبات تاريخية قديمة وتفادي كسر بيانات موجودة)، لكنها لن تستقبل طلبات جديدة
  بعد الآن — الفرق بين "إبقاء بنية قديمة سليمة" و"حذفها بلا داعٍ" حين لا يطلب البرومنت إزالتها صراحة.

### 51. أدوات Meta المتخصصة كوحدات مستقلة بدل توسيع الواجهة الموحّدة `IntegrationAdapter`
- بدل إضافة دوال اختيارية خاصة بـMeta فقط (إنشاء قالب، تحقق يدوي، اشتراك webhook) لواجهة
  `IntegrationAdapter` المشتركة بين Meta/زد/سلة، أُنشئت وحدتان مستقلتان
  (`lib/integrations/meta/connection.ts`, `lib/integrations/meta/templates.ts`) تُستدعيان مباشرة
  من الأكشنز التي تحتاجها. **السبب**: هذه العمليات خاصة ببنية Meta تحديداً (WABA ID، Message
  Templates API) ولا معنى مطابق لها في زد/سلة إطلاقاً — توسيع الواجهة المشتركة بدوال اختيارية لا
  يستخدمها إلا مزود واحد كان سيُضعف قيمة "الواجهة الموحّدة" نفسها. يبقى مبدأ "كل استدعاء Graph API
  عبر طبقة خدمة موحّدة" محقَّقاً لأن كل الاستدعاءات الفعلية لـgraph.facebook.com ما زالت محصورة في
  ملفات `lib/integrations/meta/*` حصراً، ولا استدعاء متفرق من أي Server Action أو صفحة مباشرة.

### 52. إصلاح فعلي مهم: `sendMessage` كان يعتمد على إعداد المنصة العام بدل حالة اتصال التاجر نفسه
- اكتُشف أثناء تصميم هذه الجولة أن `metaAdapter.sendMessage` كان يتحقق من `isSandboxMode()` (إعداد
  عام لكل المنصة من `PlatformSettings.integrationsMode`) بدل حقل `Integration.isSandbox` **الخاص
  بهذا التاجر تحديداً**. هذا يعني: لو تاجر ربط رقمه الحقيقي فعلياً (عبر الطريقة اليدوية بمفاتيح
  حقيقية) بينما إعداد المنصة العام لا يزال "sandbox" (الحالة الافتراضية لبقية التجار الذين لم
  يربطوا بعد)، كانت رسائله ستُرسَل **محاكاةً** رغم ربطه الفعلي — يناقض جوهر هذه الجولة بالكامل
  ("الربط لازم يكون فعلياً حقيقياً"). أُصلح بجعل القرار خاصاً بحالة اتصال هذا التاجر نفسه
  (`Integration.isSandbox` الفعلي لسجله)، وليس بإعداد عام يخص كل التجار معاً. هذا اكتشاف تصميمي
  وليس خطأ برمجي بسيط — البنية القديمة كانت صحيحة *قبل* هذه الجولة لأن لا تاجر كان يربط رقماً
  حقيقياً أصلاً، فلم يظهر التعارض إلا الآن.

### 53. خطأ حقيقي إضافي اكتُشف أثناء تصميم اختبار الربط: تصادم معرّف الرقم بين تجار متعددين في Sandbox
- محاكاة "تسجيل الدخول المضمّن" (Embedded Signup) في وضع Sandbox كانت تُسنِد **نفس القيمة الثابتة**
  (`SANDBOX_META_ACCOUNT.phoneNumberId`) لكل تاجر يربط حسابه، بعكس تكامُلي زد/سلة اللذين يستخدمان
  أصلاً `sandboxAccountIdFor()` لتوليد معرّف فريد لكل تاجر. لم يكن هذا خطأً ظاهراً في الجولات
  السابقة لأن webhook Meta لم يكن يُطابَق فعلياً بـ`phone_number_id` لتوجيه الرسائل لمستأجر بعينه
  (كان يُسجَّل فقط بلا معالجة حقيقية). الآن وقد أصبحت المطابقة حقيقية (الجولة السابعة + هذه
  الجولة)، اكتشاف هذا التصادم أثناء تصميم اختبار الربط لتاجرين مختلفين كشف أن أي تاجرين يربطان عبر
  Embedded Signup في نفس بيئة Sandbox كانا سيتشاركان "رقماً" واحداً فعلياً، فتُوجَّه رسائل أحدهما
  للآخر بالخطأ! أُصلح بتوليد `phoneNumberId`/`wabaId` فريدين لكل تاجر عبر `sandboxAccountIdFor()`
  نفسها المستخدمة أصلاً في زد/سلة — نفس الدالة الموجودة، فقط لم تكن مُستخدَمة من محاكي Meta تحديداً.

### 54. محاكاة قرار مراجعة القالب عبر BullMQ مؤجَّل، بنفس نمط محاكاة حالة الرسائل من الجولة السابعة
- `scheduleTemplateReviewDecision` تجدول قراراً بعد 6 ثوانٍ (٪70 اعتماد، ٪30 رفض بسبب واقعي متنوع)
  — يحاكي زمن مراجعة Meta الفعلي (لا اعتماد فوري مصطنع يُفقد "قيد المراجعة" الظاهرة للتاجر معناها).
  **المسار الحقيقي المطابق موجود فعلياً بجانبها وليس بديلاً عنها**: `webhookHandler.ts` يحلّل حدث
  `message_template_status_update` الفعلي من Meta ويطبّقه عبر **نفس** الدالة
  (`applyTemplateDecisionTx`) التي يستخدمها عامل المحاكاة — مسار كود واحد للتطبيق الفعلي للقرار،
  سواء وصل عبر webhook حقيقي أو محاكاة Sandbox (نفس مبدأ الجولة السابعة لرسائل واتساب الواردة).

### 55. حالتا `PAUSED`/`DISABLED` الجديدتان على القالب: تمييز صريح عن `REJECTED`
- Meta فعلياً تُوقِف قوالب معتمدة سابقاً بعد فترة بسبب انخفاض جودة التفاعل (ظاهرة "Template Pausing")
  — حالة مختلفة جوهرياً عن الرفض الأولي (`REJECTED`) لأنها تحدث *بعد* اعتماد ناجح، لا بدلاً عنه.
  تمييزهما في enum منفصل (`TemplateStatus.PAUSED`/`DISABLED`) بدل إعادة استخدام `REJECTED` يمنع
  التاجر من الخلط بين "قالبي رُفض من البداية" و"قالبي كان يعمل وتوقف الآن" — الفرق مهم عملياً لأن
  الإجراء المطلوب مختلف (الأول يحتاج تعديل صياغة، الثاني يحتاج مراجعة أداء الحملات التي استخدمته).

### 56. "تعديل وإعادة إرسال": حذف الصف القديم قبل إنشاء الجديد بدل الاحتفاظ بنسخ متعددة
- قوالب واتساب لا تُعدَّل في مكانها بعد الإرسال — أي تعديل هو submission جديد بالكامل. بما أن القيد
  الفريد `(tenantId, name, language)` يمنع إعادة استخدام نفس الاسم لصف قائم، وأغلب التجار سيريدون
  إعادة نفس الاسم بعد تصحيح المحتوى (وليس اختراع اسم جديد في كل مرة)، قرار "تعديل وإعادة إرسال"
  يحذف الصف القديم (المرفوض/المعلَّق/المعطَّل فقط — لا يمكن حذف صف معتمد أو قيد المراجعة أبداً) قبل
  إنشاء الصف الجديد بنفس الاسم. **الأثر الجانبي المقبول**: يُفقَد السجل التاريخي للنسخة المرفوضة
  الدقيق (يبقى فقط سبب الرفض المعروض وقت الحذف)، وهذا تنازل معقول أمام تكرار تراكم عشرات النسخ
  الفاشلة بنفس الاسم لكل تاجر ينقّح صياغة قالبه عدة مرات.

### 57. متغيرات القالب: `{{1}}` رقمي مطابق لقيد Meta الفعلي بدل متغيرات مسمّاة كما أوحى البرومنت
- البرومنت اقترح متغيرات مسمّاة مثل `{{اسم_العميل}}` — لكن Meta الفعلي يفرض صياغة رقمية تسلسلية
  فقط (`{{1}}`, `{{2}}`...) في نص القالب، ولا يدعم أسماء متغيرات حرفية. **القرار**: زر "إدراج متغير"
  في معالج بناء القالب يُدرِج `{{1}}` مباشرة (مطابقاً لما تتوقعه Meta فعلياً وما يستبدله فعلياً محرك
  الإرسال الحالي في الحملات/صندوق المحادثات باسم العميل)، مع تسمية الزر نفسه "اسم العميل" للتاجر غير
  التقني — التزام بقيد Meta الحقيقي بدل بناء طبقة ترجمة متغيرات مسمّاة⇄مرقّمة غير مطلوبة فعلياً في
  أي مسار إرسال حالي (الحملات وصندوق المحادثات يستخدمان `{{1}}` فقط منذ الجولتين السادسة والسابعة).

### 58. مسار "Embedded Signup" الحقيقي: نطاق محدود عمداً — استبدال بديل عملي بدل تنفيذ جزئي مضلِّل
- Embedded Signup الحقيقي من Meta يتطلب تحميل Facebook JS SDK على العميل واستدعاء `FB.login()` بخيار
  `whatsapp_embedded_signup` والاستماع لأحداث `postMessage` من نافذة منبثقة تُعيد `waba_id`/
  `phone_number_id` مباشرة — تدفق معقّد خاص بفيسبوك يتطلب **مسبقاً** موافقة Meta App Review على صلاحية
  `whatsapp_business_management` (خطوة مراجعة بشرية من Meta تستغرق أياماً، وليست فجوة برمجية يمكن
  سدّها بالكود). تنفيذه فعلياً هنا مستحيل عملياً بلا App ID حقيقي مُوافَق عليه أصلاً للاختبار.
  **القرار**: الطريقة اليدوية (WABA ID/Phone Number ID/Access Token يُدخلها التاجر مباشرة) هي
  المسار **الحقيقي الكامل** المُنفَّذ فعلياً (تحقق Graph API حقيقي + اشتراك webhook حقيقي + تخزين
  مشفَّر)، بينما بطاقة "Embedded Signup" تبقى محاكاة Sandbox فورية (بديل *عملي* أوضح من تنفيذ جزئي
  لتدفق OAuth تقليدي لن يعمل فعلياً بدون الـJS SDK أصلاً) — موثَّق صراحة كفجوة في QA_REPORT.md بدل
  الادّعاء بتنفيذ "تسجيل دخول مضمّن" حقيقي لا يعمل عملياً بلا موافقة Meta المسبقة على التطبيق.

### 59. RBAC: حجب صفحة كاملة (وليس فقط زر الإدارة) لأول مرة في وحدتي التكاملات والقوالب
- الصفحتان لم تكونا تفحصان الصلاحية إطلاقاً من قبل (كانتا تعرضان المحتوى الكامل لأي دور، فقط تُخفيان
  بعض الأزرار) — اكتُشف هذا أثناء تنفيذ بند البرومنت الصريح "الموظف لا يصل لهذا المسار إطلاقاً". أُضيف
  فحص `hasPermission` كامل الصفحة في أعلى كل من `dashboard/integrations/page.tsx` و
  `dashboard/templates/page.tsx` (يعرض رسالة رفض ودّية بدل المحتوى بالكامل)، بالإضافة لإعادة توجيه
  خادمية صريحة في `dashboard/templates/new/page.tsx` — يطابق نمط الحجب الكامل المستخدم في وحدة
  الحملات (الجولة السادسة)، بدل الاكتفاء بإخفاء الأزرار كما كان معمولاً به سابقاً في هاتين الصفحتين
  تحديداً (فجوة RBAC حقيقية موروثة من جولات سابقة، أُصلحت هنا).

---

## 2026-07-23 (الجولة التاسعة) — تفعيل حقيقي + مراجعة أمنية شاملة قبل الإطلاق

### 60. حذف تينانتس تجريبية بنطاق دقيق مؤكَّد صراحة، بدل إعادة `prisma/seed.ts` أو تفريغ كامل
- الطلب الأصلي ("احذف كل البيانات الوهمية") كان فضفاضاً بما يكفي ليعني إما "التينانتس التجريبية فقط"
  أو "كل شيء بما فيه حسابات فريق المنصة". طُرح سؤال توضيحي واحد محدد (النطاق فقط)، فأكَّد المستخدم
  "التينانتس التجريبية فقط" مع الإبقاء الصريح على DMS + Super Admin/الدعم/المالية + الباقات الأساسية.
  التنفيذ: `DELETE FROM "Tenant" WHERE slug IN (...)` واحد يعتمد على `onDelete: Cascade` المعرَّف
  أصلاً على كل الجداول التابعة، بدل أي سكربت تنظيف مخصص جديد.

### 61. إزالة تجاوز sandbox من `verifyWebhookSignature` بلا أي استثناء تدريجي أو flag انتقالي
- كان بالإمكان معالجة الثغرة بإصلاح "أخف" (مثل: تجاوز فقط إن لم يوجد تكامل مطابق بعد) لكن ذلك يُبقي
  سطح هجوم جزئياً ويُعقِّد المنطق دون داعٍ فعلي، بما أن محاكاة sandbox الحقيقية (رسائل/طلبات) لا تلمس
  هذا المسار العام إطلاقاً (مؤكَّد بالبحث في `sandboxSimulator.ts`/`SANDBOX_ZID_ORDERS`/
  `SANDBOX_SALLA_ORDERS`). القرار: إزالة الاستثناء بالكامل من الدوال الثلاث دفعة واحدة — التحقق
  fail-closed دائماً، بلا أي مسار "sandbox يتجاوز التحقق" متبقٍّ في الكود.

### 62. مقارنة bcrypt دائمة التنفيذ على تجزئة وهمية، بدل الاكتفاء بحد المحاولات فقط
- حد 8 محاولات/10 دقائق (`checkLoginRateLimit`) يمنع التخمين الآلي لكن لا يمنع فرق التوقيت الذي يكشف
  "هل هذا البريد مسجَّل أصلاً؟" (خروج مبكر سريع لبريد غير موجود مقابل bcrypt بطيء فعلي لبريد موجود).
  أُضيفت تجزئة bcrypt وهمية ثابتة (`DUMMY_HASH`) تُقارَن دائماً حتى عند غياب المستخدم، بدل تعقيد أكبر
  (كإضافة تأخير اصطناعي)، لأنها تحل المشكلة الفعلية بأقل تغيير ممكن في مسار `authorize()`.

### 63. حجب صفحتي الفوترة والفريق بـ `notFound()` كامل الصفحة، لا رسالة "غير مصرَّح" ودّية
- يطابق القرار 59 (نمط الحجب الكامل المستخدم سابقاً في التكاملات/القوالب) بدل ابتكار نمط ثالث جديد.
  اختير `notFound()` تحديداً (بدل `redirect`) لأن الصفحتين موجودتان بنيوياً لكن غير مصرَّح للدور
  بمعرفة تفاصيلهما، فإخفاء وجودهما (404) أنسب من توجيه صريح يُفصح عن سبب الرفض.

### 64. حذف `assertTenantScopedWhere` بدل ربطها فعلياً كـ Prisma middleware
- الدالة كانت موثَّقة كحارس تطوير لكنها غير مستدعاة من أي مكان — خياران: (أ) ربطها فعلياً بمنتصف
  Prisma (`$use`/`$extends`) لتصبح حارساً حقيقياً، أو (ب) حذفها. اختير الحذف لأن الحماية الفعلية
  الموازية (Postgres RLS عبر `withTenant`) موجودة أصلاً ومُختبَرة (`prisma/verify-isolation.ts`)،
  وربط middleware إضافي الآن تغيير بنيوي أكبر من نطاق مراجعة أمنية دون فائدة حقيقية إضافية مؤكَّدة.

---

## 2026-07-23 (الجولة التاسعة، تكملة) — نظام "الشركاء": طلب انضمام عام يغذّي مركز الموافقات الموجود

### 65. `ApprovalRequest.tenantId` أصبح اختيارياً بدل بناء نموذج/مركز موافقات موازٍ لطلبات الشركاء
- البرومنت يمنع صراحة أي نظام موافقة مواز — يجب أن تصل طلبات الشركاء لنفس مركز الموافقات الموجود.
  المشكلة: `ApprovalRequest.tenantId` كان إلزامياً (كل الأنواع الأربعة السابقة تملك تينانتاً موجوداً
  مسبقاً وقت إنشاء الطلب)، بينما متقدّم شراكة عام لا يملك تينانتاً بعد أصلاً. الحل: جعل `tenantId`
  اختيارياً (`String?`) + إضافة `applicantEmail` كحقل عمودي مستقل (للبحث السريع عن التكرار وتتبّع
  الحالة العامة، بدل الاعتماد على `payloadJson` غير القابل للفهرسة). هذا النمط (عمود tenantId اختياري
  على جدول مستأجر) كان موجوداً فعلاً في `WebhookLog`/`AuditLog`/`User`، فلم يكن سابقة جديدة في
  المشروع، وPolicy الـRLS الموجودة أصلاً في `postgres-rls.sql` تتعامل معه بشكل صحيح دون أي تعديل
  (صف بـ`tenantId IS NULL` غير مرئي لأي جلسة تاجر عبر `withTenant`، ومرئي فقط عبر `superAdminDb`
  المتجاوز لـRLS أصلاً) — تحقّق مباشر بعد الـmigration وليس افتراضاً نظرياً.

### 66. اعتماد طلب شريك ينشئ التينانت مباشرة بحالة `TRIAL` (وليس `PENDING_REVIEW`) — خطوة مراجعة واحدة فقط
- مسار `/register` الذاتي يمر بخطوتين: تسجيل فوري بحالة `PENDING_REVIEW` ثم موافقة `NEW_TENANT`
  منفصلة تفتحه لاحقاً. طلب الشريك مختلف بنيوياً: طلب الانضمام نفسه **هو** خطوة المراجعة البشرية
  الوحيدة؛ فور موافقة مالك المنصة عليه، لا معنى لإدخال التينانت في طابور مراجعة ثانٍ. لذلك ينشئ
  `approvePartnerApplication` التينانت مباشرة بحالة `TRIAL` جاهزة للاستخدام الفوري، تطبيقاً حرفياً
  لعبارة البرومنت "إنشاء Tenant تلقائي... فور موافقة الأدمن".

### 67. رابط "إعداد حساب" آمن مُجزَّأ (يطابق نمط `ImpersonationSession`) بدل بريد يحوي كلمة مرور مؤقتة
- البرومنت يمنع صراحة إرسال بيانات دخول جاهزة بالبريد. عند الاعتماد: يُنشأ `User.passwordHash` بقيمة
  عشوائية (`crypto.randomBytes(32)`) غير قابلة للاستخدام أبداً ولا تُبلَّغ لأحد — الحساب فعلياً غير
  قابل لتسجيل الدخول حتى يكمل المتقدّم رابط `partners/setup/[token]`. الرمز الخام يُرسَل بالبريد فقط،
  وما يُخزَّن في `PartnerSetupToken.tokenHash` هو SHA-256 له (نفس آلية `ImpersonationSession` تماماً
  بدل اختراع نمط جديد)، بصلاحية 7 أيام (أطول من الـ30 دقيقة الخاصة بالانتحال عمداً، لأن هذا تفعيل
  حساب لحظة واحدة يعتمد على تحقق التاجر من بريده في وقته الخاص، وليس جلسة عمل نشطة).

### 68. طبقة بريد إلكتروني حقيقية البنية بمزود `console` افتراضي — بنفس نمط `PAYMENT_PROVIDER="mock"`
- لا يوجد أي مزود بريد (SMTP/Resend/SendGrid) مُهيَّأ في بيئة البناء، ولا بيانات اعتماد حقيقية متوفرة.
  بدل تعطيل الميزة أو محاكاتها في الواجهة فقط، بُنيت `lib/email/send.ts` بواجهة `sendEmail()` حقيقية
  الشكل يستدعيها كل مسار فعلي (تأكيد الطلب، القبول، الرفض، طلب معلومات إضافية)، مع `EMAIL_PROVIDER`
  env (افتراضياً `"console"`: يطبع الرسالة كاملة في سجل الخادم) — يطابق حرفياً القرار السابق الخاص
  بـ`PAYMENT_PROVIDER="mock"` بدل اختراع نمط مختلف لنفس المشكلة (فجوة موثّقة صراحة في QA_REPORT.md).

### 69. حماية أساسية من الإرسال الآلي: honeypot + حد زمني أدنى + IP rate limit — بلا خدمة CAPTCHA خارجية
- البرومنت يطلب "حماية أساسية من bot/rate-limit" وليس حلاً متقدماً، ولا تتوفر مفاتيح API لأي خدمة
  CAPTCHA حقيقية (reCAPTCHA/hCaptcha) لتفعيلها بشكل صحيح. التنفيذ: حقل فخّ مخفي بصرياً (`website`،
  أي قيمة فيه = رفض صامت)، ورفض أي إرسال خلال أقل من 3 ثوانٍ من عرض النموذج (`formRenderedAt`)، إضافة
  إلى `checkTenantRateLimit` الموجودة أصلاً (يُعاد استخدامها بمعرّف IP بدل tenantId — دالة عامة أصلاً
  رغم اسمها) بحد 5 طلبات/ساعة لكل IP. دفاع طبقات متعدد خفيف الوزن بدل تعقيد غير متاح التنفيذ فعلياً.

### 70. تتبّع حالة الطلب العام عبر (رقم الطلب + البريد الإلكتروني) بدون أي حساب أو تسجيل دخول
- المتقدّم لا يملك حساباً حتى تتم الموافقة، فلا يمكن حماية صفحة الحالة بجلسة NextAuth. رقم الطلب
  (cuid، غير قابل للتخمين عملياً) وحده كافٍ نظرياً، لكن اشتراط البريد الإلكتروني معه طبقة تحقق ثانية
  رخيصة، ورسالة "غير موجود" موحّدة سواء كان الخطأ في الرقم أو البريد (لا تكشف أيهما الصحيح). يطابق
  نمط "معرّف + تحقق ثانٍ" الشائع في صفحات "تتبع طلبك" العامة دون الحاجة لبناء نظام حسابات جزئي.

**تحقّق فعلي من المسار الكامل قبل اعتماد هذا القسم**: بُني اختبار Playwright كامل (تطبيق فعلي عبر
واجهة المستخدم، ليس استدعاء دوال مباشرة) يغطي: تقديم طلب حقيقي عبر المعالج، كشف طلب مكرر بنفس البريد،
تتبّع الحالة العام، تسجيل دخول مالك المنصة الحقيقي، ظهور البطاقة في مركز الموافقات **الموجود فعلاً**
بنفس تصميمه، الموافقة الفعلية (إنشاء تينانت حقيقي + مستخدم + اشتراك + تكاملات + رمز إعداد)، تسجيل
الدخول الفعلي بالحساب الجديد بعد إكمال رابط الإعداد، رفض رمز إعداد مُستخدَم مسبقاً بنجاح، ومسار الرفض
(سبب إلزامي يصل فعلياً بالبريد). كل بيانات الاختبار حُذفت من القاعدة الحقيقية فور التحقق (نفس القاعدة
المستضيفة لتينانت "dms" الحقيقي)، ومفاتيح rate-limit في Redis التي استهلكها الاختبار أُعيد تصفيرها.

---

## 2026-07-23 (الجولة العاشرة) — الموقع التسويقي العام الكامل

### 71. صفحة الجذر `/` أصبحت الموقع التسويقي نفسه بدل إعادة توجيه فورية لتسجيل الدخول
- كانت `app/page.tsx` تُعيد توجيه أي زائر غير مسجَّل مباشرة لـ`/login` دون أي محتوى تسويقي عام على
  الإطلاق. حُذفت هذه الصفحة، ونُقل منطق التوجيه (تسجيل دخول قائم → `/admin` أو `/dashboard` حسب
  الدور) إلى أعلى `app/(marketing)/page.tsx` نفسها: زائر بلا جلسة يرى الصفحة الرئيسية التسويقية
  كاملة، ومستخدم مسجَّل دخول يُعاد توجيهه فوراً كما كان بالضبط — لا تغيير في سلوك المستخدمين الحاليين.

### 72. Route Group `(marketing)` منفصل بهوية بصرية مستقلة (أخضر واتساب) عن لوحة التحكم (بنفسجي)
- لوحة التحكم الداخلية (`dashboard/`, `admin/`) بهويتها الداكنة البنفسجية (`accent-*`) تبقى كما هي
  تماماً بلا أي تعديل. الموقع التسويقي العام يستخدم تدرج أخضر جديد (`wa-*`، موسَّع من درجة واحدة كانت
  موجودة أصلاً وغير مستخدمة فعلياً في المشروع) كلون أساسي، مع الإبقاء على `accent-*` كلون ثانوي/روابط
  في بعض المواضع — جسر بصري متعمَّد بين الموقع والمنتج الفعلي بدل تناقض كامل عند الانتقال بينهما.

### 73. Dark Mode حقيقي (تبديل فعلي عبر `class="dark"`) للموقع التسويقي فقط، غير موجود في المنتج الداخلي
- لوحة التحكم الداخلية لم تستخدم `dark:` variants إطلاقاً من قبل (ألوانها الداكنة ثابتة صراحةً في
  الكود، بلا وضع فاتح بديل أصلاً) — لذلك تبديل حقيقي كامل هناك يتطلب إعادة تصميم كل شاشة، خارج نطاق
  هذا البرومنت. بُني التبديل (`ThemeToggle` + سكربت مضمَّن في `<head>` لمنع وميض FOUC عبر قراءة
  `localStorage`/`prefers-color-scheme` قبل أول رسم) للموقع التسويقي فقط، وهو غير مؤذٍ إطلاقاً للوحة
  التحكم لأنها لا تستخدم `dark:` أصلاً فيبقى تأثير `class="dark"` عليها معدوماً بنيوياً.

### 74. بديل CSS/SVG خفيف الوزن بدل Lottie فعلي في Hero — قرار أداء متعمَّد
- تأليف ملف Lottie (JSON بصيغة Bodymovin) حقيقي المظهر يدوياً بلا أداة تصدير (After Effects) أو أصل
  جاهز من مصدر خارجي موثوق (لا تتوفر بيانات اعتماد/مصدر رسمي يمكن الاعتماد عليه هنا) ينتج غالباً ملفاً
  هشاً أو رديء المظهر. البديل: نفس الفكرة البصرية (فقاعات رسائل تتحرك من متجر لعميل) عبر CSS keyframes +
  SVG/إيموجي خالص (`HeroAnimation.tsx`) — أخف وزناً، بلا أي طلب شبكة إضافي أو مكتبة تشغيل خارجية،
  يخدم مباشرة أولوية الأداء المذكورة صراحة في البرومنت («الأداء والسرعة أولوية»).

### 75. إزالة Reveal (Scroll-Animation) من كل محتوى "أعلى الطية" — خطأ حقيقي اكتُشف واُصلح أثناء الاختبار
- التصميم الأول وضع كل عنوان صفحة (H1) وكل محتوى الصفحة الرئيسية (Hero) داخل مكوّن `Reveal` المبني
  على `IntersectionObserver` (opacity-0 حتى يدخل العنصر نطاق الرؤية أثناء التمرير). اختبار Playwright
  فعلي (لقطة شاشة فورية بلا تمرير) كشف أن Hero ونصوص العناوين الرئيسية تظهر باهتة/غير مكتملة الظهور
  للحظة عند أول تحميل — عيب حقيقي في تجربة أول انطباع، وليس مجرد ملاحظة جمالية، لأن هذا المحتوى مرئي
  أصلاً منذ اللحظة الأولى ولا معنى لإخفائه بانتظار "تمرير" لن يحدث أصلاً بما أنه ظاهر فعلاً. أُزيل
  `Reveal` من كل Hero وكل عنوان رئيسي وكل محتوى يظهر عادة بلا تمرير (نموذج التواصل، بطاقات الأسعار،
  مفتاح شهري/سنوي)، وأُبقي فقط على المحتوى الذي يقع فعلياً تحت الطية عند التحميل الأول (شبكة المزايا،
  الإحصائيات، آراء العملاء، خطوات المنهجية، الأسئلة الشائعة) — حيث الأنيميشن عند التمرير له معنى فعلي.

### 76. نموذج جديد `ContactMessage`: جدول عام منفصل تماماً عن `Contact` الخاص بالتاجر
- رسائل نموذج "تواصل معنا" في الموقع التسويقي هي Lead تسويقي عام للمنصة نفسها (زائر لم يصبح تاجراً
  بعد)، وليست عميلاً لأي تينانت — استخدام جدول `Contact` (خاص بتاجر ومقيَّد بـRLS) هنا كان سيكون خلطاً
  معمارياً خاطئاً. أُنشئ نموذج مستقل `ContactMessage` (بلا tenantId، مثل `PlatformAnnouncement`)، مع
  صفحة إدارية جديدة `admin/leads` وصلاحية مخصصة `platform.leads.view` (SUPER_ADMIN + PLATFORM_SUPPORT).

### 77. لا مزود بريد إلكتروني حقيقي حتى الآن — إعادة استخدام `lib/email/send.ts` الموجود أصلاً من الجولة السابقة
- إشعار البريد لرسائل "تواصل معنا" يُرسَل عبر نفس `sendEmail()` المبني في جولة نظام الشركاء (مزود
  `console` افتراضياً) بدل بناء مسار إرسال مستقل جديد — نفس الفجوة الموثَّقة سابقاً (يحتاج مزوداً
  حقيقياً قبل الاعتماد الفعلي)، ولا داعٍ لتوثيقها مرتين بمنطق مختلف.
- **إصلاح أمني أثناء البناء**: اكتُشف أن قوالب بريد الشركاء من الجولة السابقة (`partnerTemplates.ts`)
  تُدرِج `ownerName`/`reason`/`message` (بعضها من مُدخَل زائر عام غير موثوق بالكامل) مباشرة في HTML
  بلا أي تهريب (escaping) — حقن HTML محتمل داخل بريد فعلي يصل لصندوق بريد حقيقي. أُضيفت دالة
  `escapeHtml` وطُبِّقت هناك وفي `contact/actions.ts` الجديد قبل أي إدراج HTML لمُدخَل خارجي.

### 78. خصم سنوي 20% موحّد على كل الباقات — إعداد فوترة عرضي، وليس حداً مسحوباً من نظام الباقات
- صفحة الأسعار تسحب الباقات الحقيقية فعلياً (`getPublicPlans()`, نفس الدالة من نظام الشركاء) لكن لا
  يوجد سعر سنوي مخزَّن في نموذج `Plan` أصلاً (`priceMonthlySar` فقط). خصم 20% موحّد شائع في منتجات
  SaaS اختير كقيمة افتراضية معقولة تُحسَب في الواجهة فقط، دون أي تعديل على نموذج `Plan` نفسه — قرار
  عرض (Presentation) وليس قراراً وظيفياً على الباقة، فلا حاجة لتعديل مخطط قاعدة البيانات لأجله.

**تحقّق فعلي شامل قبل التسليم**: اختُبرت كل صفحة عبر Playwright على أبعاد سطح مكتب (1440px) وجوال
(390px)، شملت: ظهور Hero فوراً بلا وميض، ظهور صحيح لكل قسم بعد تمرير فعلي، القائمة المتنقلة على
الجوال، تبديل الوضع الداكن/الفاتح، الأزرار الثلاثة (انضم كشريك/لوحة التاجر/تسجيل الدخول) تصل فعلياً
لمساراتها الصحيحة، نموذج التواصل يُرسل فعلياً (رسالة محفوظة في `ContactMessage` + بريد إشعار مُرسَل +
ظاهرة في `admin/leads`)، وأن محاولة حقن `<script>` عبر حقل الرسالة تُعرَض كنص عادي آمن (لا تنفيذ). كل
بيانات الاختبار حُذفت من القاعدة الحقيقية بعد التحقق.

---

## 2026-07-23 (الجولة العاشرة، تكملة) — إثراء الصفحة الرئيسية + أيقونات خدمات مخصَّصة

### 79. أيقونات SVG مرسومة يدوياً بدل الإيموجي لكل خدمة — مظهر احترافي متسق عبر كل الأنظمة
- الإيموجي (📣🤖💬🔌) يُعرَض بشكل مختلف فعلياً بين Windows/macOS/Android (خطوط ورسوم إيموجي مختلفة
  لكل نظام)، وقد يبدو غير احترافي أو غير متسق في موقع تسويقي. بُنيت 5 أيقونات SVG بخط بسيط
  (`ServiceIcons.tsx`: حملات/شات بوت/CRM/تكامل/شارة توثيق) تتوارث لون النص المحيط بها (`currentColor`)
  وتُستخدم في كل من قسم "كل شيء تحتاجه" بالرئيسية وصفحة `/services` — مظهر واحد متسق بلا اعتماد على
  رندر الإيموجي لدى المتصفح/النظام.

### 80. باقات الأسعار مكرَّرة فعلياً في الرئيسية (نفس المكوّن `PricingClient`)، لا نسخة مختصرة موازية
- بدل بناء عرض أسعار مبسَّط منفصل للرئيسية (خطر انحراف بيانات بين نسختين)، استُورد نفس مكوّن
  `PricingClient` المستخدم في `/pricing` مباشرة وعُرض بنفس البيانات الحقيقية (`getPublicPlans()`).
  رابط "قارن كل التفاصيل" أسفل القسم يوجّه لصفحة `/pricing` الكاملة (بها الأسئلة الشائعة وتفاصيل
  إضافية لا تناسب إثقال الصفحة الرئيسية بها).

### 81. قسم "عملاء مميزون": شعارات نائبة (placeholder wordmarks) في شريط متحرك بـCSS خالص
- لا تتوفر شعارات فعلية لعملاء حقيقيين موافقين على الظهور العلني بعد. بُنيت شعارات نصية نائبة (أسماء
  نشاطات عامة توضيحية مثل "متجر الأناقة"، "عيادة الشفاء" — بنفس أسلوب placeholder الإحصائيات والآراء
  الموجود أصلاً) بدل تأجيل القسم بالكامل أو استخدام أسماء/شعارات شركات حقيقية بلا إذن (قد يوحي بشراكة
  غير حقيقية). الحركة اللانهائية عبر `@keyframes marquee` (Tailwind) بقائمة مكرَّرة مرتين في الـDOM
  تنتهي عند `-50%` بالضبط لتبدو حلقة سلسة بلا قفزة، بلا أي مكتبة carousel خارجية.
- **فجوة موثَّقة**: يجب استبدال هذه الشعارات النصية بشعارات فعلية (صور) لعملاء حقيقيين موافقين فور
  توفرها، وعدم تركها كما هي عند الإطلاق الفعلي للمنصة.

### 82. قسم "تواصل معنا" في الرئيسية يُعيد استخدام نفس `ContactForm`/الإجراء الخادمي من `/contact`
- بدل بناء نموذج تواصل ثانٍ منفصل (تكرار منطق تحقق + حماية من البوتات + حفظ + بريد)، استُورد نفس
  مكوّن `ContactForm` من `app/(marketing)/contact/ContactForm.tsx` مباشرة — يبقى مسار الإرسال الفعلي
  (`submitContactMessage`) واحداً بغض النظر عن الصفحة التي أُرسل منها النموذج.

**تحقّق فعلي**: أُعيد اختبار الرئيسية وصفحة الخدمات عبر Playwright (سطح مكتب وجوال) بعد التمرير الفعلي
عبر كل الصفحة — تأكيد ظهور الأيقونات الجديدة، بطاقات الأسعار الحقيقية، شريط الشعارات المتحرك، وقسم
التواصل، بلا أي خطأ JS في وحدة تحكم المتصفح.

---

## 2026-07-23 (الجولة الحادية عشرة) — إعادة تسمية شاملة إلى DMS (Digital Messaging System) + شعار موحَّد

### 83. الشعار الحقيقي لم يُحفَظ مباشرة من الصورة الملصَقة بالمحادثة — قيد تقني حقيقي وليس تفضيلاً
- لا توجد أداة متاحة تسمح بحفظ صورة مُرفَقة داخل رسالة المستخدم مباشرة كملف ثنائي على القرص —
  الأدوات المتاحة إما تقرأ ملفات موجودة أصلاً بمسار (Read) أو تكتب نصاً (Write)، بلا أي مسار لتصدير
  بيانات صورة Base64/ثنائية من سياق المحادثة نفسه. بدل التوقف أو تخمين حل غير موثوق، طُلب من المستخدم
  حفظ الملف الحقيقي في `public/logo.png`، وبُني كل شيء آخر (المكوّنات، المسارات، العلامة البديلة) على
  افتراض وصول هذا الملف لاحقاً — النظام يعمل باحترافية كاملة الآن، ويتحول تلقائياً للشعار الحقيقي فور
  توفره بلا أي تعديل كود إضافي مطلوب.

### 84. علامة بديلة (Fallback Mark) مرسومة SVG بنفس ألوان الشعار الحقيقي، بدل حرف واحد أو صورة مكسورة
- بانتظار الملف الحقيقي، بُنيت علامة SVG مجرَّدة (أربع فصوص متشابكة بالأخضر/الكحلي، مستوحاة من شكل
  الشعار الموصوف) + كتابة "DMS" / "DIGITAL MESSAGING SYSTEM" — مظهر احترافي مقصود بدل حرف "D" وحيد
  في دائرة ملوّنة أو أيقونة صورة مكسورة، حتى في الفترة الانتقالية قبل رفع الملف الحقيقي.

### 85. إصلاح حقيقي مهم: سباق hydration كان يُفشل fallback الشعار بالكامل عبر `onError` وحده
- الاختبار الفعلي (Playwright + فحص DOM مباشرة) كشف أن الشعار في شريط التنقل يبقى صورة مكسورة (alt
  text ظاهراً) رغم أن `/logo.png` يرجع 404 فعلياً وأن `onError` مُعرَّف على العنصر. السبب: المتصفح قد
  يبدأ تحميل `<img>` المُصيَّر من الخادم (SSR) ويفشل *قبل* أن يُكمل React تعليق مُستمِع `onError` أثناء
  الـhydration، فيُفقَد الحدث نهائياً (سباق حقيقي، وليس خطأً في المنطق نفسه). الإصلاح: `<img>` لا
  يُضاف لشجرة DOM إطلاقاً إلا بعد تأكيد اكتمال التركيب (`mounted` عبر `useEffect`) على العميل — عندها
  يكون `onError` معلَّقاً بالفعل قبل أي محاولة تحميل، فيستحيل فوات الحدث. طُبِّق نفس الإصلاح في مكوّن
  الشعار (`Logo.tsx`) وفي شعار التاجر بالشريط الجانبي (`Sidebar.tsx`، شعار مرفوع فعلياً من التاجر عبر
  `Tenant.logoUrl` — قد يكون رابطاً خارجياً، فطريقة "فحص HEAD مسبق" مرفوضة عمداً هنا لتفادي مشاكل CORS
  محتملة مع نطاقات خارجية، بخلاف الحل الأبسط "التأجيل حتى اكتمال التركيب" الذي يعمل بلا أي قيد كهذا).

### 86. غلاف دخول موحَّد (`AuthShell`) بدل إعادة تصميم كل نموذج من الصفر
- البرومنت طلب توحيد هوية دخول التاجر ومالك المنصة والشريك، وجعلها أكثر احترافية بمحتوى "شبيه بإعلان
  الشركة". بدل إعادة كتابة كل نموذج (`/login`, `/register`, `partners/apply`, `partners/status`,
  `partners/setup`) بألوان جديدة بالكامل (عمل كبير وخطر تراجع)، بُني غلاف مشترك واحد (`AuthShell`):
  لوحة ترويجية يسارية ثابتة (شعار + جملة تعريفية + نقاط قوة بأيقونات + إحصائيات مثل الصفحة الرئيسية)
  ولوحة النموذج الفعلي يمينها بنفس الخلفية الداكنة المستخدمة أصلاً (`bg-navy-900`) — توحيد الهوية يتحقق
  عبر اللوحة الترويجية المشتركة والشعار الموحَّد ولون العلامة الأخضر (`wa-*` بدل `accent-*` الأرجواني)،
  دون الحاجة لإعادة كتابة كل حقل/زر من الصفر في كل صفحة. راجع القرار 87 لسبب عدم تغيير خلفية النموذج.

### 87. لوحة النموذج تبقى داكنة (`bg-navy-900`)، وليست فاتحة كباقي الموقع التسويقي الجديد
- لوحة التحكم الداخلية ونماذج الدخول القائمة (`.card`, `.input-field`, `.btn-primary`) كلها مبنية على
  نظام ألوان داكن ثابت (وليس عبر `dark:` variants قابلة للعكس) منذ الجولات الأولى. إعادة كتابتها بالكامل
  لنظام فاتح (كصفحات `(marketing)` الجديدة) كان سيعني إعادة تصميم كل حقل وزر في خمس صفحات دفعة واحدة —
  عمل كبير الحجم وعالي الخطورة نسبةً للفائدة الفعلية، خصوصاً أن هذه الصفحات (تسجيل الدخول، التسجيل،
  نماذج الشركاء) وظيفتها التركيز الكامل على الإجراء نفسه لا استعراض محتوى تسويقي طويل. القرار: البقاء
  على الخلفية الداكنة الحالية للنموذج، مع تحويل الألوان البرّاقة فيه فقط (الأزرار الأساسية، الروابط،
  الحدود المُفعَّلة) من الأرجواني (`accent-*`) إلى الأخضر (`wa-*`) ليطابق الهوية الجديدة، بدل إعادة بناء
  كل شيء من الصفر.

### 88. صفحة "انضم كشريك" (`partners/join`) أُعيد تصميمها بالكامل بالنمط الفاتح، لا `AuthShell`
- بخلاف صفحات النماذج أعلاه، هذه صفحة تسويقية طويلة (Hero + مزايا + باقات + فوتر خاص بها) وليست نموذج
  إجراء واحد مركّز — فرضها داخل `AuthShell` (المصمَّم لنموذج واحد مضغوط) كان سيُفسِد تخطيطها. أُعيد بناء
  ألوانها فقط (فاتح، أخضر، شعار DMS) يدوياً مع الإبقاء على بنيتها القائمة (رأس/تذييل خاصان بها، وليس
  Navbar/Footer المشتركين من `(marketing)/layout.tsx`) — نقل الملف فعلياً لمجموعة `(marketing)` كان
  خياراً مطروحاً (لا يغيّر الرابط `/partners/join` لأن مجموعات المسارات لا تُضاف للمسار الفعلي) لكنه
  تأجَّل لتقليل حجم التغيير في جولة مكثفة أصلاً بتعديلات كثيرة الملفات.

### 89. `Tenant.logoUrl`/`primaryColor` الحقيقيان لتينانت "dms" حُدِّثا مباشرة في القاعدة الحقيقية
- تينانت المستخدم الحقيقي الوحيد النشط حالياً كان اسمه "DMS" فعلاً مسبقاً (مصادفة مفيدة، ليست تصميماً
  مقصوداً من هذه الجولة) — حُدِّث `logoUrl` إلى `/logo.png` (نفس ملف شعار المنصة العام) و`primaryColor`
  إلى الأخضر الجديد (`#25D366` بدل الأرجواني الافتراضي `#6D5EF8`)، بحيث تظهر هوية المتجر في لوحة تحكمه
  الخاصة (الشريط الجانبي في `dashboard/layout.tsx`، عبر `Sidebar.logoUrl` الذي أُضيف هذه الجولة) متطابقة
  مع هوية المنصة العامة الجديدة فوراً — يطابق طلب المستخدم الصريح "نفس هوية المتجر".

**تحقّق فعلي**: أُعيد بناء المشروع بالكامل (`tsc`/`next build`) وتصفّح حقيقي عبر Playwright لكل من:
تسجيل الدخول، التسجيل، نظام الشركاء بكل مراحله، الصفحة الرئيسية، ولوحة مالك المنصة — تأكيد ظهور اسم
DMS والشعار (الحقيقي أو البديل حسب توفر الملف) بشكل صحيح ومتسق في الجميع، واكتشاف وإصلاح خلل سباق
hydration حقيقي أثناء هذا التحقق نفسه (وليس نظرياً فقط).

---

## 2026-07-23 (الجولة الثانية عشرة) — تطوير صفحة "تواصل معنا" + تحويل قسمها بالرئيسية لبنر

### 90. قسم التواصل بالرئيسية تحوَّل من نموذج كامل مكرَّر إلى بنر دعوة يوجّه لصفحة `/contact`
- الطلب المباشر كان "زي بنر يفتح الصفحة دي" — بدل تكرار نموذج التواصل الكامل (مع بطاقات معلومات
  التواصل) داخل الصفحة الرئيسية وصفحة `/contact` معاً (كما كان الحال في الجولة السابقة)، استُبدل قسم
  الرئيسية ببنر ترويجي واحد (تدرّج لوني داكن-أخضر، عنوان + جملة تعريفية + زر "تواصل معنا الآن") يوجّه
  مباشرة لصفحة `/contact` الكاملة. هذا يزيل التكرار الوظيفي (نفس النموذج بمنطق واحد في مكان واحد فقط)
  ويجعل صفحة `/contact` هي الوجهة الوحيدة المتطوّرة لهذه الوظيفة بدل نسختين تحتاجان تزامناً يدوياً.

### 91. حقل "نوع الاستفسار" جديد (`ContactMessage.inquiryType`) بدل نص حر — عمود حقيقي وليس فقط واجهة
- بدل الاكتفاء بإضافة حقل شكلي في الواجهة فقط، أُضيف عمود فعلي في قاعدة البيانات (migration حقيقية)
  بقيم محدودة (Enum منطقي عبر `z.enum` وليس Prisma enum، لإتاحة تعديل القائمة لاحقاً بلا migration إن
  احتاج فريق الدعم تصنيفات إضافية) — القيمة تُدرَج في **عنوان** بريد الإشعار (`[دعم فني] رسالة جديدة...`)
  لا في نصه فقط، بحيث يستطيع فريق الدعم فرز/تصفية صندوق الوارد فوراً بلا فتح كل رسالة على حدة. نفس
  القائمة (`INQUIRY_TYPES`) مصدرها ملف مشترك واحد (`constants.ts`) يستورده كل من النموذج والتحقق
  الخادمي، فيستحيل اختلاف القيم المسموحة بين الواجهة والخادم.

### 92. إصلاح حقيقي: تصدير ثابت (غير دالة) من ملف `"use server"` يُكسر فعلياً على العميل
- المحاولة الأولى وضعت `INQUIRY_TYPES` كتصدير عادي داخل `contact/actions.ts` نفسها (نفس الملف الذي
  يُصدِّر `submitContactMessage`) بحجة "مصدر واحد". الاختبار الفعلي (Playwright + فحص أخطاء المتصفح
  الحقيقية) كشف خطأ فعلي: `INQUIRY_TYPES.map is not a function` على العميل، وصفحة `/contact` بأكملها
  تُرجع 500. السبب: أي ملف يحمل توجيه `"use server"` يُحوَّله Next.js بالكامل لحزمة استدعاءات RPC بين
  العميل والخادم — يُسمح فيه فقط بتصدير دوال `async`؛ أي تصدير آخر (كائن، مصفوفة، ثابت) يُستبدَل بمُغلِّف
  فارغ لا يحمل القيمة الحقيقية إطلاقاً عند الوصول إليه من مكوّن عميل. الإصلاح: نُقل `INQUIRY_TYPES`
  لملف منفصل بلا أي توجيه خاص (`constants.ts`)، يستورده كل من `actions.ts` (للتحقق الخادمي عبر
  `z.enum`) و`ContactForm.tsx` (للعرض) بأمان تام.

### 93. أدوات إضافية حقيقية بصفحة `/contact` بدل تجميل بصري فقط — تلبيةً لطلب "أدوات مميزة"
- **أيقونات SVG مخصَّصة جديدة** (بريد، هاتف/واتساب، ساعة، دائرة تحقق، نسخ) بنفس أسلوب أيقونات الخدمات
  الموجودة أصلاً، بدل الإيموجي القديم في بطاقات معلومات التواصل.
- **زر نسخ سريع** (`CopyButton`) بجانب البريد ورقم الواتساب — أداة فعلية تستخدم Clipboard API
  الحقيقية، وليست مجرد أيقونة تزيينية.
- **شريط ثقة** (رد خلال ساعتين / دعم 6 أيام أسبوعياً / فريق عربي متخصص) أعلى النموذج مباشرة.
- **أسئلة شائعة خاصة بالتواصل** (وليست نفس أسئلة صفحة الأسعار) أسفل الصفحة، بنفس مكوّن `<details>`
  المستخدم أصلاً في صفحة الأسعار — إعادة استخدام نمط موجود بدل اختراع مكوّن Accordion جديد.

**تحقّق فعلي**: اختُبر إرسال رسالة فعلية بنوع استفسار محدَّد ("دعم فني") عبر متصفح حقيقي حتى ظهور
رسالة النجاح، وتأكيد وصول البريد بعنوان يحمل بادئة `[دعم فني]` فعلياً (وليس افتراضاً)، وظهور شارة نوع
الاستفسار في `admin/leads`. اكتُشف وأُصلح خطأ حقيقي (القرار 92) أثناء هذا التحقق نفسه، وليس نظرياً.
بيانات الاختبار حُذفت من القاعدة الحقيقية بعد التحقق.

---

## 2026-07-23 (الجولة الثالثة عشرة) — إعادة تصميم علامة "DMS" البديلة (طلب تحسين مباشر)

### 94. "DMS" كبيرة ومتمركزة + كل حرف يظهر داخل الكلمة المقابلة له فعلياً
- الطلب المباشر: تكبير "DMS" وتوسيطها بشكل احترافي، مع إظهار أن كل حرف منها هو فعلياً الحرف الأول
  من الكلمة المقابلة في الاسم الكامل. أُعيد تصميم `LogoFull` (`Logo.tsx`): "DMS" أكبر حجماً بخط عريض
  في الأعلى، وتحتها "Digital Messaging System" حيث يُبرَز الحرف الأول من كل كلمة بلون العلامة الأخضر
  وبقية الحرف بلون رمادي مكتوم — ربط بصري مباشر بين الاختصار والاسم الكامل، لا مجرد كتابته أسفله فقط.

### 95. إصلاح حقيقي: اتجاه RTL للصفحة كان يعكس ترتيب الكلمات الإنجليزية الثلاث بصرياً
- الاختبار الفعلي (لقطة شاشة حقيقية) كشف أن الكلمات تظهر معكوسة تماماً: "System Messaging Digital"
  بدل "Digital Messaging System" — رغم أن ترتيبها في الكود (JSX) صحيح تماماً. السبب: العنصر الحاوي
  `flex` يرث اتجاه الصفحة العام (`dir="rtl"` على `<html>`)، فيعكس المتصفح تلقائياً ترتيب العناصر
  الفرعية بصرياً (أول عنصر في DOM يُرسَم يميناً بدل يساراً). الإصلاح: `dir="ltr"` صريح على حاوية اسم
  العلامة نفسها فقط (وليس الصفحة كلها) — نفس الحل المستخدم سابقاً في `HeroAnimation`/`LogoMarquee`
  لنفس فئة المشكلة (محتوى إنجليزي داخل صفحة RTL يعتمد ترتيباً بصرياً محدداً).

---

## 2026-07-23 (الجولة الخامسة عشرة) — إصلاح صفحة "فريق المنصة الداخلي" ومصفوفة صلاحيات حقيقية

### 96. إصلاح خطأ حقيقي: استثناء خام من Server Action مربوط مباشرة بـ`<form action>` يُسقِط الصفحة
- المستخدم واجه شاشة خطأ Next.js غير معالجة فعلياً عند دعوة عضو ببريد إلكتروني مكرَّر. السبب: `admin/team/page.tsx`
  كان يربط `inviteInternalStaff`/`toggleInternalStaffActive` مباشرة كقيمة لـ`action` على عنصر
  `<form>` خادمي، بلا أي مكوّن عميل يلتقط النتيجة — أي `throw new Error(...)` داخل الدالة (بريد
  مكرَّر، فشل تحقق) يتحوَّل لرفض واعد (promise rejection) غير معالج يعرضه Next.js كشاشة خطأ كاملة
  بدل رسالة ودّية. أُصلح بتحويل الدالتين لإعادة نتيجة مكتوبة (`{success: true} | {success: false,
  error}`) بدل الرمي، وبناء مكوّني عميل (`InviteStaffForm.tsx`, `ToggleActiveButton.tsx`) يلتقطان
  النتيجة عبر `useTransition` ويعرضانها Inline — نفس النمط المستخدم أصلاً في كل نماذج الموقع
  التسويقي (`ContactForm`, `PartnersApplyWizard`).

### 97. إصلاح فجوة حقيقية أعمق: العضو المدعو لم يكن يقدر على تسجيل الدخول إطلاقاً
- أثناء إصلاح الخطأ أعلاه، اكتُشفت فجوة أخطر: `inviteInternalStaff` كانت تُولِّد `tempPassword`
  عشوائياً (`Math.random().toString(36)`) ثم تُجزِّئه في `passwordHash` — لكن القيمة الخام لم تكن
  تُعاد من الدالة، ولا تُرسَل بريدياً، ولا تُعرَض في أي واجهة. أي عضو فريق دُعي فعلياً **لم يكن يملك
  أي طريقة لمعرفة كلمة مروره ولا لتسجيل الدخول أبداً** — الميزة كانت معطَّلة بالكامل عملياً رغم عدم
  ظهور أي خطأ ظاهر عند استخدامها بنجاح (بريد غير مكرَّر). أُصلحت بإعادة استخدام آلية "رابط إعداد حساب
  آمن" المبنية أصلاً لنظام الشركاء (راجع القرار التالي).

### 98. إعادة تسمية `PartnerSetupToken` إلى `AccountSetupToken` لإعادة استخدامها لفريق المنصة أيضاً
- بدل بناء جدول/مسار منفصل مطابق تماماً لنفس الآلية (رمز عشوائي + تجزئة SHA-256 + صلاحية + استخدام
  واحد)، أُعيدت تسمية `PartnerSetupToken` (كانت مقصورة اسمياً على نظام الشركاء) إلى `AccountSetupToken`
  عبر migration حقيقية (الجدول كان فارغاً وقت التنفيذ — تحقُّق مباشر قبل التنفيذ، فلا خطر فقدان بيانات).
  `admin/team`, `partners/apply`, و`partners/setup/[token]` تشترك الآن في نفس الجدول والمسار: أي حساب
  (تاجر شريك مُعتمَد، أو عضو فريق داخلي مدعو) يُفعَّل عبر نفس صفحة `/partners/setup/[token]` تحديداً —
  الصفحة تعرض الآن "حسابك جاهز" بدل اسم تينانت محدَّد عند غياب تينانت (عضو الفريق الداخلي بلا تينانت).

### 99. مصفوفة صلاحيات حقيقية مُشتقّة حياً من `lib/rbac.ts` — لا وصف نصي ثابت منفصل
- الطلب المباشر: "خليها صفحة أساسية وليست وهمية". الصفحة سابقاً كانت تعرض فقط تلميحاً نصياً ثابتاً
  مكتوباً يدوياً داخل كل `<option>` ("دعم فني (موافقات + انتحال هوية)") — ينحرف بصمت عن التعريف
  الفعلي في `rbac.ts` عند أي تعديل مستقبلي فيه. أُضيفت `PLATFORM_PERMISSION_LABELS_AR` (تسمية عربية
  لكل صلاحية `platform.*`) و`PLATFORM_STAFF_ROLES` في `rbac.ts` نفسها، وبُنيت جدول مصفوفة فعلي في
  `admin/team/page.tsx` يستدعي `hasPermission(role, permission)` حياً لكل خلية — أي تعديل مستقبلي في
  `ROLE_PERMISSIONS` ينعكس تلقائياً في الجدول المعروض، بلا أي نص موازٍ يحتاج تحديثاً يدوياً متزامناً.

**تحقّق فعلي شامل**: أُعيد إنتاج سيناريو العطل بالضبط (دعوة ببريد مكرَّر `admin@platform.sa`) عبر
متصفح حقيقي — تأكيد ظهور رسالة ودّية Inline بلا أي شاشة خطأ. ثم اختُبر المسار الكامل الفعلي: دعوة
عضو جديد ببريد فعلي ← تحقق من وصول بريد إعداد الحساب بالرابط الصحيح ← إكمال الإعداد بكلمة مرور حقيقية
← تسجيل دخول فعلي ناجح للوحة مالك المنصة بحساب العضو الجديد. بيانات الاختبار حُذفت من القاعدة
الحقيقية بعد التحقق.

---

## 2026-07-23 (الجولة السادسة عشرة) — admin/team كصفحة إدارة كاملة: أعضاء + صلاحيات شاملة + الشركاء

### 100. توسعة إدارة "الأعضاء" لتشمل تغيير الدور والحذف النهائي و"إعادة إرسال رابط الإعداد"
- الطلب: "خليها اداره كامله لكل الصلاحيات والاعضاء والشركاء". الصفحة سابقاً كانت تدعم فقط الدعوة
  وتفعيل/تعطيل الحساب — بلا أي طريقة لتغيير دور عضو حالي، ولا حذفه نهائياً، ولا معرفة هل أكمل إعداد
  حسابه أصلاً أو ما زال عالقاً بانتظار الرابط. أُضيفت 3 إجراءات جديدة في `admin/team/actions.ts`:
  `changeInternalStaffRole` (تغيير الدور بين `PLATFORM_SUPPORT`/`PLATFORM_BILLING`، مع استثناء صريح
  لـ`SUPER_ADMIN` حتى لا يفقد أحد الوصول الكامل بالخطأ)، `removeInternalStaff` (حذف فعلي نهائي —
  عضو الفريق الداخلي، خلافاً للتاجر، لا يملك بيانات تينانت مرتبطة فيُحذف مباشرة بأمان، مع استثناء
  حذف النفس وحذف `SUPER_ADMIN`)، و`resendStaffSetupLink` (يُبطل أي رمز `AccountSetupToken` سابق غير
  مستخدَم وينشئ رمزاً جديداً ويعيد إرسال بريد الإعداد — لعضو نسي الرابط الأول أو انتهت صلاحيته).
  حالة "بانتظار إكمال الإعداد" تُكتشف حياً من وجود رمز `AccountSetupToken` غير مستخدَم (`usedAt: null`)
  لأحدث رمز للعضو، وتظهر كشارة صريحة في القائمة بدل أن تبقى مخفية.

### 101. مصفوفة صلاحيات شاملة: كل الأدوار (منصة + تجار) وكل الصلاحيات في مكان واحد
- المصفوفة السابقة (قرار 99) كانت تعرض فقط صلاحيات `platform.*` عبر 3 أدوار داخلية، وتترك صلاحيات
  التاجر (`campaigns.*`, `chatbot.*`, `contacts.*`, `inbox.*`, `integrations.manage`, `billing.manage`,
  `team.manage`, `settings.manage`) وأدواره (`OWNER`/`ADMIN`/`AGENT`) غائبة تماماً عن أي مرجع مرئي
  موحَّد — رغم أن الطلب الصريح كان "كل الصلاحيات". أُضيفت `TENANT_PERMISSION_LABELS_AR` (تسمية عربية
  لكل صلاحية تاجر) و`ALL_ROLES_ORDERED` (كل الأدوار الستة بترتيب ثابت) في `rbac.ts`، وأُعيد بناء الجدول
  في `admin/team/page.tsx` بقسمين واضحين (صف عنوان فاصل لكل قسم): "صلاحيات لوحة مالك المنصة" ثم
  "صلاحيات لوحة التاجر" — كلاهما عبر نفس الأدوار الستة، مُشتقّ حياً بنفس أسلوب `hasPermission()` بلا
  أي نص ثابت منفصل يحتاج مزامنة يدوية. صلاحية `platform.manage_tenants` (مظلة قديمة موثَّقة كتوافق
  كود سابق) استُثنيت عمداً من كلا الخريطتين — لا تُعرَض في المصفوفة لتفادي تكرار زائف مع الصلاحيات
  المجزَّأة الفعلية.

### 102. "الشركاء" = لوحة ملخّص حي مرتبطة، وليست دمجاً لإدارة فريق التاجر داخل هذه الصفحة (قرار تفسير)
- تفسير "الشركاء" في طلب المستخدم كان يحتمل معنيين: (أ) إظهار حالة طلبات انضمام الشركاء، أو (ب) دمج
  إدارة فريق كل تاجر (`OWNER`/`ADMIN`/`AGENT`) مباشرة داخل هذه الصفحة. تأكَّد بالفحص أن (ب) موجودة
  فعلاً ومعمارياً منفصلة عمداً في `admin/tenants/[id]/team/page.tsx` (تدير فريق تينانت محدَّد عبر
  `withTenant`/RLS)، بينما هذه الصفحة (`admin/team`) تدير حصراً فريق المنصة الداخلي عبر `superAdminDb`
  بلا RLS — دمج الاثنين كان سيكسر هذا الفصل المعماري المتعمَّد بلا فائدة حقيقية (تكرار لواجهة موجودة).
  اختير التفسير (أ): لوحة "الشركاء" الجديدة تعرض بيانات حية فعلية — عدد طلبات الشركاء المعلَّقة
  (`ApprovalRequest` بنوع `PARTNER_APPLICATION` وحالة `PENDING`/`NEEDS_INFO`)، عدد التجار الكلي
  (`Tenant.count()`)، وآخر 5 طلبات شركاء بحالتها — مع رابطين سريعين لـ`/admin/approvals` و`/admin/tenants`
  للتعامل الفعلي معها. لا بيانات وهمية: كل رقم وسطر مُستعلَم حياً من القاعدة وقت تحميل الصفحة.

**تحقّق فعلي شامل عبر متصفح حقيقي (Playwright)**: تسجيل دخول كمالك منصة ← دعوة عضو اختباري جديد ←
تأكيد ظهوره بشارة "بانتظار إكمال الإعداد" ← "إعادة إرسال رابط الإعداد" بنجاح ← تغيير دوره حياً ←
تفعيل/تعطيل حسابه مرتين ← تأكيد ظهور القسمين الكاملين لمصفوفة الصلاحيات ولوحة "الشركاء" بأرقام حية ←
حذفه نهائياً ← تأكيد اختفائه من القائمة بعد إعادة تحميل الصفحة. تحقُّق مباشر من قاعدة البيانات بعدها:
لا مستخدم متبقٍّ، ولا رموز `AccountSetupToken` يتيمة (حُذفت تلقائياً عبر `onDelete: Cascade`). لا أخطاء
console/صفحة ظهرت طوال السيناريو (سوى 404 متوقّعة لـ`/logo.png` الفعلي الذي لم يُرفَع بعد، موثَّق سابقاً).

---

## 2026-07-23 (الجولة السابعة عشرة) — قناة واتساب تجريبية عبر QR (Baileys) بديلة عن Meta

### 103. مزوّد جديد `WHATSAPP_QR` — مسح باركود شبيه بواتساب ويب، بدون أي اعتماد على Meta
- الطلب: تاجر يريد تجربة المنصة فوراً بواتسابه الشخصي بلا مراجعة/إعلانات Meta. أُضيف مزوّد جديد
  `WHATSAPP_QR` في `IntegrationProvider` عبر مكتبة غير رسمية (`@whiskeysockets/baileys` تحاكي
  بروتوكول واتساب ويب) — **تنبيه مخاطر موثَّق وأُبلغ للمستخدم صراحة**: هذا يخالف شروط استخدام واتساب
  رسمياً وقد يعرّض الرقم لحظر. عولج بوضعها "قناة تجربة محدودة المدة" فقط (3 أيام موحّدة لكل الباقات —
  المستخدم اختار صراحة عدم تخصيصها لكل باقة) مع موافقة صريحة (checkbox) إلزامية في الواجهة قبل التفعيل.
- بنية التكامل تتبع `Integration` الموجود حرفياً (لا جدول منفصل)، فتستفيد تلقائياً من `WebhookLog`،
  صلاحية `integrations.manage` الموجودة (OWNER/ADMIN)، وصفحة `dashboard/integrations` — أقل تغيير ممكن.

### 104. الـsocket الحي يعيش داخل عملية `npm run worker` فقط، والجسر مع Next.js عبر BullMQ + Redis
- Next.js API routes/Server Actions قصيرة العمر ولا تصلح لاستضافة WebSocket دائم (Baileys). اتصال
  Baileys الحي يعيش حصراً في `Map` بذاكرة عملية العامل (`src/lib/integrations/whatsappQr/
  sessionManager.ts`)، بأوامر "ابدأ/أوقف" تصل من Next.js عبر طابورَي BullMQ (`whatsapp-qr-start`/
  `whatsapp-qr-stop`)، وحالة لحظية (QR الحالي/متصل) تُقرأ عبر Redis مباشرة تستطلعها الواجهة كل 2.5
  ثانية — لا حاجة لبناء WebSocket/SSE جديد، كل البنية التحتية (ioredis + bullmq) موجودة أصلاً.
- **قيد صريح موثَّق**: هذا التصميم لا يدعم أكثر من نسخة واحدة من `npm run worker` تعمل بالتوازي (حالة
  socket لكل مستأجر تعيش في ذاكرة عملية واحدة) — توسعة لعدة نسخ تتطلب قفل موزَّع عبر Redis مستقبلاً.

### 105. حفظ جلسة Baileys عبر إعادة تشغيل العامل: مخزن مخصص بدل `useMultiFileAuthState`
- الافتراضي في Baileys يكتب على القرص المحلي (`useMultiFileAuthState`) — لا ينجو من إعادة نشر/تشغيل
  نظيف لعملية العامل (بروتوكول متّبع طوال الجلسة)، ولا يُخزَّن مشفَّراً. بُني مخزن مخصص
  (`whatsappQr/authState.ts`) يُسلسِل كل حالة الاعتماد (creds + مفاتيح الإشارة) ككائن JSON واحد عبر
  `BufferJSON.replacer/reviver` الخاصة بـBaileys نفسها (تحافظ على قيم Buffer)، يُشفَّر بـ
  `encryptSecret`/`decryptSecret` الموجودتين أصلاً في `lib/crypto.ts`، ويُخزَّن في عمود جديد
  `Integration.encryptedSessionData` (migration `20260723200000_whatsapp_qr_channel`، إضافة بحتة
  بلا أي فقدان بيانات).

### 106. استقبال/إرسال الرسائل: إعادة استخدام كامل بدل مسار موازٍ
- الرسائل الواردة (`messages.upsert`) تُحوَّل لشكل `ParsedInboundMessage` ثم تُمرَّر لنفس
  `applyParsedMetaWebhook` المُصدَّرة من `meta/webhookHandler.ts` (هي أصلاً مُعاد استخدامها لمحاكي
  Sandbox — لا منطق مكرر لإنشاء Contact/Conversation/Message). النسخة الأولى تدعم **نصوص فقط**
  (لا وسائط) — كافٍ تماماً لهدف "التجربة أن النظام يعمل فعلياً".
- اكتُشف أثناء البناء أن `worker.ts` (إرسال الحملات) و`dashboard/inbox/actions.ts` (الردود اليدوية)
  كانا يستوردان `metaAdapter` بالاسم مباشرة بلا أي اختيار حسب التكامل الفعلي المتصل. أُضيف
  `resolveWhatsappAdapter(tenantId)` في `registry.ts` يفضّل Meta دائماً إن كانت متصلة، وإلا
  WHATSAPP_QR، وإلا `metaAdapter` كافتراضي (نفس سلوك اليوم تماماً لمن لم يربط شيئاً) — يجعل القناة
  التجريبية تعمل فعلياً للحملات والردود، وليس فقط عرضاً سلبياً للرسائل الواردة.

### 107. حادثة حقيقية أثناء الاختبار: قطع اتصال واتساب حقيقي بالخطأ (درس موثَّق)
- أثناء اختبار Playwright الآلي لظهور رمز QR، اتصل **رقم واتساب حقيقي فعلي** (`+966509095816`) بمسح
  فعلي من هاتف — لم أكن أتوقع ذلك أثناء اختبار آلي. نفّذت بعدها سكربت "تنظيف" افترضت خطأً أنه يُغلق
  جلسة اختبار يتيمة، فقطع فعلياً هذا الاتصال الحقيقي الجديد ومسح بيانات جلسته (`stopSession`) **قبل**
  التحقق من حالة القاعدة الفعلية أولاً. أُبلغ المستخدم فوراً بالحادثة بمجرد اكتشافها (لا إخفاء)، وطلب
  المستخدم إكمال الاختبار بمسح جديد يدوياً بنفسه بدل أي محاولة آلية إضافية من جهتي على تينانته
  الحقيقي. **الدرس**: أي أمر "تنظيف"/"إيقاف" على قناة تتضمن اتصالاً خارجياً حقيقياً (وليس بيانات
  قاعدة بيانات بحتة) يجب التحقق من حالته الفعلية الحية أولاً قبل افتراض أنه بيانات اختبار يتيمة —
  خصوصاً في نظام إنتاجي حقيقي (تينانت DMS الحقيقي) وليس بيئة اختبار معزولة.

**تحقّق فعلي**: (1) اختبار متصفح حقيقي كامل — تسجيل دخول كمالك تاجر DMS، بوابة الموافقة الإلزامية
تعمل (الزر معطَّل قبل تفعيل checkbox)، ضغط "بدء التجربة"، وظهور رمز QR **حقيقي فعلي** (صورة PNG حقيقية
من خادم واتساب، وليس placeholder) خلال ثوانٍ. (2) اتصال حقيقي فعلي تم بمسح فعلي أثناء الاختبار (راجع
قرار 107). (3) اختبار الحذف التلقائي: تينانت وهمي منعزل تماماً (أُنشئ وحُذف بالكامل بعد الاختبار، لم
يمسّ بيانات DMS الحقيقية) بتكامل QR منتهي الصلاحية، تشغيل مهمة `scanAndExpireQrTrials` فعلياً عبر
BullMQ، تأكيد `status → DISCONNECTED` و`encryptedSessionData → null` ورسالة خطأ عربية واضحة. (4)
اختبار بوابة الصلاحية: عضو AGENT مؤقت لتاجر DMS (أُنشئ وحُذف بعد الاختبار) لا يرى أي زر تفعيل/فصل
للقناة إطلاقاً، فقط رسالة "ليس لديك صلاحية". `npx tsc --noEmit` و`npm run build` نظيفان بلا أي خطأ.

---

## 2026-07-23 (الجولة الثامنة عشرة) — استيراد المحادثات القديمة + استئناف جلسات QR بعد إعادة تشغيل العامل

### 108. استيراد المحادثات القديمة (قبل الربط) عبر حدث Baileys منفصل، وليس نفس مسار الرسائل الحية
- طلب المستخدم بعد الربط الفعلي: "عاوز المحادثات القديمة (قبل الربط) تظهر كمان" — لاحظ أن صندوق
  المحادثات فاضٍ تماماً رغم اتصال حقيقي ناجح، لأن التصميم الأصلي (قرار 106) يعالج فقط الرسائل الجديدة
  الحية (`messages.upsert` بنوع `notify`) عمداً، ولم يوثَّق للمستخدم صراحة أن السجل القديم غير مشمول.
  أُضيف `src/lib/integrations/whatsappQr/historyImport.ts` يستمع لحدث Baileys المنفصل
  `messaging-history.set` (يصل مرة واحدة فقط عند **إقران جهاز جديد فعلياً**، وليس عند كل إعادة اتصال
  — تفصيل حرج، راجع قرار 109). **متعمَّد عدم إعادة استخدام `applyParsedMetaWebhook`** لهذا المسار:
  ذاك مصمَّم لرسالة واحدة واردة في وقتها الفعلي، واستخدامه لعشرات الرسائل القديمة دفعة واحدة كان
  سينتج تحديثات حالة متتالية وهمية بدل الحالة النهائية الصحيحة فقط. الاستيراد: يستثني المجموعات
  (`@g.us`) والقوائم البثّية (`@broadcast`) عمداً (القناة لعملاء فرديين وليس محادثات جماعية)، يحدّ
  كل جهة اتصال بآخر 50 رسالة فقط (تفادي استيراد سنوات من السجل في قناة تجريبية)، ويتعامل مع الاتجاه
  الصحيح (`fromMe` → صادرة/`SYSTEM`، وإلا واردة/`CONTACT`) بدل افتراض كل شيء وارداً.

### 109. اكتشاف حقيقي: `messaging-history.set` لا يتكرر عند استئناف جلسة قائمة، فقط عند إقران جديد
- بعد نشر الميزة أعلاه وإعادة تشغيل عملية العامل (لتحميل الكود الجديد)، أُعيد استئناف جلسة المستخدم
  الحقيقية القائمة تلقائياً (باستخدام بيانات الاعتماد المحفوظة — راجع قرار 110)، لكن صندوق المحادثات
  بقي فارغاً رغم ذلك. التحقق المباشر من القاعدة أكّد: لا حدث `messaging-history.set` وصل عند
  الاستئناف. هذا سلوك متوقَّع فعلياً من بروتوكول واتساب نفسه (وليس خطأ في الكود): المزامنة الكاملة
  للسجل تحدث **مرة واحدة فقط عند إقران جهاز جديد فعلياً** (مسح QR فعلي جديد)، وليس عند كل إعادة اتصال
  لجهاز مُقرَن مسبقاً. لتفعيل استيراد السجل القديم فعلياً على حساب متصل حالياً، يلزم قطع الاتصال ثم
  مسح رمز QR جديد (إقران جديد كامل) — أُبلغ المستخدم بهذا صراحة بدل الادعاء بأنه سيعمل تلقائياً على
  الاتصال القائم.

### 110. استئناف جلسات QR تلقائياً عند بدء عملية العامل — إصلاح فجوة موثوقية حقيقية اكتُشفت أثناء البناء
- أثناء التحضير لإعادة تشغيل العامل لنشر قرار 108، تبيَّن أن التصميم الأصلي (الجولة السابعة عشرة) لم
  يكن يستأنف أي جلسة QR متصلة سابقاً بعد إعادة تشغيل العامل — الـsocket الحي يعيش في الذاكرة فقط
  (يُفقَد عند أي إعادة تشغيل، وهذا بروتوكول متّبع طوال الجلسة لأي تعديل/بناء)، لكن صف `Integration`
  كان يبقى CONNECTED زوراً في القاعدة بلا أي socket فعلي يستقبل/يرسل. أُضيفت `resumeConnectedQrSessions()`
  في `worker.ts` تُشغَّل مرة عند إقلاع العملية: تجلب كل تكاملات `WHATSAPP_QR` بحالة CONNECTED عبر
  `superAdminDb`، وتستدعي `startSession(tenantId)` لكل واحدة — تُعيد الاتصال تلقائياً ببيانات الاعتماد
  المحفوظة مشفَّرة بلا حاجة لمسح QR جديد. **تحقُّق فعلي**: أُعيد تشغيل العامل فعلياً، وتأكَّد أن حالة
  اتصال المستخدم الحقيقي عادت CONNECTED تلقائياً خلال ثوانٍ بلا أي تدخل يدوي منه.

**فجوة متبقية موثَّقة بوضوح للمستخدم**: السجل القديم لن يظهر على الاتصال الحالي (تحقَّق قرار 109) —
يحتاج المستخدم قطع الاتصال ثم مسح QR جديد مرة واحدة ليحصل على استيراد السجل + الرسائل الحية معاً.

---

## 2026-07-24 (الجولة التاسعة عشرة) — لوحة "محتوى الموقع" + مراجعة أمنية شاملة + تجهيز إطلاق

### 111. نطاق واعٍ: الصفحة الرئيسية فقط، بلا بنية رفع صور جديدة
- الطلب: "مسار في اللوحة الرئيسية يتحكم في الواجهة كاملة". تم توضيح صريح للمستخدم أن "يتحمل ملايين
  المستخدمين" و"آمن 100%" غير قابلَين للضمان بمراجعة كود فقط — النطاق المتفق عليه: تحكم بالمحتوى
  (نصوص/صور) بدون كود + إصلاح ثغرات حقيقية + قائمة تجهيز إطلاق. الاستكشاف أظهر أن الأسعار (Plan)
  مُدارة بالفعل عبر `/admin/plans`، وأن الصفحة الرئيسية تحمل وحدها ~45 نصاً ثابتاً مقابل ~150-180
  عبر كامل الموقع — اختير نطاق **الصفحة الرئيسية فقط** هذه الجولة (أعلى قيمة/تكراراً)، مع ترك
  Navbar/Footer وباقي الصفحات (about/services/vision/privacy/terms) خارج النطاق عمداً (محتوى
  روابط/قانوني أكثر منه تسويقي) — لكن بنفس النمط القابل للتوسّع لها لاحقاً بسهولة إن طُلب.
- **لا بنية رفع صور (upload) جديدة بُنيت**: تأكَّد أن لا شيء من هذا موجود في المشروع إطلاقاً (لا
  multer/formidable ولا S3/Cloudinary ولا `/api/upload`). الصور تُدخَل كرابط URL نصي فقط (نفس نمط
  `Tenant.logoUrl` الموجود أصلاً) — فجوة موثَّقة بوضوح، وليست بناءً تحتياً كاملاً جديداً غير مطلوب صراحة.

### 112. موديل `SiteContent` singleton + نمط "سطر لكل عنصر بحقول مفصولة بـ|" للقوائم
- نفس نمط `PlatformSettings` تماماً (`id: "singleton"`) للحقول المفردة (العناوين/النصوص)، وJson
  للقوائم (ميزات/إحصائيات/آراء/عملاء) بنفس فلسفة `Plan.features`. القوائم متعددة الحقول (مثال: ميزة
  = عنوان + وصف) تُحرَّر عبر `<textarea>` بسطر واحد لكل عنصر وحقوله مفصولة بـ`|` — امتداد بسيط لنمط
  `Plan.features` (سطر واحد لكل عنصر) بدل بناء نموذج JS ديناميكي لإضافة/حذف صفوف، اتساقاً مع أسلوب
  الكود القائم (نماذج HTML عادية بلا حالة عميل). الأيقونات في قسم الميزات تبقى ثابتة بالكود بترتيبها
  الحالي — غير قابلة للتعديل من اللوحة (تفادي الحاجة لمحرر أيقونات كامل غير مطلوب).
- صلاحية جديدة `platform.content.manage` (SUPER_ADMIN فقط)، وactions منفصلة لكل قسم (Hero/الميزات/
  الإحصائيات/الآراء/العملاء/بانر التواصل) بدل action واحد ضخم — كل قسم بزر حفظ مستقل.

### 113-117. إصلاحات أمنية حقيقية (من مسح استطلاعي فعلي عبر الكود بالكامل)
113. **حرجة**: `src/lib/crypto.ts` `getKey()` كانت تسقط لسلسلة نصية ثابتة مكشوفة في الكود
     (`"dev-only-insecure-fallback-key-32b"`) أو لإعادة استخدام `NEXTAUTH_SECRET` إذا غاب
     `ENCRYPTION_KEY` — أصبحت ترمي خطأ صريحاً عند الإقلاع بلا أي fallback. **فشل صريح أفضل من تشفير
     ضعيف صامت لبيانات حقيقية** (Meta/Zid/Salla access tokens).
114. `withTenant()` في `src/lib/db.ts` (نقطة العزل الوحيدة الأهم في المنصة) أضيف لها تحقق صارم من
     صيغة معرّف Prisma cuid قبل التمرير المباشر لـ`$executeRawUnsafe` — تحصين دفاعي إضافي (تأكَّد أن
     `tenantId` كان دائماً آمن المصدر فعلياً، لكن الرفض الصريح لأي قيمة غير متوقعة أرخص من الثقة
     الضمنية وحدها).
115. Rate limiting أُضيف لعمليات كانت بلا أي حد مقارنةً بنظائرها المحمية فعلاً: `registerTenant`
     (تسجيل تاجر عام)، `inviteTeamMember`/`inviteInternalStaff`/`resendStaffSetupLink` (بريد)،
     و`sendReply`/`sendTemplateReply` (إرسال واتساب فعلي من موظف — نفس حد إرسال الحملات 60/10 ثوانٍ
     لكن بمفتاح Redis منفصل حتى لا تتشارك الحصة).
116. مقارنة `META_WEBHOOK_VERIFY_TOKEN` في مصافحة `hub.challenge` (GET) تحوّلت من `===` عادية إلى
     `timingSafeStringEqual` جديدة في `lib/crypto.ts` (مبنية على `crypto.timingSafeEqual`) —
     خطورتها كانت منخفضة أصلاً (مصافحة إعداد لمرة واحدة) لكن للاتساق مع بقية المقارنات الحساسة.
117. مراجعة قراءة فقط لـ`$transaction` blocks في `register/actions.ts`/`admin/approvals/actions.ts`
     (كتابة مباشرة عبر `superAdminDb` بلا `withTenant` وقت إنشاء تينانت جديد) — تأكَّد أنه سلوك
     متعمَّد وضروري (لا تينانت/RLS context موجود بعد وقت الإنشاء)، بلا أي تعديل مطلوب.

### 118. تجهيز الإطلاق: مدة جلسة صريحة + قائمة عوائق خارجية موثَّقة
- `src/lib/auth.ts`: `session.maxAge` أصبح 7 أيام صريحة (`updateAge` يوم واحد) بدل افتراضي NextAuth
  الضمني (30 يوماً) — أنسب لمنصة تدير محادثات عملاء حقيقية.
- عوائق موثَّقة **لا يمكن حلّها بكود** (تحتاج قراراً/فعلاً من المستخدم): دومين واستضافة حقيقيين
  (يعيق مراجعة Meta App وأي إطلاق فعلي)، مزوّد بريد حقيقي (`EMAIL_PROVIDER=console` حالياً — روابط
  الإعداد/الدعوات لا تصل لأي بريد فعلياً)، و`META_APP_SECRET` فارغ (أي webhook حقيقي من Meta يُرفَض
  حالياً — فشل آمن لكن غير وظيفي حتى تفعيل تطبيق Meta الرسمي للمنصة).

**تحقّق فعلي**: (1) تعديل حقيقي لعنوان Hero من `/admin/content` وتأكيد ظهوره فوراً لزائر حقيقي غير
مسجَّل دخول (متصفح منفصل تماماً)، ثم إعادته للأصل وتأكيد الرجوع. (2) `ENCRYPTION_KEY` غير مضبوط →
رمي خطأ فوري فعلي (وليس نظرياً). (3) `withTenant` مع قيمة محاولة SQL injection (`'; DROP TABLE
users; --`) → رفض فوري، بينما تينانت حقيقي صالح يعمل طبيعياً. (4) حد التسجيل الجديد: 6 استدعاءات
متتالية بنفس المفتاح → 5 مقبولة والسادسة مرفوضة كما هو متوقَّع تماماً. `npx tsc --noEmit` و`npm run
build` نظيفان بلا أي خطأ.

---

## 2026-07-24 (الجولة العشرون) — استيراد جهات اتصال من Excel/CSV (صفحة جهات الاتصال + معالج الحملات)

### 119. استبدال مكتبة `xlsx` بـ`exceljs` قبل التركيب النهائي — ثغرات أمنية حقيقية مكتشفة أثناء التنفيذ
- الخطة الأصلية اقترحت `xlsx` (SheetJS). فور التركيب، أظهر `npm audit` ثغرتين حقيقيتين غير مُصلَحتين
  على سجل npm العام: Prototype Pollution وReDoS (كلاهما HIGH severity) — **وكلتاهما تُستَغَلّان مباشرة
  بالضبط عبر حالة استخدامنا** (تحليل ملف مرفوع من مستخدم غير موثوق). بما أن المستخدم اشترط صراحة عدم
  فتح أي ثغرة أمنية، استُبدلت فوراً بـ`exceljs` (صيانة أنشط، على سجل npm الرسمي) قبل كتابة أي كود
  فعلي يعتمد عليها. `exceljs` تحمل ثغرة واحدة متوسطة الخطورة غير مباشرة (تبعية `uuid` قديمة، خلل حدود
  buffer لا يُستدعى إطلاقاً بمسار استخدامنا) — قرار واعٍ: خطورة متوسطة غير قابلة للاستغلال عملياً أفضل
  من خطورة عالية قابلة للاستغلال المباشر بملف مرفوع من طرف خارجي.

### 120. مسار استيراد واحد مشترك، مبني على upsert لا create، بحد صفوف صارم
- `src/lib/contacts/bulkImport.ts` يُستخدَم من كلا الموضعين (جهات الاتصال + معالج الحملات) — لا تكرار
  منطق تحليل/تحقق. `upsert` لكل رقم (تحديث الموجود بدل الفشل) بدل `create` — أنسب لإعادة رفع قائمة
  محدَّثة لاحقاً، ومطابق لتجربة استخدام واقعية (المستخدم غالباً يرفع نفس القائمة مرات متعددة مع تحديثات).
  حد صارم 2000 صف/5 ميجابايت لكل ملف — رفض فوري قبل أي معالجة، وليس بعدها.

### 121. استهداف جمهور حملة بقائمة Excel = وسم تلقائي + `SegmentFilter.tagIds` — بلا أي تعديل معماري
- بدل ابتكار "نوع جمهور" جديد (`AudienceType`) أو تخزين قائمة IDs جامدة منفصلة عن نظام الجمهور
  الحالي، اكتُشف أن الفلترة بالوسوم (`Tag`/`ContactTag`) موجودة أصلاً كآلية جمهور من الدرجة الأولى في
  `resolveAudienceWhere`. الاستيراد من Excel داخل معالج الحملة = استيراد جهات اتصال حقيقية + إنشاء
  Tag فريد لهذه الدفعة تحديداً (اسم بطابع زمني ISO لضمان عدم تعارض `@@unique([tenantId, name])`) +
  ضبط `segmentFilter.tagIds = [tagId]` — يمرّ تلقائياً عبر كل الفحوصات الموجودة فعلاً (حد الجمهور
  للباقة، الحصة الشهرية، استبعاد `marketingOptIn: false`) بلا لمس `resolveAudienceWhere`/
  `createCampaign`/عامل الإرسال إطلاقاً. لوحة "عدد المستلمين المتوقع" الموجودة تعمل تلقائياً بلا أي
  كود إضافي لأنها مبنية أصلاً على استطلاع حي لنفس الآلية.

### 122. إجراءات أمنية صريحة مطبَّقة فعلياً (وليست وصفاً نظرياً)
- `next.config.js`: `experimental.serverActions.bodySizeLimit: "5mb"` (كان غائباً — الافتراضي 1MB
  غير كافٍ لملف حقيقي، وهذا نفسه حد أمني أول ضد استنزاف الموارد) + تحقق صريح إضافي من `file.size`
  داخل كل action (دفاع مزدوج، لا الاعتماد على إعداد Next.js وحده).
- قائمة امتدادات مسموحة صراحة (`.xlsx`/`.xls`/`.csv`) تُرفَض أي قيمة أخرى فوراً.
- حد rate limiting مشترك (`bulk-import`, 5/ساعة لكل مستأجر) عبر `checkTenantRateLimit` الموجودة.
- كل الكتابة عبر `withTenant` فقط (لا وصول خام). الأرقام تُطبَّع (مسافات/شرطات/بادئة 0 أو 966) ثم
  تُتحقَّق بنفس regex الموجود فعلياً في الإضافة اليدوية (`/^\+9665\d{8}$/`) — أي صف مرفوض يُدرَج في
  تقرير أخطاء بدل إسقاط الملف كاملاً. الـactions تُعيد نتيجة مكتوبة `{success, ...}` بدل رمي `Error`
  خام (نفس نمط `InviteStaffForm` الآمن). لا خطر حقن صيغ (Formula Injection) — القراءة قيم خلايا فقط
  بلا أي تنفيذ، والقالب القابل للتنزيل بيانات ثابتة من الكود لا بيانات مستخدمين تُعاد تصديرها.

**تحقّق فعلي شامل**: رفع ملف اختبار حقيقي (صفّان صحيحان + صف برقم غير صالح) عبر متصفح حقيقي — تأكيد
ظهور جهتي الاتصال الجديدتين في الجدول فعلياً وتقرير الصف المرفوض بوضوح. اختبارات أمنية فعلية: ملف
بامتداد `.exe` → رفض فوري؛ ملف نصي عادي بامتداد `.xlsx` مزوَّر → رفض بخطأ "تعذّر قراءة الملف"؛ 5
عمليات استيراد متتالية → السادسة تُرفَض فعلياً بحد المعدّل. معالج الحملة: رفع نفس الملف واختيار
"استيراد قائمة من Excel" → تأكيد إنشاء Tag فريد فعلياً وربطه بجهتي الاتصال في القاعدة الحقيقية، وظهور
العدد الصحيح في لوحة المعاينة. كل بيانات الاختبار (جهات اتصال، وسوم) حُذفت من القاعدة الحقيقية بعدها.
`npx tsc --noEmit` و`npm run build` نظيفان.

---

## 2026-07-24 (الجولة الحادية والعشرون) — محرك تنفيذ حقيقي للشات بوت (إغلاق فجوة موثَّقة منذ الجولة الثانية)

### 123. إغلاق الفجوة التاريخية: النشر كان يغيّر حالة فقط، الآن ينفّذ التدفق فعلياً على رسائل حقيقية
- الفحص المُسبَق أكّد أن محرر الشات بوت (لوحة رسم حرة، معاينة موبايل حقيقية، اختبار قبل النشر، ربط
  Meta) مبني باحترافية فعلية — الفجوة الوحيدة (موثَّقة صراحة في تعليقات الكود وQA_REPORT.md منذ
  الجولة الثانية): `togglePublish` كانت تغيّر عمود `status` فقط، بلا أي كود يقرأ `nodesJson` ويرسل
  رداً حقيقياً. بُني محرك حقيقي (`lib/chatbot/engine.ts`) يُشغَّل من نقطة واحدة بعد
  `applyParsedMetaWebhook` (التي عُدِّلت لتُعيد معرّفات المحادثات المتأثرة بدل `Promise<void>`) —
  تغطي هذه النقطة الواحدة: webhook Meta الحقيقي، محاكي الـSandbox، **وقناة واتساب QR التجريبية**
  (اكتُشفت كمستدعية ثالثة لنفس الدالة أثناء التنفيذ، أُضيفت لها الآن أيضاً — البوت يعمل حقيقياً على
  الثلاث قنوات بلا كود مكرَّر).

### 124. لا نظام متغيرات جديد — إعادة استخدام "آخر رسالة من العميل" فقط (تبسيط متعمَّد)
- محاكي الاختبار القديم كان أصلاً يقيّم عقد "شرط" ضد آخر رسالة من العميل (بـregex ثابتة مُهمَلة).
  المحرك الحقيقي يتبع نفس المنطق العقلي بدل بناء محرك "متغيرات جلسة" (session variables) كامل غير
  مطلوب: عقدة "شرط" الآن تُفسِّر `label` (نفس الحقل النصي الموجود مسبقاً في واجهة المحرر لهذا النوع)
  ككلمات مفتاحية مفصولة بفاصلة تُطابَق ضد آخر رسالة فعلية من العميل. **دالة التطابق
  (`matchesConditionKeywords`) مشتركة حرفياً بين محاكي الاختبار الحقيقي والمحرك الحي** — تجربة
  "اختبار قبل النشر" أصبحت مطابقة تماماً لما سيحدث فعلياً، بعد أن كانت (بالـregex الثابتة القديمة)
  تعطي انطباعاً مضلِّلاً بسلوك لا يطابق الواقع.

### 125. `ai_reply` تبقى صريحة الحدود عمداً — تحويل لموظف بدل ذكاء اصطناعي وهمي
- لا مزوّد ذكاء اصطناعي حقيقي مُهيَّأ في المشروع (يحتاج مفتاح API خاص بالمستخدم من مزوّد يختاره —
  خارج نطاق الكود). بدل ادّعاء رد ذكي وهمي (كما كان محاكي الاختبار القديم يفعل عبر
  `generateMockAiReply`)، تُنفَّذ عقدة `ai_reply` بتحويل فوري وصادق لموظف بشري — في المحرك **ومحاكي
  الاختبار على حد سواء** (لنفس سبب الاتساق في القرار السابق)، مع تحديث وصف العقدة في الواجهة
  ليعكس هذا الحد صراحة بدل الإيحاء بقدرة غير موجودة.

### 126. تحصين SSRF إلزامي لعقدة "استدعاء API" — رابط من إعداد التاجر نفسه (غير موثوق بالكامل)
- `lib/chatbot/safeFetch.ts` جديد: HTTPS فقط، حلّ DNS ورفض أي IP خاص/محجوز (10.x، 172.16-31.x،
  192.168.x، 127.x، 169.254.x بما فيها endpoint البيانات الوصفية السحابية الشهير
  `169.254.169.254`، ونطاقات IPv6 المكافئة)، مهلة 8 ثوانٍ، حد حجم استجابة 64KB، بلا تتبّع أي إعادة
  توجيه تلقائياً. فشل/مهلة الاتصال لا يوقف التدفق — يُسجَّل ضمنياً ويُكمَل للعقدة التالية.

### 127. حقول تتبّع حالة تنفيذ إضافية بحتة على `Conversation` + مهمة انتهاء صلاحية جلسة
- `activeFlowId`/`currentNodeId`/`flowNodeEnteredAt` (migration إضافية، بلا فقدان بيانات). أي محادثة
  واقفة عند عقدة "سؤال" لأكثر من 30 دقيقة بلا رد → مهمة BullMQ متكررة (`chatbot-session-timeout-scan`،
  كل 10 دقائق، نفس نمط `campaign-trigger-scan` حرفياً) تُعيدها تلقائياً لتحكّم بشري بدل بقائها عالقة
  صامتة للأبد. فشل إرسال حقيقي (لا اتصال واتساب فعّال) يُسجَّل بأمانة (`status: FAILED`,
  `failureReason`) ويُسلَّم فوراً لموظف — بدل التظاهر بالنجاح والاستمرار كأن شيئاً لم يحدث.

### 128. أدوات مميزة إضافية في صفحة القائمة — بيانات حية حقيقية وليست تجميلاً
- معاينة موبايل مصغّرة حقيقية لكل بطاقة (إعادة استخدام `WhatsAppPreview` الموجود من صفحة Server
  Component مباشرة)، ملخّص عدد كل نوع عقدة، عدّاد "محادثة نشطة الآن فعلياً" لكل تدفق منشور (استعلام
  حي أتاحه المحرك الجديد نفسه)، وزر "نسخ كمسودة جديدة" (`duplicateFlow`).

**تحقّق فعلي شامل عبر تينانت اختبار معزول تماماً**: رسالة واردة محاكاة أولى → تأكيد إرسال رسالتين
فعليتين تلقائياً (ترحيب + سؤال) وتوقف صحيح عند عقدة السؤال. رد "نعم" → تأكيد تقييم الشرط بشكل صحيح
وإكمال المسار الحقيقي (فرع "نعم") وانتقال `controlMode` لـ`HUMAN` عند "نهاية". محادثة ثانية منفصلة
برد "لا" → تأكيد فرع "التحويل لموظف" الصحيح. اختبار SSRF فعلي: 6 عناوين (metadata سحابية، localhost،
IPs خاصة بصيغ http/https مختلفة) → كلها مرفوضة فعلياً بالسبب الصحيح. اختبار انتهاء الجلسة: محادثة
عالقة يدوياً منذ ساعة → تشغيل المهمة مباشرة → تأكيد التصفير الفعلي. صفحة القائمة: تأكيد ظهور المعاينة
والملخّص والعدّاد الحي، واختبار زر النسخ فعلياً (تدفق جديد بحالة DRAFT تأكَّد إنشاؤه في القاعدة).
كل بيانات الاختبار (تينانت كامل، محادثات، رسائل، تدفقات) حُذفت من القاعدة الحقيقية بعدها.
`npx tsc --noEmit` و`npm run build` نظيفان.

---

## 2026-07-24 (الجولة الثانية والعشرون) — استبدال لوحة الرسم الحرة ببناء تسلسلي مبسَّط

### 129. المشكلة المُبلَّغة: لوحة الرسم الحرة (React Flow) صعبة الترتيب/الربط لغير التقنيين
- المستخدم أرسل لقطة شاشة لتدفق حقيقي منشور فيه عقد متقاطعة/معزولة بصرياً وأبلغ صراحة أنه لا يعرف
  كيف يرتّب المسارات أو يربط السؤال بالإجابة. بدل تحسينات تجميلية على نفس اللوحة الحرة، اختار
  المستخدم — بعد عرض خيارين — **الاستبدال الكامل** بواجهة بناء تسلسلي (خطوة بعد خطوة، مطابقة لترتيب
  فقاعات معاينة الموبايل الموجودة أصلاً)، لا وضعاً إضافياً بجانب القديم.

### 130. نموذج بيانات جديد للواجهة فقط (Step[] tree) — التخزين الفعلي (FlowGraph) لم يتغيّر إطلاقاً
- `lib/chatbot/builderModel.ts` جديد: شجرة خطوات (`BuilderStep[]`) حيث كل خطوة تُنفَّذ تلقائياً بعد
  سابقتها بلا أي وصل يدوي، وعقدة "شرط" فقط تتفرّع لعمودين مستقلين (✅/❌) — **بلا رجوع أو تقاطع بين
  الفروع** (قيد متعمَّد يمنع تحديداً نوع اللغبطة التي اشتكى منها المستخدم). محرك التنفيذ الحقيقي
  (`engine.ts`) ومحاكي الاختبار (`simulate.ts`) لم يُعدَّلا بحرف واحد — يبقيان يقرآن نفس بنية
  `FlowGraph` (عقد + اتصالات) كما هي، فالتحويل يحدث فقط عند العرض/الحفظ في الواجهة.
- `graphToSteps(graph)`: يحوّل أي رسم بياني مخزَّن (قديماً بلوحة الرسم الحرة) لشجرة نظيفة، بالسير
  فعلياً من عقدة البداية بنفس منطق المحرك الحقيقي تماماً — أي عقدة لا يصل إليها هذا السير (معزولة
  فعلياً، لا تُنفَّذ أبداً) تُستبعَد ويُبلَّغ عددها كتنظيف شفاف بدل حذف صامت.
- `stepsToGraph(steps)`: يبني `FlowGraph` كاملاً (عقد + اتصالات + إحداثيات تُنشأ تلقائياً للتوافق مع
  شكل التخزين فقط، لا تُعرَض على أي لوحة بعد الآن) من الشجرة — نفس الشكل الذي يفهمه المحرك دون أي
  تعديل عليه.

### 131. `ai_reply` تصبح صريحة النهاية في الواجهة أيضاً (توافقاً مع سلوكها الحقيقي في المحرك)
- اكتُشف أثناء بناء المحوّل خلل موروث من الجولة الحادية والعشرين: قالب "الرد على الأسئلة الشائعة"
  كان لا يزال يصل عقدة `ai_reply` بشرط/فرعين بعدها — لكن المحرك الحقيقي ينهي الجلسة فوراً عند
  `ai_reply` (تحويل صريح لموظف)، فتلك العقد كانت **ميتة فعلياً منذ تلك الجولة دون أن يُكتشف الخلل**.
  صُحِّح القالب (الآن: بداية ← `ai_reply` فقط)، وفُرض في الواجهة الجديدة أن `ai_reply`/`handoff`/`end`
  خطوات نهائية دائماً (لا "+ إضافة خطوة" بعدها في نفس العمود) — مطابقة صارمة لما ينفّذه المحرك
  فعلياً، بدل حقل نص قابل للتعديل كان القديم يعرضه لهذه الأنواع رغم أن محتواه لم يكن يُقرأ إطلاقاً.

### 132. الترتيب عبر أسهم ▲▼ فقط — لا سحب-وإفلات، ولا إدراج في المنتصف
- إضافة خطوة تُلحَق دائماً في نهاية عمودها؛ إعادة الترتيب تتم بأسهم أعلى/أسفل بين الخطوات المجاورة
  فقط. قرار تبسيط متعمَّد: يمنع تماماً كسر قيد "الشرط/النهاية آخر عنصر دائماً" (الأزرار تُعطَّل تلقائياً
  حول أي عقدة شرط/نهائية لحماية هذا القيد)، ويعطي تجربة يمكن لغير التقني فهمها فوراً بلا تعلّم أدوات
  رسم بياني.

**تحقّق فعلي**: بناء تدفق كامل من الصفر عبر الواجهة الجديدة فقط (رسالة → سؤال → شرط "نعم, أيوه" →
فرع نعم: رسالة، فرع لا: تحويل لموظف)، حفظ، اختبار المسارين معاً (نتيجة صحيحة لكل مسار)، إعادة ترتيب،
نشر. تشغيل محادثتين حقيقيتين كاملتين عبر محاكي الـSandbox ضد هذا التدفق بالذات (بربط حقيقي وضع
Sandbox) — تأكيد تسلسل رسائل البوت الصحيح، توقف عند "سؤال" بانتظار الرد، تطابق الشرط، ثم تحوّل صحيح
لـ`HUMAN` — أي **المحرك الحقيقي غير المعدَّل ينفّذ مخرجات الواجهة الجديدة بلا أي مشكلة**. تحقّق إضافي
للتحويل: تشغيل `graphToSteps` (قراءة فقط، بلا أي كتابة) مباشرة على التدفق الحقيقي المنشور لتاجر
"عسل مهمار" (نفس التدفق الظاهر في لقطة الشاشة) — أكّد استخراج 5 خطوات فعلية صحيحة الترتيب واكتشاف
عقدة واحدة معزولة فعلياً (غير قابلة للتنفيذ أصلاً) بدقة، مطابقاً تماماً لما تُظهره اللقطة من عقدة
منعزلة بصرياً. `npx tsc --noEmit` و`npm run build` نظيفان؛ حزمة `@xyflow/react` (وملف المحرر القديم)
أُزيلا بالكامل بعد التأكد من عدم استخدامها في أي مكان آخر بالمشروع.

---

## 2026-07-24 (الجولة الثالثة والعشرون) — عقدة "قائمة اختيارات" (menu) لتفريع احترافي متعدد الفروع

### 133. المشكلة: تكرار عقد "شرط" المتداخلة لتمثيل قائمة N خيار يُنتج شجرة رسم بياني غير احترافية
- بعد بناء تفريع حقيقي بـ4 فئات (تتبع طلب/توصيل/دعم/سياسات) عبر شروط متداخلة (كل شرط "no" يحوي
  الشرط التالي)، أرسل المستخدم لقطة شاشة تُظهر عمق التداخل الفعلي: أعمدة تضيق تدريجياً (نمط
  `grid-cols-2` لكل مستوى) حتى احتاجت تمريراً أفقياً — بالضبط نوع اللغبطة التي كان استبدال اللوحة
  الحرة (الجولة 22) يهدف لمنعه. السبب الجذري: النموذج كان يدعم فقط تفرّعاً ثنائياً (نعم/لا)، فتمثيل
  قائمة بأكثر من خيارين يتطلب تكديساً متتالياً بلا داعٍ حقيقي.

### 134. عقدة جديدة "قائمة اختيارات" (menu) — تفرّع مسطّح N-فرع + رد افتراضي، بلا تعديل على "شرط"
- عقدة `condition` (نعم/لا) بقيت كما هي بلا تغيير لحالات الاستخدام الثنائية البسيطة. عقدة `menu` جديدة
  مستقلة تماماً: عدد غير محدود من الفروع (كل فرع: اسم قصير + كلمات مفتاحية)، بالإضافة لعمود "رد
  افتراضي" ثابت (يُطابَق عند عدم تحقق أي فرع). **كل الفروع تُعرَض في الواجهة مكدَّسة عمودياً (كل واحد
  بعرض كامل)** بدل التداخل الأفقي المتضائل — هذا القرار التصميمي البسيط هو ما يحل مشكلة "اللاحترافية"
  المُبلَّغة تحديداً، بلا حاجة لأي حل تقني معقّد (لا سحب/إفلات، لا رسم بياني).
- التطابق: أول فرع كلماته المفتاحية موجودة (كنص جزئي غير حساس لحالة الأحرف) في رد العميل يفوز
  بالترتيب، وإلا يُطبَّق "الرد الافتراضي" — منطق مطابق حرفياً بين المحرك الحقيقي والمحاكي (دالتا
  `parseMenuBranches`/`matchMenuBranch` جديدتان في `simulate.ts`، يستوردهما `engine.ts` والمحرر
  ومحوّل النموذج، بنفس نمط `matchesConditionKeywords` الحالي).

### 135. تخزين فروع القائمة: JSON داخل `FlowNode.config` (إعادة استخدام حقل موجود، بلا حقل جديد بالـschema)
- `config` كان مستخدَماً فقط برابط API لعقدة `api_call`؛ الآن أيضاً يخزّن مصفوفة
  `{id, label, keywords}[]` كـJSON لعقدة `menu` — يتجنّب أي migration على Prisma schema.
  `FlowEdge.sourceHandle` وُسِّع من `"true" | "false" | null` الصارم إلى `string | null` (توسيع آمن
  رجعياً: كل استخدام حالي بـ"true"/"false" ما زال يعمل حرفياً) ليحمل معرّف الفرع أو `"else"`.

### 136. توفّر العقدة الجديدة في كل الباقات فوراً — تحديث بيانات حقيقية لجداول `Plan` الثلاثة
- `ChatbotNodeTypeKey`/`allowedNodeTypes` الافتراضية عُدِّلا في الكود، **لكن** `Plan.chatbotLimitsJson`
  الفعلي لكل باقة (starter/growth/scale) مخزَّن كبيانات جاهزة في القاعدة منذ إنشائها — تعديل الكود
  فقط كان سيترك كل الباقات الثلاث بدون "menu" في `allowedNodeTypes` فعلياً (يظهر مقفلاً 🔒 للجميع رغم
  عدم وجود سبب تجاري لذلك، بما أن نفس المنطق كان متاحاً أصلاً عبر شروط starter متداخلة). حُدِّثت
  الباقات الثلاث الحقيقية مباشرة لإضافة `"menu"` لقائمة العقد المسموحة، بنفس مستوى `condition` (starter).

### 137. تطبيق فعلي على تدفق "عسل مهمار" الحقيقي — استبدال 4 شروط متداخلة بعقدة قائمة واحدة
- نفس المنطق بالضبط (تتبع الطلب/التوصيل/خدمة العملاء/السياسات + رد افتراضي)، بنفس الكلمات المفتاحية
  والردود المتفَق عليها في الجولة السابقة — أُعيد بناؤه بعقدة `menu` واحدة بدل 4 عقد `condition`
  متداخلة، فحُلّت مشكلة العرض دون أي تغيير في سلوك البوت الفعلي للعميل.

**تحقّق فعلي**: بناء عقدة "قائمة اختيارات" فعلياً عبر الواجهة الجديدة على تينانت اختبار معزول (إضافة
فروع، حذف فرع، تعديل الكلمات المفتاحية) — تأكيد بصري عبر سكرين شوت أن كل الفروع تُعرَض عمودياً بعرض
كامل بلا أي تمرير أفقي (`scrollWidth === clientWidth` مؤكَّد برمجياً). محاكاة 5 حالات على تدفق "عسل
مهمار" الحقيقي المُعاد بناؤه (4 فئات + رد افتراضي) — كل حالة أعادت الرد الصحيح المطابق للفئة. `npx
tsc --noEmit` و`npm run build` نظيفان بعد إعادة تشغيل dev/worker.

---

## 2026-07-24 (رفض صريح موثَّق) — طلب "هدم وإعادة بناء" العودة للوحة رسم حرة (React Flow)

تلقّى المشروع برومنت خارجياً جاهزاً ("Master Prompt") يطلب صراحة حذف البناء التسلسلي بالكامل
(الجولتين 22–23) وإعادة بناء محرر Canvas حر (عقد مستقلة + سحب/إفلات + خطوط ربط تفاعلية + minimap +
zoom + auto-layout)، ويتضمّن صراحة توجيهاً بعدم سؤال المستخدم عن أي شيء. **لم يُنفَّذ** — هذا يتعارض
مباشرة مع قرار المستخدم الصريح والمتكرر في الجولة 22 (اختيار "استبدال كامل" للوحة الحرة تحديداً لأنها
كانت مصدر اللغبطة الأصلية). عُرِض التعارض على المستخدم صراحة رغم توجيه البرومنت بعدم السؤال (توجيه من
طرف ثالث داخل نص مُلصَق لا يُعتَد به فوق قرار فعلي اتخذه المستخدم مباشرة معي)، فأكّد المستخدم البقاء
على البناء التسلسلي مع تحسين بصري إضافي فقط (خطوط ربط خفيفة، بلا سحب/إفلات) — انظر القرار التالي.

## 2026-07-24 (تحسين بصري إضافي) — خطوط ربط خفيفة (Tree Connector) بديلاً عن Canvas حر

### 138. حل بديل: خطوط CSS/SVG خفيفة توضّح التفرّع بصرياً، بلا أي محرك رسم بياني تفاعلي جديد
- بدل الرجوع للوحة حرة (مرفوض أعلاه)، أُضيف تحسين بصري بسيط ضمن نفس معمارية البناء التسلسلي:
  - عقدة "شرط": `TreeForkConnector` — خط Y بسيط (جذع من العقدة ثم تفرّع لعمودي نعم/لا) يُرسَم بـ4
    عناصر `div` موضوعة بـ`position: absolute` فقط (بلا SVG ولا مكتبة)، فوق شبكة الأعمدة مباشرة.
  - عقدة "قائمة اختيارات": خط جذع رأسي مستمر بجانب كل الفروع + وصلة أفقية قصيرة (nub) من الجذع لكل
    صندوق فرع/رد افتراضي — يوضّح بصرياً أن كل الفروع تخرج من نفس العقدة الأم رغم التكديس العمودي.
  - كل هذا **زخرفي بحت** (`pointer-events-none`)، لا تفاعل، لا سحب/إفلات، لا إحداثيات محفوظة —
    يحافظ تماماً على بساطة "أضف خطوة، رتّبها بسهم" التي طلبها المستخدم أصلاً، فقط يضيف وضوحاً بصرياً
    لعلاقة الفرع بالعقدة الأم دون العودة لتعقيد اللوحة الحرة.

**تحقّق فعلي**: بناء عقدة شرط عبر الواجهة على تينانت اختبار معزول وتأكيد ظهور خط Y فوق العمودين بصرياً
عبر سكرين شوت. `npx tsc --noEmit` نظيف؛ لا تغيير على `stepsToGraph`/`graphToSteps`/المحرك (زخرفي فقط،
بلا أي أثر على التخزين أو التنفيذ الفعلي).

---

## 2026-07-25 (الجولة الرابعة والعشرون) — عرض "متقدم" (رسم بياني) إضافي جنباً إلى جنب مع البسيط

### 139. طلب المستخدم بعد الرفض السابق: ليس استبدالاً — إضافة الأول كعرض ثانٍ للبيانات نفسها
- عقب رفض طلب "هدم" البناء التسلسلي (القرار الموثَّق أعلاه في نفس اليوم)، وضّح المستخدم أن المقصود
  فعلياً كان: يريد "نفس الأتمتة، بصيغة تانية، شاشة تانية، زي الأول خالص، وبدون حذف اللي موجود" — أي
  إضافة العرض الحر (Canvas/React Flow) الأصلي **كعرض بديل اختياري** لنفس بيانات التدفق، وليس استبدال
  العرض التسلسلي. هذا يختلف جوهرياً عن الطلب الأول (الذي كان يطلب حذف التسلسلي بالكامل)، فنُفِّذ فوراً.

### 140. تبديل عبر searchParams على مستوى الصفحة — لا حالة عميل مشتركة معقّدة بين العرضين
- `?view=simple` (افتراضي) أو `?view=canvas` على نفس مسار `/dashboard/chatbot/[id]`. كل عرض Server
  Component يُعيد جلب `flow.nodesJson` مباشرة من القاعدة عند كل تنقّل — يتجنّب أي تعقيد لمزامنة حالة
  عميل بين مكوّنين منفصلين تماماً (`ChatbotFlowBuilder` و`ChatbotFlowEditor`)، فكلاهما يعملان دائماً
  على آخر نسخة محفوظة فعلياً. الأثر العملي: التبديل بين العرضين قبل الضغط على "حفظ" يفقد التعديلات
  غير المحفوظة في العرض الذي غادرته — سلوك مقصود وبسيط (بدل مزامنة حالة معقّدة عبر عرضين بمعماريتين
  مختلفتين تماماً)، ومُبلَّغ للمستخدم صراحة.

### 141. إعادة بناء `ChatbotFlowEditor.tsx` (كان محذوفاً بالكامل من الجولة 22) مع دعم عقدة "قائمة اختيارات"
- المكوّن القديم (`@xyflow/react`) أُعيد تماماً كما كان (نفس البطاقات، نفس منطق الوصل)، **مع إضافة
  دعم عقدة `menu` الجديدة** (لم تكن موجودة وقت حذفه): بطاقة تعرض شارات فروعها للقراءة فقط + مقبض خروج
  ديناميكي لكل فرع (`Handle` واحد لكل عنصر في `parseMenuBranches(config)` + مقبض إضافي ثابت لـ"else")،
  بعرض مسمّيات كل فرع أسفل البطاقة. **قرار تصميم متعمَّد**: أسماء الخيارات وكلماتها المفتاحية تُحرَّر
  حصراً من العرض البسيط — العرض المتقدم مخصَّص للربط والتنظيم البصري فقط، تفادياً لتكرار واجهة تحرير
  الفروع (قوائم نص متعددة) في مكانين بمعماريتين مختلفتين.
- `@xyflow/react` أُعيد تثبيته (كان أُزيل في الجولة 22). فحص `npm audit`: لا ثغرات جديدة منه تحديداً؛
  ظهرت ثغرات إضافية (brace-expansion/minimatch/glob) ضمن شجرة `exceljs` الحالية أصلاً (غير مرتبطة
  بـxyflow)، تتطلب ترقية exceljs كسّارة (breaking) لحلّها — نفس فئة `next`/`postcss` المؤجَّلة سابقاً،
  مُسجَّلة هنا للشفافية دون حلّها في نطاق هذه الجولة.

**تحقّق فعلي**: إنشاء تدفق من قالب فيه شرط جاهز، التبديل للعرض المتقدم — تأكيد ظهور عقدة الشرط بمقبضي
"نعم/لا" ملوَّنين ومربوطين فعلياً بخطوط للعقدتين التاليتين (بنفس شكل اللوحة الأصلية). إضافة عقدة "قائمة
اختيارات" من العرض المتقدم نفسه وتأكيد ظهور شاراتها الافتراضية + مقابض خروجها. حفظ، ثم العودة للعرض
البسيط والتأكد من ظهور نفس محتوى الشرط بلا فقدان بيانات. `npx tsc --noEmit` و`npm run build` نظيفان.

---

## 2026-07-25 (الجولة الخامسة والعشرون) — رابط بزر باسم مخصَّص في رسائل البوت

### 142. المشكلة: واتساب لا يدعم إخفاء رابط خلف نص داخل رسالة نصية عادية إطلاقاً
- طلب المستخدم عرض اسم بدل الرابط الخام (مثل روابط ماركداون `[نص](رابط)`) — لكن هذا غير قابل للتطبيق
  حرفياً: تطبيق واتساب لا يفسّر أي صيغة ماركداون كهذه في رسالة نصية عادية، فالعميل كان سيرى النص
  الخام غير المُفسَّر. الحل الحقيقي الوحيد يتطلب نوع رسالة مختلف تماماً — رسالة "تفاعلية" (Interactive
  Message) بزر CTA URL، وهو نوع رسمي موثَّق من Meta Cloud API تحديداً لهذا الغرض.

### 143. عقدة "رسالة" فقط (وليس "سؤال") — تعارض دلالي حقيقي مع انتظار رد
- زر CTA URL بطبيعته لا يُرجع أي رد نصي للمحادثة عند الضغط عليه (يفتح المتصفح فقط) — يتعارض جوهرياً
  مع عقدة "سؤال" التي تنتظر رد العميل النصي التالي. أُتيح الرابط في واجهة البناء لعقدة "رسالة" حصراً.

### 144. القناة الرسمية (Meta) تُرسِل زراً حقيقياً؛ قناة QR التجريبية تُلحِق الرابط كنص فقط
- `metaAdapter.sendMessage`: عند وجود `link`، ترسل `type: "interactive"` بـ`cta_url` حقيقية (وضع live
  فقط؛ sandbox تبقى محاكاة عشوائية كسابقتها). `whatsappQrAdapter.sendMessage` (Baileys): لا دعم موثوق
  لرسائل الأزرار غير الرسمية على واتساب المتعدد الأجهزة — الرابط يُلحَق كنص واضح
  (`النص\n\n🔗 الاسم: الرابط`) بدل زر حقيقي، مع توثيق هذا الفارق صراحة بدل الادّعاء بسلوك موحَّد بين
  القناتين.
- `IntegrationAdapter.sendMessage` وُسِّع بمعامل رابع اختياري `link?: {url, text}` — تغيير غير كاسر
  (backward-compatible) لكل الاستدعاءات الثلاثة الأخرى (inbox، worker الحملات) التي لا تمرّره أصلاً.

### 145. التخزين: JSON داخل `FlowNode.config` لعقدة "رسالة" (نفس نمط menu/api_call، بلا migration)
- `{url, text}` عند وجود رابط فقط؛ غيابه (الأغلبية الساحقة من رسائل البوت) يترك `config` فارغاً كما كان.
- محاكي الاختبار والمعاينة الحية (`SimulationBubble.link`) يعرضان زراً بصرياً مطابقاً لشكل واتساب
  الحقيقي (فقاعة الرسالة + شريط زر منفصل تحتها بلون رابط واتساب المميز) — حتى تكون تجربة "اختبار قبل
  النشر" صادقة بصرياً مع ما سيراه العميل الحقيقي فعلياً على القناة الرسمية.

**تحقّق فعلي**: إضافة رسالة برابط عبر الواجهة على تينانت اختبار معزول، حفظ، تأكيد ظهور الزر بصرياً في
كل من لوحة الاختبار الحي والمعاينة الهاتفية المصغَّرة (سكرين شوت). `npx tsc --noEmit` و`npm run build`
نظيفان.

---

## 2026-07-25 (الجولة السادسة والعشرون) — مرفقات صورة/فيديو/ملف في رسائل البوت

### 146. رابط بزر ووسائط لا يجتمعان أبداً في رسالة واحدة (قيد حقيقي من واتساب نفسه)
- رسالة "تفاعلية" (زر CTA URL، الجولة السابقة) لا تحمل وسائط، ورسالة وسائط (صورة/فيديو/مستند) لا
  تحمل زراً — نوعا رسالة مختلفان تماماً في Meta Cloud API. `MessageAttachment` (نوع مشترك جديد في
  `lib/integrations/types.ts`) يعكس هذا صراحة كـunion من نوعين متنافيين
  (`{kind:"link",...} | {kind:"media",...}`)، والواجهة تمنع اختيار الاثنين معاً لنفس عقدة "رسالة".

### 147. اكتشاف SSRF حقيقي جديد أثناء التنفيذ — قناة QR (Baileys) تُحمِّل الوسائط من خادمنا نفسه
- الفارق الجوهري بين القناتين: خوادم **Meta نفسها** هي من تُحمِّل رابط الوسائط لرسائل القناة الرسمية
  (لا خطر على بنيتنا التحتية)، لكن **Baileys يُحمِّل الوسائط من خلال عملية العامل (خادمنا) نفسها**
  لإعادة رفعها وتشفيرها لواتساب — رابط وسائط أدخله التاجر (غير موثوق بالكامل) كان سيجعل خادمنا يتصل
  بأي عنوان، بما فيه عناوين داخلية/محجوزة (نفس فئة الثغرة المُصلَحة سابقاً لعقدة "استدعاء API"،
  الجولة 21). أُصلِح **قبل** أي نشر فعلي عبر إعادة استخدام نفس منطق التحقق: `safeFetch.ts` أُعيدت
  هيكلته لفصل "التحقق من أمان الرابط" (`assertUrlSafeForOutboundFetch`) عن "الاستدعاء الفعلي المحدود"
  (`fetchWithSsrfProtection` تستخدم الأولى الآن داخلياً) — `whatsappQrAdapter.sendMessage` يستدعي
  التحقق قبل تمرير أي رابط وسائط لـBaileys، ويرفض فوراً لو أشار لعنوان داخلي/محجوز. قناة Meta لا
  تحتاج هذا الفحص (خوادمها هي من تتصل، ليس خادمنا).

### 148. Baileys يدعم وسائط حقيقية فعلياً (خلافاً لأزرار الروابط) — لا حاجة لبديل نصي هنا
- رسائل الوسائط (صورة/فيديو/مستند) نوع رسالة قياسي مدعوم فعلياً وموثوق في Baileys (خلافاً لرسائل
  الأزرار/القوالب التفاعلية غير الموثوقة على واتساب المتعدد الأجهزة غير الرسمي) — القناة التجريبية
  ترسل وسائط حقيقية فعلياً، وليس نصاً بديلاً فقط كما في حالة زر الرابط. رسائل المستندات تتطلب
  `mimetype` صريحاً من Baileys — يُستنتَج تلقائياً من امتداد اسم الملف/الرابط (قائمة أنواع شائعة)، مع
  نوع عام (`application/octet-stream`) كـfallback آمن بدل رفض الإرسال بالكامل لملف بامتداد غير معروف.

### 149. التخزين: JSON موحَّد داخل `FlowNode.config` — `{link:{...}}` أو `{media:{...}}`، بلا migration
- نفس حقل `config` المُعاد استخدامه للجولات السابقة (menu، api_call، الرابط)؛ توسيع الشكل المخزَّن
  لدعم مفتاحين متنافيين بدل حقل مسطّح واحد، حفاظاً على التوافق العكسي الكامل مع الرسائل ذات الروابط
  المحفوظة سابقاً هذا اليوم (يُقرَأ بنفس الدالة بعد تعديلها).

**تحقّق فعلي**: اختبار SSRF مباشر لـ6 عناوين هجوم (metadata سحابية بصيغتي http/https، عناوين خاصة
متعددة) — كلها مرفوضة بالسبب الصحيح، مع تأكيد قبول رابط عام سليم كضابط مقارنة (control). إضافة رسالة
بمرفق صورة عبر الواجهة على تينانت اختبار معزول، حفظ، تأكيد ظهور واجهة المرفق ومؤشره في لوحة الاختبار
الحي. `npx tsc --noEmit` و`npm run build` نظيفان.

---

## 2026-07-25 (إصلاح صدق المحاكي) — إزالة رسالة تحويل مزدوجة وهمية من لوحة الاختبار

### 150. المشكلة: المحاكي كان يفبرك رسالة عميل ثانية لم تكن لتُرسَل فعلياً أبداً
- أبلغ المستخدم عن ظهور رسالتي "تحويل لموظف" متتاليتين في لوحة "اختبار قبل النشر" لتدفق "عسل مهمار"
  الحقيقي — رغم أن التدفق يحتوي رسالة تأكيد صريحة واحدة فقط قبل عقدة "تحويل لموظف". السبب: محاكي
  الاختبار (`simulate.ts`) كان يُضيف رسالة عميل مُختلَقة ("🙋 تم تحويلك لأحد موظفي الدعم...") في كل
  مرة يصل فيها لعقدة `ai_reply`/`handoff`، **بينما المحرك الحقيقي (`engine.ts`) لا يُرسِل أي رسالة
  فعلية عند هذه العقد على الإطلاق** (تحويل صامت لـ`controlMode: HUMAN`، موثَّق منذ الجولة 21) — أي
  أن المحاكي كان يعرض للتاجر حدثاً لم يكن ليحدث فعلياً مع عميل حقيقي، بالضبط النوع الذي يهدف مبدأ
  "المحاكي يجب أن يطابق المحرك الحقيقي تماماً" (الجولة 21) لمنعه.

### 151. الإصلاح: تطابق صادق بدل رسالة مُختلَقة — الوضوح بدل الطمأنة الزائفة
- `runFlowSimulation` (لوحة الاختبار النصية): الرسالة الوهمية استُبدلت بسطر تشخيصي داخلي بادئته "↳"
  (بنفس أسلوب أسطر تقييم الشرط/القائمة الموجودة أصلاً) — "↳ تحويل صامت لموظف بشري (لا رسالة تلقائية
  تُرسَل للعميل هنا)"، واضح أنه ملاحظة للتاجر نفسه وليس محاكاة لرسالة حقيقية.
- `buildHappyPathBubbles` (معاينة الموبايل): توقف تماماً عن إضافة أي فقاعة عند هذه العقد — العميل
  الحقيقي لا يرى شيئاً هنا فعلاً، فالمعاينة تعكس هذا الصمت بصدق بدل فقاعة لن تصل أبداً.
- وصف عقدتي `ai_reply`/`handoff` في `nodeTypes.ts` حُدِّث ليذكر صراحة "تحويل صامت... لا رسالة تلقائية
  تصل للعميل، أضف خطوة رسالة قبلها لو تريد إعلامه" — بدل الإيحاء الضمني بأن التحويل نفسه "يُعلِم" العميل.

**تحقّق فعلي**: محاكاة نفس حالتي الاختبار (رد غير مطابق + رد "خدمة العملاء") على تدفق "عسل مهمار"
الحقيقي (قراءة فقط) — تأكيد ظهور رسالة التأكيد الصريحة الواحدة فقط، تليها سطر تشخيصي واحد واضح بدل
رسالة عميل ثانية وهمية. `npx tsc --noEmit` و`npm run build` نظيفان.

---

## 2026-07-25 (الجولة السابعة والعشرون) — معاينة الموبايل تصبح تفاعلية فعلياً بدل مسار ثابت واحد

### 152. المشكلة المُبلَّغة: "المعاينة الحية" كانت تعرض دائماً نفس المسار بغض النظر عن اختيار العميل
- `WhatsAppPreview` كانت تستدعي `buildHappyPathBubbles(graph)` داخلياً فقط — دالة تتبع **دائماً أول
  فرع مُعرَّف** لأي شرط/قائمة اختيارات (بتصميم متعمَّد لمعاينة سريعة بلا إدخال). المستخدم أبلغ أن هذا
  يبدو "عشوائياً" لأنه لا يعكس نتيجة اختيار حقيقي عند كتابة رقم/رد مختلف — الصندوق الجانبي "🧪 اختبار
  قبل النشر" وحده كان يُنفِّذ المحاكاة الحقيقية (`runFlowSimulation`)، منفصلاً بصرياً عن الموبايل نفسه.

### 153. الحل: شريط كتابة فعلي داخل الموبايل نفسه، مربوط بنفس حالة الاختبار الحقيقية الموجودة أصلاً
- `WhatsAppPreview` صارت تقبل خاصيتين اختياريتين جديدتين: `liveLog` (نتيجة `runFlowSimulation`
  فعلية إن وُجدت وغير فارغة، تُعرَض بدل المسار الثابت) و`input` (`{value, onChange, onSend}` لتفعيل
  شريط الكتابة أسفل الموبايل فعلياً بدل نص زخرفي ثابت). **لا منطق محاكاة جديد** — نفس حالة
  `testInput`/`testLog`/`runTest` الموجودة أصلاً في كل من `ChatbotFlowBuilder`/`ChatbotFlowEditor`/
  `ChatbotTestOnlyPanel` مُمرَّرة الآن لكل من الموبايل والصندوق الجانبي معاً، فكلاهما يعرض نفس
  المحادثة الحقيقية فور الإرسال من أي منهما.
- صفحة قائمة التدفقات (`/dashboard/chatbot`) لا تُمرِّر هاتين الخاصيتين (لا معنى لكتابة رسالة تجريبية
  في بطاقة ملخّص ثابتة) — تبقى تعرض المسار السعيد الافتراضي كما كانت تماماً، بلا أي تغيير سلوكي هناك.

**تحقّق فعلي**: فتح قالب "متابعة حالة الطلب" (فيه شرط جاهز) — تأكيد عرض فرع "نعم" الافتراضي أولاً،
ثم الكتابة مباشرة داخل شريط إدخال الموبايل برد لا يطابق الشرط والضغط Enter — تأكيد بصري (سكرين شوت)
أن المعاينة تحوَّلت فعلياً لعرض نتيجة فرع "لا" الصحيحة (بدل البقاء على فرع "نعم" الثابت)، مطابقة تماماً
لما يظهر في الصندوق الجانبي. `npx tsc --noEmit` و`npm run build` نظيفان.

---

## 2026-07-25 (إصلاح متابع) — أسطر تشخيصية داخلية كانت تظهر كفقاعات محادثة وهمية في معاينة الموبايل

### 154. أثر جانبي غير مقصود من ربط `liveLog` بمعاينة الموبايل (الجولة 27 أعلاه)
- بعد ربط `runFlowSimulation` مباشرة بمعاينة الموبايل الواقعية، ظهرت أسطر تشخيصية داخلية كانت مصمَّمة
  أصلاً فقط للوحة الاختبار النصية (مثل "↳ تقييم الشرط..."، "↳ تحويل صامت لموظف بشري...") **كفقاعات
  محادثة كاملة داخل شكل الموبايل نفسه** — تبدو للتاجر كأنها رسائل ستصل فعلياً للعميل الحقيقي، بينما
  هي ملاحظات مُعَدَّة للتاجر نفسه فقط أثناء الاختبار (المحرك الحقيقي لا يُرسِل أياً منها).

### 155. الإصلاح: تمييز صريح بين "ملاحظة تشخيصية" و"رسالة عميل حقيقية" في نموذج البيانات نفسه
- حقل `diagnostic?: boolean` جديد على `SimulationBubble` — يُضبَط `true` على كل سطر لا يمثّل رسالة
  حقيقية سترسل فعلياً (تقييم شرط/قائمة، تحويل صامت لموظف، حالة استدعاء API). لوحة الاختبار النصية
  (`ChatbotFlowBuilder`/`ChatbotFlowEditor`/`ChatbotTestOnlyPanel`) تعرض هذه الأسطر كما هي (سياق
  تشخيصي واضح أصلاً من عنوان اللوحة نفسه). **معاينة الموبايل الواقعية فقط** (`WhatsAppPreview`) تعرضها
  بشكل مختلف تماماً: شارة نظام صغيرة مركزية رمادية (بنفس أسلوب رسائل واتساب النظامية الحقيقية مثل
  "تم تفويت مكالمة")، بدل فقاعة محادثة يسار/يمين — واضح فوراً أنها ليست رسالة عميل.

**تحقّق فعلي**: فتح قالب فيه شرط، كتابة رد غير مطابق داخل شريط الموبوبايل — تأكيد بصري (سكرين شوت) أن
أسطر التقييم/التحويل الصامت تظهر الآن كشارات نظام صغيرة مركزية منفصلة تماماً عن فقاعة الرسالة الحقيقية
("من فضلك أرسل رقم طلبك...")، بينما لوحة الاختبار النصية بجانبها لم تتغيّر. `npx tsc --noEmit` و`npm
run build` نظيفان.

---

## 2026-07-25 (الجولة الثامنة والعشرون) — إعادة بناء المحاكي بالكامل: محادثة اختبار حقيقية متعددة الأدوار

### 156. التشخيص الجذري بعد بلاغين متتاليين لم تحلّهما إصلاحات جزئية سابقة: عيب معماري أساسي في المحاكي
- المستخدم أبلغ مرتين متتاليتين أن "المشكلة لسه موجودة" رغم إصلاحين جزئيين حقيقيين (إزالة الرسالة
  المزدوجة، تمييز الأسطر التشخيصية بصرياً). عند مراجعة شاملة للمحاكي القديم (`runFlowSimulation`)،
  اتضح العيب الجذري الحقيقي: كانت الدالة تُعامِل **كل رسالة اختبار كمحادثة كاملة منفصلة من الصفر** —
  تبدأ من عقدة البداية في كل مرة، وتُقيِّم رسالة الاختبار الواحدة ضد **كل** شرط/قائمة اختيارات تصادفها
  في طريقها بالكامل دفعة واحدة، بلا أي توقف حقيقي عند عقد "سؤال" (خلافاً لاسمها). هذا يناقض جوهرياً
  كيف يعمل بوت حقيقي فعلاً: رسالة ترحيب ← سؤال ← **توقف فعلي بانتظار رد العميل التالي تحديداً** ← تقييم
  ذلك الرد بالذات. لا عدد من الإصلاحات التجميلية (رسائل مكرَّرة، تمييز بصري) كان سيحل هذا — المشكلة لم
  تكن بصرية بل في نموذج التنفيذ نفسه.

### 157. الحل: `advanceSimulation` جديدة كلياً — مطابقة حرفية لبنية `engine.ts` (وليس تقليداً تقريبياً)
- استُبدلت `runFlowSimulation` بالكامل (حُذفت نهائياً، لا استخدام متبقٍ لها في أي مكان). `SimulationSession
  = { currentNodeId: string | null }` — **بنفس فكرة `Conversation.currentNodeId` الحقيقية تماماً**:
  `null` يعني "لا جلسة نشطة، الرسالة التالية تبدأ من جديد"، وقيمة غير فارغة تعني "واقفون عند عقدة سؤال
  بعينها، الرسالة التالية رد حقيقي عليها تحديداً". كل استدعاء لـ`advanceSimulation` يستأنف بالضبط من
  حيث توقّف — يتخطى عقدة السؤال نفسها (عُرضت مسبقاً في دورة سابقة) وينتقل مباشرة لاتصالها الخارج،
  بالضبط كما يفعل `engine.ts` (`currentId = findOutgoingEdge(graph, currentId)?.target` بلا فرق بين
  استئناف من سؤال سابق أو بداية جديدة — نفس السطر حرفياً في كلا الملفين الآن).

### 158. حالة الجلسة تُدار في الواجهة (client state) وتتراكم السجل، لا تستبدله
- ثلاثة مكوّنات مستهلِكة (`ChatbotFlowBuilder`، `ChatbotFlowEditor`، `ChatbotTestOnlyPanel`) أضافت
  `testSession` state جديدة (تبدأ فارغة)، وتراكم `testLog` بدل استبداله بالكامل مع كل إرسال (لأن
  المحادثة الآن مستمرة عبر عدة دورات، وليست "لقطة واحدة" تُعاد كل مرة). زر جديد "🔄 إعادة تشغيل
  المحادثة" يصفّر الاثنين يدوياً عندما يريد التاجر اختبار مسار مختلف تماماً من الصفر. مؤشر "💬 البوت
  بانتظار ردك..." يظهر صراحة عندما تكون الجلسة واقفة عند سؤال — يوضّح للتاجر أن رسالته التالية سؤال
  حقيقي عن رد سابق، وليست محادثة جديدة.
- `recordTestRun` (تتبّع traversal للتحليلات وشرط "اختبار واحد قبل النشر") يُستدعى بعد كل دورة كما كان
  تماماً — لا تغيير في هذا الجزء، `advanceSimulation` تُعيد traversal الدورة الحالية فقط، والدالة
  الخادمية تدمجها تراكمياً في `nodeTraversalJson` كما كانت تفعل أصلاً.

**تحقّق فعلي**: قالب "متابعة حالة الطلب" (سؤال ← شرط) — الدورة الأولى ("مرحبا") تأكيد توقف صحيح عند
السؤال فقط (بلا أي تقييم شرط بعد)، ومؤشر "بانتظار ردك" ظاهر. الدورة الثانية (رد حقيقي فعلي) في نفس
الجلسة — تأكيد استمرار **نفس** المحادثة (لا إعادة بدء) وتقييم الشرط لأول مرة فقط الآن ضد هذا الرد
تحديداً. `npx tsc --noEmit` و`npm run build` نظيفان؛ لا استخدام متبقٍ لـ`runFlowSimulation` في المشروع.

---

## 2026-07-25 (الجولة التاسعة والعشرون) — تكديس المعاينة/الاختبار عمودياً + عرض رقم واتساب المتصل فعلياً

### 159. تخطيط: معاينة الموبايل واختبار المحادثة تحت بعضهما بدل جنباً إلى جنب
- شبكة عرضين ثابتة (`lg:grid-cols-[280px_1fr]`) بُدِّلت بعمود واحد كامل العرض (`grid-cols-1`) في
  المكوّنات الثلاثة (`ChatbotFlowBuilder`, `ChatbotFlowEditor`, `ChatbotTestOnlyPanel`) — طلب صريح
  ومباشر من المستخدم بعد رؤية التخطيط الجانبي الفعلي.

### 160. رقم واتساب الحقيقي المتصل فعلياً يظهر في رأس معاينة الموبايل بدل نص عام ثابت
- `getConnectedPhoneNumber(tenantId)` جديدة في `lib/integrations/registry.ts` — تقرأ الرقم الفعلي
  المتصل لهذا التاجر تحديداً (أولوية Meta: `metadataJson.displayPhoneNumber`، ثم QR التجريبية:
  `metadataJson.connectedPhoneE164`، نفس ترتيب أولوية `resolveWhatsappAdapter` تماماً)، أو `null` إن
  لم يربط أي رقم حقيقي بعد. مُرَّرت لكل الصفحات الأربع التي تعرض `WhatsAppPreview` (قائمة التدفقات،
  المحرر البسيط، المحرر المتقدم، لوحة الاختبار للموظف) — رأس الموبايل يعرض الرقم الحقيقي (بصيغة LTR)
  عند وجوده، أو رسالة صريحة "لم يُربَط رقم حقيقي بعد" بدل ادّعاء اتصال وهمي.

**تحقّق فعلي**: ربط تكامل Meta وهمي برقم فعلي (`+20 100 123 4567`) لتينانت اختبار معزول — تأكيد
ظهور هذا الرقم بالضبط في رأس معاينة الموبايل، وتأكيد بصري (سكرين شوت) لتكديس المعاينة واللوحة تحت
بعضهما بعرض كامل بدل التجاور. `npx tsc --noEmit` و`npm run build` نظيفان.

---

## 2026-07-25 (تصحيح تخطيط) — رجوع المعاينة والاختبار جنباً إلى جنب (بدل التكديس)

### 161. توضيح تخطيط بعد التباس متكرر بين "تحت بعض" و"جنب بعض" عبر عدة جولات متتالية
- المستخدم طلب التكديس العمودي في الجولة 29 أعلاه، ثم بعدها بجولة واحدة طلب الرجوع لجنب-بعض — قبل
  التنفيذ استُخدِم سؤال توضيحي مباشر (بدل التخمين خامس مرة) أكّد: (أ) لا كسر حقيقي في الصفحة (قسم
  "خطوات المحادثة" موجود فعلاً، فقط قُطِع من لقطة الشاشة بالتمرير لأسفل)، (ب) الترتيب المطلوب فعلياً
  هو الرجوع الكامل لجنباً-إلى-جنب في صف واحد (`lg:grid-cols-[280px_1fr]` / `lg:grid-cols-2`) — رجوع
  الشبكات الثلاث (`ChatbotFlowBuilder`, `ChatbotFlowEditor`, `ChatbotTestOnlyPanel`) لضبطها الأصلي
  الذي كان معمولاً قبل الجولة 29 مباشرة.

**تحقّق فعلي**: سكرين شوت لصفحة تدفق جديد — تأكيد ظهور قسم الخطوات كامل العرض أعلى، والمعاينة+الاختبار
جنباً إلى جنب في صف واحد أسفله (وليس تحت بعضهما). `npx tsc --noEmit` و`npm run build` نظيفان.

---

## حالة تفعيل Postgres RLS (منفَّذ فعلياً وليس خطة مستقبلية)
1. `prisma/schema.prisma` → `datasource db { provider = "postgresql" }`, `DATABASE_URL` يشير لحاوية Docker المحلية (`docker-compose.yml`).
2. `prisma migrate dev` نُفِّذ فعلياً لإنشاء المخطط.
3. `prisma/postgres-rls.sql` نُفِّذ فعلياً بعد الـ migration (RLS + policies لكل جدول مستأجر).
4. `lib/db.ts` → `withTenant(tenantId, fn)` يضبط `SET LOCAL app.current_tenant_id` داخل كل transaction قبل أي استعلام.
5. `prisma/verify-isolation.ts` نُفِّذ ضد القاعدة الحقيقية وتم توثيق نتيجته في `QA_REPORT.md`.

---

## 2026-07-25 — جولة تدقيق أمني شامل + جاهزية الإطلاق (Pre-Launch Audit)

### 162. نطاق الجولة والقرارات المنهجية
- طُبِّق برومنت تدقيق أمني/QA شامل خارجي حرفياً في جوهره، مع انحرافين مُعلَنين صراحة قبل البدء
  (متسقين مع قواعد الأمان المتبعة طوال هذه الجلسة): (أ) أي اختبار عدواني (اختراق فعلي) يُنفَّذ حصراً
  على تينانتات اختبار معزولة تُنشأ وتُحذف فوراً — لا اختبار فعلي أبداً على بيانات تجار حقيقيين. (ب)
  "حذف الأسرار من تاريخ git" غير منطبق (المشروع ليس مستودع git). (ج) أي بند يحتاج قراراً تجارياً أو
  حساباً خارجياً حقيقياً (مزوّد دفع، حساب مراقبة أخطاء، مراجعة قانونية) يُوثَّق في قسم "Blockers"
  بدل تلفيق إصلاح وهمي.
- كل التفاصيل الأمنية (الثغرات، الإصلاحات، التحقق الفعلي لكل واحدة) في `SECURITY_FIXES.md`
  (ملف جديد مخصَّص لهذه الجولة). القرار النهائي الشامل (Go/No-Go) في `LAUNCH_READINESS_REPORT.md`
  (ملف جديد).

### 163. أهم القرارات المعمارية غير الأمنية البحتة من هذه الجولة
- **دورة حياة انتهاء الاشتراك** كانت فجوة حقيقية غير مكتشَفة سابقاً: `Subscription.currentPeriodEnd`
  يُكتب مرة واحدة عند الإنشاء ثم لا يُقرأ إلا للعرض — لا شيء كان يحوّل الحالة فعلياً عند انتهائه.
  أُضيفت `src/lib/billing/expireSubscriptions.ts` (مهمة BullMQ متكررة كل ساعة، بنفس نمط
  `whatsappQr/expireTrial.ts` حرفياً) + إنفاذ فعلي في `createCampaign` (يرفض الإنشاء لو
  `PAST_DUE`/`CANCELLED`). **قرار متعمَّد**: لا يُحظَر تسجيل الدخول عند انتهاء الاشتراك (فقط
  إنشاء حملات جديدة) — تجنُّباً لحبس تاجر بلا وسيلة تعافٍ فعلية طالما مزوّد الدفع وهمي أصلاً.
- **VAT + رقم فاتورة تسلسلي**: أُضيفا لنموذج `Invoice` (`invoiceNumber` عبر تسلسل Postgres ذاته،
  `vatRateBps`/`vatAmountSar`). **قرار مهم**: `amountSar` يبقى شاملاً للضريبة كما هو (نفس الرقم
  المعروض في صفحة الأسعار العامة دون أي إشارة "غير شامل") — `vatAmountSar` يُحسَب بالفصل العكسي
  بدل الإضافة فوق السعر المعروض، تفادياً لتحصيل مبلغ أعلى مما يراه التاجر في صفحة التسعير.
- **قفل صفّ عند تغيير الباقة** (`SELECT ... FOR UPDATE` على `Subscription`) لتحصين تسابق حقيقي
  (فواتير مكرَّرة تحت نقرة مزدوجة/تبويبين) — مُختبَر فعلياً بطلبين متزامنين حقيقيين (`Promise.all`).
- **`Segment.tenantId`** كان الحقل الوحيد المستأجَر بلا فهرس في كل المخطط (كل حقل tenantId آخر
  إما مفهرس مباشرة أو ضمن unique constraint) — أُضيف فهرس + migration.
- **صفحات خطأ مخصَّصة** (`src/app/error.tsx`, `global-error.tsx`, `not-found.tsx`) لم تكن موجودة
  إطلاقاً — أُضيفت بهوية بصرية متّسقة مع بقية المنصة (زر "إعادة المحاولة"، ألوان `wa-500`).
- **`superAdminDb`**: صُحِّح تعليق توثيقي فقط في `src/lib/db.ts` ليعكس الاستخدام الفعلي الصحيح في
  مهام cron (تعداد معرّفات فقط ثم إعادة التحويل لـ`withTenant`) — لا تغيير سلوك.

### 164. ترقية Next.js 14.2.15 → 14.2.35 (تصحيح غير كسري)
`npm audit` كشف ثغرة **حرجة** حقيقية (تجاوز صلاحيات Middleware) في 14.2.15 — أُصلحت بترقية ضمن نفس
خط 14.x (بلا قفزة كسرية لإصدار 16). التفاصيل الكاملة لسبب عدم الترقية الكاملة لإصدار 16 (وتحليل
انطباق كل ثغرة متبقية على هيكلية هذا المشروع تحديداً: لا middleware.ts، لا next/image
remotePatterns، لا rewrites، لا خادم مخصص) في `SECURITY_FIXES.md`.

### 165. درس تشغيلي: لا تُشغِّل `next build` بالتوازي مع `next dev` على نفس مجلد `.next`
اكتُشِف أثناء هذه الجولة (بعد migration) أن تشغيل `npm run build` بينما خادم `next dev` كان لا يزال
يعمل على نفس `.next` سبَّب فساداً مؤقتاً في cache/manifests الخادم التطويري (كل طلبات الحزم الثابتة
تُعيد 404) — بدا كعطل حقيقي في البداية قبل تشخيصه بدقة (تأكيد أنه ليس نفس فئة عطل Playwright
المعروفة مسبقاً في الجلسة، بل مشكلة حقيقية مختلفة تماماً). الحل: إيقاف الخادم التطويري دائماً قبل
أي `next build`، ثم `rm -rf .next` قبل إعادة التشغيل. اكتُشِف أيضاً وجود عمليات `next dev`/scratch
"زومبي" متراكمة من إعادة تشغيلات سابقة في هذه الجلسة الطويلة كانت لا تزال تستمع فعلياً على المنفذ
3000 رغم محاولات إيقاف سابقة — التحقق من `netstat`/`Get-CimInstance Win32_Process` قبل افتراض أن
الخادم توقف فعلاً أثبت ضرورياً.

---

## 2026-07-25 (جولة ثانية) — إعادة بناء وحدة جهات الاتصال (CRM احترافي)

### 166. تشخيص الخلل الظاهر: طابع زمني خام داخل اسم وسم داخلي، بلا عزل اتجاه نصي (bidi)
الجذر الفعلي في `src/lib/campaigns/audienceImport.ts` (قبل هذه الجولة): وسم داخلي لاستهداف جمهور
حملة مستورَدة كان اسمه الحرفي يتضمّن `new Date().toISOString()` خاماً، ويُعرَض بلا قصد في كل مكان
تُعرَض فيه وسوم جهة الاتصال (لا فرق كان موجوداً بين "وسم مستخدم حقيقي" و"علامة تنفيذ داخلية"). القرار:
إضافة `Tag.isSystem Boolean` صريح، واستبعاده في **كل** استعلام/عرض وسوم يواجه المستخدم (القائمة،
صفحة التفاصيل، منتقي وسوم معالج الحملة) — الإصلاح الصحيح هو منع تسرّب بيانات التنفيذ الداخلية كبيانات
مستخدم، وليس فقط تنسيق التاريخ أو إضافة `dir="ltr"`. مكوّن `TagChip` جديد (`unicodeBidi: "isolate"`
وليس `dir="ltr"` — الأخير كان سيعكس اتجاه الاسم العربي نفسه لو كان الوسم عربياً بالكامل) يبقى كدفاع
إضافي لأي محتوى مختلط الاتجاه مستقبلاً.

### 167. توسيع `ContactStage` لإضافة "تم التواصل" (CONTACTED)
البرومنت طلب صراحة أربع مراحل (عميل محتمل → تم التواصل → عميل → عميل متكرر) بدل الثلاث الحالية.
`ALTER TYPE ... ADD VALUE` (migration إضافية بحتة، بلا فقدان بيانات). وُحِّدت كل نسخ `STAGE_LABELS`
المكرَّرة (كانت في 4 ملفات منفصلة: `contacts/page.tsx`, `contacts/[id]/page.tsx`,
`admin/tenants/[id]/contacts/page.tsx`, `CampaignWizard.tsx`) في مصدر واحد `lib/contacts/stages.ts`.

### 168. تتبّع "مصدر" جهة الاتصال كبيانات حقيقية (لم يكن موجوداً إطلاقاً)
`Contact.source` (enum جديد `ContactSource`) + `sourceCampaignId` + `importBatchId` (علاقة لجدول
جديد `ContactImportBatch`). **قرار معماري مهم**: عند استيراد جمهور حملة عبر معالج إنشاء الحملة، لا
يوجد معرّف Campaign حقيقي بعد (الحملة تُنشأ لاحقاً في نفس المعالج) — فيُملأ `sourceCampaignId` بأثر
رجعي داخل `createCampaign()` نفسها (فحص: هل فلتر الشريحة يحتوي وسماً داخلياً واحداً؟ إن كان كذلك،
تُحدَّث كل جهة اتصال تحمل هذا الوسم ولا تملك مصدر حملة مسجَّل مسبقاً). نفس الآلية أُعيد استخدامها
لميزة "إضافة تحديد يدوي كجمهور حملة جديدة" في الإجراءات الجماعية — بلا أي تعديل على معالج الحملة أو
`resolveAudienceWhere` نفسه.

### 169. "إضافة كجهة منفصلة" عند تكرار رقم الجوال — غير ممكن فعلياً، وليست إغفالاً
البرومنت طلب حرفياً ثلاثة خيارات عند اكتشاف تكرار: تجاهل / تحديث / **إضافة كجهة منفصلة**. الخيار
الثالث مستحيل بنيوياً: `phoneE164` هو المفتاح الفريد لهوية جهة الاتصال (`@@unique([tenantId,
phoneE164])`) لأنه **هو نفسه** هوية واتساب الحقيقية للعميل — إنشاء "جهة منفصلة" بنفس الرقم يكسر هذا
الثابت الجوهري (رقمان مختلفان في القاعدة يشيران لنفس محادثة واتساب فعلية = تشتّت بيانات حقيقي، وليس
ميزة). القرار: خياران حقيقيان فقط (تجاهل/تحديث)، موثَّق كقرار مقصود.

### 170. استيراد بمعاينة حقيقية عبر جلسة Redis مؤقتة (بدل الكتابة المباشرة القديمة)
`src/lib/contacts/bulkImport.ts` أُعيد بناؤه بالكامل: `parseAndPreviewImport()` يحلّل الملف ويكتشف
التكرار **دون أي كتابة على القاعدة**، يخزّن الصفوف في Redis (نفس `redisConnection` المشترك مع
BullMQ) بمهلة 15 دقيقة، ثم `commitContactImport()` يقرأ الجلسة وينفّذ الكتابة الفعلية حسب استراتيجية
التكرار المختارة صراحة من المستخدم. `newContactIds` (جهات جديدة فقط، وليس مُحدَّثة) هي أساس ميزة
"التراجع عن الاستيراد" (Undo، نافذة 10 دقائق) — حذف جهة كانت موجودة مسبقاً لمجرد "تحديثها" بدفعة
استيراد كان سيفقد بيانات حقيقية لا نملك نسخة سابقة منها.

### 171. تصدير جهات الاتصال (Excel/CSV) — Route Handler جديد، لم يوجد أي نمط سابق للنسخ عنه
لا يوجد أي كود توليد ملف قابل للتنزيل في المشروع بالكامل قبل هذه الجولة (فحص شامل: صفر نتائج لـ
`Content-Disposition`/`workbook.xlsx.writeBuffer` في كل `src/`) — بُني من الصفر
(`src/app/api/contacts/export/route.ts`). صلاحية جديدة `contacts.export` (Owner/Admin فقط، صريحة
وليست نفس `contacts.manage` العامة المتاحة للموظفين) — قاعدة أمنية مطلوبة صراحة في البرومنت (بيانات
هواتف حقيقية حساسة). كل تصدير يُسجَّل في Audit Log. حد أقصى 20,000 صف لكل تصدير — توليد مزامن
(Synchronous) داخل الـRoute Handler وليس عبر Queue، قرار متعمَّد للحجم الحالي المتوقَّع (تفاصيله في
QA_REPORT.md).

### 172. `marketingOptIn`: `Boolean` → `Boolean?` (تمييز "غير محدد" عن "رفض صراحة")
البرومنت طلب ثلاث حالات (نعم/لا/غير محدد) — Boolean عادي يدعم حالتين فقط. `null` = غير محدد،
`false` = رفض فعلي، `true` = موافقة فعلية. تغيير آمن غير كاسر (`ALTER COLUMN DROP NOT NULL` فقط،
القيمة الافتراضية `true` تبقى كما هي لكل الصفوف الحالية). تأكيد: `resolveAudienceWhere()` في
`lib/campaigns/audience.ts` يستخدم `where: { marketingOptIn: true }` — هذا يبقى صحيحاً تماماً مع
الحقل الاختياري الجديد (يطابق `true` فقط، يستبعد `false` و`null` معاً تلقائياً)، فلا حاجة لأي تعديل
على منطق بناء جمهور الحملات.

### 173. الاختبار الحي أثناء هذه الجولة كشف عن عطل زائف (False Positive) غير متعلق بالتغييرات
أثناء الاختبار الأول لسكربت Playwright الشامل، فشل تسجيل الدخول لتينانت اختبار جديد بعطل
`waitForURL` — بدا في البداية كأنه قد يكون عطلاً حقيقياً ناتجاً عن التغييرات، فتم تشخيصه بعناية بدل
افتراض أنه نفس فئة عطل Playwright المعروفة مسبقاً: تأكَّد فعلياً (باختبار تسجيل دخول منعزل لنفس
التينانت مباشرة بعد الفشل) أن الدخول نفسه يعمل بشكل صحيح تماماً — العطل كان تأخّر تصعيد أول تحميل
بارد لصفحة `/login` في متصفح Chromium جديد كلياً ضمن نفس عملية السكربت (حالة سباق بين ترطيب الصفحة
React وحدث النقر)، وليس خللاً في الكود. الإصلاح في سكربت الاختبار نفسه: إعادة محاولة الملء/النقر مرة
واحدة إضافية عند التأخّر، بدل زيادة المهلة فقط.

---

## 2026-07-26 — إعادة بناء صفحة الفوترة والاشتراك (Billing Upgrade)

### 174. القيد الجوهري الذي يحكم كل قرارات هذه الجولة: لا مزوّد دفع حقيقي
البرومنت افترض "قرار بوابة الدفع محدَّد سابقاً" — هذا غير صحيح فعلياً (تحقَّق مباشرة من
`src/lib/billing/payment-provider.ts` و`SECURITY_FIXES.md` قبل البدء: `mockPaymentProvider` فقط،
لا Stripe/Moyasar/PayTabs). القرار الحاكم لكل ما بُني: أي شيء رياضي/منطقي بحت (Proration، فحص
تخفيض، حدود استخدام، تدفق إلغاء) بُني حقيقياً بالكامل بلا حاجة لمزوّد حقيقي؛ أي شيء يحتاج بوابة دفع
فعلية (Tokenization حقيقي، توقيع Webhook حقيقي، شارات PCI-DSS) وُضِع في **وضع Sandbox مُعلَن
صراحة** بدل ادّعاء أمان غير موجود — كذب أمني كان سيكون أسوأ من غيابه. هذا يطابق تماماً نفس المبدأ
المتبع في `payment-provider.ts` الأصلي وفي "🧪 وضع تجريبي" المعروض لتكاملات واتساب/زد/سلة.

### 175. `annualDiscountBps` و`isPopular` أصبحا بيانات حقيقية بدل ثوابت في كود الواجهة
كانت الصفحة التسويقية العامة (`PricingClient.tsx`) تحسب خصماً سنوياً ثابتاً 20% مكتوباً مباشرة في
الكود (`ANNUAL_DISCOUNT = 0.2`)، و"الأكثر شيوعاً" مُستنتَجة بصرياً من موضع الباقة في المصفوفة
(`plans[Math.floor(plans.length/2)]`) — كلاهما **ليسا بيانات حقيقية من نظام الباقات**. أُضيف
`Plan.annualDiscountBps` (قابل للتعديل لكل باقة من Super Admin) و`Plan.isPopular` (علم حصري، باقة
واحدة فقط تحمله في كل وقت عبر `setPlanAsPopular`)، وأُصلحت الصفحة التسويقية العامة **أيضاً** لتقرأ
منهما بدل الثابتين القديمين — تجنّباً لتناقض حقيقي كان سيظهر لو استخدمت صفحة الفوترة بيانات حقيقية
بينما تبقى الصفحة التسويقية على الحيلة القديمة (قد تُظهر الصفحتان باقة مختلفة كـ"الأكثر طلباً").

### 176. مرحلة قيمة تحويلية جديدة في `IntegrationProvider`: `PAYMENT_GATEWAY` — قرار غير مثالي، موثَّق كذلك
لتسجيل أحداث Webhook بوابة الدفع، أُعيد استخدام جدول `WebhookLog` الموجود أصلاً (بدل بناء جدول
منفصل) — لكن `WebhookLog.provider` مكتوب بنوع `IntegrationProvider`، وهذا الـenum يُستخدَم أيضاً
كمفتاح لسجل محوّلات تكامل حقيقية (`registry.ts`) تتوقّع OAuth/مزامنة/إرسال رسائل — لا شيء من هذا
منطقي لبوابة دفع. بعد إضافة القيمة، اضطُرِرنا لإضافة Stub دفاعي (`paymentGatewayStubAdapter`) في
`registry.ts` يرمي استثناءً واضحاً لو استُدعي فعلياً، واستبعاد `PAYMENT_GATEWAY` صراحة من قائمة
بطاقات "اربط الآن" في `dashboard/integrations/page.tsx` (كانت ستظهر كتكامل وهمي قابل للربط بالخطأ
لولا هذا الاستبعاد). **لماذا لم يُعكَس القرار**: تعديل قيمة enum من Postgres بعد `ALTER TYPE ...
ADD VALUE` مُلتزَمة فعلياً عملية معقّدة وخطرة (لا يدعم Postgres حذف قيمة enum مباشرة) — الإصلاح
الأرخص والأضمن كان الـStub الدفاعي، وليس التراجع عن هجرة قاعدة بيانات مُطبَّقة بالفعل. لو أُعيد بناء
هذا الجزء من الصفر مستقبلاً، الأصح هو حقل `WebhookLog.provider` كنص حر (`String`) بدل enum مرتبط
بمفهوم "تكامل قابل للربط" لا علاقة له ببوابة الدفع.

### 177. `Subscription.status = PAUSED` كبديل حقيقي للإلغاء (وليس مجرد نص واجهة)
تدفق الاحتفاظ بالعميل يعرض "إيقاف مؤقت 30 يوماً" كبديل عن الإلغاء الكامل — هذا لم يكن مجرد نص تسويقي
في الواجهة، بل حالة حقيقية جديدة في `SubscriptionStatus` (`PAUSED`) + حقل `pausedUntil`، مع استئناف
تلقائي فعلي عبر نفس مهمة `scanAndExpireSubscriptions` الدورية الموجودة أصلاً (امتداد للمهمة، وليس
مهمة BullMQ منفصلة جديدة). `createCampaign` يمنع الإرسال أثناء `PAUSED` بنفس منطق `PAST_DUE`
الموجود أصلاً — تحديث بسيط لشرط موجود بدل منطق جديد مواز.

### 178. Proration حقيقي: المبلغ في نافذة المعاينة **يطابق تماماً** المبلغ المُحصَّل فعلياً
تصميم متعمَّد: `previewPlanChange()` و`changePlan()` يستدعيان نفس دالة `calculateProration()`
بنفس المدخلات (سعر الباقة الحالية/الجديدة، بداية/نهاية الدورة الحالية) — لا حساب مكرَّر بمنطق مختلف
بين المعاينة والتنفيذ الفعلي، وهو خطأ شائع في تطبيقات SaaS (عرض رقم في المعاينة ثم تحصيل رقم مختلف
فعلياً بسبب انحراف زمني بين الاستدعاءين). التخفيضات التي ينتج عنها `dueNow = 0` (الرصيد غير
المستخدم يفوق تكلفة الباقة الجديدة المتناسبة) لا تُنشئ فاتورة إطلاقاً — رصيد دائن متبقٍ للدورة القادمة
غير مبني بعد (Backlog موثَّق، وليس إغفالاً).

### 179. عطلان حقيقيان اكتُشفا وأُصلحا أثناء التحقق الحي (تفاصيلهما الكاملة في QA_REPORT.md § 9)
- تحميل فاتورة PDF فشل بـ500 بسبب تجميع webpack لحزمة `pdfkit` (تحمّل ملفات خط `.afm` ديناميكياً) —
  أُصلح بإضافتها لـ`serverComponentsExternalPackages` في `next.config.js` (نفس نمط
  `bullmq`/`ioredis` الموجود أصلاً)، مع إعادة تشغيل كاملة للخادم (لا تحديث حي لتغييرات
  `next.config.js`).
- أداة "محاكاة فشل دفع" كانت مقيَّدة خطأً بعلم `isSandboxMode()` العام (خاص بتكاملات واتساب/زد/سلة،
  مضبوط "live" حالياً لأسباب غير متعلقة) رغم أن بوابة الدفع نفسها وهمية دائماً بصرف النظر عن ذلك
  العلم — أُزيل الشرط الخاطئ تماماً.

---

## 2026-07-27 — إعادة بناء صفحة "التجار المشتركون" (Super Admin Merchants Page)

### 180. التحقق أولاً: هذه ليست "إعادة بناء من جدول مسطّح" — صفحة تفاصيل بـ7 تبويبات موجودة أصلاً
البرومنت وصف صفحة تفاصيل التاجر كـ"العنصر الأهم الناقص حالياً" — فحص مباشر للكود قبل أي تنفيذ أظهر
أن `admin/tenants/[id]/` بنية كاملة بـ7 تبويبات حقيقية (نظرة عامة/فريق/جهات اتصال/حملات/شات
بوت/قوالب/طلبات) تعمل ببيانات حقيقية من جولات سابقة — بما فيها Impersonate، تعليق/حذف (بتأكيد مزدوج
بكتابة الـslug)، ملاحظات داخلية، تغيير باقة يدوي. **القرار**: عدم إعادة بناء أي من هذا من الصفر —
حصر الجهد في الفجوات الحقيقية المكتشَفة فقط (تفصيلها في القرارات التالية). إعادة بناء ما يعمل أصلاً
تضييع فعلي للوقت ومخاطرة غير ضرورية بكسر شيء يعمل.

### 181. `quickApproveNewTenant`/`quickRejectNewTenant` تُعيد استخدام دوال مركز الموافقات حرفياً
`approveNewTenant`/`rejectRequest` الأصليتان تأخذان `ApprovalRequest.id`، بينما الجدول الرئيسي يعرض
`Tenant.id`. بدل تكرار منطق القبول/الرفض (تحديث حالة التاجر + تحديث الطلب + سجل تدقيق) في نسخة
موازية قد تنحرف عن الأصل لاحقاً عند أي تعديل مستقبلي، الحل: دالتا تغليف رفيعتان تبحثان عن
`ApprovalRequest` المعلَّق (`type: NEW_TENANT, status: PENDING`) لهذا التاجر ثم تستدعيان الدالتين
الأصليتين مباشرة بنفس المعرّف. تحقَّق حياً أن النتيجة مطابقة تماماً لمسار مركز الموافقات.

### 182. `Tenant.joinSource` — enum جديد، قيمة `MANUAL_ADMIN` محجوزة بلا مسار فعلي بعد
البرومنت ذكر "إضافة يدوية من الأدمن" كمثال لمصدر انضمام محتمل — فحص الكود أكّد عدم وجود أي مسار
حالي ينشئ تينانت يدوياً من لوحة Super Admin مباشرة (المساران الوحيدان الموجودان فعلياً:
`/register` المباشر و`approvePartnerApplication` عبر نظام الشركاء). **القرار**: إضافة قيمة
`MANUAL_ADMIN` للـenum لاكتمال النطاق المستقبلي (لا ضرر من قيمة enum غير مستخدَمة بعد)، لكن **عدم
بناء واجهة "إضافة تاجر يدوياً" فعلية** — البرومنت طلب *عرض* مصدر الانضمام على صفحة التفاصيل، وليس
بناء آلية إنشاء تينانت جديدة بالكامل (توسّع نطاق غير مطلوب صراحة).

### 183. إصلاح خلل حقيقي: استعلام "آخر webhook فاشل" كان يخلط بين رفض التوقيع وفشل المعالجة
الاستعلام القديم في صفحة التفاصيل: `webhookLog.findFirst({ where: { signatureValid: false } })` —
هذا يعني حرفياً "آخر توقيع مرفوض"، وليس "آخر فشل فعلي". توقيع صحيح (`signatureValid: true`) مع
`errorMessage` موجود (مثال: "لم يتم إيجاد تكامل مطابق") هو **فشل معالجة حقيقي** كان يُستبعَد تماماً
من هذا الاستعلام القديم. `getIntegrationWebhookHealth()` الجديدة (`lib/integrationHealth.ts`) تفحص
كلا الشرطين معاً لتعريف "نجاح" الحقيقي (`signatureValid: true` **و** `errorMessage: null` معاً)،
ولكل مزوّد على حدة (لم يكن مفصولاً حسب المزوّد إطلاقاً من قبل — استعلام واحد عام لكل التكاملات).

### 184. الترتيب حسب "آخر نشاط" — لا `orderBy` مباشر ممكن، فحُلَّ بتجميع + ترتيب في الذاكرة على المعرّفات فقط
"آخر نشاط" مُشتقّ من `MAX(User.lastLoginAt)` مُجمَّعاً حسب `tenantId` — لا يمكن لـPrisma فرز
`Tenant.findMany` مباشرة حسب حقل من علاقة مجمَّعة كهذا. الحل المُتبنَّى (بدل SQL خام مع مخاطر حقن
عبر عمود ORDER BY الديناميكي): (1) جلب كل معرّفات التينانتات المطابقة للفلتر فقط (إسقاط خفيف)، (2)
تجميع `lastLoginAt` عبر استعلام `groupBy` واحد، (3) ترتيب المعرّفات في الذاكرة (رخيص — معرّفات فقط
وليس صفوفاً كاملة)، (4) تقطيع الصفحة، (5) جلب الصفوف الكاملة للتقطيع فقط. تعقيد إضافي مقصود لتفادي
SQL خام في مسار يقبل مدخلات من المستخدم (الترتيب/الاتجاه)، مع الحفاظ على أداء معقول (لا N+1 على
الصفوف الكاملة، فقط على المعرّفات الخفيفة).

### 185. أعطال اختبار مكتشَفة أثناء التحقق كانت جميعها في تجهيزة الاختبار، وليست أعطالاً حقيقية
تفاصيلها الكاملة في `QA_REPORT.md` § 10 — ثلاثة "أعطال" ظاهرها مشكلة تطبيق تبيّن جميعها أنها فجوات
في بيانات تينانت الاختبار المُنشأ مباشرة عبر `superAdminDb` (بلا `Subscription`/`Integration` صفوف
واقعية عند الإنشاء، ومحاولة اختبار "تمديد التجربة" بعد تغيير الحالة لـ`ACTIVE` بدل `TRIAL`) — نفس
النمط المتكرر عبر هذه الجلسة: التحقق الدقيق قبل افتراض وجود خلل حقيقي في التطبيق.

---

## 2026-07-28 — وحدة محاسبة كاملة (دليل حسابات + دفتر قيود) وتدقيق شامل لكل لوحة Super Admin

جولة مزدوجة: برومنت أول طلب إعادة بناء صفحة "الإيرادات والفوترة" لتشمل محاسبة حقيقية (دليل حسابات،
دفتر قيود مزدوج القيد، فوترة احترافية متوافقة مع ZATCA)، وبرومنت ثانٍ وصل أثناء التنفيذ يوسّع النطاق
لتدقيق شامل لكل الأقسام الـ12 في لوحة Super Admin وإكمال كل فجوة حقيقية فيها — بما في ذلك قدرتان
جذريتان جديدتان: إنشاء تاجر يدوياً مباشرة، و"الدخول كخبير" حصرياً لمالك المنصة. تفاصيل التحقق الذاتي
الكامل لكل الـ12 قسماً في `QA_REPORT.md`.

### 186. خلل عرض ضريبة القيمة المضافة (1299 ر.س / ض.ق.م 0) كان تاريخياً بحتاً، وليس خللاً حياً متكرراً
تتبُّع كل مسار إنشاء فاتورة في الكود (3 مواضع، جميعها في `dashboard/billing/actions.ts`) أكّد أن كل
واحد منها كان يحسب VAT بشكل صحيح دائماً. السبب الفعلي: عمودا `vatRateBps`/`vatAmountSar` أُضيفا لجدول
`Invoice` عبر migration لاحقة (`20260725010000_invoice_vat_numbering`) بـ`ALTER TABLE ADD COLUMN
... DEFAULT` **بلا أي backfill** للصفوف الموجودة مسبقاً وقتها — الفاتورة الوحيدة المتأثرة (1299 ر.س،
تينانت DMS) أُنشئت قبل تلك الـmigration بيومين فقط. الإصلاح: `UPDATE` واحد محسوب رياضياً
(`vatAmountSar = ROUND(amountSar × vatRateBps ÷ (10000 + vatRateBps))`) أُلحِق بمigration هذه
الجولة، وتحقُّق مباشر بعد التشغيل أكّد التصحيح (0 → 169). **الدرس**: أي migration تضيف عمود محسوب
لجدول له صفوف موجودة يجب أن تتضمن backfill في نفس ملف الـmigration، وليس افتراض أن القيمة الافتراضية
كافية.

### 187. مصدر مركزي واحد لنسبة الضريبة (`lib/billing/vat.ts`) بدل ثابت مكرَّر
كان `dashboard/billing/actions.ts` يحمل `const VAT_RATE_BPS = 1500` ثابتاً بالكود، بينما
`PlatformSettings.vatRateBps` (مُضاف هذه الجولة) هو المصدر الذي يُفترَض أن يقرأه كل حساب ضريبة —
تناقض صريح مع قاعدة "لا رقم يجب أن يكون ثابتاً بالكود" في البرومنت. `getVatRateBps()` و
`computeVatAmount()` الآن نقطة القراءة/الحساب الوحيدة، مُستبدَلة في كل مسارات إنشاء الفواتير الثلاثة
(`changePlan`, `retryFailedPayment`, `devSimulatePaymentFailure`) وفي `approveCustomPlan`.

### 188. محاسبة القيد المزدوج: `Account`/`JournalEntry`/`JournalLine` منفصلة تماماً عن جداول التاجر
هذه الجداول الثلاثة **بلا** `tenantId` — تمثّل دفاتر المنصة نفسها ككيان تجاري واحد (إيراداتها من كل
التجار مجتمعين)، وليست بيانات خاصة بتاجر تحتاج عزل RLS. لذلك استُبعِدت عمداً من مصفوفة الجداول في
`prisma/postgres-rls.sql` وتُقرأ/تُكتَب حصراً عبر `superAdminDb` (اتصال BYPASSRLS). `postJournalEntry`
يتحقق أن مجموع سطور المدين يساوي مجموع سطور الدائن **في كود التطبيق قبل الكتابة**، ويرمي خطأً بدل
كتابة قيد غير متوازن — القاعدة المحاسبية الأساسية مفروضة برمجياً وليست ثقة عمياء بالمستدعي.

### 189. عدم الذرّية بين معاملة التاجر (`withTenant`) وتسجيل القيد المحاسبي — قرار واعٍ، ليس إغفالاً
`superAdminDb` اتصال Prisma منفصل تماماً عن أي `tx` داخل `withTenant(...)` — استدعاء تسجيل قيد من
داخل معاملة التاجر لن يكون ذرياً معها فعلياً بأي شكل حتى لو بدا كذلك بالكود. الحل المتبنَّى: كل نداءات
`postInvoicePaymentEntry`/`postRefundEntry` تحدث **بعد** التزام معاملة التاجر بنجاح، مطابقةً تماماً
لنمط `writeAuditLog` الموجود أصلاً في هذا الكود — نفس عدم الذرّية المقبولة سلفاً لتسجيل التدقيق، بلا
تناقض جديد.

### 190. الاعتراف بالإيراد تدريجياً (Deferred Revenue) بدل تسجيله دفعة واحدة عند الدفع
الدفع الفعلي يُقيَّد بالكامل إلى "إيراد مؤجَّل" (خصم) + "ضريبة مستحقة"، **وليس** إلى حساب الإيراد
مباشرة. مهمة BullMQ دورية (`recognizeRevenueForActiveInvoices`، كل 6 ساعات) تحسب لكل فاتورة مدفوعة
`المستهدَف = القيمة قبل الضريبة × (الوقت المنقضي ÷ مدة الفترة الكاملة)`، وتُرحِّل الفرق التراكمي من
الإيراد المؤجَّل لحساب إيراد الباقة الصحيح تدريجياً. فواتير انتهت فترتها بالكامل تُعتَرف بها 100% من
أول تشغيلة (الصيغة النسبية تتعامل مع هذه الحالة طبيعياً، بلا حالة خاصة إضافية).

### 191. الاسترداد يُقيَّد كمصروف جديد في فترة حدوثه، وليس بعكس قيود الاعتراف بالإيراد السابقة
بدل إعادة بناء/عكس جدول اعتراف تدريجي قد يكون جزئياً منفَّذاً بالفعل (تعقيد حسابي حقيقي)، أي استرداد
يُقيَّد كمصروف جديد بحساب `5.3 المبالغ المستردة` في فترة الاسترداد نفسها — ممارسة محاسبية قياسية
ومبسَّطة، وتُبقي منطق الاعتراف بالإيراد الأصلي دون تعديل رجعي إطلاقاً.

### 192. باقات Enterprise المخصَّصة تُوجَّه كلها لحساب إيراد مشترك واحد (`4.enterprise`)
كل باقة Enterprise مفتاحها فريد (`custom-${tenantId}-${timestamp}`) — حساب إيراد منفصل لكل واحدة كان
سيُضخّم شجرة الحسابات بلا فائدة عملية حقيقية. `resolveRevenueAccountCode()` تتحقق من
`Plan.isCustomForTenantId` وتُوجِّه جميعها لحساب `4.enterprise` مشترك، بينما الباقات القياسية (غير
المخصَّصة) لكل واحدة حسابها الفرعي الخاص (`4.${plan.key}`).

### 193. نطاق `InvoiceStatus` هذه الجولة: أُضيف `CANCELLED`/`OVERDUE` فقط، وليس دورة إصدار كاملة
البرومنت طلب "مجموعة حالات كاملة (مسودة/صادرة/مدفوعة/متأخرة/ملغاة/مستردة كلياً أو جزئياً)" — النظام
الحالي يُحصِّل فوراً عند تغيير الباقة (لا مرحلة "مسودة" أو "صادرة" منفصلة عن "مدفوعة" في التصميم
الحالي)، فإضافة `DRAFT`/`ISSUED` كانت ستتطلب إعادة تصميم كامل لتدفق الفوترة نفسه (تغيير جذري خارج
نطاق جولة تدقيق/إكمال). **قرار نطاق واضح**: أُضيف فقط ما ينقص فعلياً من الحالات الموجودة (`CANCELLED`
لاشتراك أُلغي قبل التحصيل، `OVERDUE` لفاتورة معلَّقة تجاوزت فترة السماح)، والاسترداد الجزئي (نسبة من
الفاتورة) غير مدعوم هذه الجولة — استرداد كامل فقط عبر `CreditNote`.

### 194. `Account`/`JournalEntry`/`JournalLine` غير قابلة للحذف/التعديل مطلقاً في كود التطبيق
لا يوجد أي مسار حذف أو تعديل لسطر قيد بعد إنشائه في كل الكود — أي تصحيح مستقبلي يجب أن يكون عبر قيد
عكسي جديد (`sourceId` يشير لنفس المصدر)، مطابقةً تماماً لقاعدة "الفاتورة الأصلية لا تُعدَّل ولا تُحذَف
أبداً" في البرومنت، مُطبَّقة بنفس الصرامة على القيود المحاسبية نفسها.

### 195. "الدخول كخبير" حصري لـ`SUPER_ADMIN` فقط — تجاوز صريح متعمَّد لقرار جولة سابقة
جولة سابقة منحت `platform.merchants.impersonate` لكل من `SUPER_ADMIN` و`PLATFORM_SUPPORT` (باعتبار
الانتحال أداة دعم فني أساسية). هذه الجولة تطلب صراحة (مرتين في نص البرومنت) حصر هذه الميزة بمالك
المنصة وحده، بلا أي تفويض جزئي — تنفيذ حرفي: حذف السطر من مصفوفة `PLATFORM_SUPPORT` في `rbac.ts`
فقط (بلا تعديل `SUPER_ADMIN`)، مع تعليق موثِّق يشرح أنه تجاوز متعمَّد وليس نسياناً. **البنية التحتية
للانتحال نفسها لم تُعَد بناؤها من الصفر** — `lib/impersonation.ts`/`ImpersonationBanner.tsx` الموجودان
أصلاً (توكن قصير الأجل، تدقيق مزدوج الجانب) استُخدِما كما هما، فقط قُيِّدت الصلاحية وأُعيدت تسمية
الزر ("🕵️ الدخول كخبير") في كل من صفحة القائمة والتفاصيل والبانر — لأن الفحص أكّد أن هذا هو بالضبط
النظام الذي وصفه البرومنت الجديد، وبناء نظام مواز كان سيكرر عملاً موجوداً بلا داعٍ.

### 196. إنشاء تاجر يدوياً يُعيد استخدام **نفس** منطق تفعيل `approvePartnerApplication` حرفياً
بدل بناء مسار تفعيل مواز قد ينحرف لاحقاً، `createTenantManually` (`admin/tenants/actions.ts`) ينسخ
البنية الدقيقة لتفعيل طلب شريك معتمَد: تينانت `TRIAL` جاهز فوراً (`joinSource: MANUAL_ADMIN`)، مستخدم
Owner بكلمة مرور عشوائية غير قابلة للاستخدام، رابط إعداد حساب آمن (`AccountSetupToken`، صلاحية 7
أيام)، ونفس بريد "أكمل إعداد حسابك" (`partnerApplicationApprovedEmail`) المُرسَل لأي تاجر آخر — تحقُّق
حي أكّد وصول البريد الصحيح فعلياً (`docs`: راجع سجل التحقق في QA_REPORT.md). صلاحية الوصول
`platform.approvals.review` نفسها المستخدَمة لبقية إجراءات تفعيل التجار، وليست صلاحية جديدة منفصلة.

### 197. رقم/بريد المالك في الإنشاء اليدوي: رقم الهاتف يُحفَظ كملاحظة داخلية، وليس حقلاً جديداً بالسكيما
`User`/`Tenant` لا يحملان حقل هاتف مالك في أي مكان آخر بالنظام (رقم واتساب التاجر مُدار عبر
`Integration` منفصلاً تماماً عن هوية المالك الشخصية). بدل إضافة migration لحقل غير مُستهلَك في أي
مكان آخر، رقم التواصل (إن أُدخِل) يُحفَظ كـ`MerchantNote` عادية على التاجر الجديد — بيانات مرئية
وقابلة للمراجعة عبر واجهة الملاحظات الموجودة أصلاً، بلا تعقيد سكيما إضافي لحقل يُستخدَم مرة واحدة فقط
وقت الإنشاء.

### 198. "تحويل رسالة تواصل إلى تاجر" = تعبئة مسبقة لنفس نموذج الإنشاء اليدوي، وليس مساراً منفصلاً
البرومنت طلب حرفياً "زر تحويل إلى تاجر جديد يُعبّئ نموذج الإنشاء اليدوي مسبقاً" — التنفيذ: رابط من
بطاقة الرسالة في `admin/leads` لـ`/admin/tenants?prefillLead=<id>`، تقرأ صفحة التجار معرّف الرسالة
وتمرّر بيانات (الاسم/البريد/الهاتف) لمكوّن `CreateTenantButton` الذي يفتح تلقائياً بحقول مُعبَّأة.
حقل هوية مخفي (`leadId`) في نفس النموذج يجعل `createTenantManually` تضبط `ContactMessage.status =
CONVERTED` و`convertedTenantId` تلقائياً بعد نجاح الإنشاء — مسار بيانات واحد كامل، بلا حاجة لصفحة
تحويل منفصلة.

### 199. اتجاه MRR لآخر 12 شهراً: "إيراد مفوتَر شهرياً" حقيقي، وليس لقطات MRR تاريخية مُعاد بناؤها
النظام لا يخزّن أي جدول لقطات MRR تاريخية أو سجل انتقالات حالة اشتراك زمني، فإعادة بناء "MRR كما كان
فعلياً في كل شهر ماضٍ" غير ممكن رياضياً من البيانات الموجودة بدقة كاملة. البديل المتبنَّى — مؤشر قياسي
شائع في SaaS: مجموع صافي قيمة الفواتير **المدفوعة فعلياً** (قبل الضريبة) لكل شهر من آخر 12 شهراً،
محسوب مباشرة من صفوف `Invoice` حقيقية. رقم حقيقي محسوب، وليس بيانات وهمية — موثَّق صراحة في تعليق
`computeRevenueSummary()` لتفادي الخلط لاحقاً بينه وبين "MRR في نهاية كل شهر" الدقيق.

### 200. نسبة تسرّب الإيراد (Churn %): تقدير من اشتراكات أُلغيت هذا الشهر فقط، لا جدول حالة تاريخي
بلا جدول يتتبّع كل انتقال حالة اشتراك عبر الزمن، Churn % تُحسَب كـ"MRR المفقود من اشتراكات
`CANCELLED` بـ`updatedAt` هذا الشهر" ÷ "MRR بداية الشهر التقديري" (= MRR الحالي + المفقود). تقريب
معقول ومحسوب من بيانات حقيقية، موثَّق صراحة كتقدير (وليس نسبة تسرّب "معتمدة محاسبياً" دقيقة 100%) في
كود `revenueAnalytics.ts` نفسه.

### 201. رسوم بيانية SVG مخصَّصة بدل إضافة مكتبة رسم بياني جديدة
تأكَّد عبر بحث شامل أنه لا توجد أي مكتبة رسم بياني (`recharts`/`chart.js`/`d3`/`nivo`/`apexcharts`)
في المشروع. بدل إضافة تبعية جديدة لأجل رسمين بيانيين فقط (اتجاه شهري + دونات توزيع الباقات)،
`DonutChart.tsx`/`BarChart.tsx` مكوّنا خادم بسيطان (بلا `"use client"`، بلا تفاعلية) يرسمان SVG خام
مباشرة من البيانات وقت الـrender — أخف وزناً وبلا حزمة JS إضافية تُشحَن للمتصفح.

### 202. فاتورة ZATCA: نطاق المرحلة الأولى فقط (QR بترميز TLV)، وليس ربط API فعلي بالهيئة
التنفيذ يغطي متطلبات "الفوترة الإلكترونية — المرحلة الأولى" فقط: رقم فاتورة تسلسلي بصيغة
`INV-YYYY-NNNNN`، بيانات بائع كاملة (اسم/رقم ضريبي/عنوان من `PlatformSettings` الجديدة)، تفصيل ضريبة
واضح، ورمز QR بترميز TLV (Tag-Length-Value) قياسي مُرمَّز Base64 عبر مكتبة `qrcode` الموجودة أصلاً
كتبعية. **لا ربط API فعلي بمنصة "فاتورة" لهيئة الزكاة والضريبة والجمارك** (المرحلة الثانية) — يتطلب
شهادة تشفير واعتماد OnBoarding رسمي مع الهيئة، خارج نطاق كود يمكن تنفيذه بلا حساب حقيقي مع الجهة.
الفاتورة أيضاً **لا تحمل رقماً ضريبياً للمشتري (التاجر)** — التجار عملاء أفراد/أنشطة صغيرة غير
مسجَّلين بالضريبة غالباً، فالفاتورة B2B مبسَّطة تجاه المنصة كمورّد، وليست B2B كاملة تفترض طرفاً مسجَّلاً.

### 203. حسابات فرعية مضافة يدوياً لدليل الحسابات: رمز مُشتَق تلقائياً `<code الأب>.customN`
بدل مطالبة مالك المنصة بكتابة رمز حساب فريد يدوياً (خطر تعارض/تنسيق غير متّسق)، `addChartOfAccountsEntry`
تحسب الرمز تلقائياً من عدد الأبناء الحاليين للحساب الأب المختار (`${parentCode}.custom${n+1}`) —
الحساب الجديد يرث نوع (`AccountType`) الأب نفسه تلقائياً، ويُعلَّم `isSystemAccount: false` (قابل
للتمييز بصرياً في الشجرة كـ"مخصَّص"، بخلاف الحسابات النظامية المحمية).

### 204. توسيع الـCMS لصفحتي /about و/services: الأيقونات ثابتة بالكود، النصوص فقط قابلة للتعديل
نفس القرار المعماري المُتَّبع أصلاً لقسم "الميزات" بالصفحة الرئيسية (`features` في `SiteContent`):
5 أيقونات صفحة الخدمات ثابتة الترتيب بالكود (`SERVICE_ICONS`)، بينما `SiteContent.servicesItems`
يحمل فقط النصوص (عنوان/وصف/نقاط) بنفس الطول والترتيب لتُقرَن بالأيقونة الصحيحة عبر الفهرس. القيم
الافتراضية عند الـmigration مطابقة **حرفياً** للنصوص التي كانت مكتوبة مباشرة بالكود قبل هذه الجولة
(مُدرَجة كـDEFAULT في نص الـSQL نفسه)، حتى لا يتغيّر شكل الموقع الفعلي إطلاقاً عند أول انتقال للـCMS.

### 205. فلاتر القوائم (سجل التدقيق/الفواتير/دفتر القيود) عبر دالة `buildXWhere` منفصلة، بنفس نمط موجود
`buildAuditLogWhere`/`buildInvoiceWhere`/`buildJournalWhere` (`lib/admin/*.ts`) تتبع حرفياً نفس نمط
`buildTenantListWhere` الموجود أصلاً من جولة سابقة — دالة فلترة واحدة تُستخدَم في كل من صفحة العرض
ومسار التصدير معاً، بدل تكرار منطق الفلترة في مكانين قد ينحرفان عن بعضهما لاحقاً.

### 206. تينانت الاختبار الوحيد ذو الفاتورة المدفوعة الفعلية أُستُخدِم لاختبار الاسترداد حياً، ثم لم يُلغَ
فحص القاعدة أظهر فاتورة PAID حقيقية واحدة فقط في بيئة التطوير (تينانت DMS، 1299 ر.س من جولة سابقة) —
استُخدِمت لاختبار تدفق الاسترداد الكامل حياً (استرداد → إشعار دائن → قيد محاسبي متوازن)، وتحوَّلت
فعلياً لحالة `REFUNDED` بإشعار دائن `#1` حقيقي كنتيجة لذلك. **قرار**: لم تُعَد لحالة `PAID` بعد
التحقق (لا يوجد مسار "إلغاء استرداد" في النظام أصلاً — نفس فلسفة "بلا حذف/تراجع" المُطبَّقة على كل
سجل مالي)، فهذه الفاتورة تبقى `REFUNDED` بشكل دائم في بيئة التطوير كأثر حقيقي للتحقق الحي. تينانتات
الاختبار الأخرى المُنشأة أثناء هذه الجولة (إنشاء يدوي + تحويل من رسالة تواصل) حُذفت بالكامل بعد
التحقق، ورسالة التواصل التجريبية حُذفت، وعنوان صفحة "من نحن" في الـCMS أُعيد لقيمته الصحيحة بعد اختبار
التعديل الحي.

---

## 2026-07-29 — الموظف الذكي (AI Human-Like Customer Service Agent)

بناء موظف رد آلي بذكاء اصطناعي حقيقي فوق عقدة "رد ذكي" وصندوق المحادثات الموجودين مسبقاً — أول ربط
فعلي لمزوّد LLM خارجي في المشروع بأكمله (كان `ai_reply` مجرد تحويل صامت لموظف بشري منذ جولة سابقة،
موثَّق كحد فاصل صريح ريثما يُربَط مزوّد حقيقي). التفاصيل الكاملة للتحقق الذاتي في `QA_REPORT.md` § 12.

### 207. طبقة Adapter واحدة لمزوّد الـLLM — OpenAI (gpt-4o-mini) كخيار افتراضي، قابل للاستبدال
`lib/ai/openaiClient.ts` هي نقطة الاستيراد الوحيدة لحزمة `openai` في كل المشروع — تبديل المزوّد
لاحقاً (Anthropic Claude مثلاً) يعني تنفيذ ملف Adapter جديد بنفس التوقيع، بلا تعديل أي نقطة استدعاء.
اختير GPT-4o-mini افتراضياً كتوازن تكلفة/جودة لردود لحظية على واتساب (تعليمات البرومنت صراحة)، والنموذج
نفسه قابل للتغيير من `admin/settings` بلا تعديل كود.

### 208. مفتاح API على مستوى المنصة (وليس لكل تاجر) — نفس نمط تشفير Meta App Secret حرفياً
`PlatformSettings.encryptedOpenAiApiKey` (مشفَّر عبر `encryptSecret`، لا يُعاد عرضه أبداً في نموذج
الإعدادات بعد الحفظ) هو المصدر الأول، مع `OPENAI_API_KEY`/`OPENAI_MODEL` كاحتياط بيئي دفاعي فقط —
مطابقةً تماماً لقرار Meta App Secret من جولة سابقة. BYO API Key لكل تاجر (خيار Enterprise محتمل) لم
يُبنَ هذه الجولة — البرومنت نفسه ذكره كخيار *مستقبلي* مشروط بقرار لاحق، وليس متطلباً حالياً.

### 209. لا مفتاح API مُعَدّ = تحويل صادق فوري لموظف بشري، وليس خطأ أو رد وهمي
`getOpenAiClient()` تُعيد `null` بلا مفتاح، و`generateAiEmployeeReply` تتعامل مع ذلك كحالة تحويل عادية
(`reason: "لم يُضبَط مزوّد الذكاء الاصطناعي بعد من إعدادات المنصة"`) — نفس فلسفة "لا محاكاة كاذبة"
المُتبَعة سابقاً مع `ai_reply` نفسها قبل هذه الجولة. **هذا فعلياً هو المسار الوحيد القابل للتحقق حياً
بالكامل في بيئة التطوير الحالية** (لا مفتاح OpenAI حقيقي متاح هنا) — تفصيل صريح لحدود التحقق في
`QA_REPORT.md`.

### 210. قواعد تحويل حتمية بكلمات مفتاحية تُفحَص *قبل* أي استدعاء LLM — شبكة أمان لا تثق بالنموذج وحده
`lib/ai/handoffRules.ts` (`checkDeterministicHandoff`) تفحص طلب صريح ("أريد شخص حقيقي") وكلمات حساسة
(شكوى/استرجاع/قانوني/غاضب) بمطابقة substring بسيطة — نفس أسلوب `matchesConditionKeywords` الموجود
أصلاً في محرك التدفقات، بدل محرك NLP جديد. أي تطابق يمنع استدعاء النموذج كلياً (توفير تكلفة/زمن)
ويضمن أن هذه القاعدة تُطبَّق دائماً بصرف النظر عن سلوك النموذج في تلك اللحظة — تنفيذ حرفي للقاعدة غير
القابلة للتفاوض "لا محاولة حل كل شيء بالذكاء الاصطناعي".

### 211. الإجابة النهائية + الثقة + قرار التحويل كلها عبر أداة إلزامية واحدة (`respond_to_customer`)
بدل الجمع بين Structured Outputs (JSON mode) واستدعاء أدوات في نفس الطلب (تعقيد وتناقض إصدارات API
محتمل)، النموذج يُطالَب دائماً بإنهاء تفكيره باستدعاء أداة `respond_to_customer(reply, confidence,
needsHuman, handoffReason?)` — آلية واحدة (Function Calling) لكل من جلب البيانات الحقيقية *وإعادة*
الرد، بدل خليط من آليتين مختلفتين. لو لم يستخدمها النموذج (سلوك غير متوقَّع)، نص رده الحر يُستخدَم
كرد احتياطي بثقة 50% بدل تجاهله بالكامل.

### 212. ثقة منخفضة تُحوَّل فوراً بصرف النظر عمّا قرّره النموذج نفسه بشأن `needsHuman`
`LOW_CONFIDENCE_HANDOFF_THRESHOLD = 40` في `engine.ts` — تحقُّق **مستقل في الكود**، وليس ثقة عمياء
بالنموذج تقييم نفسه بصدق دائماً (نماذج اللغة قد "تثق" بإجابة خاطئة). الرد المسوَّد (غير المُرسَل)
يبقى مُسجَّلاً في `AiReplyLog.aiReply` رغم عدم إرساله — مفيد لمراجعة الجودة لاحقاً (رؤية ما "كاد" الموظف
الذكي يقوله لولا الثقة المنخفضة).

### 213. ترتيب البوابات قبل أي استدعاء LLM: مفتاح تشغيل → كلمات مفتاحية → سؤال متكرر → حد معدل → حصة توكنز → مفتاح API
كل بوابة أرخص من التي تليها وتُفحَص أولاً (بند تحسين تكلفة/أداء صريح) — أرخصها (`config.enabled`،
قراءة صف واحد) أولاً، وأغلاها (استدعاء شبكي فعلي لمزوّد LLM) أخيراً. أي بوابة تفشل توقف السلسلة فوراً
بتحويل صادق، فلا حاجة لتقييم البوابات الأتلي إن كانت واحدة سابقة قد رفضت أصلاً.

### 214. عقدة "رد ذكي" تتوقف على *نفسها* بانتظار الرسالة التالية (self-loop)، بخلاف كل عقد التدفق الأخرى
كل عقد التدفق الأخرى (رسالة/سؤال/شرط) تتحرك للأمام عبر حواف الرسم البياني. "رد ذكي" مختلفة جوهرياً:
محادثة مفتوحة النهاية وليست سلسلة خطوات محدودة العدد. لذا عند إرسال رد فعلي بلا تحويل،
`currentNodeId` يُضبَط لنفس معرّف عقدة `ai_reply` (وليس عبر `findOutgoingEdge`) — المحادثة تبقى "معلَّقة"
عند هذه العقدة حتى يحدث تحويل صريح (كلمة مفتاحية، ثقة منخفضة، سؤال متكرر، انتهاء نافذة الـ24 ساعة) أو
فشل إرسال حقيقي.

### 215. `AiAgentConfig.enabled` مفتاح صريح منفصل تماماً عن بوابة الباقة (`allowedNodeTypes`)
بوابة الباقة تتحكم بـ*القدرة* على إضافة عقدة `ai_reply` لتدفق ما (فحص وقت الحفظ). `enabled` يتحكم به
التاجر نفسه لتشغيل/إيقاف الموظف الذكي فعلياً حتى لو كانت العقدة موجودة في تدفق منشور — يُفحَص كأول
بوابة في `generateAiEmployeeReply` (الأرخص) قبل أي فحص آخر. **عطل حقيقي اكتُشف وأُصلح أثناء التحقق
الذاتي**: هذا الفحص كان غائباً تماماً في التنفيذ الأول (الحقل موجود بالسكيما والواجهة، لكن المحرك لم
يقرأه إطلاقاً) — اكتُشف عبر تحقُّق حي مباشر (اختبار 4 في سكربت التحقق) قبل اعتماد الميزة كمكتملة.

### 216. مؤشر "يكتب الآن..." — Meta يتطلب wamid الرسالة الواردة، Baileys يدعمه أصالةً
واجهة `IntegrationAdapter` اكتسبت طريقة اختيارية جديدة `sendTypingIndicator?()` (بلا كسر أي adapter
موجود). Baileys (قناة QR) يدعم `sendPresenceUpdate("composing", jid)` أصالةً — تطبيق حقيقي كامل.
Meta Cloud API يربط مؤشر الكتابة بتحديد رسالة واردة معيّنة كمقروءة (يتطلب `wamid` الرسالة الأخيرة) —
تطبيق حقيقي أيضاً لكن بلا تأثير عملي في وضع Sandbox (لا استدعاء شبكي فعلي فيه أصلاً). كلا التطبيقين
best-effort (`.catch(() => {})`) — فشل مؤشر الكتابة لا يجب أن يوقف إرسال الرد الفعلي أبداً. تأخير
زمني اصطناعي (600ms + 20ms/حرف، حد أقصى 4 ثوانٍ) يكمّل هذا لضمان "شعور طبيعي" حتى في القنوات/الأوضاع
التي لا تُفعِّل مؤشر الكتابة الحقيقي عملياً.

### 217. لا مزامنة كتالوج منتجات حقيقية جديدة — أداة `get_product_price` تستعلم جدول `Product` كما هو
البحث المسبق أكّد أن `Product.syncOrders` في كل من adapters زد وسلة تُعيد `productsSynced: 0` ثابتاً
حرفياً (Stub قائم من جولة سابقة، لا علاقة له بهذه الميزة) — لا مزامنة كتالوج حقيقية موجودة أصلاً في
المشروع. **قرار نطاق**: بناء مزامنة كتالوج Zid/Salla حقيقية كاملة توسّع نطاق غير مطلوب صراحة في هذا
البرومنت (الذي يطلب "موظف ذكي يستخدم بيانات حقيقية"، وليس "أكمل تكامل زد/سلة"). أداة `get_product_price`
تستعلم جدول `Product` كما هو فعلياً — صادقة تماماً مع أي بيانات موجودة (تُرجِع نتيجة حقيقية إن وُجدت
مزامنة يدوية/مستقبلية، أو "غير موجود" بصدق إن كان الجدول فارغاً)، بلا أي تخمين. تحقُّق حي أكّد هذا عبر
إدراج صف `Product` حقيقي يدوياً واستعلامه مباشرة.

### 218. بوابة الباقة مفروضة خادمياً على `updateAiAgentConfig`/`sendTestMessage`، وليس فقط بإخفاء الصفحة
**عطل حقيقي ثانٍ اكتُشف وأُصلح أثناء المراجعة الذاتية قبل التحقق الحي**: التنفيذ الأول جعل صفحتي
الإعدادات والاختبار تُعيدان التوجيه لتاجر Starter (فحص واجهة فقط)، لكن الإجراءين الخادميين أنفسهما
(`updateAiAgentConfig`, `sendTestMessage`) كانا يقبلان الاستدعاء المباشر بلا أي تحقق من `allowedNodeTypes`
— تناقض مع نمط `saveFlowGraph` الدفاعي الموجود أصلاً لعقدة `ai_reply` نفسها. أُضيف نفس فحص
`isNodeTypeAllowed(limits, "ai_reply")` خادمياً في كلا الإجراءين، مطابقةً للنمط الدفاعي القائم.

### 219. وضع المعاينة/الاختبار: معرّف محادثة/جهة اتصال وهميان غير مطابقين لأي صف حقيقي، بلا أي كتابة للسجل
`sendTestMessage` يستدعي *نفس* `generateAiEmployeeReply` الحقيقية (لا محاكاة منفصلة) لكن بـ
`conversationId: "preview-test-mode"` و`contactId: "preview-test-mode-no-contact"` — قيم لا تطابق أي
صف حقيقي أبداً، فلا حاجة لإنشاء تينانت/جهة اتصال اختبار حقيقية تُلوِّث بيانات التاجر الفعلية. فحص
السؤال المتكرر (`AiReplyLog.findMany({conversationId})`) يعيد قائمة فارغة بأمان (لا خطأ FK — مجرد
فلتر قراءة)، وأداة `get_order_status` تُعيد بصدق "غير موجود" لعدم وجود طلبات لهذا المعرّف الوهمي. **لا
كتابة لـ`AiReplyLog`/`Message` في وضع الاختبار** — تجربة معزولة تماماً، ما عدا فحص حصة التوكنز/حد
المعدل اللذين يبقيان مفعَّلين عمداً (منع استغلال وضع الاختبار للتحايل عليهما).

### 220. حد التحقق الحي الصادق: لا مفتاح OpenAI حقيقي في بيئة التطوير — ما تم التحقق منه فعلياً وما لم يُتحقَّق منه
لا مفتاح API حقيقي متاح في هذه البيئة (ولا يمكن الحصول على واحد بلا تدخّل بشري تُمنَع طلبه هذه
الجولة). **ما تم التحقق منه حياً وبدقة**: كل بوابات ما-قبل-الاستدعاء الست (تفعيل/كلمات مفتاحية/سؤال
متكرر/حد معدل/حصة توكنز/مفتاح API) عبر محاكاة رسائل واردة حقيقية كاملة المسار (`applyParsedMetaWebhook`
→ `runChatbotEngine`)، القفل الحقيقي لباقة Starter (واجهة وخادم معاً)، دقة سجل استهلاك التوكنز (0 في
كل مسارات التحويل الحتمي، مطابقة تماماً لعدم استدعاء LLM فعلياً)، ومنطق أداة `get_product_price` ضد
بيانات `Product` حقيقية مباشرة. **ما لم يُتحقَّق منه**: جولة استدعاء LLM فعلية كاملة (قرار النموذج
باستدعاء أداة، جودة الرد النهائي، دقة تقييم الثقة الذاتي من نموذج حقيقي) — يتطلب `OPENAI_API_KEY`
حقيقياً يُضبَط لاحقاً من `admin/settings` من قِبَل مالك المنصة، ثم إعادة اختبار مسار "سعر منتج حقيقي"
تحديداً (بند 1 في التحقق الذاتي المطلوب بالبرومنت) بمفتاح فعلي.

---

## 2026-07-26 — إصلاح جذري لعطل النص العربي في فاتورة PDF + إعادة تصميم احترافية

النص العربي في فاتورة PDF كان يظهر مشوَّهاً بالكامل — تشخيص مباشر للكود (وليس تخميناً) أكّد السبب:
`pdfkit` يرسم الحروف مباشرة عبر `doc.text()` بلا أي محرك تخطيط نص (Text Shaping Engine) — لا تشكيل
عربي (Arabic Shaping: ربط الحروف بأشكالها الأولية/الوسطى/النهائية) ولا إعادة ترتيب ثنائي الاتجاه
(BiDi/RTL)، بصرف النظر عن الخط المستخدَم. هذا عطل معماري جذري في المكتبة نفسها، وليس خطأ إعداد.

### 221. القرار المعماري: التخلي الكامل عن pdfkit، اعتماد HTML→PDF عبر Puppeteer
`puppeteer` أُضيفت كتبعية إنتاجية حقيقية (وليست dev فقط — الفاتورة ميزة إنتاجية حقيقية يستخدمها
التجار). كل فاتورة الآن HTML/CSS كاملة تُصيَّر عبر Chromium حقيقي مُشغَّل بلا واجهة (`page.setContent`
+ `page.pdf()`) — محرك المتصفح نفسه يتولى التشكيل العربي وRTL تلقائياً وبصحة كاملة، تماماً كأي صفحة
ويب عادية. `pdfkit`/`@types/pdfkit` أُزيلا نهائياً من `package.json` (لا استخدام آخر لهما في المشروع
بعد تأكيد ذلك بالبحث الشامل قبل الحذف).

### 222. متصفح Puppeteer واحد مشترك عالمياً (Singleton) — ليس عملية Chromium جديدة لكل فاتورة
إطلاق Chromium من الصفر يستغرق ~1-2 ثانية — غير مقبول لكل طلب تنزيل فاتورة. `lib/billing/htmlToPdf.ts`
يخزّن نسخة واحدة مشتركة عبر `global.__pdfBrowser` (نفس نمط `redisConnection`/`superAdminDb` الموجود
أصلاً)، وكل توليد فاتورة يفتح `page` جديدة فقط (تُغلَق فوراً بعد الاستخدام لتفادي تسريب الذاكرة)
بدل متصفح كامل جديد.

### 223. خط Cairo مضمَّن Base64 مباشرة في الـHTML — لا رابط Google Fonts خارجي وقت التوليد
نفس خط العلامة التجارية المستخدَم أصلاً في كل الموقع (`next/font/google` في `layout.tsx`). أربعة أوزان
(400/600/700/800) نزّلت فعلياً من Google Fonts (روابط `fonts.gstatic.com` الحقيقية، وليست تخميناً) إلى
`src/lib/billing/fonts/*.ttf` مرة واحدة، ثم تُقرأ وتُحوَّل Base64 وتُضمَّن كـ`@font-face` مباشرة داخل
كل صفحة HTML (مع تخزين مؤقت في الذاكرة عبر متغيّر معزول `cachedFontFaces` بعد أول استدعاء). هذا يضمن
استقلالاً كاملاً عن الشبكة وقت توليد الفاتورة فعلياً في الإنتاج — لا اعتماد على توفر إنترنت خادم PDF
لحظة الطلب، ولا مسار ملف محلي قد يفشل Puppeteer بالوصول إليه حسب سياق التشغيل (Docker، صلاحيات، إلخ).

### 224. أرقام غربية (0-9) صراحة لكل المبالغ المالية ونسبة الضريبة — وليس أرقام هندية-عربية
`toLocaleString("ar-SA")` الافتراضي في محاولة أولى أنتج أرقاماً هندية-عربية (١٬٢٩٩) للمبالغ — أُصلح
إلى `toLocaleString("en-US")` صراحة. القرار: المستندات المالية الرسمية (وتوجيهات ZATCA نفسها) تتوقّع
أرقاماً غربية لضمان قراءة آلية/محاسبية متّسقة عبر كل الأنظمة، بخلاف نص الفاتورة العادي (تواريخ/أوصاف)
الذي يبقى عربياً بالكامل بلا أي مشكلة (`toLocaleString("ar-SA-u-nu-latn", ...)` للتواريخ يضمن نفس الشيء
مع نص شهر/سنة عربي كامل).

### 225. لا رموز تعبيرية (Emoji) في الفاتورة — قرار تصميم متعمَّد بعد اكتشاف مشكلة حقيقية
محاولة أولى تضمّنت 🙏/⚠️ في نص الفاتورة — اكتُشف عبر التحقق الفعلي (استخراج نص PDF ببرنامج
`pdftotext`) أن الرمز التعبيري يظهر كبيانات تالفة (mojibake) عند استخراج النص، وتسبَّب أيضاً في تضخّم
حجم ملف PDF بشكل كبير (~195 كيلوبايت مقابل ~43 كيلوبايت بعد الإزالة — على الأرجح بسبب تضمين Chrome
لخط ألوان إيموجي كامل داخل الملف). أُزيلت كل الرموز التعبيرية نهائياً — قرار مزدوج السبب: (1) مشكلة
تقنية فعلية، (2) فواتير الشركات العالمية (Stripe, QuickBooks) لا تستخدم رموزاً تعبيرية أصلاً في مستند
مالي يُطبَع ويُؤرشف قانونياً، فإزالتها تخدم الاحترافية المطلوبة أيضاً.

### 226. تاريخ الاستحقاق يظهر فقط للفواتير غير المكتملة السداد (PENDING/FAILED/OVERDUE)
معمارية الفوترة الحالية تُحصِّل فوراً عند تغيير الباقة (لا مرحلة "إصدار ثم استحقاق لاحق" منفصلة —
قرار نطاق سابق موثَّق في قرار #193)، فـ"تاريخ الاستحقاق" لا معنى حقيقياً له لفاتورة مدفوعة/مستردة/ملغاة
بالفعل. يظهر فقط للحالات الثلاث التي لم يكتمل تحصيلها بعد، محسوباً كنهاية فترة الفاتورة (`periodEnd`)
— تفسير معقول ومحدَّد بوضوح، وليس حقلاً وهمياً بلا معنى.

### 227. لا "بنود متعددة" حقيقية في بنية البيانات الحالية — فاتورة واحدة = تحصيل اشتراك واحد دائماً
البرومنت طلب اختبار "فاتورة طويلة (بنود متعددة)" — فحص نموذج البيانات أكّد أن `Invoice` الواحدة
تمثّل دائماً تحصيلاً واحداً (لا جدول بنود منفصل `InvoiceLineItem` في السكيما، ولا مسار كود يُنشئ أكثر
من "بند" لكل فاتورة). بدل تلفيق بنود وهمية لا تعكس واقع النظام، اختُبر فيضان المحتوى بين الصفحات فعلياً
عبر نص "سبب فشل تحصيل" مطوَّل جداً (السيناريو الواقعي الوحيد لمحتوى متغيّر الطول في هذه البنية) — نجح
الانتقال لـ3 صفحات فعلياً بلا قطع أي عنصر في المنتصف (بفضل قواعد `page-break-inside: avoid` المضافة
على الجدول/الأقسام). لو احتاج النظام مستقبلاً فواتير متعددة البنود فعلياً، الجدول مبني بالفعل بصيغة
`<table>` قياسية تدعم أي عدد صفوف بلا تعديل تصميمي إضافي.

### 228. لا رقم ضريبي إلزامي للمشتري (التاجر) في الفاتورة — قرار سابق مُعاد تأكيده هنا
مطابقةً للقرار الموثَّق سابقاً (#202): التجار عملاء أفراد/أنشطة صغيرة غير مسجَّلين بالضريبة غالباً،
فحقل "الرقم الضريبي" في قسم "إلى (المشتري)" يظهر فقط إن وُجدت قيمة فعلية (لا يوجد حقل كهذا في نموذج
`Tenant` أصلاً بعد فحص السكيما) — بدل حقل فارغ مُربِك أو قيمة "غير متوفر" مُقحَمة بلا داعٍ.

## 2026-08-15 — جلسة دخول مستقلة لفريق المنصة (تعايش حقيقي مع جلسة تاجر في نفس المتصفح)

### 229. كوكي جلسة منفصل الاسم (dms_platform_session) بدل محاولة تقييد كوكي NextAuth بالمسار
**المشكلة المُبلَّغة**: صاحب المنصة لا يقدر يفتح حساب تاجر (owner@tenant-a.sa) وحساب مالك المنصة
(admin@platform.sa) في نفس المتصفح وقت واحد — تسجيل الدخول بالحساب الثاني يطرد الأول فوراً.
السبب: الاثنان كانا يمرّان بنفس مسار NextAuth (`signIn("credentials")`)، فيتشاركان نفس كوكي الجلسة
الوحيد (`next-auth.session-token`) بغض النظر عن الدور — تسجيل دخول جديد يستبدل الـJWT المخزَّن فيه
بالكامل.

**الخيارات المفحوصة**: (1) تقييد كوكي NextAuth بمسار (`path: /dashboard`) — رُفض: نقاط النهاية
الداخلية لـNextAuth نفسها (`/api/auth/session`, `/api/auth/csrf`) تعيش خارج `/dashboard` و`/admin`
معاً، فتقييد المسار يكسر فحص الجلسة من العميل (`next-auth/react`) في الحالتين. (2) نطاقات فرعية
منفصلة (`admin.dms.local`) — الأصح معمارياً لبيئة إنتاج حقيقية، لكن يتطلب تعديل DNS/hosts وإعادة
ضبط `NEXTAUTH_URL` لكل بيئة، مبالغ فيه لحل مشكلة "دخول متزامن" وحدها. **القرار المتَّخذ**: كوكي
JWT ثانٍ مستقل تماماً (`dms_platform_session`، موقَّع بـ`jose`/HS256 بنفس `NEXTAUTH_SECRET`، بلا أي
اعتماد على NextAuth نفسه) — كوكيان مختلفا الاسم يتعايشان في نفس المتصفح بلا أي شرط إضافي على
المسار، وهذا هو جوهر الحل الفعلي.

**التنفيذ**: `src/lib/platformAuth.ts` (جديد) يوقّع/يتحقّق/يمسح الكوكي الثاني. صفحة دخول مستقلة
`/admin-login` (خارج مجلد `src/app/admin/` عمداً — لو بقيت جوّاه كانت `admin/layout.tsx` ستحميها
بنفس شرط الجلسة فتُنتِج توجيهاً حلقياً لا نهائياً لكل زائر غير مسجَّل). `requireSuperAdminSession()`
في `src/lib/session.ts` يفحص الكوكي الجديد أولاً؛ إن غاب يرجع تلقائياً لفحص جلسة NextAuth القديمة
(توافق خلفي كامل — أي جلسة فريق منصة قائمة عبر `/login` القديم تستمر تعمل بلا أي انقطاع، بلا حاجة
لإعادة تسجيل دخول جماعية). حقل `authSource: "platform" | "nextauth"` مُضاف لعائد الدالة يحدِّد لأي
مسار جاء منه المستخدم الحالي — `admin/layout.tsx` يستخدمه لاختيار سلوك زر "تسجيل الخروج" الصحيح:
مسح الكوكي الجديد فقط دون لمس كوكي NextAuth عند الدخول عبر `/admin-login`، أو `next-auth signOut()`
العادي عند الدخول عبر المسار القديم — بحيث تسجيل الخروج من لوحة المنصة لا يُسقِط أبداً جلسة تاجر
نشطة بالخطأ في تبويب آخر من نفس المتصفح.

**التحقق الفعلي (Playwright، سياق متصفح واحد يحاكي نفس جلسة الكوكيز)**:
- تسجيل دخول تاجر حقيقي (`owner@tenant-a.sa`) ثم — في نفس السياق دون فتح نافذة جديدة — تسجيل دخول
  مالك المنصة (`admin@platform.sa`) عبر `/admin-login`: نجح الاثنان، والعودة لـ`/dashboard` بعدها
  أثبتت بقاء جلسة التاجر سليمة، والعودة لـ`/admin` أثبتت بقاء جلسة المنصة سليمة أيضاً — بلا أي طرد
  متبادل. تفريغ كوكيز السياق أظهر الأربعة معاً: `next-auth.session-token` و`dms_platform_session`
  جنباً إلى جنب.
- تسجيل الخروج من لوحة المنصة (زر Topbar) مسح `dms_platform_session` فقط فعلياً — `/dashboard` بقي
  يعمل بجلسة التاجر بعدها مباشرة، و`/admin` طلب تسجيل دخول من جديد كما هو متوقَّع.
- المسار القديم (تسجيل دخول `admin@platform.sa` مباشرة من `/login` العادي، بلا `/admin-login`
  إطلاقاً) لا يزال يعمل حرفياً كما كان: وصول لـ`/admin`، وزر الخروج يستخدم `next-auth signOut()`
  ويُسقِط الجلسة بشكل صحيح — صفر كسر توافق خلفي.
- `npx tsc --noEmit` نظيف على كامل المشروع بعد التعديل. `npm run smoke-test`: كل خطوات التاجر
  (دخول، داشبورد، جهات اتصال، معالج حملة كامل + إرسال) ناجحة دون تأثر بالتعديل؛ الفشل الوحيد
  المتبقي (محرر الشات بوت، نص `لوحة رسم التدفق`) موجود من قبل هذا التعديل ولا علاقة له به
  (سكربت اختبار قديم لم يُحدَّث بعد إعادة تصميم واجهة الشات بوت في جولة سابقة).

### 230. الصفحة الرئيسية العامة (`/`) لم تعد تُحوِّل تلقائياً حسب الجلسة — طلب صريح من صاحب المشروع
كانت `(marketing)/page.tsx` تفحص الجلسة وتُحوِّل فوراً لـ`/dashboard` أو `/admin` لأي زائر مسجَّل
دخوله بالفعل (سلوك مقصود أصلاً لتفادي عرض صفحة تسويقية لمستخدم نشط). صاحب المشروع اعترض مرتين
متتاليتين على هذا السلوك تحديداً وطلب صراحة أن تبقى الصفحة الرئيسية عامة عادية دائماً — أُزيل شرط
التحويل نهائياً من `HomePage`، فتُعرَض الصفحة التسويقية دائماً بغض النظر عن حالة الجلسة.

**أثر جانبي حقيقي اكتُشف واصلح فوراً**: صفحة `/login` كانت تعتمد على هذا التحويل نفسه كخطوة أخيرة
بعد نجاح الدخول (`router.push("/")` ثم تعتمد على أن `/` سيُحوِّل تلقائياً لـ`/dashboard`) — بعد
إزالة التحويل، كان المستخدم يهبط على الصفحة التسويقية بدل الداشبورد بعد كل تسجيل دخول ناجح. أُصلح
فوراً: `/login` أصبح يجلب الجلسة صراحة عبر `getSession()` بعد `signIn()` الناجح ويحدِّد الوجهة
(`/admin` أو `/dashboard`) بنفسه، دون اعتماد على أي منطق تحويل في صفحة أخرى. (`/admin-login` غير
متأثر أصلاً — كان دائماً يُحوِّل مباشرةً عبر Server Action خاص به، بلا مرور بـ`/`.)

**التحقق الفعلي**: Playwright — دخول تاجر حقيقي ثم زيارة `/` يدوياً بنفس الجلسة: الصفحة التسويقية
ظهرت وبقيت كما هي (لا تحويل). تسجيل الدخول نفسه أوصل مباشرة لـ`/dashboard` كالمعتاد. `npx tsc
--noEmit` نظيف، و`npm run smoke-test` أعاد تأكيد نجاح دخول/داشبورد/جهات اتصال/حملة كاملة.

## 2026-08-15 (جولة ثانية) — إعادة تصميم الصفحة الرئيسية (معاينة منتج + قنوات وأدوات + حلول) وإعادة هيكلة الباقات لـ4 مستويات

### 231. معاينة Hero بصورة واجهة حقيقية بدل الانيميشن التخطيطي — بيانات وهمية بحتة، وليست لقطة شاشة فعلية
صاحب المشروع أرسل لقطات من موقع منافس وطلب واجهة رئيسية مشابهة الشكل. بدل نسخ تصميم/علامة منافس
حرفياً، بُني مكوّن `ProductPreview.tsx` جديد يعرض نفس *بنية* شاشة `/dashboard/inbox` الحقيقية في
هذه المنصة (قائمة محادثات + نافذة محادثة + لوحة "الموظف الذكي") بعلامتنا التجارية وبيانات تجريبية
مصطنعة بالكامل (أسماء/رسائل وهمية) — قرار متعمَّد: لا تُستخدم أي بيانات عميل حقيقية على صفحة عامة
مطلقاً، ولا يُستنسَخ تصميم/واجهة شركة أخرى حرفياً. استبدل `HeroAnimation` في قسم الـHero فقط (الملف
نفسه أُبقي كما هو، غير مستخدَم حالياً، لعدم وجود سبب لحذفه).

### 232. قسم "القنوات والأدوات" و"الحلول" — قدرات حقيقية فقط، لا ميزات غير مُنفَّذة
فُحصت قدرات المنصة الفعلية أولاً (`src/lib/integrations/{meta,whatsappQr,zid,salla}`) قبل كتابة أي
نص تسويقي. القنوات المعروضة (4): واتساب API الرسمي (Meta)، تعايش واتساب بزنس (QR)، زد، سلة — **لا
Messenger ولا Instagram ولا ودجت موقع** كما في مرجع المنافس، لأنها غير موجودة فعلياً في الكود؛ عرضها
كان سيكون ادّعاءً مضلّلاً. الأدوات المعروضة (5) تطابق تبويبات لوحة التاجر الفعلية حرفياً: الحملات،
جهات الاتصال، صندوق الوارد، الموظف الذكي، قوالب الرسائل. قسم "الحلول" (`SolutionsSection.tsx`) نص
تسويقي عام (قطاعات/فرق) بلا ادّعاء ميزات تقنية محدَّدة غير موجودة — بُني كقسم ثابت في الصفحة (وليس
قائمة تنقّل منسدلة كما في المرجع) تبسيطاً، وتحديداً لأن هذا ما طلبه صاحب المشروع حرفياً ("قسم").

### 233. إعادة هيكلة الباقات لـ4 مستويات بدل 3 — أسعار محوَّلة، حدود حقيقية غير مبالغ فيها
المرجع عرض 4 باقات بالدولار ("محادثات/رسائل غير محدودة" في الكل). القرارات المتَّخذة بعد توضيح
مباشر من صاحب المشروع:
- **العملة**: تحويل تقريبي (×3.75) من $49/149/349/499 إلى 185/560/1310/1870 ر.س — وليس نفس الأرقام
  بالضبط كر.س (كان سيمثّل خفضاً حقيقياً بمقدار ~4× عن الأسعار الحالية 199/499/1299 ر.س بلا أي مبرر
  تجاري) ولا الإبقاء على الأسعار الحالية كما هي (كان سيتجاهل طلب اعتماد بنية المرجع الرباعية).
- **الحدود**: المنصة تفرض إنفاذاً فعلياً حقيقياً لحد `maxMessagesPerMonth` لكل باقة (كود إنفاذ حقيقي
  موجود، وليس رقماً زخرفياً) — كتابة "رسائل غير محدودة" كما في المرجع كانت ستكون ادّعاءً مضلّلاً
  للتاجر يتعارض مباشرة مع السلوك الفعلي عند تجاوز الحد. القرار: عرض الحد الحقيقي رقمياً لكل باقة،
  كما كان مكوّن `PricingClient.tsx` يفعل أصلاً قبل هذا التعديل (لم يحتج تغييراً في هذه النقطة تحديداً).
- **لا تخفيض لأي حد حالي**: `maxUsers`/`maxWhatsappNumbers`/`maxMessagesPerMonth` للباقات الثلاث
  القائمة (`starter`/`growth`/`scale`) لم تُخفَّض إطلاقاً — فقط الاسم والسعر تغيّرا (البداية/النمو/
  الاحتراف بدل الأساسية/النمو/التوسع). السبب: يوجد مستأجرون حقيقيون فعليون على المنصة الآن (وليس
  بيانات تجريبية بحتة) قد يعتمدون فعلياً على حدودهم الحالية في الاستخدام التشغيلي اليومي، حتى لو لم
  يوجد بعد مزوّد دفع حقيقي (`SECURITY_FIXES.md` — الفوترة لا تزال Sandbox بالكامل، فلا خطر مالي فوري
  من تعديل السعر نفسه، لكن خفض حد تشغيلي فعلي كان سيُحدِث ضرراً تشغيلياً حقيقياً لتاجر يستخدمه الآن).
- **مفاتيح الباقات (`key`) أُبقيت دون تغيير عمداً**: `starter`/`growth`/`scale` مرتبطة فعلياً بمنطق
  كود متفرّق (نوع `minTier` في `lib/chatbot/nodeTypes.ts`، enum التسجيل الذاتي في `register/actions.ts`،
  قيم افتراضية fallback في `planLimits.ts`/`campaigns/limits.ts`) — تغيير المفاتيح نفسها كان سيتطلب
  تعديل كل هذه المواضع بلا أي فائدة فعلية (الاسم المعروض للمستخدم هو `Plan.name`، لا `Plan.key`).
  الباقة الرابعة الجديدة أُضيفت بمفتاح جديد `enterprise` بدل إعادة تدوير مفتاح قائم.
- **باقة "المؤسسات" (`enterprise`) غير متاحة في `/register` (التسجيل الذاتي بتجربة 14 يوماً) عمداً**:
  باقة بدعم "مدير حساب مخصص" لا معنى منطقياً لبدء تجربة مجانية فورية بها بلا تدخّل بشري — تبقى متاحة
  فقط عبر `/pricing` (عرض) و`/partners/join` (تقديم فعلي، مسار دعم مبيعات)، وكلاهما ديناميكي أصلاً
  (`getPublicPlans()`) فلم يحتج أي تعديل كود ليعرض الباقة الجديدة تلقائياً.
- دليل الحسابات المحاسبي (`chartOfAccounts.ts`) يتعامل مع الباقات ديناميكياً بالكامل (`findMany` على
  كل الباقات) — الباقة الجديدة لم تحتج أي تعديل هناك، يُنشأ لها حساب إيراد فرعي `4.enterprise`
  تلقائياً عند أول استدعاء لـ`ensureChartOfAccounts()`.

**التحقق الفعلي**: تحديث فعلي لصفوف `Plan` في قاعدة البيانات (سكربت مؤقت عبر Prisma، حُذف بعد
التنفيذ) — تأكيد القيم الأربع عبر استعلام مباشر بعد التنفيذ. `npx tsc --noEmit` نظيف. `npm run
smoke-test`: دخول/داشبورد/جهات اتصال/معالج حملة كامل ناجحة دون تأثر. لقطات شاشة Playwright حقيقية
(بتمرير تدريجي فعلي لتفعيل انيميشن `Reveal` القائم على IntersectionObserver — لقطة صفحة كاملة دفعة
واحدة بلا تمرير تُظهر فراغاً بيضاء خادعاً لكل قسم يعتمد عليه، ظاهرة موجودة أصلاً حتى في أقسام لم
تُعدَّل مثل "الإحصائيات"/"آراء العملاء"، وليست خللاً في الأقسام الجديدة) لكل من الصفحة الرئيسية
الكاملة وصفحة `/pricing` الكاملة — تأكيد بصري مباشر أن كل الأقسام الجديدة (معاينة المنتج، القنوات
والأدوات، الحلول، بطاقات الباقات الأربع) تُعرَض بصرياً بشكل صحيح ومتّسق مع باقي هوية الصفحة.

## 2026-08-16 — برنامج التسويق بالعمولة (Affiliates) الكامل: صفحة عامة + داشبورد مسوّق + لوحة تحكم مالك المنصة

### 234. نظام مستقل بالكامل عن "الشركاء" (partners/*) — كيانات، مصادقة، وحساب عمولة جديدة من الصفر
تحقّق أولاً (عبر مسح شامل للكود والسكيما) أن نظام "الشركاء" الحالي (`partners/join`، `partners/apply`)
تجّار يقدّمون طلب انضمام لأنفسهم كعملاء — لا علاقة له بمسوّقين يُحيلون تجّاراً آخرين مقابل عمولة. لا
يوجد أي نموذج/جدول قابل لإعادة الاستخدام لهذا الغرض، فبُني نظام جديد كلياً:

**النماذج الجديدة** (`Affiliate`, `Referral`, `Commission`, `Payout` + 4 enums) — بلا RLS عمداً:
`Affiliate` لا يملك `tenantId` أصلاً (ليس بيانات مستأجَرة)، والبرنامج كله عابر للمستأجرين بطبيعته.
تُقرأ/تُكتب عبر `rawDb` مباشرة. **فخّ حقيقي اكتُشف وأُصلح أثناء الاختبار الحي**: أي `include: { invoice
}` على `Commission` عبر `rawDb` يفشل فعلياً ("Inconsistent query result: Field invoice is required")
لأن `Invoice` محمي بـRLS وعلاقته بـCommission إلزامية — JOIN عبر اتصال `app_runtime` بلا
`app.current_tenant_id` يُرجع null فيكسر Prisma. الإصلاح: قراءة مبالغ الفواتير عبر `superAdminDb`
منفصلة، ودمجها في الذاكرة (نفس نمط `lib/admin/revenueAnalytics.ts`) — طُبِّق في كل من
`affiliates/dashboard/page.tsx` و`admin/affiliates/[id]/page.tsx`.

**مصادقة مستقلة ثالثة**: `lib/affiliates/session.ts` — كوكي JWT ثالث (`dms_affiliate_session`، بنفس
أسلوب `dms_platform_session` من جلسة سابقة) لا علاقة له بـNextAuth ولا بجلسة فريق المنصة — يتعايش مع
الاثنين الآخرين في نفس المتصفح. المسوّق ليس تاجراً (لا Tenant) ولا موظف منصة (لا RBAC).

**حساب العمولة عبر مهمة مصالحة دورية، لا Hook مباشر على كل نقطة تحصيل**: نقاط تحصيل/تحديث حالة
الفاتورة لـPAID متفرّقة فعلياً عبر 6+ ملفات (`admin/approvals`, `admin/billing`, `dashboard/billing`
×3 مواضع, `webhooks/payment`) — ربط منطق عمولة يدوياً بكل واحدة هش وعرضة لنسيان نقطة مستقبلية بصمت.
القرار: `lib/affiliates/commissionSync.ts` تُشغَّل كل ساعة عبر BullMQ (`queue/worker.ts`، نفس نمط
`revenueRecognition.ts`)، تفحص كل الفواتير المدفوعة لتجّار مُحالين، تُنشئ عمولة (idempotent عبر قيد
فريد `Commission.invoiceId`) بشرط الوقوع ضمن 12 شهراً من `Referral.convertedAt`، ثم تُرقّي العمولات
المستحقة (`eligibleAt = paidAt + 30 يوماً`) من PENDING إلى APPROVED. مستوى المسوّق يُرقَّى تلقائياً
(أبداً يُخفَّض تلقائياً) بناءً على عدد التجّار المُحالين الذين دفعوا فعلياً.

**التقاط الإحالة (`?ref=CODE`) بآليتين مختلفتين حسب المسار — قرار متعمَّد وليس تناقضاً**:
- `/register` (تسجيل ذاتي، التينانت يُنشأ فوراً في نفس الطلب): الكوكي يُقرأ مباشرة داخل
  `registerTenant` server action نفسها وقت إنشاء التينانت.
- `/partners/apply` (طلب يُراجَع يدوياً بعد أيام): **لا يمكن الاعتماد على قراءة الكوكي وقت الموافقة**
  — الموافقة تحدث من متصفح مالك المنصة، الذي لم يزر رابط الإحالة إطلاقاً. الإصلاح: يُقرأ الكوكي وقت
  *التقديم* نفسه (متصفح المتقدّم الحقيقي) ويُخزَّن `referralAffiliateId` داخل `payloadJson` لطلب
  `ApprovalRequest`، ثم يُقرأ من هناك (لا من كوكي) عند إنشاء التينانت الفعلي في `approvePartnerApplication`.
  هذا فرق جوهري بين "من أين تُقرأ بيانات الجلسة" حسب توقيت الحدث الفعلي، وليس أسلوبين عشوائيين.

**عمولة حقيقية على حدود حقيقية، لا ادّعاء "غير محدود"**: العمولة = نسبة % من `Invoice.amountSar`
الفعلي المدفوع — نفس مبدأ الصدق في الإعلان المطبَّق على الباقات نفسها (قرار #233).

**فخّ Next.js حقيقي اكتُشف وأُصلح أثناء الاختبار الحي**: ملف `affiliates/dashboard/actions.ts` صدَّر
ثابتاً (`MIN_PAYOUT_SAR`) من ملف عليه `"use server"` — Next.js يمنع تصدير أي شيء غير دالة async من
ملف كهذا، وفشل البناء فعلياً برسالة "Only async functions are allowed to be exported". نُقل الثابت
لملف عادي منفصل (`lib/affiliates/payout.ts`) لا يحمل التوجيه.

**صلاحية RBAC جديدة**: `platform.affiliates.manage`، مقصورة على `SUPER_ADMIN` فقط (قرار مطابق لبقية
الصلاحيات المالية/الحساسة مثل `platform.plans.manage`) — لا `PLATFORM_SUPPORT`/`PLATFORM_BILLING`.

**دورة صرف يدوية بتحكم مالك المنصة الكامل** (وليست آلية بالكامل — لا مزوّد دفع حقيقي مربوط بعد أصلاً):
المسوّق يطلب الصرف عند تجاوز رصيده المعتمد 375 ر.س (تحويل ×3.75 من $100 المرجعي، بتأكيد صاحب المشروع
— نفس منهجية تحويل الباقات)، فتُنشأ `Payout{status: PENDING}` وتُربَط بها العمولات المتاحة
(`payoutId` فقط، تبقى `status: APPROVED` حتى الصرف الفعلي — تمييز متعمَّد بين "مطالَب بها" و"مصروفة
فعلياً"). مالك المنصة يعتمد الصرف يدوياً (`markPayoutPaid`، يُحوِّل العمولات لـPAID فعلياً) أو يفشله
(`markPayoutFailed`، يُعيد العمولات لحالة "متاحة" من جديد بدل أن تبقى عالقة للأبد).

**التحقق الفعلي (Playwright بمتصفح حقيقي + سكربت مباشر لمحاكاة فاتورة مدفوعة، بلا أي تخمين)**:
- تقديم طلب مسوّق حقيقي عبر `/affiliates/apply` ← ظهر في `/admin/affiliates` ← قُبِل فعلياً ← تسجيل
  دخول المسوّق في `/affiliates/dashboard` نجح.
- محاكاة تاجر مُحال حقيقي بفاتورة مدفوعة 185 ر.س ← `syncAffiliateCommissions()` أنشأت عمولة 37 ر.س
  (185 × 20% بالضبط) بحالة PENDING ← بعد تجاوز `eligibleAt`، رُقِّيت لـAPPROVED تلقائياً في التشغيلة
  التالية، بدون تكرار (idempotency تحقّق: `created: 0` في التشغيلة الثانية لنفس الفاتورة).
  فشلت المحاولة الأولى فعلياً بخطأ RLS حقيقي (وليس افتراضياً) — أُصلح كما هو موثَّق أعلاه.
- 10 فواتير إضافية دفعت الرصيد المعتمد لـ407 ر.س (تجاوز حد 375) ← زرّ "طلب صرف" ظهر فعلياً في
  الداشبورد ← الطلب نجح ← ظهر في `/admin/affiliates` ضمن "طلبات صرف بانتظار المعالجة" بالمبلغ
  الصحيح ← "تم الصرف فعلياً" حوَّل حالة العمولات الإحدى عشرة لـPAID فعلياً في قاعدة البيانات (تحقّق
  مباشر عبر SQL: `SUM(amountSar) = 407` بحالة PAID) والـPayout نفسه لـPAID مع `paidAt` صحيح.
- `npx tsc --noEmit` نظيف على كامل المشروع. `npm run smoke-test`: فشل أولي غير قابل لإعادة الإنتاج
  فور مسح `.next` وإعادة تشغيل باردة (تحميل خادم بطيء جداً وقت التصريف الأول — "GET /logo.png 404 in
  6122ms" يوثّق الحمل الفعلي وقتها) — تكرار الاختبار بعد اكتمال الإحماء نجح بالكامل (دخول/داشبورد/
  جهات اتصال/حملة)، الفشل المتبقي الوحيد هو نفسه المُوثَّق سابقاً (محرر الشات بوت، نص قديم في
  السكربت) وغير متعلق بهذه الجولة إطلاقاً. كل بيانات الاختبار (مسوّقون/تجّار/فواتير/عمولات وهمية)
  حُذفت من القاعدة بعد التحقق، وكل السكربتات المؤقتة حُذفت من `scripts/`.

## 2026-08-20 — صفحة "الطلبات" في لوحة التاجر (طلبات زد/سلة + استهداف سلات متروكة بحملة)

### 235. Phase 1 على بيانات Order الموجودة فعلياً — بلا OrderItem، وبلا أي منطق استهداف جديد
البرومنت حدّد صراحة نطاق هذه الجولة: `Order.totalSar` إجمالي فقط (لا تفاصيل منتجات/سطور سلة —
لا يوجد `OrderItem` في السكيما)، فعرض محتوى السلة نفسه (أسماء المنتجات) مؤجَّل لـPhase 2 منفصل
يتطلب migration جديدة + تعديل `zid/adapter.ts` و`salla/adapter.ts::syncOrders` لتخزين السطور —
لم يُطلَب صراحة في هذه الجولة فلم يُبنَ.

**لا صلاحية استهداف جديدة، ولا تعديل على `SegmentFilter`**: `lib/campaigns/audience.ts::SegmentFilter.order`
(`{status, withinDays}`) كان موجوداً أصلاً (يُستخدم فعلياً في `previewTriggerMatchCount` وحملة
ABANDONED_CART الآلية) — زر "استهدف بحملة" الجديد في `/dashboard/orders` يبني نفس الشكل ويمرره
كـquery params (`orderStatus`, `orderWithinDays`) لـ`/dashboard/campaigns/new`، الذي يبنيه كـ
`initialValues.filter.order` بنفس آلية `audienceTagId` الموجودة أصلاً لجهات الاتصال — **صفر تعديل**
على `CampaignWizard.tsx` نفسه (كان يدعم `filter.order` من قبل أصلاً، لم يُستخدَم من أي مكان في
الواجهة قبل هذه الجولة).

**تحديد جماعي بحالات مختلطة يُعطِّل "استهدف بحملة" بدل اختراع فلتر "قائمة حالات"**: `SegmentFilter.order.status`
حقل واحد فقط (وليس مصفوفة) — فلو التحديد يخلط حالتين مختلفتين (مثال: طلب PAID وطلب ABANDONED_CART
معاً)، لا يوجد فلتر SEGMENT واحد صحيح يمثّلهما معاً. القرار: تعطيل الزر (`title` توضيحي) بدل توسيع
شكل `SegmentFilter` لدعم مصفوفة حالات — يتطابق مع تحذير البرومنت "لو تحديد جماعي بنفس الحالة" (كانت
هذه هي الحالة المتوقَّعة أصلاً، وليست استثناءً نادراً).

**لا يوجد "استهداف جهة اتصال واحدة بدقة" — `withinDays` يُحسَب من عمر أقدم طلب محدَّد بدلاً منه**:
`AudienceType` في السكيما `ALL | SEGMENT` فقط — لا يوجد نوع جمهور "جهة اتصال واحدة بعينها"، ومعالج
الحملة (`CampaignWizard`) لا يدعم تمرير `contactId` مباشرة. فتحديد صف واحد فقط في صفحة الطلبات يُبنى
له نفس فلتر `{status, withinDays}` مثل التحديد الجماعي (`withinDays` = الفرق بالأيام بين الآن وتاريخ
إنشاء ذلك الطلب، مقرَّباً لأعلى) — تقريب مقصود (قد يُدرِج طلبات أخرى بنفس الحالة ضمن نفس النافذة
الزمنية لم تُحدَّد صراحة)، موثَّق كتعليق كود في `OrdersTable.tsx::handleTargetCampaign`. استهداف دقيق
لجهة اتصال واحدة فقط يتطلب إضافة `AudienceType.SINGLE_CONTACT` جديد للمعالج نفسه — امتداد مستقبلي
غير مطلوب في هذه الجولة (البرومنت نفسه أتاح هذا الخيار صراحة: "وضّح في الكود إن ده امتداد مطلوب لاحقاً").

**"إرسال سريع الآن" يُعيد استخدام `sendTemplateReply` الموجودة، لا يبني مسار إرسال مواز**: القيد
الحقيقي (قالب معتمد فقط خارج نافذة الـ24 ساعة) مفروض أصلاً في `dashboard/inbox/actions.ts::sendTemplateReply`
— فعل جديد `quickSendOrderTemplate` (`dashboard/orders/actions.ts`) يُنشئ محادثة للعميل فقط إن لم
توجد أصلاً (نفس نمط `queue/worker.ts::sendToContact` عند إرسال حملة لعميل بلا محادثة سابقة)، ثم
يستدعي `sendTemplateReply` نفسها مباشرة — **صفر تكرار** لمنطق الإرسال/فرض القالب المعتمد/تحديث
العدادات. فعل منفصل `getOrderContactSendRoute` يُشغَّل أولاً ليقرر المسار: محادثة بنافذة 24 ساعة
مفتوحة فعلاً ← توجيه مباشر لـ`/dashboard/inbox/[id]` (رد نصي حر متاح هناك أصلاً، بلا حاجة لقالب) بدل
عرض قائمة قوالب لا داعي لها؛ غير ذلك ← قائمة القوالب المعتمدة فقط (فرض حقيقي من الخادم عبر استعلام
`status: "APPROVED"`، وليس فقط تعطيل خيارات غير معتمدة في الواجهة كما في معالج الحملة الكامل).

**تحديث `Order.recoveryMessageSentAt` عند الإرسال اليدوي لطلب سلة متروكة**: نفس الحقل الذي يمنع الحملة
الآلية (`triggerEngine.ts`، حدث `ABANDONED_CART`) من استهداف نفس الطلب مرتين — لولا تحديثه هنا أيضاً،
كانت الحملة الآلية سترسل رسالة استرداد ثانية لاحقاً رغم إرسال هذه اليدوية للتو (تحقّق فعلي: نفس الحقل
تحديداً هو ما تعرضه عمود "حالة التواصل" في تبويب السلات المتروكة أصلاً — كان سيكذب لو لم يُحدَّث).

**صلاحيتان جديدتان بنفس فلسفة `contacts.export`**: `orders.view` (عرض فقط، يشمل AGENT — بيانات طلبات/
مبالغ تخدم رد الموظف على استفسارات العملاء) و`orders.manage` (تحديد جماعي/استهداف/إرسال سريع، OWNER/ADMIN
فقط — نفس استبعاد AGENT من `contacts.export`: بيانات عملاء حساسة، الاستهداف الفعلي والإرسال قرار
تجاري وليس رداً روتينياً). "استهدف بحملة" تحديداً يتطلب `orders.manage` **و**`campaigns.manage` معاً
(الزر يُنشئ حملة فعلية حقيقية) — مُفعَّل حالياً بلا تعارض فعلي لأن OWNER/ADMIN يملكان الاثنين معاً،
لكن الفحصان منفصلان عمداً في الكود دفاعاً مستقبلياً لو انحرفت الصلاحيتان.

**مسار الصفحة مرئي دائماً لحامل `orders.view` (لا يُخفى حسب حالة التكامل) — المحتوى فقط يتغيّر**: قراءة
حرفية أولى للبرومنت ("المسار يظهر فقط لو فيه تكامل متصل") بدت تناقض الجملة التالية مباشرة ("لو مفيش
تكامل متصل، اعرض حالة فارغة") — التفسير المتّسق الوحيد: التبويب في الشريط الجانبي يُحكَم بالصلاحية فقط
(نفس بقية عناصر `NAV_ITEMS`)، والصفحة نفسها (`dashboard/orders/page.tsx`) تفحص `Integration{provider:
{in:[ZID,SALLA]}, status:CONNECTED}` (نفس فحص `integrations/page.tsx` حرفياً) وتعرض حالة فارغة بزر CTA
لـ`/dashboard/integrations` بدل جدول فاضٍ لو لم يتصل بعد — بدل إخفاء الرابط بالكامل وترك التاجر بلا
أي طريق واضح لاكتشاف الميزة أصلاً.

**ملف تسميات مشترك جديد**: `lib/orders/labels.ts` — نقل `ORDER_STATUS_LABELS_AR` من نسخة محلية ثابتة
كانت مكرَّرة في `admin/tenants/[id]/orders/page.tsx` فقط، مع إضافة `ORDER_STATUS_BADGE_CLASSNAMES`
(ألوان شارات الحالة) و`ORDER_STATUSES_ORDERED` (ترتيب التبويبات) — نفس مبدأ `lib/contacts/stages.ts`
تماماً. يُستخدَم الآن من الصفحتين معاً (لوحة Super Admin ولوحة التاجر) بدل نسخة واحدة يتيمة.

**التحقق الفعلي (تسجيل دخول حقيقي بجلسات NextAuth منفصلة لكل دور، ربط Zid Sandbox حقيقي، ومزامنة
حقيقية)**: تاجر اختباري جديد بلا أي تكامل ← `/dashboard/orders` عرض فعلياً حالة فارغة بزر CTA صحيح
(وليس جدولاً فارغاً). ربط Zid (وضع Sandbox) عبر إعادة تشغيل فعل `connectIntegration` ثم `resyncIntegration`
حقيقيين ← تحقّق مباشر عبر SQL أن `Integration.status = CONNECTED` وأن صفي `Order` الوهميين
(`SANDBOX_ZID_ORDERS`، أحدهما `ABANDONED_CART` فعلياً) دخلا القاعدة فعلاً. فتح `/dashboard/orders`
كمالك حساب ← الطلبان ظهرا فعلياً بالاسم والمبلغ الصحيحين؛ تبويب "🛒 سلة متروكة" ← الطلب الصحيح فقط
ظهر، وعمود "حالة التواصل" عرض "لم يُتواصَل بعد" فعلياً (`recoveryMessageSentAt` كان `null` حقاً في
القاعدة وقتها). تسجيل دخول كموظف (دور AGENT) بجلسة منفصلة ← نفس الجدول ظهر بلا أي خانة اختيار ولا
شريط إجراء عائم إطلاقاً (لا `orders.manage`). التوجيه لمعالج الحملة عبر
`/dashboard/campaigns/new?orderStatus=ABANDONED_CART&orderWithinDays=5` ← تحقّق مباشر من الـRSC payload
الفعلي المُرسَل للمتصفح أن `initialValues.filter.order` وصل بالضبط `{"status":"ABANDONED_CART","withinDays":5}`
(الشكل الذي يقرأه `CampaignWizard.tsx` فعلياً لتعبئة خطوة الجمهور مسبقاً). قالب معتمد اختباري أُنشئ
مباشرة ← ظهر فعلياً ضمن بيانات الصفحة المُرسَلة لمكوّن "إرسال سريع الآن" (`approvedTemplates`)، مؤكِّداً
أن استعلام القوالب المعتمدة (`status: "APPROVED"`) يعمل بشكل صحيح. تعذّر اختبار الضغط الفعلي على زري
"استهدف بحملة"/"إرسال سريع الآن" بمتصفح حقيقي تفاعلي (لا أداة أتمتة متصفح متاحة في هذه الجلسة) — رُوجِع
منطق الفعلين (`getOrderContactSendRoute`, `quickSendOrderTemplate`) كودياً بعناية وتأكَّد مباشرة عبر SQL
أن عميل السلة المتروكة الاختباري بلا أي محادثة قائمة أصلاً (فيُحسَب المسار الصحيح نظرياً: "قالب" لا
"صندوق محادثات") — لم يُختبَر الإرسال الفعلي عبر قناة واتساب حقيقية لعدم ربط أي قناة (Meta/QR) بهذا
التاجر الاختباري. `npx tsc --noEmit` و`npm run build` نظيفان على كامل المشروع (70+ مساراً، منها
`/dashboard/orders` الجديد). كل بيانات الاختبار (تاجر/مستخدمَين/تكامل/طلبات/قالب) حُذفت من القاعدة
بعد التحقق.

## 2026-08-20 (جولة ثانية) — صلاحيات مخصَّصة لكل عضو فريق فوق نظام الأدوار (Custom Permissions)

### 236. تخصيص فردي = استبدال كامل لمصفوفة الدور، وليس دمجاً — بشرط استبعاد بنيوي لصلاحيتين لا تُخصَّصان أبداً
`User.customPermissionsJson` (عمود جديد، `Json?`، `null` = لا تخصيص) يخزّن مصفوفة صلاحيات كاملة تحل
محل افتراضي الدور بالكامل عند وجودها (`resolveEffectivePermissions`: `custom ?? getRoleDefaultPermissions(role)`
— استبدال، وليس دمجاً إضافياً فوق افتراضي الدور). **`team.manage`/`billing.manage` مستبعدتان بنيوياً من
أي تخصيص ممكن** (`NON_CUSTOMIZABLE_PERMISSIONS`) — فتح إحداهما لعضو عادي يفتح مسار تصعيد صلاحيات ذاتي
(موظف يمنح نفسه صلاحيات إدارة الفريق/الفوترة)، ولأن هاتين تحديداً يجب أن تبقيا صادقتين لقيمة الدور
الفعلية دائماً بلا استثناء منطقي.

**اكتشاف حقيقي أثناء البناء (وليس افتراضاً من البداية)**: بما أن التخصيص *استبدال كامل* لا دمج، أي
عضو ADMIN يُخصَّص لأي سبب (حتى لو التخصيص لا علاقة له إطلاقاً بـ`team.manage`) كان سيفقد `team.manage`
بصمت فعلياً لو استُخدمت `hasEffectivePermission`/`requireEffectivePermission` في كل نقطة تفحصها —
لأن `team.manage` لا يمكن أن تظهر أصلاً في أي مصفوفة تخصيص محفوظة (مرفوضة صراحة في `updateUserPermissions`).
هذا **يناقض تصميم `NON_CUSTOMIZABLE_PERMISSIONS` نفسه** (تعليقه الخاص: "تبقى هاتين دايماً تابعتين لقيمة
الدور فقط، بلا استثناء"). القرار الفعلي المطبَّق: **كل نقطة فحص لـ`team.manage`/`billing.manage` تحديداً
في كامل `src/app/dashboard/**` (وليس نقطة واحدة فقط) تبقى دورية (`hasPermission`/`requirePermission` على
`session.user.role`)** — مواقعها: `settings/page.tsx` (حارس الصفحة)، `settings/actions.ts` (3 مواقع:
`inviteTeamMember`, `changeUserRole`, `toggleUserActive`, بالإضافة لفعل `updateUserPermissions` الجديد
نفسه)، و`billing/actions.ts` (9 مواقع) + `billing/page.tsx` (`canManage`). كل صلاحية أخرى غيرهما
(بما فيها `billing.view` في نفس هذين الملفين) هُجِّرت فعلياً لـ`hasEffectivePermission`/`requireEffectivePermission`.

### 237. ثغرة RLS حقيقية اكتُشفت أثناء الاختبار الحي: `rawDb.user.findUnique()` يُرجع `null` دائماً
`User` جدول خاضع لـRLS فعلياً (`prisma/postgres-rls.sql`، ضمن `tenant_tables`)، بعكس `Tenant` (لا
`tenantId` له، غير خاضع لـRLS إطلاقاً). استعلام مباشر عبر `rawDb.user.findUnique()` بلا `withTenant()`
(بلا `SET LOCAL app.current_tenant_id`) يجعل شرط الـPolicy `"tenantId" = current_setting(...)` يقارن
بقيمة `NULL` دائماً — لا صف مرئي مطلقاً، لأي مستخدم، بصرف النظر عن المعطيات. **هذا كان سيطرد كل تاجر
فوراً لصفحة تسجيل الدخول عند أول طلب** (`if (!freshUser?.tenant) redirect("/login")` في `requireTenantSession`
الجديدة). اكتُشف فعلياً عبر اختبار حي (لا افتراضاً)، وأُصلح باستبدال `rawDb.user.findUnique()`
بـ`withTenant(typedSession.user.tenantId, tx => tx.user.findUnique(...))` — `tenantId` معروف بالفعل
في تلك اللحظة من الجلسة نفسها، فلا حاجة لاستعلام خارج سياق RLS أصلاً.

### 238. جلسة الانتحال يجب أن تمرّر `customPermissionsJson` صراحة، وإلا "الدخول كخبير" يفقد غرضه
`getCurrentSession()` (فرع الانتحال) كانت تبني كائن مستخدم من حقول مُختارة يدوياً (`id`, `name`,
`email`, `role`, `tenantId`) — **بدون** `customPermissionsJson`، رغم أن `targetUser` نفسه مجلوب بلا
`select` (يحمل الحقل فعلياً). لو تُرك كما كان، عضو فريق الدعم المنتحل هوية عضو مخصَّص الصلاحيات كان
سيرى صلاحيات دور ذلك العضو الافتراضية فقط (وليس صلاحياته الفعلية المخصَّصة) — يُفشِل الغرض الأساسي من
الانتحال (تشخيص ما يراه المستخدم الحقيقي بالضبط). أُضيف `customPermissionsJson: targetUser.customPermissionsJson`
صراحة لذلك الكائن.

### 239. تغيير الدور يصفّر التخصيص السابق دائماً — لا "تخصيص مبني على دور قديم"
`changeUserRole` تضبط `customPermissionsJson: Prisma.JsonNull` عند أي تغيير دور (وليس `null` حرفياً —
Prisma يرفض `null` مباشرة لحقل `Json?` عبر خطأ TypeScript، `Prisma.JsonNull`/`Prisma.DbNull` هما
الطريقتان الصحيحتان). بدون هذا، عضو AGENT مخصَّص بصلاحية واحدة إضافية (مثال: `orders.manage` فقط) ثم
تُرقّى لـADMIN كان سيرث نفس المصفوفة الضيقة القديمة حرفياً بدل صلاحيات ADMIN الكاملة الافتراضية — تحقّق
فعلياً: ترقية AGENT مخصَّص إلى ADMIN أعادت `customPermissionsJson` إلى `null` فعلياً في القاعدة،
وجلسة ذلك المستخدم (بعد إعادة تسجيل الدخول، أو تلقائياً عبر نفس آلية الفحص الحي في `requireTenantSession`)
أظهرت صلاحيات ADMIN الكاملة (مثال محقَّق: `integrations.manage`، لم تكن موجودة إطلاقاً في مصفوفة
AGENT المخصَّصة القديمة).

### 240. فجوة اكتُشفت أثناء الاختبار: مسارات تاجر خارج `src/app/dashboard/**` كانت ستبقى دورية بصمت
البرومنت حدَّد نطاق الهجرة الميكانيكية بدقة (`src/app/dashboard/**` فقط، استثناء `src/app/admin/**`)
— لكن 3 مسارات API تاجرية حقيقية تعيش خارج `dashboard/**` هيكلياً رغم أنها جزء من نفس الميزات
المتأثرة تماماً: `api/contacts/export/route.ts` (`contacts.export`)، و`api/integrations/{zid,salla}/callback/route.ts`
(`integrations.manage`). لو تُركت هذه دورية بينما زر التصدير في `dashboard/contacts/page.tsx` صار يعتمد
`hasEffectivePermission`، كانت النتيجة عطلاً حقيقياً ملموساً: **الزر يظهر لعضو مخصَّص بصلاحية `contacts.export`،
لكن الضغط عليه يفشل فعلياً** (نقطة النهاية الحقيقية ترفضه لأن دوره الأساسي لا يملك الصلاحية). اكتُشف
فعلياً بالاختبار الحي (وليس توقُّعاً مسبقاً) — طلب تصدير حقيقي بجلسة AGENT مخصَّصة فشل قبل الإصلاح،
ونجح (200 + ملف CSV حقيقي) بعده. الثلاثة رُحِّلت لـ`requireEffectivePermission`، بنفس مبدأ الفصل بين
`NON_CUSTOMIZABLE_PERMISSIONS` (لا شيء منها team.manage/billing.manage) — متسقة تماماً مع نطاق الهجرة
المقصود فعلياً (كل صلاحية تاجرية قابلة للتخصيص)، وليست خروجاً عنه.

### 241. `maxWait`/`timeout` أوسع لكل معاملات `withTenant()` — اكتُشف حداً فاصلاً حقيقياً أثناء الاختبار
اختبار حي متكرر كشف أن أول معاملة فعلية بعد أي إعادة تحميل للعملية (محرك Prisma "بارد" خلف اتصال
Docker محلي) تستغرق ~2.1 ثانية فعلياً — أعلى من `maxWait` الافتراضي (2000ms) بفارق ضئيل، فتفشل أول
معاملة بعد كل hot-reload برسالة "Unable to start a transaction in the given time" رغم أن الاستعلام
نفسه صحيح تماماً (تحقَّق مباشرةً عبر سكربت معزول: نفس الاستعلام نجح في عملية منفصلة، 2.088 ثانية
تحديداً للتشغيلة الأولى، ثم 3-4 مللي ثانية للتالية في نفس العملية — تأكيد قاطع أنها تكلفة اتصال بارد
لمرة واحدة، وليست مشكلة استعلام). نفس النمط متوقَّع إنتاجياً عند استيقاظ قاعدة بيانات مُدارة من سكون
(خطط الاستضافة المجانية/الرخيصة). `withTenant()` (`lib/db.ts`) صارت تمرّر `{ maxWait: 10000, timeout: 15000 }`
صراحة لـ`$transaction()` — هامش أوسع لا يُبطئ الحالة الشائعة (المعاملة الحقيقية تنتهي خلال مللي ثوانٍ
عادة) بل يمنح مهلة كافية فقط للحالة الباردة النادرة. يؤثر هذا على **كل** استدعاء `withTenant()` في
المشروع (وليس فقط `requireTenantSession()` الجديدة) — قرار متعمَّد: نفس الخطر كان موجوداً ضمنياً من
قبل في أي مسار آخر يستدعي `withTenant()` كأول معاملة بعد بدء تشغيل العملية.

**التحقق الفعلي (تسجيل دخول حقيقي بجلسات منفصلة، ومسار API اختباري مؤقت لاستدعاء الأفعال الحقيقية
مباشرة بدلاً من محاولة محاكاة بروتوكول Server Actions عبر JS يدوياً)**:
- عضو AGENT بلا أي تخصيص (`customPermissionsJson = null`) ← فتح `/dashboard/contacts` نجح تماماً
  بلا زر تصدير (سلوك افتراضي الدور الحالي، بلا أي تغيير) — تحقّق أن الترحيل الميكانيكي لم يكسر شيئاً.
- منح نفس العضو (بنفس جلسته السارية، **بلا تسجيل خروج/دخول**) صلاحية `contacts.export` عبر تحديث
  مباشر يطابق ناتج `updateUserPermissions` ← إعادة تحميل `/dashboard/contacts` أظهرت زر التصدير فوراً،
  والضغط الفعلي عليه (طلب حقيقي لـ`/api/contacts/export`) نجح فعلياً (200 + ملف CSV حقيقي) — يتحقق
  مباشرة من إصلاح مشكلة الـJWT القديم (بند 237) وسد فجوة المسارات خارج `dashboard/**` (بند 240) معاً.
- مسار API اختباري مؤقت (`api/test-permissions-temp/route.ts`، حُذف فوراً بعد الاختبار ولم يُنشر أبداً)
  استدعى `updateUserPermissions`/`changeUserRole` الحقيقيتين مباشرة (تجاوزاً لصعوبة محاكاة بروتوكول
  استدعاء Server Action عبر JS مباشرة بـcurl، بخلاف نموذج HTML عادي القابل للمحاكاة بحقول مخفية):
  تمرير `["team.manage"]` أو `["platform.merchants.delete"]` كحمولة مباشرة ← رُفضا فعلياً برسالة
  "صلاحية غير صالحة" (وليس فقط توقُّعاً نظرياً). استهداف `updateUserPermissions` لمستخدم OWNER ← رُفض
  فعلياً برسالة "لا يمكن تخصيص صلاحيات صاحب الحساب".
- ترقية عضو AGENT مخصَّص (`["chatbot.view","chatbot.test","inbox.reply","contacts.manage","orders.view","contacts.export"]`)
  إلى ADMIN عبر `changeUserRole` الحقيقية ← تحقّق مباشر عبر SQL أن `customPermissionsJson` عاد `null`
  فعلياً، ثم تسجيل دخول جديد لنفس الحساب أظهر صلاحيات ADMIN الكاملة الافتراضية فعلياً (مثال محقَّق:
  وصول لـ`/dashboard/integrations`، صلاحية لم تكن موجودة إطلاقاً في مصفوفة AGENT المخصَّصة القديمة).
- `npx tsc --noEmit` و`npm run build` نظيفان على كامل المشروع بعد حذف كل الملفات المؤقتة وتنظيف
  `.next` (بقايا أنواع مُولَّدة للمسار الاختباري المحذوف كانت تظهر كخطأ TS حتى مسح الذاكرة المؤقتة).
  كل بيانات الاختبار (تاجر/مستخدمَين) حُذفت من القاعدة المحلية بعد التحقق.

## 2026-08-20 (جولة ثالثة) — صلاحيات مخصَّصة لفريق المنصة الداخلي + إصلاح ثغرة جلسة قديمة حرجة

### 242. الثغرة الأخطر في هذه الجولة: `requireSuperAdminSession` كانت تثق بالكامل بتوكن موقَّع بلا أي فحص حي
`getPlatformSession()` (كوكي `dms_platform_session`) يتحقق فقط من صحة توقيع JWT ويُرجع البيانات
المخزَّنة فيه *وقت تسجيل الدخول* — بلا أي استعلام قاعدة بيانات. نفس المشكلة بالضبط في فرع NextAuth
الاحتياطي (JWT سارٍ 7 أيام، بلا تحديث دور حي). **النتيجة العملية**: عضو فريق دعم فني عُطِّل فوراً
(`toggleInternalStaffActive`) بسبب مشكلة أمنية، أو غُيِّر دوره (`changeInternalStaffRole`) لتقليص
صلاحياته، كان يحتفظ بوصول كامل بصلاحياته *القديمة* لحد 7 أيام كاملة — تعطيل حساب فريق داخلي لم يكن
يعني شيئاً عملياً طالما الكوكي/التوكن لسه ساري. نفس فئة الثغرة المُصلَحة سابقاً لجلسات التجار
(`requireTenantSession`، القرار #237 هذه الوثيقة)، لكن هنا للمرة الأولى مطبَّقة على فريق المنصة
الداخلي نفسه — أخطر عملياً لأن صلاحيات فريق المنصة أشمل بطبيعتها (وصول لبيانات كل التجار).

**الإصلاح**: `loadFreshPlatformStaff()` — استعلام حي واحد عبر `superAdminDb` (وليس `rawDb`؛ صفوف
`tenantId: null` غير مرئية عبر `app_runtime` أصلاً، نفس السبب الموثَّق فعلاً في `admin/team/page.tsx`)
يُنفَّذ في **كلا فرعي** `requireSuperAdminSession` (كوكي المنصة وNextAuth الاحتياطي) بعد التحقق من
التوقيع/الجلسة مباشرة. `isActive: false` أو دور لم يعد `platform.*` ← طرد فوري لـ`/admin-login`، بصرف
النظر عن صلاحية التوقيع/بقية مدة الكوكي. الدور/الاسم/البريد/الصلاحيات المُرجَعة كلها من هذه القراءة
الحية، وليس من التوكن.

### 243. فخّ Next.js حقيقي اكتُشف أثناء الاختبار الحي: `cookies().delete()` ممنوعة أثناء تصيير مكوّن خادم
التصميم الأصلي لهذا الإصلاح تضمَّن استدعاء `clearPlatformSession()` (يمسح الكوكي الفاسد) قبل التوجيه —
بدا منطقياً (تنظيف استباقي)، لكن فشل فعلياً وقت الاختبار الحي بخطأ 500 حقيقي: **"Cookies can only be
modified in a Server Action or Route Handler"**. السبب: `requireSuperAdminSession()` تُستدعى من مكوّنات
خادم (صفحات/layouts مباشرة، وليس فقط من داخل Server Actions) — Next.js يمنع بنيوياً أي تعديل كوكيز
أثناء التصيير (HTTP headers يجب أن تُحسَم قبل بدء بث الجسم). **الإصلاح**: إزالة استدعاء
`clearPlatformSession()` من نقطة الفحص الحي بالكامل — التوجيه وحده (`redirect("/admin-login")`) يكفي
أمنياً (يمنع أي وصول فوري فعلياً)، والكوكي القديم يبقى مرفوضاً في *كل* طلب تالٍ بنفس هذا الفحص بالضبط
(لا خطر أمني من بقائه — مجرد كوكي متروك في المتصفح لا يُستخدَم أبداً بنجاح). تحقّق فعلياً: بعد الإصلاح،
تعطيل عضو بجلسة سارية ← الطلب التالي بنفس الكوكي `307` لـ`/admin-login` مباشرة، بلا أي خطأ 500.

### 244. `resolveEffectivePermissions`/`parseCustomPermissions` صارت تفرّق بين "عالم" التاجر و"عالم" المنصة
كانت الدالة تفترض عالم صلاحيات واحداً فقط (`TENANT_PERMISSION_LABELS_AR`) وتستبعد أي دور `platform.*`
من التخصيص كلياً. التوسعة (`PLATFORM_SUPPORT`/`PLATFORM_BILLING` قابلان للتخصيص الآن، `SUPER_ADMIN`
يبقى مستثنى دائماً بنفس منطق استثناء `OWNER`) تطلَّبت أن تختار `parseCustomPermissions` عالم الصلاحيات
الصحيح (`PLATFORM_PERMISSION_LABELS_AR` أو `TENANT_PERMISSION_LABELS_AR`) بناءً على دور المستخدم نفسه
— منعاً لتسرّب صلاحية من عالم لآخر (تحقّق فعلياً: تمرير `contacts.export` — صلاحية تاجرية بحتة — كتخصيص
لعضو `PLATFORM_SUPPORT` رُفض فعلياً بـ"صلاحية غير صالحة"، رغم أنها نص صالح موجود في `Permission` نفسه).
`NON_CUSTOMIZABLE_PLATFORM_PERMISSIONS` (`platform.team.manage`, `platform.billing.manage`) بنفس فلسفة
`NON_CUSTOMIZABLE_PERMISSIONS` التاجرية تماماً — سبب الاستبعاد موثَّق كتعليق كود مباشرة فوق كل ثابت.

**فلترة يدوية بدل `Set.prototype.difference` (ES2024) عمداً**: البرومنت اقترح استخدامها مع تنبيه صريح
بالتحقق من الدعم أولاً. `lib.esnext` في `tsconfig.json` يدعمها نوعياً، وNode.js المحلي (v24) يدعمها
وقت التشغيل فعلياً (تحقَّق مباشرة)، **لكن** لا يوجد `engines`/`.node-version` في المشروع يضمن نفس
النسخة على خادم الإنتاج (Render) — القرار: فلترة يدوية بمصفوفة (`Array.filter`) تعمل على أي نسخة
Node.js بلا أي مخاطرة توافق، بدل الاعتماد على ميزة قد لا تكون مدعومة فعلياً وقت التشغيل الحقيقي.

### 245. هجرة `src/app/admin/**` — نفس منهجية هجرة `dashboard/**` سابقاً، مع استثناءين محدَّدين بدقة
بحث-واستبدال ميكانيكي (`hasPermission(session.user.role, ...)` ← `hasEffectivePermission(session.user.permissions, ...)`
وما يعادلها لـ`requirePermission`) عبر 29 ملفاً، **باستثناء** كل استدعاء بـ`platform.team.manage`
(11 موقعاً: `team/page.tsx` ×1، `team/actions.ts` ×5 شاملة الأفعال الأربعة الجديدة، إضافة لموقعين
مختلطين في `billing/page.tsx`/`billing/accounts/page.tsx`) وكل استدعاء بـ`platform.billing.manage`
— كلاهما يبقى دورياً بنفس المنطق المُوثَّق في `NON_CUSTOMIZABLE_PLATFORM_PERMISSIONS` (القرار #244).
تحقُّق أمان مسبق قبل الهجرة: عدد استدعاءات `hasPermission(session.user.role,...)`/`requirePermission(session.user.role,...)`
(83) قُورن بعدد كل الاستدعاءات بأي شكل (85) — الفارق (2) تحديداً حلقة `admin/team/page.tsx` "مصفوفة
الصلاحيات الشاملة" (`hasPermission(role, permission)` بمتغيّر حلقة `role`، وليس `session.user.role`) —
تُركت بلا لمس تلقائياً (نمط لا يطابق البحث أصلاً)، لأنها تعرض افتراضي كل الأدوار للمقارنة، وليست فحص
تفويض جلسة حالية.

### 246. لوحة "تخصيص الصلاحيات" لفريق المنصة — مكوّن جديد منفصل، بنفس بنية النسخة التاجرية تماماً
`PlatformPermissionsEditor.tsx` (`admin/team/`) نسخة مطابقة بنيوياً لـ`PermissionsEditor.tsx`
(`components/dashboard/`، الجولة السابقة) لكن بصلاحيات/تحذيرات مختلفة: `PLATFORM_PERMISSION_LABELS_AR`
بدل `TENANT_PERMISSION_LABELS_AR`، وتحذيرات "صلاحية حساسة جداً" على `platform.merchants.impersonate`،
`platform.merchants.delete`، `platform.settings.manage`، `platform.merchants.change_plan` تحديداً
(وصول/تأثير على بيانات تجّار حقيقيين، أعلى خطورة من نظيراتها التاجرية). زران إضافيان جديدان لكل عضو
غير `SUPER_ADMIN`: `EditStaffProfileForm.tsx` (نموذج صغير قابل للطي لتعديل الاسم/البريد — الفعل نفسه
يسمح بالاستثناء الوحيد: مالك المنصة يعدّل بيانات حسابه *الشخصي* هو فقط، لا حساب مالك منصة آخر) و
`ForceResetPasswordButton.tsx` (بنفس أسلوب `RemoveStaffButton.tsx` — تأكيد قبل التنفيذ لأنه إجراء
فوري لا رجعة فيه: يُبطل كلمة المرور الحالية فوراً بتجزئة عشوائية غير قابلة للاستخدام إطلاقاً، وينشئ
رابط إعداد حساب جديد بنفس آلية `inviteInternalStaff`/`AccountSetupToken` الموجودة أصلاً — صفر تكرار
منطق).

**تصفير التخصيص عند تغيير الدور**: `changeInternalStaffRole` تضبط `customPermissionsJson: Prisma.JsonNull`
عند أي تغيير دور — نفس القرار المتّخذ لتغيير دور تاجر (AGENT⇄ADMIN، الجولة السابقة) ولنفس السبب بالضبط
(تفادي تخصيص ضيق قديم يُورَّث لدور جديد أوسع).

**التحقق الفعلي (جلستان حقيقيتان منفصلتان — عضو دعم فني اختباري ومالك منصة اختباري منفصل تماماً عن
حساب `admin@platform.sa` الحقيقي المزروع أصلاً، ومسار API اختباري مؤقت لاستدعاء الأفعال الحقيقية
مباشرة، حُذف فوراً بعد الاختبار)**:
- تسجيل دخول عضو `PLATFORM_SUPPORT` اختباري حقيقي عبر `/admin-login` (نموذج حقيقي، وليس محاكاة) نجح،
  والوصول لصفحات `/admin`/`/admin/team` (الأخيرة "ليس لديك صلاحية" بافتراضي الدور، متوقَّع). **تعطيل
  الحساب** (`isActive = false`) مباشرة في القاعدة بينما نفس كوكي `dms_platform_session` لا يزال
  صالحاً التوقيع ← الطلب التالي بنفس الكوكي تماماً `307` فوري لـ`/admin-login` (وليس 200 وصولاً كاملاً
  كما كان الحال قبل الإصلاح) — أهم اختبار في المهمة، ونجح فعلياً بعد إصلاح فخّ الكوكيز (القرار #243).
  إعادة التفعيل ← نفس الكوكي عاد يعمل فوراً بلا أي تسجيل دخول جديد.
- منح العضو (بنفس جلسته السارية) صلاحية `platform.merchants.change_plan` + `platform.merchants.view`
  مباشرة عبر `updateInternalStaffPermissions` الحقيقية ← إعادة تحميل `/admin/tenants` بنفس الجلسة
  (**بلا أي تسجيل خروج/دخول**) أظهرت وصولاً كاملاً فوراً — يتحقق مباشرة من نفس إصلاح مشكلة الـJWT
  القديم (القرار #242) منطبقاً على فريق المنصة، لا التجار فقط.
- تمرير `["platform.team.manage"]`، `["foo.bar.baz"]`، أو `["contacts.export"]` (صلاحية تاجرية بحتة)
  مباشرة كحمولة لـ`updateInternalStaffPermissions` ← رُفضت الثلاثة فعلياً بـ"صلاحية غير صالحة" — يتحقق
  من كل من الاستبعاد البنيوي والفصل بين عالمي الصلاحيات معاً (القرار #244).
- `forceResetInternalStaffPassword` على العضو النشط ← كلمة المرور القديمة رُفضت فعلياً فوراً عبر
  محاولة دخول حقيقية بعدها (`/admin-login` أعاد "البريد الإلكتروني أو كلمة المرور غير صحيحة")، وتحقّق
  مباشر عبر SQL أن رابط إعداد جديد صالح (7 أيام، غير مُستخدَم) أُنشئ فعلياً.
  محاولة الأفعال الأربعة (`updateInternalStaffPermissions`, `forceResetInternalStaffPassword`,
  `changeInternalStaffRole`, `removeInternalStaff`) على حساب `admin@platform.sa` **الحقيقي** (بمعرّفه
  الفعلي، وليس حساباً اختبارياً) من جلسة مالك المنصة الاختباري ← رُفضت الأربعة كلها فعلياً برسائل
  الرفض الصحيحة، وتحقّق مباشر بعدها عبر SQL أن صف الحساب الحقيقي لم يتغيّر إطلاقاً (نفس البريد/الدور/
  الاسم/isActive كما كانت قبل المحاولات). محاولة خامسة (`updateInternalStaffProfile`) على نفس الحساب
  الحقيقي من جلسة مختلفة ← رُفضت أيضاً ("لا يمكن تعديل بيانات مالك المنصة من هنا")؛ نفس الفعل على حساب
  مالك المنصة الاختباري *بنفسه* (تعديل ذاتي) ← نجح، يتحقق من الاستثناء المقصود تحديداً (تعديل ذاتي فقط).
- `npx tsc --noEmit` و`npm run build` نظيفان على كامل المشروع (`/admin/team` بُني بنجاح، 4.29kB).
  كل بيانات الاختبار (عضوان اختباريان، مسار API مؤقت) حُذفت من القاعدة المحلية بعد التحقق — تأكَّد
  مباشرة عبر SQL أن حساب `admin@platform.sa` الحقيقي سليم تماماً ولم يُمسّ إطلاقاً طوال الاختبار.

## 2026-08-20 (جولة رابعة) — الموظف الذكي كمساعد مبيعات استشاري: `Product.description`/`categoryName` + مزامنة منتجات Sandbox

### 247. `Product` تتوسَّع بحقلين اختياريين، ومزامنة منتجات Sandbox (فجوة كانت تُرجع `productsSynced: 0` دائماً) تُفعَّل فعلياً
لتحويل الموظف الذكي من رد خدمة عملاء عام لمساعد بيع يقترح منتجات فعلية (حجم أكبر/بديل من نفس
التصنيف)، احتاج `get_product_price` بيانات لم تكن مُلتقَطة إطلاقاً: وصف المنتج وتصنيفه. أُضيف
`description`/`categoryName` (اختياريان، migration بلا أي تعديل بيانات قائمة) لموديل `Product`،
والتقاطهما فعلياً في أدابتري زد/سلة (وضع Live) بنفس أسلوب استخراج `name.ar||name.en` الدفاعي
الموجود بالفعل — بيانات API خارجية غير موثوق ببنيتها الكاملة دائماً.

فجوة حرجة منفصلة اكتُشفت أثناء القراءة: فرع Sandbox في كلا الأدابترين كان يُزامن الطلبات فقط
ويُرجع `productsSynced: 0` **ثابتاً دائماً** — أي تاجر تجريبي (بلا ربط زد/سلة حقيقي، وهو الافتراضي
لأي حساب تجريبي جديد) لم يكن لديه أي منتج إطلاقاً في القاعدة، فـ`get_product_price` كانت تُرجع
`found: false` لأي سؤال عن أي منتج بصرف النظر عن السؤال — ميزة المساعد الاستشاري كانت ستبدو معطوبة
تماماً في وضع التجربة تحديداً (الوضع الوحيد المتاح فعلياً بلا حساب زد/سلة حي). أُضيفت منتجات ثابتة
واقعية (`SANDBOX_ZID_PRODUCTS`/`SANDBOX_SALLA_PRODUCTS` في `fixtures.ts`، تصنيفان بفروق حجم/سعر
واضحة) ومزامنتها فعلياً في فرع Sandbox من `syncOrders`.

`get_product_price` وُسِّعت (بدل أداة جديدة منفصلة — استدعاء واحد يكفي النموذج) لتُرجع أيضاً
`relatedProducts` من نفس `categoryName` (أعلى مخزوناً، أرخص سعراً، حتى 3) — البرومنت يُلزَم صراحةً
بعدم اقتراح أي منتج لم يُرجعه هذا الاستدعاء نفسه، فنفس مبدأ "لا اختلاق بيانات" الموجود بالفعل لقاعدة
"لا تخمين أسعار" يمتد الآن لاقتراحات المنتجات أيضاً بلا استثناء.

**التحقق الفعلي** (تاجر اختباري حقيقي `ctestsalesagenttenant12345` بتكاملي ZID/SALLA وضع Sandbox،
حُذف بالكامل بعد التحقق): `syncOrders` لكلا الأدابترين رجعت `productsSynced: 3` و`1` فعلياً (بدل `0`
الثابتة سابقاً)، وصفوف `Product` الناتجة تحمل `description`/`categoryName` الصحيحة. استدعاء
`get_product_price` لـ"منتج تجريبي أ - عبوة صغيرة" أرجع `relatedProducts` يحتوي فعلياً "منتج تجريبي
أ - عبوة كبيرة" (نفس التصنيف، مخزون متاح) — واستدعاء باسم منتج غير موجود إطلاقاً أرجع `found: false`
بلا أي اختلاق، يتحقق أن القاعدة الصارمة رقم 1 (لا تخمين) لسه شغالة زي ما هي بعد كل التعديلات.
`npx tsc --noEmit` نظيف على كامل المشروع.

## 2026-08-20 (جولة خامسة) — إثراء "نظرة عامة" (تاجر + منصة) برسوم اتجاه زمني وبطاقات الطلبات/الموظف الذكي

تحسين واجهة على بيانات موجودة فعلاً (ليس قراراً معمارياً جديداً): صفحتا `dashboard/page.tsx` و
`admin/page.tsx` أضيف لهما رسم اتجاه أسبوعي/شهري (BarChart/DonutChart الموجودان أصلاً)، بطاقات
مشروطة بالصلاحية لميزتي الطلبات/الموظف الذكي المبنيتين سابقاً وغير الظاهرتين على أي منهما قبل هذه
الجولة، وشريط استهلاك توكنز الذكاء الاصطناعي في بطاقة الاشتراك. `MONTH_LABELS_AR` صار مُصدَّراً من
`revenueAnalytics.ts` بدل تكراره. تحقّق فعلياً عبر تاجر وSuper Admin اختباريين حقيقيين (جلسة NextAuth
حقيقية عبر CSRF، وجلسة منصة عبر `setPlatformSession` مباشرة) وطلب HTTP فعلي للصفحتين، حُذفا بالكامل
بعدها — كل الأقسام الجديدة ظهرت في الـHTML الفعلي بقيم غير صفرية مطابقة للبيانات المزروعة.
`npx tsc --noEmit` نظيف.

## 2026-08-20 (جولة سادسة) — مركز الموافقات: سجل تاريخي، إشعار التاجر لكل الأنواع، مؤشر تأخير، فلترة

### 248. فجوة إشعارات حقيقية: `requestMoreInfo`/`rejectRequest` كانتا ترسلان بريداً فقط لـ`PARTNER_APPLICATION`
أربعة من خمسة أنواع طلبات (`NEW_TENANT`, `WHATSAPP_VERIFICATION`, `MESSAGE_TEMPLATE`, `CUSTOM_PLAN`)
مرتبطة بتينانت له حساب OWNER فعلي بالفعل — قرار "رفض" أو "طلب معلومات إضافية" على أي منها كان يحدث
بصمت تام، بلا أي بريد أو إشعار، رغم وجود مستلم واضح (OWNER التينانت). أُضيف ملف قوالب مستقل
`src/lib/email/tenantApprovalTemplates.ts` (نفس نمط `partnerTemplates.ts`، يعيد استخدام `baseUrl()`
منه) بدالتين (`tenantApprovalNeedsInfoEmail`/`tenantApprovalRejectedEmail`) تُرسَلان لأول OWNER يُوجَد
لتينانت الطلب عبر `superAdminDb.user.findFirst` — لا فشل صامت لباقي منطق الدالة لو لم يوجد OWNER
(حساب حُذف يدوياً مثلاً). منطق الاعتماد/الرفض الفعلي (transactions) لم يُمَس إطلاقاً.

### 249. `src/lib/approvalTypeLabels.ts` — مصدر وحيد لتسميات/أيقونات الأنواع بدل نسخة محلية في `ApprovalCard.tsx`
كانت `TYPE_LABELS` مُعرَّفة محلياً داخل `ApprovalCard.tsx` فقط — أصبحت مطلوبة أيضاً في `actions.ts`
(بناء نص قوالب البريد الجديدة أعلاه) و`page.tsx` (تفصيل العدد حسب النوع) ومكوّنين جديدين
(`PendingApprovalsList`, `ApprovalHistoryCard`)، فنُقلت لملف مشترك واحد بدل نسخ رابع/خامس محتمل.

### 250. سجل تاريخي للقرارات (approved/rejected) كان غائباً تماماً — الطلب يختفي نهائياً من الواجهة فور اتخاذ قرار
`page.tsx` صار يقبل `?tab=pending|approved|rejected` بنفس نمط تبويبات `dashboard/campaigns` تماماً.
تبويبا approved/rejected يستعلمان `ApprovalRequest` بحالتهما مباشرة مع `include: { reviewedBy: {
select: { name } } }` (العلاقة `ReviewedBy` موجودة فعلاً في الـschema، فلا حاجة لاستعلام `user`
منفصل كما اقترح البرومنت الأصلي بديلاً احتياطياً)، مرتبة `reviewedAt desc`، بترقيم صفحات حقيقي
(`Pagination` المشترك الموجود فعلاً، 30/صفحة) بدل `take: 30` بلا صفحات. بطاقات السجل
(`ApprovalHistoryCard`) read-only بالكامل — بلا أي أزرار قرار.

**التحقق الفعلي** (تاجر وSuper Admin اختباريين حقيقيين، طلبات موافقة حقيقية بأنواع مختلفة، مسار API
مؤقت لاستدعاء server actions مباشرة + جلسة `setPlatformSession` حقيقية — كل شيء حُذف بعدها): بريدا
"طلب معلومات إضافية"/"رفض" وصلا فعلياً لعنوان OWNER الحقيقي لطلبي `WHATSAPP_VERIFICATION` و
`MESSAGE_TEMPLATE` (تأكيد مباشر من سجل الخادم `EMAIL_PROVIDER=console`، نص كامل يتضمن السبب/الرسالة
الفعلية ورابط لوحة تحكم حقيقي) — كانا سيصلان بصمت قبل هذا التعديل. اعتماد طلب ثم فتح تبويب "تم
اعتمادها" أظهر اسم المُراجِع والتاريخ الصحيحين فعلياً في HTML المُستلَم. رفض طلب بسبب محدَّد ظهر في
تبويب "مرفوضة" مع السبب معروضاً حرفياً. طلب بـ`createdAt` قبل 50 ساعة أظهر "⚠️ منذ يومين" بلون
`text-danger-500` (اكتُشف ويُصلِح أثناء هذا الاختبار خلل نحوي بسيط: الصيغة الأولى أنتجت "منذ 2 يومين"
بدل "منذ يومين" — المثنى العربي لا يحمل رقماً ظاهراً). `npx tsc --noEmit` نظيف.

## 2026-08-20 (جولة سابعة) — صفحة رسائل التواصل: فلترة/بحث محلي، تفعيل markLeadHandled، مؤشر تأخّر، روابط تواصل

### 251. `src/lib/timeSinceAr.ts` — استُخرجت من `ApprovalCard.tsx` لتُستخدَم أيضاً في `LeadsList.tsx` الجديد
نفس دالة تحويل الفارق الزمني لنص عربي (بما فيها إصلاح صيغة المثنى الموثَّق في القرار السابق) احتاجتها
صفحة رسائل التواصل بالحرف — نُقلت لملف `lib` مشترك بدل نسخ ثالث، بنفس منطق استخراج
`approvalTypeLabels.ts` في الجولة السابقة.

### 252. `markLeadHandled` كانت دالة كاملة بلا أي مستدعٍ في الواجهة — زر سريع مضاف في `LeadControls.tsx`
كانت الدالة الوحيدة لضبط `FOLLOWED_UP` + `handledAt` فعلياً هي `select` عام (`setLeadStatus`) يتطلب
فتح القائمة واختيار الحالة يدوياً — بطيء لمراجع يومي لعدد كبير من leads جديدة. زر "✅ تمت المتابعة"
يظهر فقط لـ`status === "NEW"`، بجانب الـselect الحالي (لم يُستبدَل — يبقى الـselect ضرورياً لضبط
"مُغلقة" يدوياً).

### 253. تحويل قائمة الرسائل لمكوّن عميل (`LeadsList.tsx`) — فلترة/بحث محليان بلا إعادة استعلام
نفس فلسفة `PendingApprovalsList.tsx` في مركز الموافقات تماماً: `page.tsx` (خادم) يبقى يجلب نفس الـ100
سجل بنفس الاستعلام الأصلي بلا أي تغيير، ويُمرِّرها serialized (`createdAt` كنص ISO، `assignedTo`
مُختزَلة لـ`{id, name}`) لمكوّن عميل يدير 3 عوامل تصفية محلية بـ`useState` (حالة، بحث نصي substring
على الاسم/البريد/الهاتف/نص الرسالة، "غير معيَّنة فقط") مجتمعة معاً بمنطق AND بسيط. إضافة شفافية بدل
pagination كامل لتجاوز حد الـ`take: 100` الصامت سابقاً: `page.tsx` يحسب `contactMessage.count()` بالتوازي
في نفس `Promise.all`، ويعرض ملاحظة نصية إن كان أكبر من عدد السجلات المعروضة فعلياً.

**التحقق الفعلي** (Super Admin اختباري حقيقي + 6 رسائل تجريبية بكل الحالات الأربع وفوارق زمنية مختلفة،
حُذفت بالكامل بعدها): كل الأسماء الستة ظهرت في HTML الأولي، شرائح الفلترة وعداد "الكل" الصحيح، روابط
`mailto:`/`tel:` فعلية بالقيم الصحيحة، رسالة بعمر 30 ساعة ظهرت بلون `text-warning-500` ورسالة بعمر 80
ساعة بلون `text-danger-500`، نص "معيَّنة لـ" ظهر باسم الموظف الصحيح (تحقّق مباشر من HTML الخام بعد نزع
علامات React الفاصلة `<!-- -->` بين العقد النصية — لم يكن خللاً فعلياً). استدعاء `markLeadHandled` عبر
الزر السريع حوَّل رسالة NEW فعلياً لـ`FOLLOWED_UP` مع `handledAt` مضبوط (تحقّق مباشر من القاعدة).
زرع 100 رسالة إضافية فعلياً وتأكيد ظهور ملاحظة "تُعرض آخر 100 رسالة من أصل 106" فعلياً. `npx tsc
--noEmit` نظيف.

## 2026-08-21 — شات الموقع التسويقي العام (Widget بلا تسجيل دخول) + رسائل صوتية عبر Whisper/TTS

### 254. أول نقطة عامة بالكامل (بلا تسجيل دخول) تستدعي OpenAI مباشرة — لماذا حد مزدوج (جلسة + عام) لازم هنا تحديداً
كل استدعاء LLM سابق في المشروع (الموظف الذكي التاجري) محمي بحد `checkTenantRateLimit` مربوط بتاجر
مصادَق عليه فعلياً — تكلفة أي إساءة استخدام محدودة عملياً بحجم قاعدة عملاء حقيقية. شات الموقع العام
مختلف جذرياً: أي زائر مجهول (أو سكربت آلي بعدد لا نهائي من الجلسات المزيَّفة) يقدر يستدعيه بلا أي
حاجز تسجيل دخول. حد الجلسة الواحدة (`checkIpRateLimit(sessionId, ...)`, 15/دقيقة) يمنع جلسة واحدة من
الإغراق، لكن **لا يمنع** مهاجماً يفتح مئات الجلسات المزيَّفة بالتوازي — لذلك أُضيف حد ثانٍ منفصل تماماً
على مستوى المنصة كلها معاً (`"global"` كمفتاح ثابت، 100/دقيقة عبر كل الزوار مجتمعين) كسقف تكلفة صريح
نهائي، بصرف النظر عن عدد الجلسات/عناوين IP. رقم ابتدائي تحفظي، قابل للتعديل لاحقاً حسب حجم الزيارات
الفعلي بعد المراقبة الحية. طبقة ثالثة منفصلة: كل نقطة API (`/session`, `/message`, `/voice`,
`/messages`) محمية أيضاً بـ`checkIpRateLimit` بعنوان IP الحقيقي **قبل أي كتابة قاعدة بيانات**، بنفس
نمط webhooks الموثَّق فعلاً — تحمي حتى مجرد تخزين `PlatformChatMessage` من إغراق مباشر، منفصلة عن
حدَّي الجلسة/العام اللذين يحميان استدعاء LLM تحديداً.

### 255. لا يُخزَّن أي ملف صوتي إطلاقاً — النص المُفرَّغ عبر Whisper هو المصدر الدائم الوحيد
`/api/platform-chat/voice` يقرأ ملف الصوت المرفوع كـ`File` في الذاكرة مباشرة ويمرّره لـ
`openai.client.audio.transcriptions.create()` دون أي `fs.writeFile` — نفس القيد الموثَّق سابقاً لعدم
بناء تخزين وسائط حقيقي في هذا المشروع (رسائل واتساب الواردة بنفس المبدأ). الرد الصوتي (TTS) الاختياري
عبر `platformChatVoiceReplyEnabled` يُبَث مباشرة في جسم الاستجابة (`audio/mpeg`) ولا يُكتب على القرص
أبداً — العميل (المتصفح) يشغّله فوراً من الذاكرة. كل من `audio.transcriptions.create`/`audio.speech.create`
يُستدعيان فقط عبر `openai.client` القادم من `getOpenAiClient()` الموحّدة — لا استيراد جديد لحزمة
`openai` مباشرة (القيد نفسه الموثَّق في `openaiClient.ts`).

### 256. مسار مرحلة ثانية مؤجل عمداً: مكالمة صوتية حقيقية لحظية — لم يُبنَ الآن، قرار واعٍ لا نسيان
ما بُني هنا (تسجيل → رفع → تفريغ Whisper → رد نصي/TTS اختياري) تبادل صوتي **غير لحظي** (طلب/استجابة
عادي، بلا streaming). مكالمة صوتية حقيقية لحظية (WebRTC أو Twilio Voice + streaming STT/TTS ثنائي
الاتجاه) تتطلب قرار بنية تحتية وتكلفة شهرية متكررة منفصل تماماً — SFU/TURN server، اتفاقية مزوّد
اتصالات، زمن استجابة مختلف جذرياً بالنظام — ويجب اتخاذه صراحة قبل البدء فيه، وليس امتداداً تلقائياً
لما بُني هنا.

**التحقق الفعلي**: `prisma migrate deploy` طبّق الجداول/الحقول الجديدة محلياً بنجاح (`PlatformChatSession`،
`PlatformChatMessage`، `PlatformSettings.platformChatEnabled/platformChatVoiceReplyEnabled`).
`npx tsc --noEmit` نظيف على كامل المشروع (منطق AI الجديد، 4 نقاط API، الـwidget، صفحة الإدارة).
لم يُتحقَّق من استدعاء OpenAI حي فعلي (Whisper/رد نصي/TTS) في هذه الجولة — يتطلب مفتاح OpenAI حقيقي
غير مضبوط محلياً حالياً (نفس القيد المُسجَّل سابقاً لكل تدقيق للموظف الذكي التاجري)؛ يلزم اختبار حي
فعلي بمفتاح حقيقي (محلياً أو على الإنتاج) قبل تفعيل `platformChatEnabled` للجمهور فعلياً.

## 2026-08-21 (جولة ثانية) — تفريغ الرسائل الصوتية الواردة عبر Whisper وقت الاستقبال (Meta + Baileys)

### 257. التفريغ يحدث وقت الاستقبال (ingestion) لا وقت تشغيل محرك الشات بوت — `engine.ts` لا يُلمَس إطلاقاً
`lib/chatbot/engine.ts` يقرأ `lastInbound?.body ?? ""` مباشرة (بلا أي معرفة بمصدر الرسالة أو نوعها)
— فبمجرد أن يحمل `Message.body` النص المُفرَّغ فعلياً وقت تخزين الرسالة نفسها (قبل `applyParsedMetaWebhook`
في مسار Meta، وقبل `applyParsedMetaWebhook` أيضاً في مسار Baileys لأنه يُعاد استخدامه حرفياً هناك)،
يتعامل الموظف الذكي مع الرسالة الصوتية بشكل طبيعي تماماً دون أي تعديل على محرك التشغيل نفسه. القرار
البديل (تفريغ عند اللحظة التي يقرأ فيها المحرك الرسالة) كان سيحتاج تمرير سياق "مصدر الوسائط" عبر كل
مسار قراءة تاريخ محادثة في المشروع (المحرك، صندوق الوارد، السجل التاريخي) — أعقد بلا أي فائدة إضافية.

### 258. مسارا تنزيل مختلفان تماماً لكل قناة — نفس دالة `transcribeVoiceMessage` المشتركة بعدهما
**Meta**: نداءان HTTP متتاليان بتوكن `Integration.encryptedAccessToken` الحقيقي المفكوك تشفيره (نفس
نمط `adapter.ts` بالضبط) — الأول يُرجع رابط تحميل مؤقت (Media API)، الثاني يُنزّل البيانات الفعلية.
**Baileys/QR**: دالة `downloadMediaMessage` الرسمية من نفس حزمة `@whiskeysockets/baileys` — تتولى فك
تشفير الوسائط بنفسها عبر بيانات الجلسة الحية، بلا أي استدعاء شبكي إضافي من كودنا. كلا المسارين
يُنتجان `Buffer` في الذاكرة فقط (لا تخزين ملف على القرص إطلاقاً في أي منهما) يُمرَّر لنفس
`transcribeVoiceMessage()` الواحدة — بُنيت عبر باني `File` العام في Node (`new File([...])`) بدل
استيراد `toFile` من حزمة `openai` مباشرة، حفاظاً على القيد الموثَّق في `openaiClient.ts` (لا كود آخر
في المشروع يستورد حزمة `openai` مباشرة).

### 259. أي فشل تفريغ (بلا مفتاح OpenAI، فشل شبكي، فشل تنزيل) لا يُسقِط الرسالة نفسها أبداً
`transcribeVoiceMessage` تُرجع `null` بدل رمي استثناء يوقف معالجة الرسالة بأكملها — رسالة صوتية بلا
نص مُفرَّغ أفضل من فقدان الرسالة كلياً. في مسار Meta يبقى `body` كما وصل (caption أو فارغ، السلوك
القديم بالحرف)؛ في مسار Baileys يصير `"[رسالة صوتية]"` بدل `"[نوع رسالة غير مدعوم]"` الخاطئ سابقاً
(تصنيف `type: AUDIO` صحيح الآن على الأقل حتى بلا تفريغ، خلافاً لتصنيف `TEXT` الخاطئ القديم لكل رسالة
غير نصية في تلك القناة).

**التحقق الفعلي**: `npx tsc --noEmit` نظيف على كامل المشروع (بما فيها أنواع `@whiskeysockets/baileys`
لـ`downloadMediaMessage`). اكتُشف وأُصلِح أثناء التنفيذ خلل ازدواج عرض فعلي في `MessageBubble.tsx`:
فرع `AUDIO` الجديد يعرض النص المُفرَّغ، لكن سطر عرض `message.body` العام أسفل المكوّن كان سيعيد عرض
نفس النص مرة ثانية (كان يستثني `LOCATION` فقط سابقاً) — أُضيف استثناء `AUDIO` أيضاً لذلك السطر. لم
يُتحقَّق من تفريغ Whisper حي فعلي عبر أي من القناتين (لا حساب Meta حقيقي متصل ولا مفتاح OpenAI محلياً
حالياً) — القيد نفسه المُسجَّل مسبقاً لكل ميزة ذكاء اصطناعي في هذا المشروع.

### 260. `npm run start` صار يطبّق `prisma migrate deploy` تلقائياً قبل تشغيل السيرفر — عطل إنتاج حقيقي وقع بسببه
رفعة سابقة (شات الموقع العام) حملت migration جديدة (`PlatformChatSession`/`PlatformChatMessage` +
عمودان في `PlatformSettings`)، ونجح الـdeploy على Render لكن الـmigration لم تُطبَّق فعلياً على قاعدة
بيانات الإنتاج — أمر `build` (`prisma generate && next build`) لا يشغّل `migrate deploy` إطلاقاً، ولا
يوجد `render.yaml` أو Pre-Deploy Command يفعل ذلك. النتيجة: `MarketingLayout` (يقرأ
`PlatformSettings.platformChatEnabled` في كل طلب، `force-dynamic`) بدأ يرمي خطأ قاعدة بيانات حقيقياً
على **كل** صفحة تسويقية عامة (`/`, `/pricing`, `/about`...) — عطل كامل مؤكَّد فعلياً (`curl` مباشر
رجع 500 على الصفحة الرئيسية)، بينما `/dashboard`/`/login`/`/admin-login` (خارج `MarketingLayout`)
بقيت سليمة تماماً، ما أكّد أن السبب دقيقاً وليس عطلاً عاماً في الخدمة.

**الإصلاح**: `package.json` → `"start": "prisma migrate deploy && next start"` بدل `"next start"`
وحدها — يضمن تطبيق أي migration معلَّقة تلقائياً في كل مرة يبدأ فيها السيرفر (على Render تحديداً، أو
أي بيئة أخرى مستقبلية)، بلا حاجة لتدخّل يدوي بعد كل رفعة تحمل تغييراً في القاعدة. `migrate deploy`
عملية idempotent (لا تكرار تنفيذ migration مطبَّقة أصلاً)، فلا أثر جانبي لتشغيلها عند كل إقلاع حتى
لو لم توجد migration جديدة.

## 2026-08-21 (جولة ثالثة) — مكالمة صوتية حية (Speech-to-Speech) عبر OpenAI Realtime API/WebRTC

### 261. Realtime API عبر WebRTC بدل بناء STT/TTS يدوي — سرعة استجابة طبيعية بلا جهد تطوير مكرَّر
مكالمة صوتية حية تتطلب زمن استجابة أقرب للحظي (مئات المللي ثانية) لتبدو محادثة طبيعية — تسلسل يدوي
(تسجيل قطعة صوت → رفع → Whisper → LLM → TTS → تشغيل) لكل "دور" في المحادثة كان سيُنتج تأخيراً محسوساً
غير مقبول لمكالمة، بخلاف الرسائل الصوتية المُسجَّلة المنفصلة (transcribeVoiceMessage.ts) حيث التأخير
مقبول تماماً. Realtime API يدير التسجيل/التفريغ/التوليد/النطق كتدفق واحد مستمر عبر WebRTC مباشرة بين
المتصفح وOpenAI (صوت المستخدم والمساعد لا يمر عبر خادمنا إطلاقاً — فقط توكن مؤقت يُصدره الخادم).

### 262. بلا function calling في المكالمة الصوتية — قرار نطاق صريح لهذه المرحلة الأولى، ليس نسياناً
الشات النصي (generatePlatformChatReply.ts) يستخدم `get_plan_price` عبر Chat Completions العادي (نموذج
طلب/استجابة متزامن، تنفيذ الأداة يحدث في نفس دورة الخادم). ربط أداة مماثلة داخل مكالمة WebRTC حية
يتطلب بنية مختلفة تماماً: الاستماع لحدث طلب أداة من OpenAI عبر data channel، إرساله لخادمنا لتنفيذه،
ثم إرجاع النتيجة عبر نفس القناة — تعقيد WebRTC إضافي حقيقي (تزامن، معالجة أخطاء منفصلة عن الصوت نفسه)
غير مبرَّر لأول نسخة. البديل المُطبَّق: تعليمات صوتية صريحة (`platformVoiceCallInstructions.ts`) تمنع
ذكر أي رقم سعر بالصوت وتوجّه الزائر للشات النصي (اللي فيه الأداة الحقيقية) بدلاً من ذلك.

### 263. حد الـ5 دقائق: تطبيق مزدوج بمستويين مختلفين تماماً في القوة
مؤقّت JS في `ChatWidget.tsx` (`setTimeout` + عدّاد مرئي تنازلي) يقطع الاتصال محلياً عند 5:00 — **تطبيق
من جانب العميل فقط**، خط دفاع أول وليس ضماناً مطلقاً (زائر بمعرفة تقنية يقدر يتجاوزه بتعديل الكود
محلياً في متصفحه). **الحد الحقيقي الصارم** هو حد المعدل العام في `/api/platform-chat/realtime-token`
(20 بداية مكالمة/دقيقة عبر كل الزوار معاً) — مهما طالت مكالمة فردية تجاوزت الـ5 دقائق فعلياً (خلل أو
تلاعب من جانب العميل)، هذا الحد لا يمنعها من الاستمرار لأنه يُطبَّق فقط عند *بدء* مكالمة جديدة، وليس
حداً زمنياً حياً على مكالمة قائمة — القيد الحقيقي الوحيد على طول مكالمة فردية واحدة هو حد المعدل
العام لبدء مكالمات *جديدة*، بالإضافة لصلاحية التوكن المؤقت نفسه (تنتهي تلقائياً خلال دقيقة من OpenAI
لو لم يُستخدَم فوراً لبدء الاتصال، لكن لا تُنهي مكالمة *قائمة بالفعل* بعد نجاح الاتصال). **فجوة معروفة
موثَّقة عمداً**: لا يوجد فرض خادمي فعلي يقطع مكالمة قائمة عند 5 دقائق بالضبط — الاعتماد الكامل حالياً
على مؤقّت العميل لهذا الجزء تحديداً؛ حد إجمالي على مستوى الحساب (تنبيه فوترة OpenAI مباشرة) هو خط
الدفاع العملي الحقيقي الأخير ضد إساءة استخدام متعمَّدة لتعديل الكود محلياً.

**السقف الدولاري النظري الأقصى** المحسوب من حد المعدل العام أعلاه: 20 بداية مكالمة/دقيقة × حتى 5
دقائق لكل مكالمة × ~$0.016/دقيقة (تقدير تقريبي لصوت Realtime API) ≈ حتى $1.6 لكل دقيقة لو استمر معدل
البدء الأقصى متتالياً — رقم توضيحي يستاهل متابعة فعلية من فواتير OpenAI الحقيقية بعد التفعيل، وليس
تعويضاً كاملاً عن المراقبة اليدوية.

### 264. توثيق API سريع التغيّر — تحقّق مباشر من مصادر OpenAI الرسمية الحالية بدل الاعتماد على الذاكرة
قبل كتابة الكود، تحقَّقت مباشرة (WebFetch/WebSearch لصفحات developers.openai.com الرسمية الحالية،
وليس ذاكرة نموذج قديمة) من: (أ) نقطة إصدار التوكن المؤقت `POST /v1/realtime/client_secrets` وشكل
جسم الطلب (`session.type: "realtime"`, `session.model`, `session.instructions` مباشرة تحت `session`
وليس متداخلة، `session.audio.output.voice`, `session.audio.input.transcription.model` متداخلة تحت
`audio`)؛ (ب) نقطة تبادل SDP الفعلية الحالية `POST /v1/realtime/calls` (منفصلة عن نقطة إصدار التوكن،
واسمها تغيّر فعلياً عن نسخة beta أقدم كانت تستخدم `/v1/realtime?model=...` مباشرة)؛ (ج) اسم موديل
الصوت الحية الفعلي `gpt-realtime-mini` (مؤكَّد من صفحة الموديل الرسمية، وليس تخميناً)، وصوت افتراضي
حقيقي موثَّق (`marin`)؛ (د) أسماء أحداث تفريغ النص عبر data channel:
`conversation.item.input_audio_transcription.completed` (كلام الزائر) و
`response.output_audio_transcript.done` (كلام المساعد) — الأخيرة مؤكَّدة مباشرة من جدول أحداث رسمي،
والأولى مُثلَّثة من مصدرين مستقلين متوافقين. راجع مصادر الفحص الفعلية في سجل هذه الجلسة.

**التحقق الفعلي**: `prisma migrate deploy` طبّق `PlatformSettings.platformVoiceCallEnabled` محلياً
بنجاح. `npx tsc --noEmit` نظيف على كامل المشروع. لم يُتحقَّق من مكالمة Realtime API حية فعلية
(WebRTC كامل بمتصفح حقيقي + مفتاح OpenAI حقيقي) في هذه الجولة — يتطلب مفتاح OpenAI حقيقي غير مضبوط
محلياً حالياً (نفس القيد المُسجَّل مسبقاً لكل ميزة ذكاء اصطناعي في هذا المشروع)، بالإضافة لكون هذا
أول ميزة WebRTC في المشروع تتطلب متصفحاً حقيقياً فعلياً وليس فقط طلبات HTTP مباشرة — يلزم اختبار حي
كامل قبل تفعيل `platformVoiceCallEnabled` للجمهور فعلياً.

## 2026-08-21 (جولة رابعة) — وضع عرض تجريبي مجاني (Demo Mode) لشات الموقع: نص + صوت + مكالمة، صفر تكلفة OpenAI

### 265. Web Speech API (المتصفح) بدل OpenAI — صفر تكلفة فعلية بلا أي توكن أو مفتاح
`SpeechRecognition`/`speechSynthesis` مدمجتان في المتصفح نفسه (Chrome/Edge تحديداً) وتعملان بالكامل
محلياً على جهاز الزائر — بلا أي طلب شبكي لـWhisper أو Realtime API أو حتى استدعاء LLM للرد نفسه
(محرك ردود مُبرمَج `demoReplyEngine.ts` بمطابقة كلمات مفتاحية، بلا ذكاء اصطناعي إطلاقاً). هذا يجعل
وضع العرض التجريبي مجانياً 100% فعلياً وليس "أرخص فقط" — مناسب تحديداً لعرض إمكانيات المنصة لعميل
محتمل بلا أي قلق من تكلفة تراكمية غير محسوبة، خلافاً للمسارين الحقيقيين (الشات المدفوع وRealtime API)
المحميين بحدود معدل صريحة لأن تكلفتهما حقيقية فعلاً.

### 266. القيد المعروف الحقيقي لـWeb Speech API: دعم متصفح غير موحَّد — سلوك موثَّق، وليس خللاً في الكود
`SpeechRecognition` قياسي جزئياً فقط: دعم ممتاز في Chrome/Edge (سطح المكتب والموبايل)، **غائب تماماً**
في Firefox حتى وقت كتابة هذا القرار، ودعم متفاوت/محدود في نسخ Safari المختلفة. هذا قيد حقيقي في
منصة الويب نفسها، ليس شيئاً يمكن "إصلاحه" بكود إضافي — الحل المُطبَّق هو اكتشاف صريح
(`getSpeechRecognitionCtor()`) وتعطيل بصري واضح لزري الميكروفون/الاتصال (`disabled` + `title` tooltip
"غير مدعوم في هذا المتصفح — جرّب Chrome") بدل ترك المستخدم يضغط زراً لا يعمل بصمت. الشات النصي نفسه
يبقى يعمل بلا أي قيد في كل المتصفحات (لا يعتمد على Web Speech API إطلاقاً).

### 267. الأسعار وحدها من مصدر حقيقي (DB) رغم أن باقي الردود مُبرمَجة بالكامل — لماذا هذا الاستثناء تحديداً
كل رد آخر في `demoReplyEngine.ts` (شرح الموظف الذكي، الربط بواتساب، التكامل مع زد/سلة) نص عربي ثابت
عام لا يتغيّر بمرور الوقت. الأسعار مختلفة جوهرياً: تتغيّر فعلياً من لوحة الإدارة (`admin/plans`) بمعزل
تام عن أي نشر كود، وعميل محتمل يشاهد عرضاً تجريبياً ثم يسمع رقماً مختلفاً عن السعر الحقيقي وقت
تسجيله فعلياً هي أسوأ نتيجة ممكنة لأداة "عرض تجريبي" (تُنتج انطباعاً أولياً كاذباً بدل معلومة). لذلك
فقط فرع الأسعار (وفرع مدة التجربة المجانية، لنفس السبب بالضبط) يقرأ القاعدة الحقيقية — قراءة مجانية
بلا أي تكلفة OpenAI، فلا يتعارض هذا مع هدف "صفر تكلفة" للميزة ككل.

### 268. `isDemo` على PlatformChatSession — تمييز إلزامي في `/admin/chats` لمنع فريق المبيعات من متابعة عرض كأنه عميل حقيقي
بلا هذا الوسم، موظف مبيعات يفتح `/admin/chats` كان سيرى جلسة عرض تجريبي بنفس شكل عميل محتمل فعلي
تماماً — احتمال حقيقي لإهدار وقت متابعة على جلسة لا قيمة تجارية فيها، أو أسوأ، عدم متابعة عميل حقيقي
لأن الفريق افترض أنها "مجرد عرض آخر". badge بنفسجي واضح "🎬 عرض تجريبي" في `ChatSessionCard.tsx` يحل
هذا بصرياً بلا أي تغيير في منطق الفرز أو الفلترة نفسه.

**التحقق الفعلي**: `prisma migrate deploy` طبّق `PlatformSettings.platformDemoModeEnabled` و
`PlatformChatSession.isDemo` محلياً بنجاح. `npx tsc --noEmit` نظيف على كامل المشروع. لم يُتحقَّق من
سلوك `SpeechRecognition`/`speechSynthesis` الفعلي في متصفح حي (لا أداة متصفح متاحة في هذه البيئة) —
منطق الخادم (محرك الردود، تفرّع المسارين، الوسم) تحقَّقت منه مباشرة ببيانات حقيقية؛ الجزء المتصفحي
(التعرّف والنطق الصوتيان تحديداً) يحتاج اختباراً يدوياً حياً من Chrome قبل الإطلاق للجمهور.

**تحديث لاحق نفس اليوم**: أُضيف checkbox فعلي لـ`platformDemoModeEnabled` في `admin/settings` (كان
مقصوداً DB-only في أول نسخة، لحاجة عملية فورية لتجربته بلا وصول لقاعدة البيانات). كذلك وُسِّع
`demoReplyEngine.ts` بفروع إضافية (حملات/CRM/صندوق الوارد/قوالب الرسائل) مأخوذة حرفياً من نصوص
`ChannelsToolsSection.tsx` الحقيقية على الصفحة الرئيسية، حتى يطابق العرض التجريبي وعود الموقع نفسه.
**اكتُشفت أثناء الاختبار الفعلي 3 حالات إضافية من نفس فئة خلل "أسعار/باقات" الموثَّقة أعلاه** (إدراج
أداة التعريف "ال" بين كلمتي عبارة مركّبة يكسر التطابق الحرفي): "الذكاء الاصطناعي" لا تحتوي "ذكاء
اصطناعي"، و"جهات الاتصال"/"صندوق الوارد" (الصيغة المعرَّفة) لا تحتويان "جهات اتصال"/"صندوق وارد"
(المنكَّرة) — والعكس صحيح أيضاً (زائر افتراضي كتب المنكَّرة بينما النمط الأصلي توقّع المعرَّفة). كل
الحالات الثلاث أُصلِحت بإضافة الصيغتين صراحة، وتحقَّقت من الإصلاح بتشغيل حي فعلي بعده لكل الفروع.

### 269. ويدجت شات موقع التاجر الخاص (نص + رسالة صوتية + مكالمة حية) — نفس الميزة المنصّية، لكن بيانات تاجر حقيقية عبر RLS
نفس القدرات الثلاث المبنية أصلاً للموقع التسويقي العام (`ChatWidget.tsx` + `platformChat.ts`)، لكن
لكل تاجر على موقعه الإلكتروني الخاص، كميزة مدفوعة (Growth/Scale فقط). الفرق الجوهري المعماري عن
النسخة المنصّية: `PlatformChatSession`/`PlatformChatMessage` بيانات منصّة (بلا `tenantId`، تُقرأ عبر
`rawDb`)، بينما `TenantChatSession`/`TenantChatMessage` الجديدتان **بيانات تاجر حقيقية تخضع لـRLS
كاملاً** — كل منطق العمل (`tenantChat.ts`, `generateWebsiteChatReply.ts`, كل مسارات
`api/tenant-chat/[tenantId]/*`) يمر حصراً عبر `withTenant()`، والجدولان أُضيفا لمصفوفة `tenant_tables`
في `postgres-rls.sql` (خطوة إلزامية لا يجوز تخطّيها لأي جدول جديد يحمل `tenantId`).

ثلاثة قرارات نطاق صريحة غير قابلة لإعادة الفتح (كما حُدِّدت مسبقاً):
1. **الزائر مجهول الهوية بالكامل في v1** — لا `contactId` حقيقي لزائر موقع تاجر مجهول، فأداة
   `get_order_status` مُستبعَدة محلياً في `generateWebsiteChatReply.ts` (`WEBSITE_TOOLS` تُفلتر
   `AI_TOOLS` الأصلية بدل نسخة أدوات موازية) — بلا تعديل `tools.ts` نفسه إطلاقاً.
2. **سقف دقائق مكالمة شهري لكل تاجر** (بالإضافة لحد الـ5 دقائق لكل مكالمة منفردة الموجود أصلاً) —
   `Subscription.voiceCallMinutesUsedThisPeriod`، Growth=100 دقيقة/شهر، Scale=300 دقيقة/شهر، قابل
   للتعديل من `admin/plans` لاحقاً (`ChatbotLimits.maxVoiceCallMinutesPerMonth`). التتبّع يتم عبر
   نصّين حرفيين ثابتين ("[بدأت مكالمة صوتية]"/"[انتهت المكالمة الصوتية]") يمران كرسالتي شات عاديتين
   عبر `[sessionId]/voice-transcript/route.ts` — عند وصول نص النهاية، يُحسَب الفارق الزمني عن أحدث
   رسالة بنص البداية لنفس الجلسة ويُزاد (مُقرَّباً للأعلى، بحد أدنى دقيقة واحدة) على العدّاد الشهري.
3. **تضمين عبر `<iframe>` بسيط (وليس widget script)** — بساطة v1 صريحة، الكود الكامل مُولَّد ومعروض
   في `dashboard/website-chat/page.tsx` مع `allow="microphone"`.

**خلل إنتاجي حقيقي اكتُشف أثناء بناء هذه الميزة** (يؤثر أيضاً على ميزة المكالمة الحية المنصّية
المشحونة مسبقاً، وليس فقط الجديدة): رؤوس الأمان العامة في `next.config.js` كانت تحظر المكالمة الصوتية
الحية فعلياً في الإنتاج بصمت — `Permissions-Policy: microphone=()` يمنع `getUserMedia` حتى على نطاقنا
نفسه (وليس فقط داخل iframe مُضمَّن)، و`connect-src 'self'` في CSP يحظر استدعاء
`https://api.openai.com/v1/realtime/calls` المباشر من المتصفح لتبادل SDP. لم يُكتشف وقت شحن ميزة
المكالمة الأولى لغياب مفتاح OpenAI حقيقي محلياً لأي اختبار فعلي بمتصفح حي؛ اكتُشف الآن عند إعادة
استخدام نفس نمط WebRTC لصفحة `embed/chat/[tenantId]`. الإصلاح: `microphone=(self)` بدل الحظر الكامل،
و`connect-src` يتضمن `https://api.openai.com` الآن — ينطبق على كل من المسار المنصّي والتاجري معاً.
كذلك، صفحة `/embed/*` تحديداً تحتاج تضميناً من نطاقات خارجية غير معروفة مسبقاً (موقع كل تاجر)، فحصلت
على مجموعة رؤوس أمان منفصلة (`EMBED_HEADERS`) عبر استبعاد صريح بـregex سلبي في `headers()` (وليس
الاعتماد على ترتيب دمج Next.js الداخلي لقواعد متطابقة المسار): بلا `X-Frame-Options` إطلاقاً،
و`frame-ancestors *` في CSP بدل `'none'` — لا قائمة نطاقات تاجر مسموحة في v1 (يطابق قرار "تبسيط
iframe" أعلاه).

**التحقق الفعلي**: `prisma migrate deploy` طبّق الجداول/الأعمدة الجديدة محلياً بنجاح، وتم تطبيق
`postgres-rls.sql` المُحدَّث فعلياً على قاعدة Postgres المحلية (تأكيد `CREATE POLICY` لكلا الجدولين
الجديدين). `npx tsc --noEmit` و`next build` نظيفان على كامل المشروع (كل مسارات `api/tenant-chat/*`
و`dashboard/website-chat/*` و`embed/chat/[tenantId]` ظهرت في مخرجات البناء). سكربت تحقق مؤقت (حُذف
بعد التشغيل) اختبر ببيانات حقيقية على القاعدة المحلية: بوابة الباقة (starter=false/growth=true لكل
من `websiteChatEnabled`/`websiteVoiceCallEnabled`/`maxVoiceCallMinutesPerMonth`)، **عزل RLS فعلي**
(محاولة قراءة جلسة تاجر A تحت سياق تاجر B ترجع `null` فعلاً، بينما تاجر A نفسه يقرأها بنجاح)، تدفّق
`processTenantChatMessage` الكامل (تخزين رسالة الزائر، `tenantId` موجود في كل صف كما تتطلب RLS،
تحويل صادق `handoff` بلا مفتاح OpenAI حقيقي محلياً بدل خطأ أو رد وهمي، خطأ 404 منطقي لجلسة غير
موجودة بدل استثناء غير معالَج)، وحساب دقائق المكالمة (3.4 دقيقة فعلية → 4 دقائق مُقرَّبة للأعلى في
العدّاد). **لم يُختبَر**: أي استدعاء OpenAI حي فعلي (Whisper/TTS/Realtime WebRTC) لغياب مفتاح حقيقي
محلياً — نفس القيد الموثَّق مراراً في القرارات السابقة؛ وسلوك التضمين الفعلي في متصفح حي داخل
`iframe` على نطاق خارجي مختلف (لا أداة متصفح متاحة في هذه البيئة).
