# دليل الهوية البصرية — منصة واصل

## الاسم والموقع
**واصل** — منصة CRM واتساب للتجار. الاسم يعكس الفكرة الجوهرية: "الوصول" للعميل عبر القناة
التي يستخدمها فعلياً (واتساب)، بأسلوب احترافي B2B وليس تطبيق محادثة استهلاكي.

## الألوان

| الاستخدام | القيمة | ملاحظة |
|---|---|---|
| خلفية أساسية (Deep Navy) | `#0B1220` | فخامة + تباين عالٍ للقراءة الطويلة (لوحة تحكم تُستخدم ساعات) |
| خلفية بطاقات | `#111A2E` | |
| Accent أساسي (Indigo/Violet) | `#6D5EF8` | متعمّد الابتعاد عن أخضر واتساب لتفادي إيحاء "هذا واتساب نفسه" |
| نجاح | `#22C55E` | |
| تحذير | `#F59E0B` | |
| خطر | `#EF4444` | |
| لون واتساب (للإشارة فقط، ليس Accent) | `#25D366` | يُستخدم فقط كأيقونة/إشارة سياقية |

**لماذا Indigo وليس Teal؟** قرار مسجَّل في `DECISIONS.md` — Indigo/Violet يعطي إحساساً تقنياً
SaaS أقوى في السوق العربي B2B الحالي، ويميّز واجهة الإدارة عن واجهة واتساب نفسها (الأخضر) بشكل
لا لبس فيه — المستخدم يجب أن يفرّق دائماً بين "لوحة واصل" و"واتساب" رغم أن المنصة تُشغّله.

كل تاجر يقدر يخصص `primaryColor` الخاص به (يظهر في الشعار المصغّر بالشريط الجانبي) دون كسر
النظام العام — انظر `Tenant.primaryColor` في المخطط و`Sidebar.tsx`.

## الخطوط
- **العربية**: Cairo (عبر `next/font/google`، متغير `--font-cairo`) — خط عربي حديث بأوزان
  متعددة (400–800) يقرأ جيداً في كثافة بيانات لوحات التحكم.
- **اللاتيني/الأرقام**: Inter (`--font-inter`) — للأرقام والمصطلحات الإنجليزية (IDs، أكواد).

## الاتجاه (RTL)
- `<html lang="ar" dir="rtl">` من الجذر — لا صفحة تكسر هذا.
- **قاعدة إلزامية**: أي نص يعرض أرقام/نسب بصيغة `X / Y` أو قيم لاتينية صرفة يجب أن يحمل
  `dir="ltr"` صراحة، وإلا ينعكس ترتيبها البصري داخل سياق RTL (خطأ حقيقي اكتُشف وأُصلح أثناء
  الاختبار، موثّق في CHANGELOG.md). الشريط الجانبي دائماً على الجهة اليمنى بحكم RTL الطبيعي.

## المكوّنات الأساسية (`globals.css`)
- `.card` — بطاقة بخلفية `navy-800`، حدود شفافة خفيفة، ظل ناعم.
- `.btn-primary` / `.btn-secondary` / `.btn-danger` — أزرار بحالات focus/disabled واضحة.
- `.badge` — وسم حالة صغير مستدير، يُلوَّن حسب الدلالة (نجاح/تحذير/خطر/محايد).
- `.input-field` — حقل إدخال موحّد بخلفية أغمق من البطاقة، حلقة تركيز Accent.

## الوضع الداكن/الفاتح
النسخة الحالية **داكنة فقط** (قرار: الأولوية لمنتج قابل للبيع بهوية متسقة بدل تجزئة الجهد بين
وضعين في البناء الأول). دعم Light Mode بديل تصميمي محتمل لاحقاً وليس ثغرة في المتطلب — يُضاف عبر
`darkMode: "class"` الموجود مسبقاً في `tailwind.config.ts` بإضافة toggle وقيم `light:*`.

## نبرة المحتوى
عربية فصحى ميسّرة (ليست عامية، وليست ترجمة حرفية آلية) — مثال: "لا توجد جهات اتصال بعد" وليس
"لا يوجد بيانات". رسائل الأخطاء تشرح ماذا حدث وماذا يفعل المستخدم، لا تعرض أكواد تقنية خام.
