# سجل التغييرات (CHANGELOG.md)

جميع التواريخ بتوقيت الجلسة الفعلي للبناء. المشروع بدأ من مجلد فارغ بالكامل.

## 2026-07-24 (الجولة الثانية والعشرون) — استبدال محرر الشات بوت ببناء تسلسلي مبسَّط

### تحسين تجربة مستخدم كبير (بناءً على شكوى مباشرة)
- استبدال لوحة الرسم الحرة (React Flow، وصل يدوي بالماوس بين العقد) بالكامل بواجهة **بناء تسلسلي**:
  قائمة خطوات (رسالة/سؤال/شرط/استدعاء API/تحويل/نهاية) تُنفَّذ تلقائياً بالترتيب، بلا أي وصل يدوي.
  عقدة "شرط" فقط تتفرّع لعمودين (✅/❌) — كل عمود قائمة مستقلة بلا تقاطع أو رجوع بين الفروع، وهو
  القيد الذي يمنع تحديداً نوع اللغبطة (عقد متقاطعة/معزولة) التي أبلغ عنها المستخدم بلقطة شاشة حقيقية.
- الترتيب عبر أسهم ▲▼ فقط (بلا سحب وإفلات)، وإضافة خطوة تُلحَق تلقائياً بنهاية عمودها.

### بلا تغيير على التخزين أو محرك التنفيذ
- `lib/chatbot/builderModel.ts` جديد يحوّل بين الشجرة المعروضة والـ`FlowGraph` المخزَّن (نفس الشكل
  تماماً الذي يقرأه محرك التنفيذ الحقيقي ومحاكي الاختبار من الجولة الحادية والعشرين) — كلاهما لم
  يُعدَّل بحرف واحد. أي رسم بياني قديم (من اللوحة الحرة) يُحوَّل تلقائياً عند الفتح بالسير من عقدة
  البداية فعلياً؛ أي عقدة معزولة (لم تكن تعمل أصلاً) تُستبعَد مع إشعار شفاف بعددها بدل حذف صامت.

### إصلاح خلل موروث مكتَشف أثناء البناء
- قالب "الرد على الأسئلة الشائعة" كان يصل عقدة `ai_reply` بشرط بعدها رغم أن المحرك الحقيقي (منذ
  الجولة الحادية والعشرين) ينهي الجلسة فوراً عندها — تلك العقد كانت ميتة فعلياً دون اكتشاف. صُحِّح
  القالب، وأصبحت `ai_reply`/`handoff`/`end` خطوات نهائية صريحة في الواجهة الجديدة (لا إضافة بعدها).

### تحقق فعلي
بناء تدفق كامل (رسالة→سؤال→شرط→فرعين) من الصفر بالواجهة الجديدة فقط، حفظ، اختبار المسارين، نشر.
تشغيل محادثتين حقيقيتين عبر محاكي الـSandbox ضد هذا التدفق بالذات — تأكيد أن المحرك الحقيقي غير
المعدَّل ينفّذ مخرجات الواجهة الجديدة بشكل صحيح تماماً (تسلسل رسائل، توقف عند سؤال، تطابق شرط، تحويل
لموظف). تحويل قراءة-فقط (بلا كتابة) لتدفق حقيقي منشور فعلياً لتاجر حقيقي — استخراج صحيح لخطواته
واكتشاف دقيق لعقدة معزولة فعلياً فيه (نفس ما ظهر بصرياً في لقطة الشاشة المرسَلة). إزالة كاملة لحزمة
`@xyflow/react` وملف المحرر القديم بعد التأكد من عدم استخدامهما في أي مكان آخر. `tsc`/`build` نظيفان.
تفاصيل القرارات (129–132) في `DECISIONS.md`.

## 2026-07-25 (تصحيح) — رجوع المعاينة والاختبار جنباً إلى جنب

### تصحيح
- بعد التباس بين "تحت بعض" و"جنب بعض" عبر عدة رسائل متتالية، تأكَّد (عبر سؤال توضيحي مباشر بدل
  التخمين) أن الترتيب المطلوب فعلياً هو الرجوع الكامل لجنباً-إلى-جنب في صف واحد — تراجُع عن تكديس
  الجولة السابقة مباشرة في الصفحات الثلاث. تأكَّد أيضاً عدم وجود أي كسر حقيقي في الصفحة (قسم الخطوات
  موجود فعلاً، كان مقطوعاً فقط من لقطة شاشة بالتمرير).

## 2026-07-25 (الجولة التاسعة والعشرون) — تكديس المعاينة عمودياً + رقم واتساب الحقيقي في الرأس

### تحسين
- معاينة الموبايل ولوحة الاختبار أصبحتا مكدَّستين تحت بعضهما (عرض كامل لكل منهما) بدل التجاور جنباً
  إلى جنب، في كل صفحات محرر الشات بوت الثلاث.
- رأس معاينة الموبايل يعرض الآن **رقم واتساب الحقيقي المتصل فعلياً** لهذا التاجر (Meta أو القناة
  التجريبية QR، أياً كان المتصل) بدل نص عام ثابت — أو رسالة صريحة توضح عدم وجود رقم متصل بعد.

### تحقق فعلي
ربط رقم واتساب وهمي حقيقي لتينانت اختبار، تأكيد ظهوره بالضبط في رأس المعاينة، وتأكيد بصري لتكديس
اللوحتين تحت بعضهما. `tsc`/`build` نظيفان. تفاصيل القرارات (159–160) في `DECISIONS.md`.

## 2026-07-25 (الجولة الثامنة والعشرون) — إعادة بناء المحاكي: محادثة اختبار حقيقية متعددة الأدوار

### إصلاح جذري (بعد بلاغين متتاليين لم تحلّهما إصلاحات جزئية)
- اكتُشف أن محاكي الاختبار القديم كان يعامل كل رسالة اختبار كمحادثة كاملة منفصلة من الصفر، ويقيّم
  رسالة واحدة ضد **كل** شرط/قائمة اختيارات في التدفق دفعة واحدة — يناقض جوهرياً كيف يعمل بوت حقيقي
  (رسالة ← سؤال ← توقف فعلي بانتظار الرد ← تقييم ذلك الرد بالذات). هذا هو السبب الجذري وراء شكوى
  "بيبان عشوائي" التي لم تحلّها الإصلاحات الجزئية السابقة (إزالة الرسالة المزدوجة، تمييز الأسطر
  التشخيصية) لأن المشكلة لم تكن بصرية بل في نموذج التنفيذ نفسه.
- محاكي جديد كلياً (`advanceSimulation`) يطابق حرفياً بنية محرك التنفيذ الحقيقي: يتذكّر عند أي عقدة
  "سؤال" توقفت المحادثة، ويستأنف الرسالة التالية كرد حقيقي عليها بالذات — بدل إعادة بدء كل شيء من
  الصفر مع كل رسالة اختبار. زر جديد "🔄 إعادة تشغيل المحادثة" لبدء اختبار مسار مختلف من الصفر، ومؤشر
  "البوت بانتظار ردك" يوضّح حالة المحادثة الحالية.

### تحقق فعلي
اختبار دورتين متتاليتين حقيقيتين على قالب فيه سؤال ثم شرط — تأكيد التوقف الصحيح عند السؤال في الدورة
الأولى، واستمرار نفس المحادثة (لا إعادة بدء) في الدورة الثانية مع تقييم الشرط للمرة الأولى فقط الآن.
`tsc`/`build` نظيفان. تفاصيل القرارات (156–158) في `DECISIONS.md`.

## 2026-07-25 (إصلاح متابع) — إخفاء الأسطر التشخيصية عن معاينة الموبايل الواقعية

### إصلاح
- بعد تفعيل التفاعل الحقيقي في معاينة الموبايل، ظهرت أسطر تشخيصية داخلية (تقييم شرط، تحويل صامت
  لموظف) كفقاعات محادثة كاملة داخل الموبايل — تبدو كأنها ستصل فعلياً للعميل رغم أنها ملاحظات للتاجر
  فقط. أصبحت هذه الأسطر تُعرَض كشارة نظام صغيرة مركزية (مثل رسائل واتساب النظامية الحقيقية) بدل فقاعة
  محادثة، منفصلة تماماً بصرياً عن رسائل العميل الحقيقية.

### تحقق فعلي
كتابة رد غير مطابق داخل شريط الموبايل — تأكيد بصري أن أسطر التشخيص تظهر كشارات منفصلة عن فقاعة الرسالة
الحقيقية. `tsc`/`build` نظيفان. تفاصيل القرار (154–155) في `DECISIONS.md`.

## 2026-07-25 (الجولة السابعة والعشرون) — معاينة الموبايل تصبح تفاعلية فعلياً

### إصلاح/تحسين
- "المعاينة الحية" (شكل الموبايل) كانت تعرض دائماً نفس المسار الافتراضي الثابت (أول فرع فقط) بغض
  النظر عن أي اختيار حقيقي — بدت "عشوائية" لأنها لا تعكس الرد الفعلي. شريط الكتابة أسفل الموبايل
  أصبح فعلياً الآن: الكتابة والإرسال منه تُشغِّل نفس الاختبار الحقيقي الموجود أصلاً في الصندوق
  الجانبي، فيعرض الموبايل النتيجة الصحيحة المطابقة لما كُتِب فعلاً (بلا أي منطق محاكاة جديد).

### تحقق فعلي
فتح قالب فيه شرط، تأكيد عرض الفرع الافتراضي أولاً، ثم كتابة رد غير مطابق مباشرة داخل الموبايل —
تأكيد بصري لتحوّل المعاينة فعلياً لعرض الفرع الصحيح. `tsc`/`build` نظيفان. تفاصيل القرار (152–153) في
`DECISIONS.md`.

## 2026-07-25 (إصلاح) — إزالة رسالة تحويل مزدوجة وهمية من لوحة اختبار الشات بوت

### إصلاح
- لوحة "اختبار قبل النشر" كانت تعرض رسالتي "تم تحويلك لموظف" متتاليتين رغم أن التدفق يحتوي رسالة
  تأكيد واحدة فقط قبل عقدة "تحويل لموظف" — السبب: المحاكي كان يُختلِق رسالة عميل إضافية عند كل وصول
  لعقدة "تحويل لموظف"/"رد ذكي"، بينما المحرك الحقيقي لا يُرسِل أي رسالة عند هاتين العقدتين إطلاقاً
  (تحويل صامت). استُبدلت الرسالة المُختلَقة بسطر تشخيصي داخلي واضح، ومعاينة الموبايل توقفت عن إضافة
  فقاعة لن تصل للعميل الحقيقي أبداً.

### تحقق فعلي
محاكاة نفس الحالات على تدفق "عسل مهمار" الحقيقي — رسالة تأكيد واحدة فقط + سطر تشخيصي واضح بدل التكرار
الوهمي. `tsc`/`build` نظيفان. تفاصيل القرارات (150–151) في `DECISIONS.md`.

## 2026-07-25 (الجولة السادسة والعشرون) — مرفقات صورة/فيديو/ملف في رسائل البوت

### ميزة جديدة
- عقدة "رسالة" تدعم الآن إرفاق صورة أو فيديو أو ملف (رابط مباشر) بدل/إضافة لخيار زر الرابط —
  الاثنان لا يجتمعان في رسالة واحدة (قيد حقيقي من واتساب). القناة الرسمية (Meta) والقناة التجريبية
  (QR) ترسلان الوسائط حقيقياً فعلياً (خلافاً لزر الرابط الذي يبقى نصاً بديلاً على القناة التجريبية).

### إصلاح أمني حقيقي اكتُشف أثناء التنفيذ (قبل أي نشر)
- قناة QR التجريبية (Baileys) تُحمِّل رابط الوسائط من خلال **خادمنا نفسه** (خلافاً لقناة Meta حيث
  خوادمها هي من تحمّل الرابط) — رابط أدخله التاجر كان بإمكانه استهداف عناوين شبكة داخلية. أُضيف نفس
  تحصين SSRF المستخدَم لعقدة "استدعاء API" (الجولة 21) قبل أي إرسال وسائط عبر هذه القناة.

### تحقق فعلي
اختبار SSRF مباشر لـ6 عناوين هجوم — كلها مرفوضة، ورابط سليم مقبول كضابط مقارنة. إضافة رسالة بمرفق
صورة عبر الواجهة، حفظ، تأكيد ظهورها في لوحة الاختبار. `tsc`/`build` نظيفان. تفاصيل القرارات (146–149)
في `DECISIONS.md`.

## 2026-07-25 (الجولة الخامسة والعشرون) — رابط بزر باسم مخصَّص في رسائل البوت

### ميزة جديدة
- عقدة "رسالة" في البناء التسلسلي يمكن الآن إرفاق رابط بها (متجر، صفحة، أي شيء) يظهر للعميل كزر
  باسم مخصَّص (مثال: "تصفّح المتجر الآن") بدل الرابط الخام — عبر رسالة واتساب "تفاعلية" حقيقية (CTA
  URL Button) على القناة الرسمية المتصلة فعلياً. قناة واتساب التجريبية (QR/Baileys) تُلحِق الرابط
  كنص واضح بدل زر حقيقي (لا دعم موثوق لرسائل الأزرار غير الرسمية).
- محاكي الاختبار والمعاينة الحية يعرضان الزر بصرياً بشكل مطابق لواتساب الحقيقي.

### تحقق فعلي
إضافة رسالة برابط عبر الواجهة على تينانت اختبار معزول، تأكيد ظهور الزر بصرياً في الاختبار الحي
والمعاينة الهاتفية. `tsc`/`build` نظيفان. تفاصيل القرارات (142–145) في `DECISIONS.md`.

## 2026-07-25 (الجولة الرابعة والعشرون) — عرض "متقدم" (رسم بياني) اختياري جنباً إلى جنب مع البسيط

### ميزة جديدة
- زر تبديل جديد أعلى محرر أي تدفق: **📋 عرض بسيط** (الافتراضي، الحالي) و**🕸️ عرض متقدم (رسم بياني)**
  — لوحة الرسم الحرة الأصلية (`@xyflow/react`) عادت كخيار إضافي (وليس استبدالاً) لمن يفضّل الربط
  اليدوي بالسحب/الإفلات، بجانب البناء التسلسلي البسيط. كلا العرضين يعملان على نفس بيانات التدفق
  المخزَّنة تماماً، بلا أي فرق في التخزين أو التنفيذ الفعلي.
- العرض المتقدم يدعم عقدة "قائمة اختيارات" الجديدة (مقبض خروج لكل فرع + الرد الافتراضي)، مع تحرير
  أسماء الخيارات وكلماتها المفتاحية محصور بالعرض البسيط لتفادي ازدواج واجهة التحرير.

### ملاحظة استخدام
التبديل بين العرضين قبل الضغط على "حفظ" يفقد أي تعديل غير محفوظ في العرض الذي تغادره — كل عرض يعمل
دائماً على آخر نسخة محفوظة فعلياً في القاعدة.

### تحقق فعلي
تأكيد ظهور عقدة شرط جاهزة بمقبضين ملوَّنين ومربوطين فعلياً في العرض المتقدم، إضافة عقدة "قائمة
اختيارات" منه مباشرة، حفظ، والتأكد من ظهور نفس المحتوى في العرض البسيط بلا فقدان بيانات. `tsc`/`build`
نظيفان. تفاصيل القرارات (139–141) في `DECISIONS.md`.

## 2026-07-24 (تحسين بصري) — خطوط ربط خفيفة بديلاً عن العودة للوحة رسم حرة

### قرار رفض موثَّق
- طلب خارجي جاهز ("Master Prompt") طالب صراحة بحذف البناء التسلسلي (الجولتين 22–23) والعودة للوحة
  Canvas حرة (React Flow، سحب/إفلات، minimap، zoom) — **رُفض التنفيذ** لأنه يعاكس مباشرة قرار المستخدم
  الصريح في الجولة 22 (استبدال كامل للوحة الحرة تحديداً بسبب اللغبطة التي كانت تسببها). عُرِض هذا
  التعارض على المستخدم رغم توجيه البرومنت الصريح بعدم السؤال، وأكّد المستخدم البقاء على البناء
  التسلسلي.

### تحسين مُنفَّذ بدلاً منه
خطوط ربط بصرية خفيفة (CSS/`div` بسيطة، بلا SVG أو مكتبة) فوق فروع عقدتي "شرط" و"قائمة اختيارات" —
توضّح أن كل فرع يخرج من نفس العقدة الأم، دون أي سحب/إفلات أو إحداثيات محفوظة أو تعقيد جديد. زخرفي
بحت، لا أثر على التخزين أو التنفيذ الفعلي.

## 2026-07-24 (الجولة الثالثة والعشرون) — عقدة "قائمة اختيارات" لتفريع احترافي متعدد الفروع

### مشكلة مُبلَّغة (لقطة شاشة حقيقية)
- تمثيل قائمة بأكثر من خيارين عبر تكديس عقد "شرط" المتداخلة (كل "لا" يحوي الشرط التالي) أنتج لوحة
  متضائلة العرض تدريجياً تحتاج تمريراً أفقياً — نفس نوع اللغبطة التي استُبدلت اللوحة الحرة أصلاً
  لتجنّبها.

### ميزة جديدة
- عقدة **"قائمة اختيارات" (menu)**: عدد غير محدود من الخيارات (كل خيار: اسم + كلمات مفتاحية) + رد
  افتراضي عند عدم تطابق أي خيار — كل الفروع تُعرَض **مكدَّسة عمودياً بعرض كامل** بدل التداخل الأفقي.
  متاحة لكل الباقات فوراً (نفس مستوى عقدة "شرط").
- تخزين بلا أي migration على قاعدة البيانات: الفروع كـJSON داخل حقل `config` الموجود أصلاً.

### تطبيق فعلي
أُعيد بناء تدفق "عسل مهمار" الحقيقي (تتبع الطلب/التوصيل/خدمة العملاء/السياسات) بعقدة قائمة واحدة بدل
4 شروط متداخلة — نفس السلوك الفعلي للعميل، بعرض احترافي بلا تمرير أفقي.

### تحقق فعلي
بناء العقدة فعلياً عبر الواجهة على تينانت اختبار معزول (تأكيد بصري + برمجي لعدم وجود تمرير أفقي).
محاكاة 5 حالات على التدفق الحقيقي المُعاد بناؤه — كل حالة أعادت الرد الصحيح. `tsc`/`build` نظيفان.
تفاصيل القرارات (133–137) في `DECISIONS.md`.

## 2026-07-24 (الجولة الحادية والعشرون) — محرك تنفيذ حقيقي للشات بوت + تحسينات صفحة القائمة

### ميزة جديدة (إغلاق فجوة موثَّقة منذ الجولة الثانية)
- **محرك تنفيذ حقيقي** (`lib/chatbot/engine.ts`): نشر تدفق لم يعد مجرد تغيير حالة — يُنفَّذ الآن
  فعلياً ضد رسائل واتساب واردة حقيقية، من نقطة واحدة مشتركة بعد `applyParsedMetaWebhook` تغطي
  webhook ميتا الحقيقي، محاكي الـSandbox، **وقناة واتساب QR التجريبية** معاً بلا كود مكرَّر.
- عقدة "شرط" تُقيَّم الآن ضد كلمات مفتاحية حقيقية (نفس حقل `label` الموجود مسبقاً في المحرر) مقابل
  آخر رسالة عميل فعلية — بنفس منطق محاكي الاختبار حرفياً (دالة تطابق مشتركة)، فتجربة "اختبار قبل
  النشر" أصبحت مطابقة تماماً للواقع.
- عقدة "استدعاء API" تنفّذ استدعاءً حقيقياً محصَّناً ضد SSRF بالكامل (انظر أدناه).
- عقدة "رد ذكي" تحوّل الآن فوراً وبصدق لموظف بشري (لا مزوّد ذكاء اصطناعي حقيقي مربوط بعد — قرار
  نطاق صريح موثَّق، وليس نسياناً).
- مهمة BullMQ متكررة جديدة تعيد أي محادثة عالقة عند سؤال بلا رد لأكثر من 30 دقيقة تلقائياً لتحكّم بشري.
- **صفحة قائمة الشات بوت** (`/dashboard/chatbot`): معاينة موبايل حقيقية لكل بطاقة، ملخّص عدد كل نوع
  عقدة، عدّاد "محادثة نشطة الآن فعلياً" حي لكل تدفق منشور، وزر "نسخ كمسودة جديدة".

### تحصين أمني (SSRF)
- `lib/chatbot/safeFetch.ts` جديد: HTTPS فقط، حلّ DNS ورفض أي IP خاص/محجوز بعد الحلّ (بما فيها
  endpoint البيانات الوصفية السحابية `169.254.169.254` ونطاقات IPv6 المكافئة)، مهلة 8 ثوانٍ، حد حجم
  استجابة 64KB، بلا تتبّع إعادة توجيه تلقائياً — لازم لأن رابط هذه العقدة يأتي من إعداد التاجر نفسه
  (غير موثوق بالكامل).

### تغييرات هيكلية إضافية بحتة (بلا فقدان بيانات)
- حقول `Conversation.activeFlowId`/`currentNodeId`/`flowNodeEnteredAt` جديدة لتتبّع أي محادثة حقيقية
  واقفة الآن عند أي عقدة، لأي تدفق.

### تحقق فعلي
محاكاة محادثتين حقيقيتين كاملتين على تينانت اختبار معزول (مسار "نعم" الناجح ومسار "لا"/التحويل)
مع تأكيد إرسال رسائل `BOT` فعلية وتقدُّم/تصفير حالة الجلسة بشكل صحيح في كل خطوة. اختبار SSRF فعلي على
6 عناوين هجوم مختلفة — كلها مرفوضة بالسبب الصحيح. اختبار مهمة انتهاء الجلسة بمحادثة عالقة يدوياً —
تصفير فعلي مؤكَّد. Playwright لصفحة القائمة: تأكيد ظهور كل العناصر الجديدة واختبار زر النسخ فعلياً في
القاعدة. كل بيانات الاختبار حُذفت بعدها. `tsc`/`build` نظيفان. تفاصيل القرارات (123–128) في
`DECISIONS.md`.

## 2026-07-24 (الجولة العشرون) — استيراد جهات اتصال من Excel/CSV

### ميزة جديدة
- **صفحة جهات الاتصال**: استيراد جماعي من ملف Excel/CSV (upsert — يحدّث الموجود بدل الفشل عند
  التكرار)، نموذج تنزيل جاهز، بحث بالاسم/الجوال، تقرير أخطاء واضح لكل صف مرفوض.
- **معالج إنشاء حملة جديدة**: خيار ثالث "استيراد قائمة من Excel" لاستهداف جمهور الحملة — يستورد جهات
  الاتصال فعلياً، يسم الدفعة بوسم فريد، ويستهدف الحملة بهذا الوسم عبر آلية الفلترة بالوسوم الموجودة
  أصلاً — بلا أي تعديل على منطق حساب الجمهور أو حدود الباقة أو عامل الإرسال.

### قرار أمني أثناء التنفيذ
- المكتبة المخطط لها أصلاً (`xlsx`/SheetJS) تبيّن أنها تحمل ثغرتين حقيقيتين غير مُصلَحتين (Prototype
  Pollution + ReDoS) تُستَغَلّان مباشرة بحالة الاستخدام هذه (ملف مرفوع من مستخدم غير موثوق) —
  استُبدلت فوراً بـ`exceljs` قبل كتابة أي كود يعتمد عليها.

### إجراءات أمنية مطبَّقة فعلياً
حد حجم ملف صريح (5MB) + حد صفوف صارم (2000) + قائمة امتدادات مسموحة + rate limiting (5/ساعة لكل
مستأجر) + كل الكتابة عبر `withTenant` + إعادة نتيجة مكتوبة بدل رمي أخطاء خام.

### تحقق فعلي
رفع ملف حقيقي بمتصفح فعلي (صفوف صحيحة + صف مرفوض) وتأكيد ظهور جهات الاتصال فعلياً. اختبارات أمنية
فعلية: امتداد مرفوض، محتوى ملف مزوَّر، وتجاوز حد المعدّل — الثلاثة رُفضت كما هو متوقَّع. معالج الحملة:
تأكيد إنشاء الوسم وربطه بالقاعدة الحقيقية وظهور العدد الصحيح في المعاينة. بيانات الاختبار حُذفت بعدها.
تفاصيل القرارات (119–122) في `DECISIONS.md`.

## 2026-07-24 (الجولة التاسعة عشرة) — لوحة "محتوى الموقع" + مراجعة أمنية شاملة + تجهيز إطلاق

### ميزة جديدة
- **لوحة "محتوى الموقع"** (`/admin/content`، صلاحية جديدة `platform.content.manage`): مالك المنصة
  يعدّل نصوص الصفحة الرئيسية بالكامل (Hero، الميزات، الإحصائيات، آراء العملاء، العملاء المميزون،
  بانر التواصل) بدون كود — موديل `SiteContent` جديد بنفس نمط `PlatformSettings`. الصور تُدخَل كرابط
  URL (لا بنية رفع ملفات جديدة — لم تكن موجودة أصلاً في المشروع). النطاق مقصور على الصفحة الرئيسية
  هذه الجولة (قرار موثَّق)؛ باقي الصفحات تبقى كوداً ثابتاً حالياً بنفس النمط القابل للتوسّع لاحقاً.

### إصلاحات أمنية حقيقية (من مراجعة شاملة فعلية، وليست نظرية)
- **حرجة**: `lib/crypto.ts` كانت تسقط لمفتاح تشفير ثابت مكشوف في الكود عند غياب `ENCRYPTION_KEY` —
  أصبحت ترمي خطأ فوري صريح بدل تشفير ضعيف صامت.
- تحصين إضافي على `withTenant()` (نقطة عزل RLS الأهم في المنصة): رفض أي `tenantId` لا يطابق صيغة
  cuid الصحيحة قبل استخدامه في استعلام SQL خام.
- إضافة تحديد معدّل (rate limiting) لعمليات كانت بلا أي حد: تسجيل تاجر عام، دعوات الفريق (تاجر
  وداخلي)، وإرسال ردود واتساب من صندوق المحادثات.
- تحويل مقارنة رمز تحقق webhook ميتا لمقارنة بزمن ثابت بدل `===` عادية.

### تجهيز الإطلاق
- مدة جلسة NextAuth صريحة (7 أيام) بدل الافتراضي الضمني (30 يوماً).
- قائمة عوائق خارجية موثَّقة (تحتاج قرار/فعل من المستخدم لا كوداً): دومين واستضافة حقيقيين، مزوّد
  بريد حقيقي، `META_APP_SECRET` فعلي.

### تحقق فعلي
تعديل حقيقي لعنوان الصفحة الرئيسية وتأكيد ظهوره لزائر حقيقي غير مسجَّل (متصفح منفصل)، ثم إعادته.
اختبار فشل صريح فعلي لغياب `ENCRYPTION_KEY`، رفض فعلي لمحاولة SQL injection عبر `withTenant`، وتأكيد
عملي لحد التسجيل الجديد (5 مقبولة/السادسة مرفوضة). `tsc`/`build` نظيفان. تفاصيل القرارات (111–118)
في `DECISIONS.md`.

## 2026-07-23 (الجولة الثامنة عشرة) — استيراد المحادثات القديمة + استئناف جلسات QR تلقائياً

### تطوير
- **استيراد المحادثات القديمة**: بعد ملاحظة المستخدم أن صندوق المحادثات يبقى فارغاً رغم اتصال حقيقي
  ناجح، أُضيف `historyImport.ts` يستمع لحدث Baileys `messaging-history.set` ويستورد آخر 50 رسالة لكل
  جهة اتصال فردية (بلا مجموعات/قوائم بثّية)، بمسار منفصل عمداً عن معالجة الرسائل الحية.
- **اكتشاف حقيقي مهم**: هذا الحدث يصل فقط عند **إقران جهاز جديد فعلياً** (مسح QR جديد)، وليس عند
  استئناف جلسة قائمة — سلوك بروتوكول واتساب نفسه، وليس خللاً بالكود. أُبلغ المستخدم بوضوح: يحتاج قطع
  الاتصال الحالي ثم مسح QR جديد ليحصل فعلياً على السجل القديم.
- **إصلاح موثوقية**: اكتُشف أثناء التحضير لهذه الجولة أن إعادة تشغيل عملية العامل كانت تترك أي قناة
  QR متصلة سابقاً "متصلة" ظاهرياً في القاعدة بلا socket فعلي حي. أُضيفت `resumeConnectedQrSessions()`
  في `worker.ts` تستأنف تلقائياً كل الجلسات المتصلة عند إقلاع العامل ببيانات الاعتماد المحفوظة — بلا
  حاجة لمسح QR جديد. تحقُّق فعلي: أُعيد تشغيل العامل والاتصال الحقيقي عاد تلقائياً خلال ثوانٍ.

تفاصيل القرارات (108–110) في `DECISIONS.md`.

## 2026-07-23 (الجولة السابعة عشرة) — قناة واتساب تجريبية عبر QR (Baileys) بديلة عن Meta

### ميزة جديدة
- **مزوّد جديد `WHATSAPP_QR`**: مسح باركود شبيه بواتساب ويب لربط أي رقم واتساب شخصي فوراً بلا أي
  اعتماد على مراجعة/إعلانات Meta — قناة **تجربة محدودة المدة (3 أيام موحّدة لكل الباقات)** فقط، مع
  تحذير مخاطر صريح وموافقة إلزامية (checkbox) قبل التفعيل (المكتبة غير رسمية وتخالف شروط استخدام
  واتساب — خطر حظر الرقم حقيقي وموثَّق للمستخدم).
- الاتصال الحي (Baileys socket) يعيش داخل عملية `npm run worker` فقط (وليس Next.js)، بجسر عبر
  BullMQ (أوامر ابدأ/أوقف) وRedis (حالة لحظية/QR يُستطلَع من الواجهة كل 2.5 ثانية).
- جلسة الاعتماد تُحفَظ مشفّرة في قاعدة البيانات (عمود جديد `Integration.encryptedSessionData`) بدل
  الكتابة على القرص المحلي — تنجو من إعادة تشغيل عملية العامل.
- الرسائل الواردة/الصادرة تمر بنفس مسار الكود الموحَّد (`applyParsedMetaWebhook`) المستخدم أصلاً
  لواتساب Meta ومحاكي Sandbox — لا منطق مكرر. الحملات والردود اليدوية تختار تلقائياً القناة المتصلة
  فعلياً (Meta له الأولوية دائماً، ثم QR كبديل) عبر `resolveWhatsappAdapter` الجديدة.
- مهمة BullMQ متكررة (كل 30 دقيقة) تقطع أي قناة تجريبية تجاوزت 3 أيام وتمسح بيانات جلستها تلقائياً.

### حادثة حقيقية أثناء الاختبار (موثَّقة بالكامل في DECISIONS.md قرار 107)
أثناء اختبار آلي لظهور رمز QR، اتصل رقم واتساب حقيقي فعلي بمسح فعلي — سكربت تنظيف افترض خطأً أنه
جلسة اختبار يتيمة فقطعه فعلياً قبل التحقق. أُبلغ المستخدم فوراً بالحادثة، وأكمل هو الاختبار يدوياً.
الدرس: أي أمر إيقاف على قناة فيها اتصال خارجي حقيقي يحتاج تحقق حالة حية أولاً، وليس افتراض بيانات اختبار.

### تحقق فعلي
متصفح حقيقي: بوابة الموافقة تعمل، رمز QR حقيقي فعلي ظهر خلال ثوانٍ من خادم واتساب. اختبار الحذف
التلقائي عبر تينانت وهمي معزول تماماً (لم يمسّ بيانات DMS الحقيقية). اختبار بوابة الصلاحية: AGENT
مؤقت لا يرى أي زر للقناة. `tsc`/`build` نظيفان. تفاصيل القرارات (103–107) في `DECISIONS.md`.

## 2026-07-23 (الجولة السادسة عشرة) — admin/team كصفحة إدارة كاملة: أعضاء + صلاحيات شاملة + الشركاء

### تطوير
- **إدارة أعضاء كاملة**: تغيير دور عضو حالي حياً (`changeInternalStaffRole`، يستثني `SUPER_ADMIN`
  دائماً)، حذف نهائي (`removeInternalStaff`، يستثني حذف النفس وحذف `SUPER_ADMIN`)، و"إعادة إرسال
  رابط الإعداد" (`resendStaffSetupLink`) لعضو لم يكمل إعداد كلمة مروره بعد — حالته تُكتشف حياً من
  وجود `AccountSetupToken` غير مستخدَم وتظهر كشارة "بانتظار إكمال الإعداد" صريحة في القائمة.
- **مصفوفة صلاحيات شاملة**: وسِّعت من `platform.*` فقط (3 أدوار) لتغطي كل الصلاحيات الستة عشر
  الإضافية الخاصة بلوحة التاجر (`campaigns.*`, `chatbot.*`, `contacts.*`, `inbox.*`, إلخ) وكل
  الأدوار الستة (`SUPER_ADMIN`/`PLATFORM_SUPPORT`/`PLATFORM_BILLING`/`OWNER`/`ADMIN`/`AGENT`) في
  جدول واحد بقسمين واضحين — `TENANT_PERMISSION_LABELS_AR` و`ALL_ROLES_ORDERED` جديدتان في `rbac.ts`.
- **لوحة "الشركاء"**: عدد طلبات انضمام الشركاء المعلَّقة، عدد التجار الكلي، وآخر 5 طلبات بحالتها —
  بيانات حية من `ApprovalRequest`/`Tenant`، مع روابط سريعة لمركز الموافقات وقائمة التجار. قرار تفسير
  موثَّق: لم تُدمَج إدارة فريق التاجر (`OWNER`/`ADMIN`/`AGENT`) هنا لأنها موجودة بالفعل ومنفصلة عمداً
  في `admin/tenants/[id]/team`.

### تحقق فعلي
اختبار متصفح حقيقي كامل (Playwright): دعوة → شارة "بانتظار الإعداد" → إعادة إرسال الرابط → تغيير
الدور → تفعيل/تعطيل → تأكيد ظهور المصفوفة الشاملة ولوحة الشركاء بأرقام حية → حذف نهائي → تأكيد
اختفاء العضو. تحقُّق مباشر من القاعدة بعدها: صفر بيانات متبقية (حذف تتالي فعلي لرموز الإعداد). صفر
أخطاء console/صفحة. تفاصيل القرارات (100–102) في `DECISIONS.md`.

## 2026-07-23 (الجولة الخامسة عشرة) — إصلاح صفحة "فريق المنصة الداخلي" + مصفوفة صلاحيات حقيقية

### إصلاحات حقيقية
- **الخطأ المُبلَّغ عنه**: دعوة عضو ببريد إلكتروني مكرَّر كانت تُسقِط الصفحة بشاشة خطأ Next.js غير
  معالجة. السبب: استثناء خام من Server Action مربوطة مباشرة بـ`<form action>` بلا مكوّن عميل يلتقطه.
  أُصلح بتحويل `inviteInternalStaff`/`toggleInternalStaffActive` لإعادة نتيجة مكتوبة، مع مكوّني عميل
  جديدين (`InviteStaffForm`, `ToggleActiveButton`) يعرضان الأخطاء رسالة ودّية داخل الصفحة.
- **فجوة أعمق اكتُشفت أثناء الإصلاح**: العضو المدعو لم يكن يملك أي طريقة لمعرفة كلمة مروره أو تسجيل
  الدخول إطلاقاً (كلمة مرور مؤقتة تُولَّد ثم تُفقَد بلا إرسال). أُصلحت بإعادة استخدام آلية "رابط إعداد
  حساب آمن" نفسها المبنية أصلاً لنظام الشركاء — أُعيدت تسمية `PartnerSetupToken` إلى `AccountSetupToken`
  (جدول/مسار مشترك الآن بين نظام الشركاء وفريق المنصة الداخلي بدل التكرار).

### تطوير
- **مصفوفة صلاحيات حقيقية** في `admin/team`: جدول يعرض فعلياً ما يملكه كل دور (مالك المنصة/دعم فني/
  مالي) من صلاحيات `platform.*`، مُشتقّ حياً من `hasPermission()` في `lib/rbac.ts` — يتحدّث تلقائياً
  مع أي تعديل مستقبلي في تعريف الصلاحيات، بدل نص ثابت منفصل قد ينحرف عن الواقع.

### تحقق فعلي
أُعيد إنتاج سيناريو العطل بالضبط عبر متصفح حقيقي (تأكيد الإصلاح)، ثم اختُبر المسار الكامل: دعوة عضو
جديد ← بريد إعداد حساب فعلي ← تحديد كلمة مرور ← تسجيل دخول ناجح فعلياً بالحساب الجديد. تفاصيل القرارات
(96–99) في `DECISIONS.md`.

## 2026-07-23 (الجولة الرابعة عشرة) — إصلاح انيميشن Hero: إيموجي مكسورة استُبدلت بأيقونات واتساب فعلية

### إصلاح حقيقي
- انيميشن الـHero بالصفحة الرئيسية كان يستخدم إيموجي عامة (🏬💬🛒✅🙂) — تبيَّن من لقطة شاشة حقيقية
  أنها تُعرَض مكسورة (مربعات فارغة) أو بغلاف مختلف تماماً حسب نظام التشغيل/الخط، ولا تحمل أي طابع
  "واتساب" واضح. استُبدلت بالكامل بأيقونات SVG مرسومة: أيقونة متجر وأيقونة عميل احترافيتان، وفقاعة
  رسالة مرسومة بأسلوب واتساب الفعلي (فقاعة خضراء + ذيل + علامة استلام مزدوجة بيضاء ✓✓ — نفس عنصر
  واجهة واتساب الحقيقي، وليس رمزاً عاماً).
- تكبير الحاوية والعناصر (من h-64 إلى h-80، ومن أيقونات 20×20 إلى 24×24) وإضافة تسميات "المتجر"/
  "العميل" توضيحية أسفل كل أيقونة — مظهر أكبر وأوضح كما طُلب مباشرة.

## 2026-07-23 (الجولة الثالثة عشرة) — إعادة تصميم علامة "DMS" البديلة

### تحسين
- **علامة "DMS" البديلة** (تظهر حتى يُرفَع ملف الشعار الحقيقي) أُعيد تصميمها: "DMS" أكبر وأكثر
  بروزاً في المنتصف، مع إبراز الحرف الأول من كل كلمة في "Digital Messaging System" بلون العلامة
  الأخضر — يُظهر بصرياً أن الاختصار مبني فعلياً من هذه الكلمات الثلاث.

### إصلاح حقيقي
- اكتُشف بالاختبار الفعلي أن اتجاه الصفحة RTL كان يعكس ترتيب الكلمات الإنجليزية الثلاث بصرياً
  (تظهر معكوسة: System Messaging Digital). أُصلح بإضافة `dir="ltr"` صريح لحاوية اسم العلامة فقط.
  تفاصيل في `DECISIONS.md` (قرارات 94–95).

## 2026-07-23 (الجولة الثانية عشرة) — تطوير صفحة "تواصل معنا" وتحويل قسمها بالرئيسية لبنر

### إضافات
- **قسم "تواصل معنا" في الصفحة الرئيسية أصبح بنر دعوة** (تدرّج لوني + عنوان + زر) يوجّه لصفحة
  `/contact` الكاملة، بدل تكرار النموذج بالكامل في مكانين.
- **صفحة `/contact` طُوِّرت بأدوات فعلية جديدة**: حقل "نوع الاستفسار" (مبيعات/دعم فني/شراكة/اقتراح/
  أخرى) يُدرَج في عنوان بريد الإشعار للتوجيه السريع، زر نسخ حقيقي بجانب البريد والواتساب، أيقونات SVG
  مخصَّصة بدل الإيموجي، شريط ثقة (رد خلال ساعتين، دعم 6 أيام، فريق عربي)، وأسئلة شائعة خاصة بالتواصل.
- **`admin/leads`** يعرض الآن شارة نوع الاستفسار لكل رسالة واردة.
- عمود جديد `ContactMessage.inquiryType` في قاعدة البيانات (migration حقيقية، وليس حقلاً شكلياً في
  الواجهة فقط).

### إصلاح حقيقي اكتُشف أثناء الاختبار
- تصدير `INQUIRY_TYPES` كثابت عادي من `contact/actions.ts` (ملف `"use server"`) كان يُكسر الصفحة
  بالكامل على العميل (`INQUIRY_TYPES.map is not a function`، الصفحة تُرجع 500) — أي ملف بتوجيه
  `"use server"` يُسمح فيه فقط بتصدير دوال `async`. أُصلح بنقل الثابت لملف مشترك منفصل بلا أي توجيه
  خاص (`constants.ts`).

### تحقق فعلي
اختُبر إرسال رسالة فعلية بنوع استفسار محدَّد حتى ظهور رسالة النجاح، وتأكيد وصول البريد بعنوان يحمل
بادئة النوع فعلياً، وظهوره في `admin/leads`. تفاصيل القرارات (90–93) في `DECISIONS.md`.

## 2026-07-23 (الجولة الحادية عشرة) — إعادة تسمية شاملة إلى DMS (Digital Messaging System)

### إضافات
- **إعادة تسمية المنصة بالكامل** من "واصل" إلى **DMS — Digital Messaging System** عبر كل الموقع
  التسويقي، صفحات الدخول والتسجيل ونظام الشركاء، رسائل البريد الإلكتروني، وعنوان الصفحة (metadata).
- **مكوّن شعار موحَّد** (`Logo.tsx`): يعرض ملف الشعار الحقيقي (`public/logo.png`) فور توفره، وإلا
  علامة بديلة احترافية مرسومة (SVG بنفس ألوان الهوية + كتابة DMS) — لا حاجة لأي تعديل كود عند رفع
  الملف الحقيقي لاحقاً.
- **غلاف دخول موحَّد جديد (`AuthShell`)**: لوحة ترويجية بأسلوب "إعلان شركة" احترافي (شعار + جملة
  تعريفية + نقاط قوة + إحصائيات) مطبَّقة الآن على تسجيل الدخول (تاجر ومالك منصة معاً)، التسجيل الذاتي،
  وكل مراحل نظام الشركاء (تقديم الطلب، تتبع الحالة، إعداد الحساب) — هوية بصرية واحدة بدل صفحات متفرقة.
  الأزرار والروابط الرئيسية تحوَّلت من الأرجواني القديم إلى الأخضر الجديد لمطابقة الهوية.
- **صفحة "انضم كشريك" (`partners/join`)** أُعيد تصميمها بالكامل بالنمط الفاتح ليطابق الموقع التسويقي.
- **دعم شعار حقيقي لكل تاجر** في الشريط الجانبي للوحة تحكمه (`Sidebar.logoUrl`، مبني على `Tenant.logoUrl`
  الموجود أصلاً في المخطط لكن غير المُستخدَم من قبل) — تينانت المستخدم الحقيقي "dms" (الاسم كان DMS
  فعلاً مسبقاً) حُدِّث ليستخدم نفس شعار المنصة ولونها الأخضر الجديد.

### إصلاح حقيقي مهم اكتُشف أثناء الاختبار
- **سباق hydration** كان يمنع ظهور العلامة البديلة للشعار بالكامل: `<img>` مُصيَّر من الخادم قد يفشل
  تحميله (404) *قبل* أن يُعلِّق React مُستمِع `onError` أثناء الـhydration، فيُفقَد الحدث ويبقى الشعار
  صورة مكسورة دائماً. أُصلح بتأجيل إضافة `<img>` للـDOM حتى اكتمال التركيب الفعلي على العميل، في كل من
  مكوّن الشعار العام والشعار الخاص بكل تاجر في الشريط الجانبي.

### تحقق فعلي
اختُبر عبر Playwright: تسجيل الدخول، التسجيل، نظام الشركاء الكامل، الصفحة الرئيسية، ولوحة مالك المنصة —
تأكيد ظهور اسم DMS والشعار (الحقيقي أو البديل) بشكل صحيح ومتسق، مع اكتشاف وإصلاح خلل السباق أعلاه أثناء
هذا التحقق نفسه. تفاصيل القرارات (83–89) في `DECISIONS.md`.

**ملاحظة للمستخدم**: الشعار الحقيقي (الصورة المرفَقة) لم يُحفَظ تلقائياً من المحادثة — لا توجد أداة
لاستخراج ملف من صورة مُرسَلة داخل الرسالة مباشرة. يرجى حفظ الملف باسم `public/logo.png` في المشروع
(وإضافة نسخة مربعة في `src/app/icon.png` إن رغبت بأيقونة تبويب مخصصة)، وسيظهر تلقائياً في كل مكان بلا
أي تعديل كود إضافي.

## 2026-07-23 (الجولة العاشرة، تكملة) — إثراء الصفحة الرئيسية بناءً على طلب مباشر

### إضافات
- **باقات الأسعار الحقيقية معروضة الآن في الصفحة الرئيسية أيضاً** (نفس المكوّن والبيانات الحقيقية
  المستخدمة في `/pricing`)، مع رابط لصفحة الأسعار الكاملة بتفاصيلها والأسئلة الشائعة.
- **قسم "عملاء مميزون" جديد** بشريط شعارات متحرك لا نهائي (CSS خالص، بلا مكتبة خارجية) — شعارات نائبة
  حالياً بانتظار شعارات فعلية لعملاء حقيقيين موافقين على الظهور (موثَّق كفجوة في DECISIONS.md).
- **قسم "تواصل معنا" مختصر مباشرة قبل الفوتر** في الصفحة الرئيسية (نفس نموذج التواصل ومساره الخادمي
  الحقيقي من صفحة `/contact`، وليس نسخة موازية) — لا حاجة لمغادرة الرئيسية للتواصل مع الفريق.
- **أيقونات SVG مخصَّصة لكل خدمة** (حملات، شات بوت، CRM، تكامل، شارة توثيق واتساب) بدل الإيموجي، في
  كل من الصفحة الرئيسية وصفحة `/services` — مظهر احترافي متسق بغض النظر عن نظام تشغيل الزائر.

### تحقق فعلي
اختُبرت الرئيسية وصفحة الخدمات عبر Playwright بعد تمرير فعلي كامل للصفحة — تأكيد ظهور كل الأقسام
الجديدة بشكل صحيح على سطح المكتب والجوال، بلا أي خطأ JavaScript. تفاصيل القرارات (79–82) في
`DECISIONS.md`.

## 2026-07-23 (الجولة العاشرة) — الموقع التسويقي العام الكامل

### إضافات
- **موقع تسويقي عام كامل** بهوية بصرية مستقلة (أخضر مستوحى من واتساب، وليس شعاره الرسمي) عن لوحة
  التحكم الداخلية: الرئيسية، الخدمات، من نحن، رؤيتنا ومنهجيتنا، الأسعار، تواصل معنا، الشروط، الخصوصية.
- **صفحة الجذر `/` أصبحت الموقع التسويقي نفسه** لزوار بلا جلسة دخول (بدل إعادة توجيه فورية لتسجيل
  الدخول كما كانت)، مع الحفاظ الكامل على سلوك إعادة التوجيه القائم لمن هو مسجَّل دخول بالفعل.
- **Dark Mode حقيقي** (تبديل فعلي، ليس تصميماً داكناً ثابتاً) للموقع التسويقي فقط، بمنع كامل لوميض
  تغيّر اللون (FOUC) عبر سكربت مضمَّن يقرأ التفضيل المحفوظ قبل أول رسم.
- **انيميشن Hero** يوضّح بصرياً فكرة الخدمة (فقاعات رسائل تتحرك بين متجر وعميل) عبر CSS/SVG خفيف
  الوزن بدل Lottie فعلي (قرار أداء موثَّق — راجع DECISIONS.md قرار 74)، مع Scroll Animations خفيفة
  (IntersectionObserver خام، بلا مكتبة خارجية) للأقسام الواقعة فعلياً تحت الطية فقط.
- **صفحة الأسعار** تسحب الباقات الحقيقية حياً من قاعدة البيانات (نفس دالة نظام الشركاء)، مع مفتاح
  شهري/سنوي (خصم 20%) وقسم أسئلة شائعة.
- **نموذج "تواصل معنا"** فعلي: يحفظ الرسالة في جدول جديد (`ContactMessage`، منفصل عن `Contact` الخاص
  بالتجار)، يرسل إشعاراً بريدياً لفريق الدعم، ومحمي أساسياً من الإرسال الآلي (حقل فخّ + حد زمني أدنى +
  حد IP). صفحة إدارية جديدة `admin/leads` لمراجعة الرسائل الواردة.
- **زر واتساب عائم ثابت** في كل صفحات الموقع التسويقي (رقم حقيقي من إعدادات المنصة، يختفي تلقائياً
  إن لم يُضبط بعد)، وفوتر شامل بأيقونات سوشيال ميديا ونقاط دخول الحسابات الثلاث كأزرار واضحة.
- **SEO أساسي**: `sitemap.xml`/`robots.txt` (تستثنيان لوحات التحكم وروابط الإعداد الحسّاسة من
  الفهرسة)، وعنوان/وصف مخصص لكل صفحة.

### إصلاحات حقيقية اكتُشفت أثناء الاختبار الفعلي
- عيب تجربة مستخدم حقيقي: محتوى Hero وعناوين الصفحات الرئيسية كانت مغلَّفة بنفس مكوّن الأنيميشن عند
  التمرير، فتظهر باهتة للحظة عند أول تحميل رغم كونها مرئية أصلاً منذ اللحظة الأولى. أُزيل الأنيميشن من
  كل محتوى "أعلى الطية" (راجع قرار 75).
- **إصلاح أمني**: قوالب بريد نظام الشركاء (من الجولة السابقة) كانت تُدرِج بيانات مُدخَلة من زائر عام
  (اسم المتقدّم) مباشرة في HTML بلا تهريب — حقن HTML محتمل داخل بريد فعلي. أُصلح بإضافة `escapeHtml`
  وتطبيقها في كل قوالب البريد ذات الصلة.

### تحقق فعلي
اختُبر الموقع كاملاً عبر متصفح حقيقي (Playwright) على أبعاد سطح مكتب وجوال: ظهور فوري بلا وميض، عمل
الأقسام المتحركة بعد تمرير فعلي، القائمة المتنقلة، تبديل الوضع الداكن، الأزرار الثلاثة (انضم كشريك/
لوحة التاجر/تسجيل الدخول) تصل لمساراتها الصحيحة فعلياً، ونموذج التواصل يعمل من الإرسال حتى الظهور في
لوحة الإدارة — بما في ذلك تأكيد أن محاولة حقن `<script>` تُعرَض كنص آمن. تفاصيل كاملة (قرارات 71–78)
في `DECISIONS.md`.

## 2026-07-23 (الجولة التاسعة، تكملة) — نظام "الشركاء": صفحة انضمام عامة تغذّي مركز الموافقات

### إضافات
- **صفحة تسويقية عامة `/partners/join`** مستقلة تماماً عن صفحة الدخول، تعرض مزايا المنصة والباقات
  الحقيقية الفعلية من جدول Plan (وليست بيانات ثابتة في الكود)، مع دعوة واضحة لتقديم طلب انضمام.
- **معالج طلب انضمام متعدد الخطوات `/partners/apply`**: نوع النشاط ← بيانات العمل ← اختيار الباقة
  (باقات حقيقية مسحوبة حياً من قاعدة البيانات، قابلة للتعديل من `admin/plans`) ← الموافقة على الشروط
  ← مراجعة وإرسال. حماية أساسية من الإرسال الآلي (حقل فخّ مخفي + حد زمني أدنى 3 ثوانٍ) + حد 5
  طلبات/ساعة لكل عنوان IP + كشف طلب مكرر بنفس البريد الإلكتروني قبل الإنشاء.
- **صفحة تتبع حالة عامة `/partners/status`**: يدخل المتقدّم رقم طلبه وبريده الإلكتروني (بدون أي حساب)
  لعرض الحالة الحالية وأي ملاحظة من المراجع.
- **تكامل كامل مع مركز الموافقات الموجود فعلاً** (وليس نظاماً موازياً): طلبات الشركاء تصل كنوع جديد
  `PARTNER_APPLICATION` في نفس `ApprovalRequest`/`ApprovalCard` المستخدمين لكل الطلبات الأخرى (تسجيل
  تاجر، تحقق واتساب، قوالب، باقات مخصصة) — تظهر لفريق المنصة في نفس الصفحة بنفس تصميم البطاقات.
- **إنشاء تينانت تلقائي كامل عند الموافقة**: تينانت حقيقي بحالة TRIAL جاهزة فوراً، مستخدم OWNER،
  اشتراك بالباقة المطلوبة فعلياً بحدودها الصحيحة، وثلاث تكاملات (Meta/Zid/Salla) بحالة DISCONNECTED
  الافتراضية — نفس تسلسل الإنشاء المستخدم في `/register` الذاتي.
- **رابط "إعداد حساب" آمن بدل بريد بكلمة مرور جاهزة**: بريد يحوي رابطاً برمز عشوائي مُجزَّأ (SHA-256،
  صلاحية 7 أيام، استخدام واحد) — الحساب غير قابل لتسجيل الدخول إطلاقاً حتى يحدد المتقدّم كلمة مرور
  حقيقية بنفسه عبر `/partners/setup/[token]`.
- **إشعارات بريدية تلقائية** عند: استلام الطلب، القبول (رابط الإعداد)، الرفض (السبب — إلزامي الآن
  لهذا النوع تحديداً، لا يمكن للأدمن الرفض بلا سبب)، وطلب معلومات إضافية.
- طبقة إرسال بريد جديدة (`lib/email/`) بمزود `console` افتراضي (يطبع الرسالة في سجل الخادم) — بنفس
  نمط `PAYMENT_PROVIDER="mock"` الموجود أصلاً، جاهزة بنيوياً لمزود SMTP حقيقي لاحقاً.

### تحقق فعلي
اختُبر المسار الكامل عبر متصفح حقيقي (Playwright، ليس استدعاء دوال مباشرة): تقديم طلب → كشف تكرار →
تتبع حالة → دخول مالك المنصة الحقيقي → ظهور الطلب في مركز الموافقات → الموافقة الفعلية (تينانت حقيقي
كامل) → تسجيل الدخول الفعلي بالحساب الجديد بعد إكمال رابط الإعداد → رفض إعادة استخدام رمز مُستهلَك →
مسار الرفض (سبب إلزامي يصل بالبريد فعلياً). كل بيانات الاختبار حُذفت من القاعدة الحقيقية بعد التحقق.
تفاصيل القرارات المعمارية (65–70) في `DECISIONS.md`.

## 2026-07-23 (الجولة التاسعة) — تفعيل حقيقي للمنصة + تصلّب أمني شامل قبل الإطلاق

### تفعيل حقيقي (وليس محاكاة)
- **ربط رقم واتساب حقيقي فعلياً**: تاجر جديد كامل ("dms"، الرقم 996509095816) رُبط عبر الطريقة
  اليدوية ببيانات Meta Business حقيقية (WABA ID، Phone Number ID، Access Token)، مع تحقق Graph API
  حقيقي ناجح واشتراك webhook فعلي.
- **إعداد المنصة العام تحوَّل إلى `live`** (`PlatformSettings.integrationsMode`) بدلاً من `sandbox` —
  أي ربط تكامل جديد من الآن فصاعداً يحاول Graph API حقيقياً افتراضياً بدل محاكاة.
- **تنظيف بيانات وهمية**: حذف نهائي (كاسكيدي) للتينانتس التجريبية الثلاثة (متجر الأناقة، بوتيك لمسة،
  التينانت المعلّق) بطلب صريح من المستخدم، مع الإبقاء على تينانت "dms" الحقيقي وحسابات فريق المنصة
  والباقات الأساسية سليمة تماماً.
- **تدوير كامل للأسرار الإنتاجية**: `ENCRYPTION_KEY`، `NEXTAUTH_SECRET`، `META_WEBHOOK_VERIFY_TOKEN`،
  وكلمات مرور أدوار Postgres الثلاثة — تدوير حقيقي على القاعدة الحيّة (`ALTER ROLE`) وليس فقط في ملف
  `.env`، بعد تحقق مسبق أن لا أسرار مشفَّرة قديمة كانت ستُفقَد.

### مراجعة أمنية شاملة + إصلاحات حقيقية
- **إصلاح حرِج**: تحقق توقيع webhook (Meta/Zid/Salla) كان يتجاوَز بالكامل عند إعداد المنصة العام
  (وليس التاجر تحديداً) sandbox — ثغرة كانت تسمح بتزوير أحداث واتساب/طلبات غير موقّعة لأي تاجر. أُزيل
  التجاوز نهائياً؛ التحقق الآن fail-closed دائماً في الحالتين.
- **حد محاولات تسجيل الدخول**: 8 محاولات فاشلة/10 دقائق لكل بريد إلكتروني (Redis)، مع مقارنة bcrypt
  دائمة التنفيذ (حتى عند غياب المستخدم) لتفادي فرق توقيت يكشف البريد المسجَّل.
- **RBAC**: حجب كامل لصفحتي "الفوترة" و"الفريق والإعدادات" عن الأدوار التي لا تملك الصلاحية (كانت
  تُعرَض بلا أي فحص)، وفحص صلاحية على دالتي معاينة جمهور الحملة، وإخفاء روابط التنقل المقابلة.
- **رابط وسائط واردة**: قائمة نطاقات Meta CDN موثوقة قبل عرض أي `mediaUrl` كصورة في صندوق المحادثات.
- **تنظيف**: إزالة أزرار "حسابات تجريبية سريعة" من صفحة تسجيل الدخول العامة (كانت تُشير لتينانتس
  محذوفة)، وحذف دالة حراسة ميتة (`assertTenantScopedWhere`) لم تكن مستدعاة من أي مكان.
- تفاصيل كاملة (كل ثغرة، السبب، الإصلاح) في `QA_REPORT.md` (قسم 6 الجديد) و`DECISIONS.md` (قرارات
  60+).

### فجوة تشغيلية حرِجة موثَّقة (لم تُحل بعد — تتطلب إجراءً من المستخدم)
- **`META_APP_SECRET` لا يزال فارغاً**، وبعد إصلاح ثغرة التجاوز أعلاه، التحقق أصبح fail-closed دائماً
  — أي **كل رسالة واتساب واردة حقيقية لتاجر "dms" تُرفَض حالياً (401)** حتى يُضبط App Secret الحقيقي
  من Meta App Dashboard. هذا لم يكن يعمل قبل هذه الجولة أيضاً (السبب نفسه)، لكنه أصبح موثَّقاً بوضوح
  الآن كأولوية تشغيلية قصوى.

## 2026-07-23 (الجولة الثامنة) — مسار ربط Meta/واتساب المتكامل + إدارة القوالب واعتمادها

### إضافات
- **معالج ربط Meta/واتساب حقيقي**: بطاقة "التكاملات" أصبحت معالجاً بخطوتين — "ربط عبر Facebook"
  (Embedded Signup، محاكى في Sandbox) و"ربط يدوي" (WABA ID + Phone Number ID + Access Token، مع
  تحقق Graph API حقيقي فعلي في وضع live: رسالة نجاح/خطأ حقيقية من Meta نفسها، وليست رسالة عامة).
  الاشتراك في webhooks (رسائل، حالة رسائل، حالة قوالب) يحدث تلقائياً فور نجاح الربط.
- **لوحة صحة الاتصال**: حالة الرقم، تقييم الجودة (🟢/🟡/🔴)، حد الرسائل (Messaging Tier)، حالة
  الاشتراك في Webhook مع زر "إعادة الاشتراك" الفعلي عند الانقطاع، وزر "قطع الاتصال" بتأكيد صريح
  يوضح عواقبه (توقف استقبال/إرسال الرسائل فوراً).
- **إرسال رسائل حقيقي حسب حالة اتصال كل تاجر**: `sendMessage` كان يعتمد خطأً على إعداد المنصة
  العام (sandbox/live) بدل حالة اتصال هذا التاجر تحديداً — أُصلح ليرسل رسائل حقيقية فعلياً لأي تاجر
  ربط رقماً حقيقياً، حتى لو بقيت بقية المنصة على وضع Sandbox.
- **وحدة قوالب برفع اعتماد Meta حقيقي**: معالج بناء قالب كامل (فئة مع شرح مبسط لكل نوع، ترويسة/نص/
  تذييل/أزرار، معاينة حية على شكل واتساب)، يرسل القالب فعلياً عبر Graph API (`POST /message_templates`)
  ويستقبل معرّف Meta الحقيقي. الحالة تتحدث **تلقائياً** عبر webhook حقيقي وارد
  (`message_template_status_update`) أو محاكاة مؤجَّلة واقعية في Sandbox (نفس دالة التطبيق في
  الحالتين) — لا اعتماد فوري مصطنع ولا زر "تحديث يدوي".
- **حالتا PAUSED/DISABLED جديدتان** للقوالب المعتمدة سابقاً التي أُوقفت بسبب جودة أداء منخفضة، مع
  "تعديل وإعادة إرسال" فعلي (يفتح المعالج مسبق التعبئة، يحذف النسخة القديمة، ويرسل نسخة جديدة).
- **RBAC كامل الصفحة** لأول مرة على وحدتي التكاملات والقوالب: موظف (Agent) يُحجَب بالكامل حتى عبر
  الرابط المباشر (لم تكونا تفحصان الصلاحية إطلاقاً من قبل، فقط تُخفيان بعض الأزرار).

### إصلاحات حرجة اكتُشفت أثناء التصميم والاختبار الفعلي
- محاكاة الربط في Sandbox كانت تُسنِد **نفس معرّف الرقم** لكل تاجر يربط حسابه — تصادم حقيقي كان
  سيوجّه رسائل تاجرين مختلفين لبعضهما بالخطأ الآن وقد أصبحت مطابقة الـwebhook حقيقية فعلياً (الجولة
  السابعة). أُصلح بتوليد معرّف فريد لكل تاجر (نفس الآلية المستخدمة أصلاً في زد/سلة).
- استُبدل مسار موافقة القوالب القديم (محاكاة عبر مركز موافقات مالك المنصة، من الجولة الرابعة) بمسار
  Meta الفعلي — كان دائماً محاكاة مؤقتة موثَّقة صراحةً بهذا الوصف قبل بناء الاتصال الحقيقي.
- تفاصيل كاملة في `DECISIONS.md` (قرارات 50–59) و`QA_REPORT.md`.

## 2026-07-23 (الجولة السابعة) — صندوق محادثات واتساب حقيقي متكامل

### إضافات
- **ربط webhook حقيقي بمعالجة فعلية**: كان مسار `api/webhooks/meta` يسجّل الحمولة الواردة فقط دون
  أي معالجة — أصبح الآن يحلّل شكل حمولة Meta الفعلي (رسائل نصية/صور/مستندات/صوت/موقع/ردود تفاعلية
  + تحديثات حالة الرسائل الصادرة) وينشئ/يحدّث جهات الاتصال والمحادثات والرسائل فعلياً، عبر دالة
  معالجة واحدة مشتركة (`lib/integrations/meta/webhookHandler.ts`) يستخدمها أيضاً محاكي Sandbox
  الجديد (`lib/inbox/sandboxSimulator.ts`) — لا منطق مكرر بين المسارين.
- **شاشة محادثة حقيقية كاملة**: فقاعات صادر/وارد متمايزة بالألوان، دعم كامل للوسائط (صورة، مستند،
  رسالة صوتية، موقع مع الإحداثيات، رد مقتبس على رسالة محددة)، علامات حالة الرسالة الصادرة (✓ أُرسلت
  / ✓✓ رمادي تم التسليم / ✓✓ أزرق تمت القراءة) تتحدث تلقائياً عبر محاكاة تقدُّم واقعية (BullMQ
  delayed jobs)، عداد نافذة الـ24 ساعة حي أعلى صندوق الكتابة يتحول تلقائياً لإجبار اختيار قالب
  معتمد بعد انتهائها، وردود سريعة محفوظة (اكتب "/" لاستدعائها).
- **تصنيف حالة حقيقي مبني على منطق فعلي**: أربع حالات (جديد/يحتاج رد/بانتظار العميل/تم الحل) تُدار
  عبر آلة حالة صريحة (`lib/inbox/conversationStatus.ts`) بدل عداد ثابت — أصلح خطأً حقيقياً مكتشَفاً
  في الكود القديم كان يضع **كل** رد كـ"محلولة" تلقائياً. مؤشر SLA أحمر (⏰) على المحادثات المتأخرة
  أكثر من 15 دقيقة، إشعار صوتي + تحديث لحظي عبر استطلاع دوري (polling) كل 4 ثوانٍ.
- **تعاون فريق كامل**: إسناد محادثة لعضو فريق محدد، تحويلها لعضو آخر (مسجَّل في سجل التدقيق)،
  "استيلاء" فعلي على التحكم من البوت، وملاحظات داخلية في جدول منفصل تماماً عن الرسائل (استحالة
  بنيوية لتسريبها للعميل، وليس فقط تمييزاً بصرياً).
- **بحث وفلترة حقيقيان**: بحث بالاسم/الجوال/نص الرسالة، فلترة حسب الحالة/العضو المسنَد/التحكم
  (بوت أو موظف)، عبر نقطة API مستقلة (`/api/inbox/conversations`) لتجاوز قيد Next.js الذي يمنع
  تمرير معاملات الرابط لملفات layout.tsx.
- **إنفاذ فعلي لحدود الباقة**: `Plan.maxUsers` كان موجوداً منذ الجولة الأولى لكن غير مُفعَّل أبداً —
  أصبح يُفرض عند دعوة عضو فريق جديد.

### إصلاحات حرجة اكتُشفت بالاختبار الفعلي
- دعوة عضو فريق فوق حد الباقة كانت تُسبِّب شاشة انهيار خام (React error overlay) بدل رسالة ودّية —
  أُصلح بإضافة طبقة التقاط أخطاء عميل لنموذج الدعوة.
- رابط جديد ("⚡ الردود السريعة") ونموذج جديد ("حفظ حدود الحملات" من الجولة السادسة) صادما محدِّدات
  في سكربتات اختبار الدخان القائمة — أُصلحت المحدِّدات لتكون أكثر دقة.
- تفاصيل كاملة في `DECISIONS.md` (قرارات 40–49) و`QA_REPORT.md`.

## 2026-07-23 (الجولة السادسة) — وحدة الحملات المتكاملة End-to-End

### إضافات
- **معالج إنشاء حملة متعدد الخطوات**: يستبدل النموذج الفارغ القديم بمعالج بخطوات واضحة (نوع الحملة
  → الجمهور/الحدث المُشغِّل → القالب → الجدولة → المراجعة والتأكيد)، مع عدّاد جمهور حي يتحدّث فوراً
  مع كل تعديل في الفلاتر، ومعاينة نهائية على شكل واتساب حقيقي (بإعادة استخدام مكوّن إطار الموبايل
  من الجولة الخامسة).
- **شرائح جمهور بفلاتر حقيقية**: مرحلة العميل، الوسوم، نشاط المحادثة (نشط خلال/غير نشط منذ)، وحالة
  طلب من زد/سلة خلال نافذة زمنية (مثال: سلات متروكة خلال 7 أيام) — قابلة للتركيب معاً، مع فرض
  إلزامي لموافقة استقبال الرسائل التسويقية (`Contact.marketingOptIn`) بصرف النظر عن اختيار التاجر.
- **حصة الرسائل الشهرية أصبحت مُنفَذة فعلياً**: كان `Subscription.messagesUsedThisPeriod` يُعرض في
  4 صفحات مختلفة دون أن يُزاد في أي مكان بالكود — أُصلح جذرياً: كل رسالة حملة فعلية تستهلك من الحصة،
  وإنشاء حملة يتجاوز الجمهور المتبقي من الحصة يُرفض من الخادم مع توجيه لترقية الباقة.
- **صفحة تفاصيل حملة كاملة**: ملخص بصري بنسب مئوية (مُرسل/تسليم/قراءة/رد/فشل)، قائمة مستلمين فردية
  بحالة كل رسالة وسبب الفشل الفعلي عند وجوده (رقم غير صحيح، انتهاء نافذة الـ24 ساعة، حظر الرقم)،
  تتبع تحويل إيراد حي (يربط تلقائياً بأي طلب فعلي حدث لنفس العميل خلال 7 أيام من الإرسال)، أزرار
  "إعادة إرسال للفاشلين" و"تكرار الحملة".
- **فصل كامل بين الحملات لمرة واحدة والحملات الآلية**: تبويب مستقل "الحملات الآلية" يعرض كل حملة
  بحالة حقيقية (🟢 نشطة / ⏸️ متوقفة، وليس "مكتملة" أبداً)، مع إحصائية تراكمية حية (مرات التشغيل،
  إجمالي الإيراد المسترد)، وزري "إيقاف مؤقت" و"فحص الآن" (تشغيل فوري لمنطق اكتشاف الأحداث الجديدة
  دون انتظار الفحص الدوري).
- **محرك أحداث آلي حقيقي**: حملات "سلة متروكة" و"عميل غير نشط منذ X يوم" تُفعَّل عبر مهمة BullMQ
  متكررة كل 5 دقائق تفحص كل المستأجرين، بالإضافة لجدولة الحملات المستقبلية عبر تأخير BullMQ المدمج
  — بدل بناء نظام cron منفصل.
- **جدول حملات بفلاتر وبحث وترقيم صفحات**: الحالة، القالب، نطاق تاريخ، بحث بالاسم، عمود "معدل
  التسليم %" محسوب تلقائياً، وشريط استهلاك حصة الرسائل أعلى الصفحة.

### إصلاحات وملاحظات اكتُشفت بالاختبار الفعلي
- **عملية العامل (`npm run worker`) لم تكن تعمل إطلاقاً** في جلسة العمل قبل هذه الجولة — اكتُشف أن
  كل حملة تبقى "مجدولة" للأبد بلا هذه العملية منفصلة عن خادم الويب. وثِّق كمتطلب تشغيلي إلزامي.
- `prisma/seed.ts` بلا خطوة تصفير (نفس الفئة من الجولة الخامسة) تسبّب مجدداً في تراكم بيانات اختبار
  أخفت سلوكاً حقيقياً خلف حدود باقة زائفة — عولج بـ`TRUNCATE` يدوي، والتوصية بـ`npm run db:reset`
  رسمي لا تزال قائمة.
- تفاصيل كاملة في `DECISIONS.md` (قرارات 30–39) و`QA_REPORT.md` (القسم -3).

## 2026-07-23 (الجولة الخامسة) — محرر شات بوت بلوحة رسم حرة (Canvas) + معاينة موبايل حقيقية

### إضافات
- **لوحة رسم تدفق حرة شبيهة بأدوات الأتمتة** (`@xyflow/react`): محرر الشات بوت (`ChatbotFlowEditor`)
  أصبح لوحة سحب-وإفلات حقيقية بدل قائمة خطوات مرتبة — عقد قابلة للسحب بحرية، اتصالات (edges) تُرسم
  يدوياً بين المقابض، عقدة "شرط" لها مقبضا خروج منفصلان ملوّنان (أخضر "نعم"/أحمر "لا")، خريطة مصغّرة
  (MiniMap) وعناصر تحكم بالتكبير/التصغير (Controls)، وشبكة خلفية (Background). كل بوابات الباقة
  ("🔒 Pro/Business" على أنواع العقد المحظورة) وبوابات الأدوار (مالك/مدير/موظف) القائمة سابقاً بقيت
  كما هي حرفياً — فقط طبقة العرض تغيّرت.
- **معاينة واتساب كإطار موبايل حقيقي**: `WhatsAppPreview` أصبح إطار جهاز كامل (حواف، notch علوي،
  شريط حالة بالوقت الفعلي، رأس محادثة واتساب، فقاعات، شريط إدخال سفلي، مؤشر هوم) بدل صندوق مسطّح.
- **نموذج بيانات رسم بياني حقيقي**: `FlowNode` (بموقع `x/y`) + `FlowEdge` (`source`/`target`/
  `sourceHandle`) بدل حقلي `trueNext`/`falseNext` النصيين على عقدة الشرط. القوالب الجاهزة الخمسة
  (ترحيب، حالة الطلب، سلة متروكة، أسئلة شائعة، فارغ) ومحرك المحاكاة (`runFlowSimulation`) ومحرك
  معاينة "المسار السعيد" (`buildHappyPathBubbles`، مستخرج الآن كدالة مشتركة) كلها أُعيد بناؤها
  لتعمل على الشكل الجديد (اجتياز عبر الحواف بدل حقول قفز نصية).

### إصلاحات وملاحظات اكتُشفت بالاختبار الفعلي
- `MiniMap` كانت تتراكب افتراضياً (أسفل-يمين) فوق محتوى آخر عقدة عند تكبير اللوحة تلقائياً
  (`fitView`) لتدفقات صغيرة — نُقلت لأعلى-يمين مع تصغير حجمها لتفادي التراكب.
- **فجوة في بيانات الاختبار وليس في المنتج**: `prisma/seed.ts` لا يملك خطوة تصفير قبل الزرع، فتراكمت
  تدفقات شات بوت زائدة عبر تشغيلات متكررة لسكربتات Playwright حتى تجاوز تاجر تجريبي حده الفعلي لعدد
  التدفقات، فأخفى الزر الحقيقي خلف نافذة "وصلت للحد الأقصى" وتسبّب بفشل ظاهري غير متعلق بالتعديلات
  نفسها. عولج بتفريغ الجداول يدوياً (`TRUNCATE ... CASCADE`) قبل كل إعادة زرع أثناء هذه الجولة؛
  موصى بإضافة `npm run db:reset` رسمي لاحقاً. تفاصيل كاملة في `DECISIONS.md` (قرار 29).

## 2026-07-23 (الجولة الرابعة) — لوحة مالك المنصة كغرفة تحكم شاملة فعلياً

### إضافات
- **إدارة الباقات الكاملة (CRUD)**: إنشاء باقة جديدة بالكامل، تعديل بياناتها الأساسية (اسم/سعر/
  حدود مستخدمين/أرقام واتساب/رسائل/ميزات)، تفعيل/تعطيل، وعرض عدد المشتركين — إضافة لحدود
  الشات بوت التي كانت الوحيدة القابلة للتعديل سابقاً.
- **تبويبات بيانات شاملة لكل تاجر**: `/admin/tenants/[id]` أصبح بتبويبات (الفريق، جهات الاتصال،
  الحملات، الشات بوت، قوالب الرسائل، الطلبات والمنتجات) بدل صفحة واحدة مكدّسة.
- **موافقة قوالب الرسائل**: نوع رابع (`MESSAGE_TEMPLATE`) في مركز الموافقات القائم يحاكي مراجعة
  Meta الفعلية لقوالب واتساب. صفحة تاجر جديدة بالكامل (`/dashboard/templates`) لم تكن موجودة
  إطلاقاً — التجار الآن يقدرون فعلياً على تقديم قوالب رسائل جديدة للمراجعة.
- **إعدادات المنصة العامة** (`/admin/settings`): وضع Sandbox/Live للتكاملات أصبح **قابلاً للتغيير
  حياً من قاعدة البيانات** بدل env var ثابت يتطلب إعادة نشر، وضع صيانة عام يمنع كل التجار فوراً
  (باستثناء جلسة انتحال هوية لفريق الدعم)، بريد/هاتف دعم فني، مدة تجربة مجانية افتراضية قابلة
  للتعديل (تُستخدم فعلياً في تسجيل تاجر جديد).

### إصلاحات حرجة اكتُشفت بالاختبار الفعلي
- `admin/settings/actions.ts` استخدم اتصال RLS عادي (`rawDb`) بدل BYPASSRLS (`superAdminDb`)
  لكتابة سجل التدقيق — **نفس فئة الخطأ المكتشفة 3 مرات في جولات سابقة**، رغم التوثيق الصريح لها.
- `verifyWebhookSignature` وكل استدعاءات `isSandboxMode()` حُوِّلت لدوال async (3 adapters + 3
  نقاط webhook) لدعم القراءة الحية من قاعدة البيانات، مع الحفاظ على fallback دفاعي لمتغير البيئة.

### تحسين لاحق — نظرة عامة حقيقية بدل قيم Enum خام
كان ملاحظاً أن صفحة `/admin` (نظرة عامة) ما زالت تعرض قيم enum خام (`PENDING_REVIEW`, `ACTIVE`)
بلا تنسيق، رغم إضافة كل الصفحات الجديدة حولها. أُضيف `lib/tenantStatus.ts` (تسميات عربية موحّدة +
ألوان) يُستخدم الآن في `/admin` (شريط توزيع تناسبي ملوّن + شارات)، `/admin/tenants`،
`/admin/tenants/[id]`، وسايدبار التاجر نفسه. كما أُضيف لصفحة `/admin` نفسها: بطاقات قابلة للنقر
(الموافقات المعلّقة → مركز الموافقات مباشرة)، صف "إجراءات سريعة" (روابط للصفحات الأكثر استخداماً
حسب صلاحية الدور)، وتغذية "أحدث النشاط" الحية من سجل التدقيق — بحيث الصفحة الرئيسية نفسها تُحس
كمركز قيادة فعلي، وليس مجرد 4 بطاقات أرقام.

## 2026-07-23 (الجولة الثالثة) — لوحة "مالك المنصة" كغرفة تحكم فعلية

### إضافات
- **مركز الموافقات** (`/admin/approvals`): كل تاجر جديد يُنشأ بحالة `PENDING_REVIEW` ولا يُفعَّل
  تلقائياً — يظهر في مركز موافقات موحّد (تسجيل جديد / طلب باقة مخصصة / تحقق ربط واتساب) بأزرار
  قبول/رفض (مع سبب)/طلب معلومات إضافية. تسجيل الدخول لتاجر معلَّق/ملغى/مرفوض **يُرفض من جذوره**
  داخل `authorize()` نفسه، وليس فقط إخفاء المحتوى بعد الدخول.
- **صفحة تفاصيل تاجر متعمقة**: استخدام فعلي مقابل حدود الباقة (رسائل/تدفقات/مستخدمين)، صحة
  التكاملات مع آخر webhook فاشل، فريق التاجر مع تعليق مستخدم بعينه، ملاحظات داخلية (CRM لفريق
  المنصة)، تغيير باقة يدوي، حذف نهائي بتأكيد مزدوج (كتابة معرّف المتجر)، وتسجيل دخول كالتاجر.
- **انتحال الهوية (Impersonation)**: كوكي httpOnly منفصل عن جلسة NextAuth الحقيقية (لا يمسّها
  إطلاقاً)، رمز مُجزَّأ SHA-256 صلاحيته 30 دقيقة، شريط تحذير أحمر دائم أثناء الجلسة، وسجل تدقيق
  **مزدوج** فوري (سجل المنصة + سجل التاجر نفسه) لحظة البدء.
- **لوحة حدود الباقات** أصبحت تُنشئ فعلياً باقات Enterprise مخصصة لتاجر بعينه عبر الموافقة على
  طلب "باقة مخصصة"، مستبعدة من الكتالوج العام.
- **صحة المنصة** (`/admin/health`): اتصال واتساب عالمياً، معدل فشل webhooks، تجار قريبون من حد
  باقتهم، ومؤشرات تسرب حقيقية (تسجيل دخول متأخر، استخدام شات بوت شبه صفري).
- **الإيرادات والفوترة العميقة** (`/admin/billing`): MRR، توزيع حسب الباقة، تجديدات 7/30 يوماً،
  مدفوعات فاشلة (حالة فارغة صادقة حالياً).
- **RBAC داخلي حقيقي لفريق المنصة**: دوران جديدان `PLATFORM_SUPPORT`/`PLATFORM_BILLING` بجانب
  `SUPER_ADMIN`، بصلاحيات مجزّأة مُنفَّذة على مستوى الـ API (server actions) وليس إخفاء واجهة
  فقط. صفحة `/admin/team` لدعوة/تعطيل أعضاء الفريق الداخلي.
- **إعلانات المنصة** (`/admin/announcements`): تواصل جماعي in-app (لا مزود بريد مربوط بعد)
  لكل التجار أو باقة محددة أو تجار محددون، مع بانر قابل للإغلاق في لوحة كل تاجر.
- **سجل تدقيق شامل** (`/admin/audit-log`): كل إجراء حساس على مستوى المنصة أو داخل أي تاجر،
  بترقيم صفحات — أُضيف بعد اكتشاف أنه لم يكن هناك أي واجهة لعرض ما كان يُسجَّل فعلاً.
- بحث وترقيم صفحات فعليان على جدول التجار (`/admin/tenants`).

### إصلاحات حرجة اكتُشفت بالاختبار الفعلي
- صفحات `/admin/billing`, `/admin/health`, `/admin/approvals` كانت ترمي استثناء خام (500) عند
  وصول مستخدم غير مخوَّل مباشرة عبر الرابط بدل رسالة "ليس لديك صلاحية" — وُحِّد النمط عبر كل
  صفحات الإدارة.
- `dismissAnnouncement` كانت تثق بـ `tenantId` قادم من العميل مباشرة (ثغرة تصريح محتملة) — أُصلح
  باشتقاقه من الجلسة الحقيقية حصراً.
- كتالوج الباقات العام (لوحة التاجر + الفوترة) كان يُظهر باقات مخصصة (Enterprise) لتجار آخرين —
  أُصلح بفلترة `isCustomForTenantId: null`.
- صفحة تفاصيل التاجر كانت تحسب حد "تدفقات الشات بوت" من حقل خاطئ (`maxWhatsappNumbers` بدل
  `chatbotLimitsJson.maxActiveFlows`) وتعرض "جهات اتصال" بدل "تدفقات" رغم أن البرومنت يطلب
  (رسائل/تدفقات/مستخدمين) تحديداً.

## 2026-07-22 (الجولة الثانية) — إعادة بناء وحدة الشات بوت: بوابات باقة وأدوار + بساطة أولاً

### إضافات
- **مكتبة قوالب جاهزة** (`lib/chatbot/templates.ts`): 5 قوالب بأسماء عربية بسيطة (ترحيب+قائمة،
  متابعة طلب، سلة متروكة، أسئلة شائعة، فارغ للمتقدمين) — أي تدفق جديد يبدأ منها إلزامياً بدل لوحة
  فارغة، عبر صفحة جديدة `/dashboard/chatbot/new`.
- **تفرّع شرطي حقيقي**: عقدة "شرط" الآن لها مخرجان قابلان للتخصيص (تحقّق/عدم تحقّق) — الخطوة
  التالية، إنهاء المحادثة، أو قفزة لعقدة محددة.
- **معاينة حية بشكل واتساب** (`WhatsAppPreview`) بجانب المحرر، تعكس تعديلات المستخدم فوراً.
- **بوابات باقة فعلية (Frontend + Backend)**: 3 مستويات (الأساسية/الاحترافية/الأعمال) تتحكم في
  عدد التدفقات، أنواع العقد (رد ذكي، استدعاء API)، مستوى القوالب، ومستوى التحليلات — مخزَّنة في
  `Plan.chatbotLimitsJson` القابل للتعديل من لوحة Super Admin الجديدة `/admin/plans` دون كود.
  الإنفاذ خادمي حقيقي (وليس واجهة فقط) في `saveFlowNodes` و`createFlowFromTemplate`.
- **بوابات أدوار مجزّأة**: 6 صلاحيات شات بوت بدل صلاحية واحدة (عرض/تعديل/نشر/حذف مسودة/حذف
  منشور/اختبار). المدير لا يحذف تدفقاً منشوراً؛ الموظف عرض واختبار فقط عبر صفحة
  `/dashboard/chatbot/[id]/test` مخصصة، مع رفض خادمي (redirect لصفحة "ليس لديك صلاحية") لأي
  محاولة وصول مباشر للمحرر عبر الرابط.
- **شرط "اختبار قبل النشر" مُلزَم من الخادم**: لا يمكن نشر تدفق لم يُختبر حياً مرة واحدة على
  الأقل، حتى عبر استدعاء مباشر لـ server action (وليس فقط تعطيل زر الواجهة).
- شارة "وضع تجريبي" في لوحة التاجر وصفحة الشات بوت أصبحت **ديناميكية فعلياً** (تعتمد على حالة
  اتصال واتساب الحقيقية) بدل قيمة ثابتة دائمة العرض.

### إصلاحات حرجة اكتُشفت بالاختبار الفعلي (Playwright حقيقي، وليس مراجعة نظرية)
- **فئة كاملة من استعلامات RLS الصامتة الفشل**: 3 مواقع (`getTenantChatbotLimits`, dynamic
  sandbox badge في `dashboard/layout.tsx`, فحص تكامل واتساب في صفحة الشات بوت) كانت تستعلم عن
  جداول مستأجرة عبر `rawDb` مباشرة خارج `withTenant()`، فترجع دائماً `null` بسبب RLS — مما كان
  يعني عملياً **معاملة كل التجار (حتى المشتركين بباقات مدفوعة كاملة) كأنهم على الباقة الأساسية**.
  تم تدقيق كل استدعاء `rawDb.*` في المشروع للتأكد من عدم وجود حالات أخرى.
- `prisma/seed.ts` كان يتصل بدور `app_runtime` (خاضع لـ RLS) بدل دور BYPASSRLS، فيفشل عند أي
  محاولة زرع بيانات عابرة للمستأجرين — أُصلح بتوجيهه لاتصال `SUPER_ADMIN_DATABASE_URL`.
- `redirect()` بنص عربي خام في query string (`?from=...`) كان يسبب
  `TypeError [ERR_INVALID_CHAR]` فعلياً في وضع الإنتاج — أُصلح بـ `encodeURIComponent`.
- تمرير مصفوفة قوالب تحوي دوال (`buildNodes`) من Server Component لـ Client Component — ممنوع في
  React Server Components، أُصلح بفصل البيانات القابلة للتسلسل عن منطق البناء الخادمي.
- ملف `"use server"` كان يُصدّر class ودالة متزامنة بجانب server actions — ممنوع صراحة في
  Next.js، نُقلا لملفين منفصلين.

## 2026-07-22 — الإصدار الأول (v0.1.0): بناء المنصة الكاملة من الصفر

### البنية التحتية
- تهيئة مشروع Next.js 14 (App Router) + TypeScript strict + Tailwind CSS.
- تشغيل PostgreSQL 16 وRedis 7 فعلياً عبر Docker Compose (`docker-compose.yml`).
- مخطط Prisma كامل (`prisma/schema.prisma`): 20 موديل تغطي المستأجرين، المستخدمين، CRM،
  المحادثات، الحملات، الشات بوت، التكاملات، الفوترة، وسجل التدقيق.
- **تفعيل Row Level Security فعلي على Postgres** (`prisma/postgres-rls.sql`) لكل جدول مستأجر،
  مع دورين منفصلين لقاعدة البيانات (`app_runtime` بدون تجاوز RLS، `app_superadmin` بـ BYPASSRLS
  فقط)، بعد اكتشاف أن الدور الافتراضي (superuser) يتجاوز RLS تلقائياً بغض النظر عن `FORCE`.
- سكربت تحقق فعلي (`prisma/verify-isolation.ts`) يشغَّل ضد Postgres حقيقي، يؤكد 4 سيناريوهات
  عزل (قراءة عادية، قراءة بمعرف مباشر لمستأجر آخر، إدخال بـ tenantId مزوّر) — كلها مرفوضة.
- طبقة تشفير AES-256-GCM لمفاتيح تكاملات التجار (`lib/crypto.ts`).

### المصادقة والصلاحيات
- NextAuth (Credentials + JWT) مع RBAC مخصص (`SUPER_ADMIN`/`OWNER`/`ADMIN`/`AGENT`) في `lib/rbac.ts`.
- اكتُشف أثناء الاختبار الفعلي أن استعلام تسجيل الدخول (بحث بالبريد قبل معرفة tenantId) يحتاج
  اتصال BYPASSRLS مخصص — مُوثَّق كاستثناء متعمد في `lib/auth.ts`.

### الوحدات
- **Onboarding**: تسجيل تاجر جديد (`/register`) مع اختيار باقة وتجربة مجانية 14 يوماً.
- **لوحة التاجر (Overview)**: مؤشرات لحظية حقيقية من قاعدة البيانات.
- **CRM**: جهات اتصال، وسوم، مراحل عميل (Lead/Customer/Repeat)، بطاقة عميل موحّدة.
- **صندوق المحادثات الموحّد**: قائمة مفلترة بالحالة، عرض نافذة الـ24 ساعة، رد مباشر.
- **الحملات**: إنشاء حملة (شريحة + قالب) → إرسال فعلي عبر BullMQ + Redis حقيقي → تقرير
  مُرسل/مُسلَّم/فشل لكل مستلم.
- **الشات بوت**: محرر خطوات تسلسلي (قرار معماري: بديل واقعي لمحرك canvas كامل، موثّق في
  DECISIONS.md) مع اختبار حي فوري.
- **التكاملات**: Meta WhatsApp Cloud API + زد + سلة عبر adapter pattern موحّد، وضع sandbox
  افتراضي (بيانات وهمية واقعية) عند غياب مفاتيح حقيقية، webhooks بتحقق توقيع HMAC فعلي.
- **الفوترة**: باقات، استخدام، فواتير، ترقية عبر طبقة مزود دفع قابلة للاستبدال (mock حالياً).
- **Super Admin**: كل التجار، الإيرادات المجمّعة، صحة التكاملات، تعليق/تفعيل حساب.
- **الفريق والإعدادات**: دعوة أعضاء، تبديل الأدوار، سجل تدقيق (Audit Log) لكل نشاط حساس.

### إصلاحات اكتُشفت أثناء الاختبار الفعلي (Playwright + Chromium حقيقي)
- `rawDb.auditLog.create()` كان يُستدعى خارج `withTenant()` في 4 ملفات (settings, billing,
  contacts, integrations actions) فيفشل بخطأ RLS حقيقي (`42501`) — تم استبداله بـ
  `lib/audit.ts → writeAuditLog()` الذي يضبط سياق المستأجر دائماً.
- خطأ **bidi RTL**: نسب الاستخدام ("٣٤٠٠ / ١٠٠٠٠") كانت تُعرض بترتيب بصري معكوس داخل صفحة RTL
  لغياب `dir="ltr"` — أُصلح في `dashboard/page.tsx`, `billing/page.tsx`, `admin/tenants/page.tsx`.
- `createContact` كان يفشل بخطأ Prisma خام غير مقروء عند تكرار رقم الجوال — أُضيفت معالجة
  `P2002` برسالة عربية واضحة.
- علاقة Prisma ناقصة: `CampaignRecipient.contact` لم تكن معرَّفة (فقط `contactId` سكيلر) —
  أُضيفت العلاقة الكاملة + migration.

### التحقق النهائي
- `npm run build` ناجح بدون أخطاء (20 مسار).
- `npx tsc --noEmit` بدون أي خطأ.
- اختبار دخان شامل (`scripts/smoke-test.js`, `scripts/smoke-register.js`) بمتصفح Chromium حقيقي
  ضد نسخة الإنتاج (`next build && next start`): **صفر أخطاء console** عبر كل الوحدات.

---

## 2026-07-25 — تدقيق أمني شامل + جاهزية الإطلاق (Pre-Launch Security Audit)

جولة تدقيق كاملة (وليست بناء ميزات) قبل فتح المنصة لتجار حقيقيين — تفاصيل كل بند في
`SECURITY_FIXES.md` والقرار النهائي في `LAUNCH_READINESS_REPORT.md`.

### إصلاحات أمنية حقيقية
- إضافة رؤوس أمان HTTP كاملة (HSTS, CSP, X-Frame-Options, X-Content-Type-Options, إلخ) —
  كانت غائبة تماماً قبل هذه الجولة.
- ترقية Next.js من 14.2.15 إلى 14.2.35 — أزالت ثغرة **حرجة** حقيقية (تجاوز صلاحيات Middleware)
  مؤكَّدة عبر `npm audit` قبل/بعد.
- إضافة `error.tsx`/`global-error.tsx`/`not-found.tsx` (لم تكن موجودة إطلاقاً).
- إضافة `scripts/backup-db.sh` لنسخ احتياطي فعلي لقاعدة Postgres.

### إصلاحات الفوترة/الاشتراكات
- **دورة حياة انتهاء الاشتراك**: كانت مفقودة تماماً — أي اشتراك تجاوز فترته كان يبقى نشطاً للأبد.
  أُضيفت مهمة فحص دورية تحوّله لـPAST_DUE، مع إنفاذ فعلي يمنع إنشاء حملات جديدة.
- **رقم فاتورة تسلسلي + ضريبة القيمة المضافة** أُضيفا لنموذج الفاتورة (شفافية، بلا تغيير في
  المبلغ الفعلي المُحصَّل).
- **تحصين تسابق حقيقي** في تغيير الباقة (قفل صفّ) — مُختبَر بطلبين متزامنين حقيقيين.
- فهرس `Segment.tenantId` المفقود (الحقل الوحيد المستأجَر بلا فهرس في كل المخطط).

### تأكيدات أمنية (لا ثغرة، اختبار عدواني حي)
- عزل المستأجرين (RLS): 7/7 محاولات اختراق فعلية (قراءة/تعديل/حذف بيانات تينانت آخر، حقن SQL)
  فشلت كما هو متوقَّع.
- لا XSS/حقن مستغَل في أي نموذج مستخدم (تواصل، شركاء، قوالب، حملات، شات بوت).
- توقيع Webhooks (Meta/Zid/Salla) يرفض فعلياً أي طلب مزوَّر (401 حقيقي، مُختبَر بـcurl).

### التحقق النهائي
- `npx tsc --noEmit` و`npm run build` نظيفان بعد كل تغيير.
- اختبار دخان حي متكامل (تسجيل تاجر → موافقة → دخول → حملة → فاتورة → شات بوت) ضد تينانتات اختبار
  معزولة، بلا أي أثر على بيانات تجار حقيقيين.

---

## 2026-07-25 (جولة ثانية) — إعادة بناء وحدة "جهات الاتصال" (CRM احترافي متكامل)

جولة إعادة بناء كاملة (وليست تعديلاً سطحياً) لوحدة جهات الاتصال — تفاصيل كل قرار في `DECISIONS.md`
والتحقق الذاتي الكامل في `QA_REPORT.md`.

### إصلاح الخلل الظاهر من جذره
عمود "المرحلة" كان يعرض أحياناً نصاً مشوَّهاً بصرياً (طابع زمني خام مدموج مع نص عربي). السبب الحقيقي:
وسم داخلي (لاستهداف جمهور حملة مستورَدة) كان اسمه يحمل طابعاً زمنياً خاماً ويُعرَض بالخطأ كوسم عادي.
الإصلاح: تمييز صريح بين وسوم المستخدم ووسوم النظام الداخلية (`Tag.isSystem`)، لا الأخيرة تُعرَض
للمستخدم إطلاقاً بعد الآن.

### ميزات جديدة كاملة
- **تقسيم فعلي حسب المصدر**: تبويبات (الكل/حسب الحملة/استيراد ملفات/إضافة يدوية/من تكامل المتجر)
  مبنية على تتبّع حقيقي جديد لمصدر كل جهة اتصال (لم يكن موجوداً إطلاقاً سابقاً).
- **تصدير حقيقي** (Excel/CSV، الكل/المُصفَّى/المُحدَّد يدوياً) — لم يوجد أي تصدير من قبل. مقيَّد
  بصلاحية Owner/Admin فقط (بيانات هواتف حساسة)، ومُسجَّل في سجل التدقيق.
- **استيراد بمعاينة حقيقية**: معاينة أول 10 صفوف + كشف تكرار فعلي قبل أي كتابة على القاعدة، اختيار
  صريح لاستراتيجية التكرار (تحديث/تجاهل)، تقرير أخطاء واضح، وإمكانية **التراجع** عن دفعة استيراد
  كاملة خلال 10 دقائق.
- **صفحة تفاصيل جهة اتصال** موسَّعة: تعديل مباشر للبيانات، مرحلة عميل من 4 خطوات (أُضيفت "تم
  التواصل")، محرر وسوم فعلي، حالة موافقة تسويقية ثلاثية (موافق/غير موافق/غير محدد)، و**سجل حملات
  فعلي (Activity Timeline)** لم يكن موجوداً إطلاقاً.
- **إجراءات جماعية**: تحديد متعدد، حذف/وسم جماعي، تصدير التحديد، و"إضافة كجمهور حملة جديدة" (تكامل
  مباشر مع معالج إنشاء الحملة الموجود أصلاً).
- فلاتر متقدمة (وسم، مدينة، مرحلة، حالة موافقة، نطاق تاريخ) + ترقيم صفحات فعلي (لم يكن موجوداً).

### التحقق النهائي
- `npx tsc --noEmit` و`npm run build` نظيفان.
- اختبار دخان حي شامل (11 فحصاً منفصلاً) ضد تينانت اختبار معزول: استيراد بتكرار → معاينة → تجاهل →
  تراجع، تصدير (نجاح لـOwner، رفض فعلي 403 لموظف حتى عند استدعاء الرابط مباشرة)، سجل حملات حقيقي في
  صفحة التفاصيل، إجراءات جماعية، وتأكيد نهائي بعدم وجود أي طابع زمني خام ظاهر للمستخدم في أي مكان.

---

## 2026-07-26 — إعادة بناء صفحة "الفوترة والاشتراك" (Billing & Subscription Upgrade)

جولة إعادة بناء كاملة لصفحة الفوترة — من عرض بسيط لباقة واحدة + قائمة ترقية، إلى صفحة SaaS احترافية
متكاملة. تفاصيل كل قرار في `DECISIONS.md` والتحقق الذاتي الكامل في `QA_REPORT.md`.

### قيد مركزي محكوم به هذه الجولة
مزوّد الدفع لا يزال وهمياً بالكامل (لا حساب حقيقي مربوط) — كل ميزة تحتاج بوابة دفع فعلية (طريقة
الدفع، Webhook) بُنيت في وضع **Sandbox مُعلَن صراحة** بدل ادّعاء أمان غير موجود، بينما كل ما هو
رياضي/منطقي بحت (Proration، منع تخفيض، حدود استخدام) بُني حقيقياً بالكامل بلا أي محاكاة.

### ميزات جديدة كاملة
- **نظرة عامة موسَّعة**: تنبيه فترة تجريبية بعدد أيام حقيقي، 4 أشرطة استخدام منفصلة بتلوين تلقائي،
  وتنبيه تلقائي عند تجاوز أي مورد 80% من حده.
- **إدارة طريقة دفع (Sandbox)**: بطاقة محفوظة معروضة مقنَّعة، تحديث عبر نموذج واضح المعالم كاختبار،
  لا تخزين لرقم بطاقة كامل أبداً (حتى في وضع الاختبار) — فقط آخر 4 أرقام + Token وهمي.
- **مقارنة باقات تفصيلية حقيقية**: مفتاح شهري/سنوي بخصم حقيقي لكل باقة (`Plan.annualDiscountBps`،
  قابل للتعديل من Super Admin — كان ثابتاً في كود الواجهة سابقاً)، جدول مقارنة كامل من نفس مصدر
  بيانات الباقات، وشارة "الأكثر طلباً" حقيقية (`Plan.isPopular`) بدل استنتاج بصري بالموضع.
- **معاينة Proration حقيقية**: حساب فعلي للأيام المتبقية وفرق السعر يُعرَض قبل أي تأكيد، والمبلغ
  المُحصَّل فعلياً بعد التأكيد يطابقه تماماً (نفس دالة الحساب في المعاينة والتنفيذ).
- **منع تخفيض حقيقي**: فحص حي ضد الاستخدام الفعلي (أعضاء الفريق، أرقام واتساب، تدفقات منشورة) قبل
  السماح بأي تخفيض باقة.
- **تدفق إلغاء احترافي**: سبب → عرض احتفاظ (إيقاف مؤقت 30 يوماً، حالة `PAUSED` جديدة تُستأنَف
  تلقائياً) → تأكيد نهائي يوضّح أن الوصول يستمر حتى نهاية الدورة المدفوعة.
- **سجل فواتير مع تحميل PDF حقيقي** (مكتبة `pdfkit` جديدة)، ومحاكاة فشل دفع + إعادة محاولة حقيقية
  لاختبار تنبيهات الفشل بلا انتظار مزوّد دفع حقيقي.
- **صلاحيات مالية حقيقية**: `billing.view` (Owner+Admin) منفصلة عن `billing.manage` (Owner فقط) —
  Admin يرى الصفحة كاملة لكن كل الأزرار المالية تُستبدَل برسالة صلاحية، مفروضة من الخادم فعلياً.

### التحقق النهائي
- `npx tsc --noEmit` و`npm run build` نظيفان.
- اختبار دخان حي شامل (9 أقسام) ضد تينانت اختبار معزول: ترقية مع Proration مطابق، تخفيض مُمنوع
  فعلياً عند تجاوز الحدود، طريقة دفع Sandbox بلا تسريب بيانات بطاقة، محاكاة فشل دفع + إعادة محاولة
  ناجحة، تدفق إلغاء كامل (إيقاف مؤقت → استئناف → إلغاء نهائي)، تحميل PDF حقيقي، وتأكيد أن Admin لا
  يملك أي قدرة تنفيذ مالي فعلية رغم رؤيته الصفحة كاملة.
- عطلان حقيقيان اكتُشفا وأُصلحا أثناء الاختبار الحي (وليسا مجرد Playwright flakiness): تجميع
  webpack لحزمة `pdfkit` يحتاج استثناءً في `next.config.js`، وأداة محاكاة فشل الدفع كانت مقيَّدة
  خطأً بعلم Sandbox غير متعلق (تفاصيل كاملة في `DECISIONS.md`).

---

## 2026-07-27 — إعادة بناء صفحة "التجار المشتركون" (Super Admin)

جولة تحسين مُستهدَفة (وليست إعادة بناء شاملة) لصفحة التجار المشتركين — الفحص الأولي أكّد أن صفحة
تفاصيل التاجر موجودة أصلاً بـ7 تبويبات حقيقية تعمل من جولات سابقة، فتركّز الجهد على الفجوات
الحقيقية فقط. تفاصيل كل قرار في `DECISIONS.md` والتحقق الذاتي الكامل في `QA_REPORT.md`.

### ميزات جديدة في الجدول الرئيسي
- فلاتر فعلية (الحالة، الباقة، اكتمال التكاملات) وترتيب قابل للنقر (الاسم، تاريخ الانضمام، آخر
  نشاط) وبحث موسَّع (+بريد صاحب الحساب، +رقم واتساب المربوط فعلياً).
- عمود "آخر نشاط" جديد كلياً (لم يكن مُجمَّعاً على مستوى التاجر إطلاقاً من قبل) — استعلام تجميعي
  واحد فعّال، بلا N+1.
- مؤشر صحة بصري حقيقي (🟢/🟡/🔴) — أحمر تحديداً لتكامل معطّل فعلياً (`ERROR`/`NEEDS_REAUTH`)، وليس
  فقط غير مربوط بعد.
- إجراءات مباشرة حسب حالة كل تاجر: قبول/رفض فوري لـ"بانتظار المراجعة" (يعيد استخدام منطق مركز
  الموافقات حرفياً)، تمديد تجربة بعدد أيام محدَّد، تعليق/تفعيل بتأكيد صريح.
- تصدير القائمة (Excel/CSV) — لم يكن موجوداً إطلاقاً في أي مكان بلوحة Super Admin من قبل.

### إصلاح خلل حقيقي في صحة التكاملات
استعلام "آخر webhook فاشل" في صفحة تفاصيل التاجر كان يخلط بين "توقيع مرفوض" و"فشل معالجة فعلي" —
أُصلح ببناء فحص صحيح لكل مزوّد على حدة (نجاح = توقيع صحيح **و** بلا رسالة خطأ معاً).

### إضافات على صفحة تفاصيل التاجر
- مصدر انضمام التاجر (تسجيل مباشر/نظام شركاء) — بيانات حقيقية جديدة (`Tenant.joinSource`).
- بطاقة استخدام رابعة (أرقام واتساب المربوطة) لاستكمال أشرطة التقدّم الأربعة.
- سجل فواتير التاجر (قراءة فقط) مع رابط لصفحة الفوترة الكاملة.
- مكوّن ترقيم صفحات مشترك جديد (كان مكرَّراً 3 مرات منفصلة عبر اللوحة).

### التحقق النهائي
- `npx tsc --noEmit` و`npm run build` نظيفان.
- اختبار دخان حي شامل (10 أقسام) ضد تينانت اختبار معزول: قبول مباشر من الجدول يطابق نتيجة مركز
  الموافقات تماماً، تعليق يمنع دخول المالك فعلياً وفوراً (وليس مجرد تغيير شارة)، Impersonate يُسجَّل
  في سجل التدقيق لكلا الطرفين، فريق الفوترة مُنِع فعلياً من رؤية القائمة والتصدير (403 حقيقي)، فريق
  الدعم يرى Impersonate لكن لا يرى الحذف (تطابق دقيق مع مصفوفة الصلاحيات)، ومؤشر الصحة يتحوّل لأحمر
  فعلياً عند تعطّل تكامل حقيقي.
- ثلاثة أعطال ظهرت أثناء الاختبار الأول كانت جميعها فجوات في تجهيزة الاختبار (بيانات تينانت اختبار
  غير مكتملة)، وليست أعطالاً في التطبيق — تفاصيل التشخيص في `DECISIONS.md`.

---

## 2026-07-28 — وحدة محاسبة كاملة (دليل حسابات + دفتر قيود مزدوج) + تدقيق شامل لكل لوحة Super Admin

جولة مزدوجة كبيرة: (1) إعادة بناء صفحة "الإيرادات والفوترة" لتشمل محاسبة حقيقية بقيد مزدوج وفوترة
متوافقة مع ZATCA المرحلة الأولى، (2) تدقيق شامل لكل الأقسام الـ12 في لوحة Super Admin وإكمال كل فجوة
حقيقية — بما فيها قدرتان جديدتان: إنشاء تاجر مباشرة، و"الدخول كخبير" حصرياً لمالك المنصة. التفاصيل
الكاملة (كل قرار وسبب) في `DECISIONS.md` #186-206، والتحقق الذاتي الكامل بكل الـ12 قسماً في
`QA_REPORT.md`.

### إصلاح خلل حقيقي: عرض ضريبة القيمة المضافة كصفر على فاتورة حقيقية
- فاتورة 1299 ر.س كانت تعرض "ض.ق.م 0" رغم أن الباقة خاضعة للضريبة — السبب فجوة تاريخية لمرة واحدة
  (migration أضافت عمود الضريبة بلا backfill للفواتير الموجودة وقتها)، وليس خللاً حياً متكرراً. أُصلح
  بـbackfill محسوب رياضياً + تحقُّق مباشر من القاعدة (0 → 169 ر.س).
- نسبة الضريبة أصبحت تُقرأ من مصدر مركزي واحد (`PlatformSettings.vatRateBps` عبر `lib/billing/vat.ts`)
  بدل ثابت مكرَّر كان موجوداً بالكود.

### محاسبة قيد مزدوج حقيقية (جديدة كلياً)
- دليل حسابات كامل بـ5 فروع جذرية (أصول/خصوم/حقوق ملكية/إيرادات/مصروفات)، شجرة قابلة للطي والتوسيع،
  رصيد كل حساب محسوب فعلياً من دفتر القيود (لا أرقام ثابتة)، وإمكانية إضافة حسابات فرعية يدوياً.
- كل حدث مالي (تحصيل فاتورة، استرداد، اعتراف بإيراد) يُنشئ قيداً محاسبياً متوازناً تلقائياً (تحقُّق
  توازن مدين=دائن في الكود قبل الكتابة، رفض أي قيد غير متوازن).
- **الإيراد يُعتَرف به تدريجياً** (Deferred Revenue) عبر مهمة دورية كل 6 ساعات، وليس دفعة واحدة عند
  الدفع — الدفع يُقيَّد أولاً كإيراد مؤجَّل، ثم يُرحَّل تدريجياً حسب نسبة الوقت المنقضي من فترة الفاتورة.
- شاشة "دفتر القيود" الكاملة، قابلة للفلترة بالتاريخ ونوع الحدث.
- تصدير كل من دليل الحسابات ودفتر القيود بصيغة CSV بترتيب أعمدة يفهمه أي محاسب خارجي
  (متوافق مع أدوات استيراد يدوي في QuickBooks/Zoho Books).

### فوترة احترافية + استرداد بإشعار دائن (بدل حذف/تعديل الفاتورة الأصلية)
- استرداد فاتورة مدفوعة يُنشئ **إشعار دائن** حقيقي (`CreditNote`) مرقَّم تسلسلياً، ويُسجِّل قيداً
  محاسبياً عكسياً — الفاتورة الأصلية **لا تُعدَّل ولا تُحذَف إطلاقاً** (فقط `status → REFUNDED`).
  تحقُّق حي كامل: استرداد → إشعار دائن #1 → قيد متوازن (1130+169 مدين = 1299 دائن).
- فاتورة PDF جديدة متوافقة مع ZATCA المرحلة الأولى: رقم تسلسلي بصيغة `INV-YYYY-NNNNN`، بيانات بائع
  كاملة (اسم/رقم ضريبي/عنوان — إعداد جديد في "إعدادات المنصة")، تفصيل ضريبة واضح، ورمز QR بترميز TLV.
- لوحة الإيرادات: MRR، ARR (=MRR×12)، نسبة تسرّب الإيراد الشهرية، رسم بياني لاتجاه الإيراد آخر 12
  شهراً، رسم دونات لتوزيع الاشتراكات حسب الباقة (SVG مخصَّص بلا مكتبة خارجية)، وقسم مدفوعات فاشلة
  بزر "إعادة محاولة تحصيل" فعلي.
- قائمة الفواتير الآن قابلة للفلترة (الحالة/الباقة/التاجر/نطاق تاريخ) وتعرض طريقة الدفع الفعلية
  المحفوظة للتاجر.

### قدرتان جديدتان جذريتان في صفحة "التجار"
- **إضافة تاجر مباشرة**: زر جديد ينشئ تاجراً فعّالاً فوراً (بلا مرور بمركز الموافقات)، بنفس منطق
  التفعيل المستخدَم عند قبول طلب شريك حقيقي حرفياً — نفس بريد "أكمل إعداد حسابك" يصل للمالك الجديد.
  لمبيعات الهاتف، عملاء VIP، تجار تجريبيين.
- **"الدخول كخبير"**: أصبحت حصرية لمالك المنصة (SUPER_ADMIN) فقط — أُزيلت الصلاحية من فريق الدعم
  الفني (كانت متاحة له في جولة سابقة)، والزر أُعيدت تسميته في كل من صفحة القائمة والتفاصيل والبانر.
  التحقق الحي أكّد: الزر غائب تماماً عن واجهة فريق الدعم والفريق المالي، **وأي استدعاء مباشر لإجراء
  بدء الانتحال من حساباتهما يُرفَض فعلياً من الخادم** (وليس إخفاء واجهة فقط).

### رسائل التواصل: حالات متعددة + تعيين + تحويل مباشر لتاجر جديد
- حالة الرسالة أصبحت 4 مستويات (جديدة/تمت المتابعة/تحوَّلت لتاجر/مُغلقة) بدل علم ثنائي فقط، مع إمكانية
  تعيين كل رسالة لعضو فريق داخلي محدَّد.
- زر "تحويل إلى تاجر جديد" يفتح نموذج الإنشاء المباشر **مُعبَّأً مسبقاً** ببيانات الرسالة (اسم/بريد/
  هاتف)، وعند نجاح الإنشاء تُعلَّم الرسالة تلقائياً كـ"تحوَّلت لتاجر" مع ربطها بمعرّف التاجر الجديد.

### سجل التدقيق: فلاتر فعلية + تصدير (لم تكن موجودة إطلاقاً من قبل)
- فلترة حسب الموظف الداخلي، التاجر المتأثر، نوع الحدث، ونطاق تاريخ — على نفس صفحة سجل التدقيق
  الموجودة أصلاً (كانت بلا أي فلتر، فقط ترقيم صفحات).
- تصدير CSV لكل نتيجة مفلترة.

### الموقع التسويقي: توسيع لوحة "محتوى الموقع" لصفحتي "من نحن" و"الخدمات"
- كانتا نصوصاً ثابتة بالكامل بالكود — أصبحتا قابلتين للتعديل الكامل من `admin/content` بلا أي إعادة
  نشر، بنفس نمط قسم "الميزات" بالصفحة الرئيسية (الأيقونات الخمس تبقى ثابتة بالكود، النصوص فقط قابلة
  للتعديل). تحقُّق حي أكّد: تعديل عنوان "من نحن" من اللوحة ظهر فوراً في الصفحة العامة بلا أي إعادة بناء.

### التحقق النهائي
- `npx tsc --noEmit` و`npm run build` نظيفان طوال الجولة (صفر أخطاء).
- تحقُّق حي شامل عبر Playwright (17 فحصاً منفصلاً عبر 3 حسابات أدوار مختلفة): تسجيل دخول مالك المنصة/
  فريق الدعم/الفريق المالي، ظهور/اختفاء زر "الدخول كخبير" و"إضافة تاجر مباشرة" حسب الدور، عرض
  الإيرادات والفواتير بلا خلل ضريبة، عرض دليل الحسابات ودفتر القيود، إنشاء تاجر مباشرة كامل (وصول
  البريد فعلياً)، تحويل رسالة تواصل كاملة لتاجر (تعبئة مسبقة + تعليم CONVERTED)، استرداد فاتورة كامل
  (إشعار دائن + قيد متوازن حقيقي في القاعدة)، تصدير دفتر القيود ودليل الحسابات (CSV حقيقي)، وتعديل
  محتوى CMS يظهر حياً بلا إعادة بناء — **17/17 نجح**.
- التحقق على مستوى الكود (RBAC): `hasPermission`/`requirePermission` تؤكدان أن الانتحال حصري
  لـ`SUPER_ADMIN` فعلياً، وترفض الطلب صراحة لكل من `PLATFORM_SUPPORT`/`PLATFORM_BILLING`.
- تنظيف كامل لكل بيانات الاختبار (تينانتات، رسالة تواصل تجريبية) بعد التحقق، باستثناء الفاتورة
  الحقيقية الوحيدة المسترَدة أثناء اختبار تدفق الاسترداد (لا مسار "تراجع عن استرداد" في النظام أصلاً —
  قرار موثَّق في `DECISIONS.md` #206).

---

## 2026-07-29 — الموظف الذكي (AI Human-Like Customer Service Agent)

أول ربط فعلي لمزوّد ذكاء اصطناعي خارجي (LLM) في المشروع — عقدة "رد ذكي" في محرر الشات بوت كانت
تحوّل صامتاً لموظف بشري (حد فاصل صريح موثَّق من جولة سابقة)، أصبحت الآن موظف خدمة عملاء حقيقي يرد
بذكاء اصطناعي متماسك عبر عدة رسائل، يستعلم بيانات حقيقية بدل التخمين، ويحوّل تلقائياً للموظف البشري
عند الحاجة. التفاصيل الكاملة لكل قرار في `DECISIONS.md` #207-220، والتحقق الذاتي الكامل في
`QA_REPORT.md` § 12.

### محرك محادثة حقيقي بدل رد آلي جامد
- شخصية قابلة للتخصيص لكل تاجر: اسم، نبرة حديث (رسمية/ودودة/مرحة)، حدود وقواعد صريحة، أسئلة شائعة،
  وسياسات المتجر (شحن/استرجاع/ساعات عمل) — صفحة إعدادات جديدة `/dashboard/ai-agent`.
- يحافظ على سياق المحادثة عبر آخر 15 رسالة (وليس رداً منعزلاً لكل رسالة بمفردها)، ويرد بلغة العميل
  ولهجته قدر الإمكان.
- **لا تخمين في بيانات حرجة إطلاقاً**: سعر/توفر أي منتج وحالة أي طلب يُستعلَمان دائماً من قاعدة البيانات
  الحقيقية (Function Calling) قبل الرد — تحقُّق حي أكّد رداً مبنياً على سعر منتج حقيقي، ورفضاً صادقاً
  ("غير موجود") لمنتج غير موجود بدل اختلاق سعر.
- مؤشر "يكتب الآن..." حقيقي (قناة QR التجريبية) أو best-effort (Meta)، مع تأخير طبيعي متناسب مع طول
  الرد قبل الإرسال.

### تحويل تلقائي وموثوق للموظف البشري — لا محاولة "حل كل شيء بالذكاء الاصطناعي"
- طلب صريح من العميل ("أريد شخص حقيقي")، كلمات حساسة (شكوى/استرجاع/قانوني/غاضب)، ثقة منخفضة في الرد
  (أقل من 40%، تحقُّق مستقل في الكود وليس ثقة عمياء بتقييم النموذج لنفسه)، أو نفس السؤال متكرر 3 مرات
  بلا حل — كلها تُحوِّل المحادثة فوراً، مع رسالة انتقالية طبيعية للعميل ("سأحوّلك لأحد أفراد فريقنا").
- ملخص تلقائي يُكتَب كملاحظة داخلية على المحادثة فور التحويل — يوفّر وقت أول موظف بشري يفتحها.
- تحقُّق حي عبر محاكاة رسائل واردة حقيقية أكّد كل حالة تحويل بسببها الصحيح بالضبط.

### حماية التكلفة والاستخدام
- حد توكنز شهري قابل للضبط لكل باقة (`admin/plans`)، تجاوزه يحوّل المحادثة تلقائياً بدل استمرار
  استهلاك غير محسوب.
- حد معدل استدعاء لكل تاجر (20/دقيقة) يمنع إساءة الاستخدام.
- مفتاح تشغيل/إيقاف صريح لكل تاجر، منفصل عن بوابة الباقة — عطل حقيقي اكتُشف وأُصلح أثناء التحقق (كان
  هذا المفتاح موجوداً بالواجهة لكن غير مقروء فعلياً من المحرك).
- الميزة مقفلة (Soft-lock) للباقة الأساسية — مفروضة على مستوى الواجهة **والخادم معاً** (عطل ثانٍ
  اكتُشف وأُصلح: الإجراءات الخادمية كانت تقبل الاستدعاء المباشر بلا فحص الباقة).

### وضع معاينة واختبار + مراجعة جودة
- صفحة "جرّب موظفك الذكي" (`/dashboard/ai-agent/test`) لمحادثة حقيقية مع الموظف الذكي بلا التأثير على
  بيانات التجار الحقيقية.
- صفحة "مراجعة الجودة" (`/dashboard/ai-agent/reviews`) تعرض كل رد مع مؤشر ثقة، وتُبرِز الردود التي
  تحتاج مراجعة (ثقة منخفضة أو تحويل)، مع تقييم 👍/👎 لكل رد لتحسين الإعدادات لاحقاً.

### التحقق النهائي
- `npx tsc --noEmit` و`npm run build` نظيفان (صفر أخطاء).
- تحقُّق حي كامل عبر محاكاة رسائل واردة حقيقية (مسار الإنتاج الفعلي بالكامل، وليس دالة معزولة): كل
  بوابة من ست بوابات ما-قبل-استدعاء الذكاء الاصطناعي (تفعيل، كلمات مفتاحية، سؤال متكرر، حد معدل،
  حصة توكنز، مفتاح API) أنتجت سبب التحويل الصحيح بالضبط، مع رسالة انتقالية حقيقية وملخص تلقائي وسجل
  استهلاك توكنز دقيق (0 في كل الحالات — صحيح، لأن لا استدعاء LLM فعلياً حدث في أيٍّ منها).
- تحقُّق حي عبر Playwright: قفل باقة Starter (واجهة)، ظهور صفحات الإعدادات/الاختبار/المراجعة لباقة
  Growth، وتحويل فوري ظاهر في واجهة المعاينة نفسها.
- **حد صادق للتحقق**: لا مفتاح OpenAI حقيقي متاح في بيئة التطوير، فلم يُتحقَّق حياً من جولة استدعاء
  LLM فعلية كاملة (قرار استدعاء أداة، جودة رد نهائي حقيقي) — تفصيل كامل في `DECISIONS.md` #220. كل
  ما لا يعتمد على استدعاء شبكي فعلي للنموذج (البوابات الست، منطق الأدوات، القفل، سجل الاستخدام) تحقَّق
  منه حياً وبدقة كاملة.

---

## 2026-07-26 — إصلاح جذري لعطل النص العربي في فاتورة PDF + إعادة تصميم احترافية كاملة

فاتورة PDF كانت تُظهر نصاً عربياً مشوَّهاً بالكامل — عطل جذري في محرك التوليد نفسه (`pdfkit` يرسم
الحروف مباشرة بلا أي محرك تخطيط نص، فلا تشكيل عربي ولا اتجاه RTL صحيح بصرف النظر عن الخط المستخدَم)،
وليس مجرد خط ناقص. التفاصيل الكاملة لكل قرار في `DECISIONS.md` #221-228.

### الإصلاح الجذري: التخلي الكامل عن pdfkit، اعتماد HTML→PDF عبر Puppeteer حقيقي
- الفاتورة الآن صفحة HTML/CSS كاملة تُصيَّر عبر Chromium حقيقي بلا واجهة — نفس محرك عرض أي متصفح ويب
  عادي، فيتولى التشكيل العربي وإعادة ترتيب RTL بصحة كاملة تلقائياً.
- خط Cairo (نفس خط العلامة التجارية في كل الموقع) مضمَّن Base64 مباشرة داخل كل فاتورة — لا اعتماد على
  إنترنت خادم الإنتاج وقت التوليد، ولا مسار ملف قد يفشل الوصول إليه.
- متصفح Puppeteer واحد مشترك عبر كل الطلبات (وليس عملية جديدة لكل فاتورة — بطيء جداً)، بنفس نمط
  الاتصالات المشتركة الأخرى (Redis، قاعدة البيانات) المستخدَم أصلاً في المشروع.
- `pdfkit`/`@types/pdfkit` أُزيلا نهائياً من التبعيات بعد تأكيد عدم استخدامهما في أي مكان آخر.

### إعادة تصميم احترافية كاملة (مستوى Stripe/QuickBooks)
- ترويسة واضحة: اسم البائع القانوني، رقم فاتورة تسلسلي (`INV-2026-00001`)، شارة حالة ملوَّنة (أخضر
  للمدفوعة، أصفر للمعلَّقة، أحمر للفاشلة/المتأخرة).
- قسمان منفصلان "من/إلى" ببيانات البائع (المنصة) والمشتري (التاجر) كاملة.
- جدول بنود احترافي بعمود وصف واضح (اسم الباقة + فترة الفوترة)، مع مجموع فرعي وضريبة وإجمالي نهائي
  بخط بارز — قيمة الضريبة تظهر صحيحة فعلياً (تحقُّق مباشر: 169 ر.س على فاتورة 1299 ر.س حقيقية، وليست
  صفراً).
- رمز QR متوافق مع ZATCA (نفس ترميز TLV/Base64 من جولة الإيرادات السابقة) مع نص توضيحي.
- خلفية بيضاء نظيفة، نص داكن عالي التباين، لون العلامة التجارية (Indigo) محدود لخط فاصل/عنوان/شارة
  حالة فقط — هوية بصرية مختلفة تماماً عن الداشبورد الداكن، مناسبة لمستند يُطبَع ويُؤرشف قانونياً.
- أرقام غربية صراحة لكل المبالغ المالية (تتوقّعها الأنظمة المحاسبية وتوجيهات ZATCA)، مع إبقاء التواريخ
  والأوصاف عربية بالكامل.
- انتقال منظّم بين الصفحات لمحتوى طويل (`page-break-inside: avoid` على كل قسم/صف) بلا قطع أي عنصر في
  المنتصف.

### التحقق النهائي (تحقُّق فعلي حقيقي، وليس افتراضاً)
- توليد PDF حقيقي فعلياً، استخراج نصه ببرنامج `pdftotext` مستقل: كل نص عربي (بما فيه اسم تاجر يحمل
  همزات/تاء مربوطة/لام ألف متنوعة) ظهر صحيحاً 100%، بلا أي رمز مشوَّه.
- فحص رمز QR بفكّ تشفير حقيقي مستقل (مكتبة `jsQR`، وليس إعادة استخدام منطق الترميز نفسه): كل حقل
  (اسم البائع، الرقم الضريبي، الإجمالي، قيمة الضريبة) طابق البيانات الأصلية تماماً.
- فاتورة طويلة (نص سبب فشل تحصيل مطوَّل جداً، السيناريو الواقعي الوحيد الممكن في بنية البيانات
  الحالية) انتقلت فعلياً لـ3 صفحات بلا قطع أي عنصر.
- فاتورة معلَّقة (PENDING) أظهرت "تاريخ الاستحقاق" فعلياً؛ فاتورة مدفوعة لم تُظهره (غير منطقي لها).
- تحميل فاتورة حقيقية فعلياً عبر مسار الإنتاج الكامل (تسجيل دخول حقيقي → استدعاء الـroute الفعلي)
  على الفاتورة الحقيقية الوحيدة في قاعدة البيانات (INV-2026-00001) — نجح، وكشف خللاً حقيقياً صغيراً
  (وصف بند مكرَّر "اشتراك اشتراك" لفاتورة بلا `planKey` تاريخي) أُصلح فوراً وتحقَّق منه مجدداً.
- `npx tsc --noEmit` و`npm run build` نظيفان طوال الجولة.

## 2026-08-02 — تدقيق بنية الاستضافة (Infrastructure)، أول جولة تغطي ما بعد الكود

### السياق
تدقيق 2026-07-25 غطّى الكود فقط (RLS، RBAC، rate limiting، رؤوس أمان، webhooks). هذه الجولة
اكتشفت أن جذر المشروع يقع داخل `C:\xampp\htdocs\Whats app bot` — مجلد جذر خادم Apache لتثبيت
XAMPP على جهاز التطوير. تفاصيل الاكتشاف والتحقق الحي الكامل في `SECURITY_FIXES.md` §13.

### تأكيد سلبي مهم (لا كشف فعلي حالياً)
`httpd.exe` يعمل فعلاً ومربوط على كل الواجهات (`0.0.0.0:80/443`)، لكن `VirtualHost` الوحيد في
`httpd-vhosts.conf` مضبوط صراحة على مشروع Laravel آخر (`dr/maisam-platform/public`). فحص حي
(`curl -v http://127.0.0.1/Whats%20app%20bot/.env`) رجّع 404 حقيقياً من ذلك التطبيق، وليس محتوى
`.env`. الخطر البنيوي يبقى قائماً (يتفعّل فوراً مع أي تعديل مستقبلي لإعداد الـvhost) لكنه غير
مُستغَل حالياً.

### إصلاحات مُنفَّذة الآن
- `docker-compose.yml`: أُزيل سرّ `POSTGRES_PASSWORD` بنص صريح، واستُبدل بـ`env_file: .env.docker`
  (ملف جديد غير متتبَّع، أُضيف لـ`.gitignore`).
- منافذ حاويتي Postgres/Redis المنشورة على المضيف قُيِّدت لـ`127.0.0.1` فقط (كانت مكشوفة على كل
  الواجهات: `"55532:5432"`/`"56379:6379"` → `"127.0.0.1:55532:5432"`/`"127.0.0.1:56379:6379"`).

### مؤجَّل عمداً (قرار بشري/بنية إنتاج غير محسومة بعد)
نقل المشروع الفعلي خارج `htdocs`، تدوير الأسرار الإنتاجية (بتأكيد صريح من صاحب المشروع: يُنفَّذ
دفعة واحدة عند النقل الفعلي بدل مرتين)، مزوّد دفع حقيقي بدل `mockPaymentProvider`. التفاصيل
والأسباب الكاملة لكل بند في `SECURITY_FIXES.md` §13.

### تنظيف إضافي (بتأكيد صريح من صاحب المشروع)
حذف `.smoke-shots/` (لقطات اختبار قديمة) و`New مستند نصي.txt` (ملف فارغ) من جذر المشروع.

### تحقق فعلي
`docker ps` بعد إعادة الإنشاء: `127.0.0.1:...` بدل `0.0.0.0:...` على المنفذين. `next dev` استمر
يعمل (استعاد الاتصال تلقائياً، `GET / 200`) بعد إعادة إنشاء الحاويتين. `npm audit --omit=dev`:
6 ثغرات (2 متوسطة + 4 عالية + 0 حرجة) — أقل من الأساس الموثَّق (12) وبدون أي ثغرة حرجة جديدة.

## 2026-08-15 — جلسة دخول مستقلة لفريق المنصة (/admin-login)

### المشكلة
تسجيل الدخول بحساب تاجر وحساب مالك المنصة في نفس المتصفح كان يطرد أحدهما الآخر فوراً — الاثنان
يشتركان في نفس كوكي جلسة NextAuth الوحيد.

### الإصلاح
صفحة دخول جديدة مستقلة `/admin-login` مخصّصة لفريق المنصة الداخلي فقط، بكوكي جلسة خاص بها
(`dms_platform_session`، موقَّع بـ`jose`) لا علاقة له بكوكي NextAuth إطلاقاً — فيتعايش مع أي جلسة
تاجر نشطة بنفس المتصفح بلا تعارض. `requireSuperAdminSession()` يفحص الكوكي الجديد أولاً ثم يرجع
تلقائياً لجلسة NextAuth القديمة إن غاب (توافق خلفي كامل — تسجيل الدخول القديم من `/login` لموظفي
المنصة ما زال يعمل حرفياً كما كان). زر تسجيل الخروج في لوحة المنصة يمسح الكوكي الصحيح حسب مصدر
الجلسة، فلا يُسقِط بالخطأ جلسة تاجر نشطة في تبويب آخر. تفاصيل القرار والبدائل المرفوضة (تقييد
الكوكي بمسار، نطاقات فرعية) في `DECISIONS.md` #229.

### تحقق فعلي
اختبار Playwright بسياق متصفح واحد (نفس جلسة الكوكيز): دخول تاجر ثم دخول مالك منصة في نفس السياق
— نجح الاثنان معاً، والتنقل بين `/dashboard` و`/admin` لم يُسقِط أياً منهما. تفريغ الكوكيز أظهر
`next-auth.session-token` و`dms_platform_session` جنباً إلى جنب. تسجيل الخروج من لوحة المنصة مسح
كوكيها فقط — جلسة التاجر بقيت شغّالة. المسار القديم (`admin@platform.sa` عبر `/login` مباشرة)
اختُبر بشكل منفصل ونجح حرفياً كما كان. `npx tsc --noEmit` نظيف. `npm run smoke-test`: كل خطوات
التاجر (دخول، داشبورد، جهات اتصال، حملة كاملة) ناجحة.

### تعديل إضافي بنفس الجولة: الصفحة الرئيسية `/` لم تعد تُحوِّل تلقائياً حسب الجلسة
بطلب صريح (مرتين) من صاحب المشروع — أُزيل تحويل `redirect("/dashboard"|"/admin")` من
`(marketing)/page.tsx` نهائياً، فتبقى صفحة عامة عادية دائماً بغض النظر عن حالة تسجيل الدخول.
أثر جانبي اكتُشف وأُصلح فوراً: `/login` كان يعتمد على هذا التحويل كخطوة أخيرة بعد الدخول الناجح —
أصبح الآن يحدِّد الوجهة بنفسه صراحة عبر `getSession()`. التفاصيل الكاملة في `DECISIONS.md` #230.

## 2026-08-15 (جولة ثانية) — إعادة تصميم الصفحة الرئيسية + إعادة هيكلة الباقات لـ4 مستويات

### إضافات بصرية جديدة للصفحة الرئيسية
- `ProductPreview.tsx` (جديد) — معاينة Hero بصورة واجهة صندوق محادثات حقيقية (بنية `/dashboard/inbox`
  الفعلية) بدل الانيميشن التخطيطي السابق (`HeroAnimation`، لا يزال موجوداً وغير محذوف لكن غير
  مستخدَم حالياً) — بيانات تجريبية بحتة، ليست لقطة شاشة فعلية ولا تحتوي أي بيانات عميل حقيقية.
- `ChannelsToolsSection.tsx` (جديد) — قسم "القنوات والأدوات": 4 قنوات حقيقية (واتساب API، تعايش
  واتساب بزنس، زد، سلة) و5 أدوات حقيقية (الحملات، جهات الاتصال، صندوق الوارد، الموظف الذكي، قوالب
  الرسائل) — كلها قدرات مُنفَّذة فعلياً في المنصة، لا وعود بميزات غير موجودة.
- `SolutionsSection.tsx` (جديد) — قسم "حلول تناسب نشاطك وفريقك": 6 قطاعات + 3 فرق، نص تسويقي عام.

### إعادة هيكلة الباقات (`Plan`) من 3 إلى 4 مستويات
البداية (185 ر.س، كان "الأساسية" 199)، النمو (560 ر.س، كان 499، الأكثر شيوعاً)، الاحتراف (1310
ر.س، كان "التوسع" 1299)، والمؤسسات (1870 ر.س، **جديدة بالكامل**، مفتاح `enterprise`، دعم "مدير
حساب مخصص"، متاحة فقط عبر `/pricing`/`/partners/join` وليس التسجيل الذاتي `/register`). الأسعار
الجديدة محوَّلة تقريبياً (×3.75) من نسخة $ في مرجع صاحب المشروع، بتأكيده الصريح. **لم يُخفَّض أي حد
استخدام حالي** (مستخدمين/أرقام واتساب/رسائل) للباقات الثلاث القائمة — فقط الاسم والسعر تغيّرا،
حفاظاً على تشغيل التجار الحقيقيين الحاليين دون تأثر. مفاتيح الباقات (`starter`/`growth`/`scale`)
أُبقيت كما هي عمداً (مرتبطة بكود متفرّق: `minTier` في نوع البيانات، enum التسجيل، حدود fallback) —
فقط `Plan.name`/`priceMonthlySar`/`supportTier` تغيّرت. التفاصيل الكاملة والبدائل المرفوضة في
`DECISIONS.md` #231-233.

### تحقق فعلي
`npx tsc --noEmit` نظيف. `npm run smoke-test`: دخول/داشبورد/جهات اتصال/حملة كاملة ناجحة. لقطات
شاشة Playwright حقيقية (بتمرير تدريجي فعلي) للصفحة الرئيسية الكاملة و`/pricing` الكاملة تؤكد بصرياً
عرض كل الأقسام الجديدة وبطاقات الباقات الأربع بشكل صحيح.

## 2026-08-16 — برنامج التسويق بالعمولة (Affiliates) الكامل

### إضافة رئيسية: نظام مسوّقين بعمولة متكررة، مستقل كلياً عن نظام "الشركاء" الحالي
نماذج جديدة (`Affiliate`, `Referral`, `Commission`, `Payout`) + مصادقة مستقلة ثالثة (كوكي
`dms_affiliate_session`، تتعايش مع جلسة التاجر وجلسة فريق المنصة في نفس المتصفح). 3 مستويات عمولة
(20%/25%/30%) تُحتسب على فواتير حقيقية مدفوعة فعلياً لتجّار مُحالين، لمدة 12 شهراً لكل تاجر، مع ترقية
تلقائية للمستوى وترقية Net 30 (30 يوماً حجز قبل اعتماد الصرف) — كل ذلك عبر مهمة مصالحة دورية كل ساعة
(`lib/affiliates/commissionSync.ts`، بنفس نمط `revenueRecognition.ts`) بدل ربط مباشر هش بكل نقطة
تحصيل فاتورة متفرّقة في الكود.

### الصفحات الجديدة
- `/affiliates` — صفحة تسويقية عامة (مستويات العمولة، تفاصيل الصرف، كيف يعمل البرنامج، لمن يناسب، أسئلة شائعة).
- `/affiliates/apply` و`/affiliates/login` — تقديم طلب انضمام ودخول مستقل للمسوّقين.
- `/affiliates/dashboard` — رابط إحالة، إحصائيات المستوى والعمولات، سجل الإحالات والعمولات، طلب صرف الرصيد.
- `/admin/affiliates` و`/admin/affiliates/[id]` — مراجعة طلبات الانضمام، تعليق/تفعيل الحسابات، تعديل
  المستوى يدوياً، واعتماد/رفض طلبات الصرف — صلاحية RBAC جديدة `platform.affiliates.manage` (SUPER_ADMIN فقط).

### قرارات وأخطاء حقيقية اكتُشفت وأُصلحت أثناء البناء (تفاصيل كاملة في `DECISIONS.md` #234)
- كوكي `?ref=CODE` يُلتقَط ويُخزَّن بطريقتين مختلفتين حسب المسار: مباشرة وقت إنشاء التينانت في
  `/register`، لكن يُحفَظ داخل `ApprovalRequest.payloadJson` وقت التقديم نفسه في `/partners/apply`
  (لأن الموافقة تحدث لاحقاً من متصفح مالك المنصة الذي لا يحمل كوكي الإحالة إطلاقاً).
- خطأ RLS حقيقي: قراءة `Invoice` عبر `include` من استعلام `rawDb.commission.findMany()` فشلت فعلياً
  (Invoice محمي بـRLS، والعلاقة إلزامية) — أُصلح بقراءة منفصلة عبر `superAdminDb`.
  خطأ Next.js حقيقي: تصدير ثابت من ملف `"use server"` كسر البناء — نُقل لملف عادي منفصل.

### تحقق فعلي
تدفق كامل حي عبر Playwright: تقديم طلب → موافقة أدمن → دخول المسوّق → محاكاة تاجر مُحال بفاتورة
مدفوعة (185 ر.س × 20% = 37 ر.س بالضبط) → ترقية Net 30 لـAPPROVED → تجاوز حد الصرف (407 ر.س) → طلب
صرف فعلي → اعتماد الأدمن → تحقّق SQL مباشر أن كل العمولات والدفعة انتقلت لـPAID بالمبلغ الصحيح. `npx
tsc --noEmit` نظيف. `npm run smoke-test` ناجح بعد اكتمال إحماء الخادم (فشل أوّلي وقت التصريف البارد
بعد مسح `.next`، غير قابل لإعادة الإنتاج بعد الإحماء). كل بيانات الاختبار حُذفت من القاعدة.

## 2026-08-16 (تصحيح) — إصلاح فشل `smoke-test.js` المزمن في خطوة محرر الشات بوت

### السبب الجذري (سكربت اختبار قديم، وليس خللاً في المنتج)
السكربت كان ينتظر نص "لوحة رسم التدفق" فور فتح أي تدفق — نص خاص حصراً بالعرض المتقدّم
(`?view=canvas`) المبني على لوحة الرسم الحرة القديمة. منذ إعادة تصميم المحرر (الجولة الثانية
والعشرون، بناء تسلسلي مبسَّط كعرض افتراضي) أصبح ذلك النص لا يظهر إطلاقاً عند الفتح العادي، فكان
السكربت يفشل بمهلة انتظار عند كل تشغيلة — فشل موثَّق ومتكرر عبر عدة جولات سابقة بلا تشخيص جذري.

### الإصلاح
سطر واحد: `waitForSelector("text=خطوات المحادثة")` بدل النص القديم — العنوان الفعلي الثابت في العرض
البسيط الافتراضي. بقية خطوات اختبار الشات بوت (حقل الاختبار بـplaceholder "مثال: أين طلبي؟"، زر
"إرسال") كانت مطابقة للواجهة الجديدة فعلاً بلا أي تغيير مطلوب.

### تحقق فعلي
`npm run smoke-test` أكمل كل الخطوات الثلاث عشرة للمرة الأولى في كل هذه الجلسة (دخول → داشبورد →
جهات اتصال → حملة كاملة → **محرر شات بوت** → تكاملات → فوترة → إعدادات → حذف جهة اتصال → لوحة مالك
المنصة → التجار). خطأ عابر واحد غير متعلق (`P2028: Unable to start a transaction`) ظهر أثناء ذروة
تجميع Next.js لعدة صفحات دفعة واحدة — نفس عرض شبكة Docker/WSL2 المُشخَّص سابقاً في هذه الجلسة، حُلّ
بـ`docker compose restart`، ولم يمنع نجاح الاختبار الكامل أصلاً.

### إضافة: برنامج التسويق بالعمولة ضمن `smoke-test.js` بشكل دائم
خطوة عاشرة جديدة في السكربت: تقديم طلب مسوّق حقيقي عبر `/affiliates/apply` (بريد فريد بالطابع
الزمني، نفس أسلوب دعوة الفريق في الخطوة 8) ← موافقة فعلية من `/admin/affiliates` (نفس جلسة
`admin@platform.sa` المستخدَمة للخطوات 12-13) ← تسجيل دخول مسوّق بجلسة مستقلة تماماً (كوكي
`dms_affiliate_session`، لا علاقة له بجلسة NextAuth للوحة المالك) ← تحقّق أن `/affiliates/dashboard`
يُصيَّر بلا خطأ. حماية انحدار دائمة لأي كسر مستقبلي في أي جزء من التدفق (الطلب، الموافقة، العمولة،
الصرف) — دون تدخّل يدوي. `npm run smoke-test` نجح بالكامل (exit code 0) بما فيها هذه الخطوة الجديدة.
